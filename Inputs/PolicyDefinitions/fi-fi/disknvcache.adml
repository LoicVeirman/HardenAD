<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>kirjoita näyttönimi tähän</displayName>
  <description>kirjoita kuvaus tähän</description>
  <resources>
    <stringTable>
      <string id="BootResumePolicy">Poista käynnistyksen ja palautumisen optimoinnit käytöstä</string>
      <string id="BootResumePolicyHelp">Tämä käytäntöasetus poistaa järjestelmän yhdistelmäkiintolevyjen käynnistyksen ja palautumisen optimoinnit käytöstä.

Jos tämä käytäntöasetus otetaan käyttöön, järjestelmä ei käytä säilyvää välimuistia käynnistyksen ja palautumisen optimoimiseen.

Jos tämä käytäntöasetus poistetaan käytöstä, järjestelmä käyttää säilyvää välimuistia käynnistyksen ja palautumisen nopeuttamiseen. Järjestelmä määrittää tiedot, jotka tallennetaan säilyvään välimuistiin käynnistyksen ja palautumisen optimoimiseksi. Tarvittavat tiedot tallennetaan säilyvään välimuistiin sammutuksen ja horrostilan aikana. Se voi pidentää hieman sammutukseen ja horrostilaan siirtymiseen kuluvaa aikaa.

Jos tätä käytäntöasetusta ei määritetä, käyttöön tulee oletuskäytäntö ja säilyvää välimuistia käytetään sekä käynnistyksen että palautumisen optimointiin.

Huomautus: tätä käytäntöasetusta käytetään vain, jos säilyvä välimuistiominaisuus on käytössä.</string>
      <string id="CachePowerModePolicy">Poista välimuistin virtatila käytöstä</string>
      <string id="CachePowerModePolicyHelp">Tämä käytäntöasetus poistaa yhdistelmäkiintolevyjen virransäästötilan käytöstä järjestelmässä.

Jos tämä käytäntöasetus otetaan käyttöön,kiintolevyjä ei siirretä säilyvän välimuistin virransäästötilaan eikä virtaa säästy.

Jos tämä käytäntöasetus poistetaan käytöstä, kiintolevyt siirretään säilyvän välimuistin virransäästötilaan. Tässä tilassa järjestelmä yrittää säästää virtaa hidastamalla selvästi levyn pyörimisnopeutta.

Jos tätä käytäntöasetusta ei määritetä, yhdistelmäkiintolevyjen siirtyminen virransäästötilaan sallitaan oletusarvoisesti.

Huomautus: tätä käytäntöasetusta käytetään vain, jos säilyvä välimuistiominaisuus on käytössä.</string>
      <string id="FeatureOffPolicy">Poista säilyvä välimuistiominaisuus käytöstä</string>
      <string id="FeatureOffPolicyHelp">Tämä käytäntöasetus poistaa käytöstä kaiken tuen säilyvälle välimuistille järjestelmän kaikilla yhdistelmäkiintolevyillä. Voit tarkistaa, onko järjestelmässä yhdistelmäkiintolevyjä, napsauttamalla Laitehallinnassa hiiren kakkospainikkeella levyasemaa ja valitsemalla Ominaisuudet. Säilyvän välimuistin avulla voidaan optimoida käynnistys ja jatkaminen lukemalla tiedot välimuistista samaan aikaan, kun levyjen pyörimisnopeutta suurennetaan. Säilyvän välimuistin avulla voidaan myös pienentää järjestelmän virrankulutusta pitämällä levyjen pyörimisnopeus hitaana silloin, kun välimuistista luetaan tai siihen kirjoitetaan tietoja.

Jos otat tämän käytäntöasetuksen otetaan käyttöön, järjestelmä ei hallitse säilyvää välimuistia eikä ota käyttöön säilyvän välimuistin virransäästötilaa.

Jos poistat tämän käytäntöasetuksen käytöstä, järjestelmä hallitsee säilyvää välimuistia levyillä, jos säilyvän välimuistin muut käytäntöasetukset on määritetty oikein.

Huomautus: tämä käytäntöasetus tulee voimaan seuraavan käynnistyksen yhteydessä.

Jos et määritä tätä käytäntöasetusta, säilyvän välimuistin tuki otetaan käyttöön oletusarvoisesti.</string>
      <string id="NvCacheCat">Säilyvä levyvälimuisti</string>
      <string id="SolidStatePolicy">Poista puolijohdetila käytöstä</string>
      <string id="SolidStatePolicyHelp">Tämä käytäntöasetus poistaa yhdistelmäkiintolevyjen puolijohdetilan käytöstä. 

Jos tämä käytäntöasetus otetaan käyttöön, tiedostoja, joihin kirjoitetaan paljon, ei ehkä tallenneta säilyvään välimuistiin. Tällaisia tiedostoja ovat esimerkiksi tiedostojärjestelmän metatieto- ja rekisteritiedostot.

Jos tämä käytäntöasetus poistetaan käytöstä, järjestelmä tallentaa usein kirjoitetut tiedot säilyvään välimuistiin. Tällöin järjestelmä voi käyttää yksinomaan säilyvää välimuistia ja katkaista levystä virran pitkiksi ajoiksi virran säästämiseksi. Huomaa, että tämä voi kuluttaa säilyvää välimuistia normaalia enemmän.

Jos tätä käytäntöä ei määritetä, käyttöön tulee oletuskäytäntö ja järjestelmä tallentaa tiedostot, joihin kirjoitetaan paljon, säilyvään välimuistiin

Huomautus: tätä käytäntöasetusta käytetään vain, jos säilyvä välimuistiominaisuus on käytössä.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
