<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="WdiScenarioCategory">Wykrywanie i rozwiązywanie problemów z wyczerpaniem zasobów systemu Windows</string>
      <string id="WdiScenarioExecutionPolicy">Konfiguracja poziomu wykonania scenariusza</string>
      <string id="WdiScenarioExecutionPolicyExplain">Określa poziom wykonywania dla funkcji wykrywania i rozwiązywania problemów z wyczerpaniem zasobów systemu Windows.

Jeśli to ustawienie zasad zostanie włączone, z menu rozwijanego należy wybrać poziom wykonania. Jeśli zostanie wybrane tylko wykrywanie i analiza problemów, usługa Zasady diagnostyki (DPS, Diagnostic Policy Service) będzie wykrywała problemy z wyczerpaniem zasobów systemu Windows i będzie próbowała określić ich główne przyczyny. Wykryte przyczyny zostaną zapisane w dzienniku zdarzeń, ale nie zostaną podjęte czynności mające na celu rozwiązanie problemów. Jeśli zostanie wybrane wykrywanie, analiza i rozwiązywanie problemów, usługa DPS będzie wykrywała problemy z wyczerpaniem zasobów systemu Windows i będzie wskazywała użytkownikowi, że jest dostępne ich rozwiązanie.

Jeśli to ustawienie zasad zostanie wyłączone, system Windows nie będzie miał możliwości wykrywania, analizy i rozwiązywania problemów z wyczerpaniem zasobów systemu Windows obsługiwanych przez usługę DPS.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, usługa DPS domyślnie włączy funkcję rozwiązywania problemów z wyczerpaniem zasobów systemu Windows.

To ustawienie zasad zacznie obowiązywać tylko wtedy, gdy nie zostanie skonfigurowana zasada dotycząca wykonywania scenariusza ogólnodiagnostycznego.

Aby to ustawienie zaczęło obowiązywać, nie jest wymagane ponowne uruchomienie systemu ani usługi: zmiany zostaną wprowadzone natychmiast.

To ustawienie zasad zacznie obowiązywać tylko wtedy, gdy usługa Zasady diagnostyki będzie uruchomiona. Gdy ta usługa zostanie zatrzymana lub wyłączona, scenariusze diagnostyczne nie będą wykonywane.  Usługę DPS można skonfigurować przy użyciu przystawki Usługi w programie Microsoft Management Console.</string>
      <string id="WdiScenarioExecutionPolicyLevelResolution">Wykrywanie, rozwiązywanie i rozpoznawanie problemów</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Tylko wykrywanie i rozwiązywanie problemów</string>
    </stringTable>
    <presentationTable>
      <presentation id="WdiScenarioExecutionPolicy">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Poziom wykonania scenariusza</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
