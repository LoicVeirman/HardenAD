<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Plik szablonu zasad grupy protokołu TCPIP</displayName>
  <description>W tym pliku admx opisano szablon zasad dla składników protokołu TCPIP</description>
  <resources>
    <stringTable>
      <string id="TCPIP">Ustawienia protokołu TCPIP</string>
      <string id="Ipv6Transition">Technologie przejściowe IPv6</string>

      <string id="ISATAP_State">Ustaw stan usługi ISATAP</string>
      <string id="ISATAP_Router_Name">Ustaw nazwę routera ISATAP</string>
      <string id="6to4_State">Ustaw stan protokołu 6to4</string>
      <string id="6to4_Router_Name">Ustaw nazwę przekaźnika 6to4</string>
      <string id="6to4_Router_Name_Resolution_Interval">Ustaw interwał rozpoznawania nazwy przekaźnika 6to4</string>
      <string id="Teredo_State">Ustaw stan protokołu Teredo</string>
      <string id="Teredo_Server_Name">Ustaw nazwę serwera Teredo</string>
      <string id="Teredo_Refresh_Rate">Ustaw szybkość odświeżania protokołu Teredo</string>
      <string id="Teredo_Client_Port">Ustaw port klienta protokołu Teredo</string>
      <string id="Teredo_Default_Qualified">Ustaw kwalifikowany domyślnie protokół Teredo</string>

      <string id="DefaultState">Stan domyślny</string>
      <string id="EnabledState">Stan włączenia</string>
      <string id="DisabledState">Stan wyłączenia</string>
      <string id="TeredoClient">Klient</string>
      <string id="TeredoEnterpriseClient">Klient przedsiębiorstwa</string>
      <string id="IPHTTPS_ClientState">Ustaw stan protokołu IP-HTTPS</string>
      <string id="IPHTTPS_ClientUrl">Adres URL serwera IP-HTTPS</string>

      <string id="ISATAP_State_Help">To ustawienie zasad umożliwia skonfigurowanie protokołu ISATAP (ang. Intra-Site Automatic Tunnel Addressing Protocol), czyli technologii automatycznego tunelowania typu adres-router i host-host, host-router i router-host, służącej do zapewniania łączności protokołu IPv6 w trybie emisji pojedynczej między hostami protokołu IPv6 w intranecie, w którym jest używany protokół IPv4. 

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie używane ustawienie hosta lokalnego.

Jeśli to ustawienie zasad zostanie włączone, protokół ISATAP będzie można skonfigurować przy użyciu jednego z następujących ustawień:

Stan domyślny zasad: na hoście nie ma interfejsów ISATAP.

Stan włączenia zasad: jeżeli nazwa ISATAP zostanie pomyślnie rozpoznana, konfiguracja protokołu ISATAP na hoście będzie obejmować adres łącza lokalnego oraz adres każdego prefiksu odebranego z routera ISATAP w ramach bezstanowej automatycznej konfiguracji adresu. Jeżeli nazwa ISATAP nie zostanie rozpoznana pomyślnie, interfejs protokołu ISATAP na hoście będzie obejmować adres łącza lokalnego.

Stan wyłączenia zasad: na hoście nie ma interfejsów ISATAP.

</string>

      <string id="ISATAP_Router_Name_Help">To ustawienie zasad umożliwia określenie nazwy routera lub adresu protokołu Internet Protocol w wersji 4 (IPv4) dla routera ISATAP.

Jeżeli to ustawienie zasad zostanie włączone, będzie można określić nazwę routera lub adres IPv4 dla routera ISATAP. Jeżeli w polu tekstowym zostanie wprowadzony adres IPv4 routera ISATAP, usługi DNS nie będą wymagane.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie używane ustawienie hosta lokalnego.



</string>


      <string id="6to4_State_Help">To ustawienie zasad umożliwia skonfigurowanie protokołu 6to4, czyli technologii przypisywania adresów i automatycznego tunelowania router-router, służącej do zapewniania łączności protokołu IPv6 w trybie emisji pojedynczej między lokacjami i hostami protokołu IPv6 w Internecie, w którym jest używany protokół IPv4. W protokole 6to4 jest używany prefiks adresu globalnego — 2002:WWXX:YYZZ::/48 — w którym litery oznaczają szesnastkową reprezentację globalnego adresu IPv4 (w.x.y.z) przypisanego do lokacji.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie używane ustawienie hosta lokalnego.

Jeżeli to ustawienie zasad zostanie włączone, protokół 6to4 będzie można skonfigurować za pomocą jednego z następujących ustawień:

Stan domyślny zasad: Protokół 6to4 jest wyłączony i łączność z protokołem 6to4 nie będzie dostępna.

Stan włączenia zasad: Jeżeli będzie dostępny globalny adres IPv4, host będzie miał interfejs 6to4. W przypadku braku globalnego adresu IPv4 host nie będzie miał interfejsu 6to4.

Stan wyłączenia zasad: protokół 6to4 jest wyłączony i łączność za pośrednictwem interfejsu 6to4 jest niedostępna.

</string>

      <string id="6to4_Router_Name_Help">To ustawienie zasad umożliwia określenie nazwy przekaźnika 6to4 dla hosta 6to4. Przekaźnik 6to4 jest używany jako brama domyślna ruchu sieciowego IPv6 wysyłanego przez hosta 6to4. Ustawienie nazwy przekaźnika 6to4 nie ma znaczenia, jeśli na hoście jest niedostępna łączność z użyciem protokołu 6to4.

Jeżeli to ustawienie zasad zostanie włączone, będzie można określić nazwę przekaźnika 6to4.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie używane ustawienie hosta lokalnego i nie będzie można określić nazwy przekaźnika dla hosta 6to4.



</string>

      <string id="6to4_Router_Name_Resolution_Interval_Help">To ustawienie zasad umożliwia określenie interwału, z jakim będzie rozpoznawana nazwa przekaźnika. Ustawienie interwału rozpoznawania nazwy przekaźnika 6to4 nie ma znaczenia, jeśli na hoście jest niedostępna łączność z użyciem protokołu 6to4.

Jeżeli to ustawienie zasad zostanie włączone, będzie można określić wartość okresu, co jaki będzie rozpoznawana nazwa hosta.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie używane ustawienie hosta lokalnego.



</string>

      <string id="Teredo_State_Help">To ustawienie zasad umożliwia skonfigurowanie protokołu Teredo, czyli technologii przypisywania adresów i automatycznego tunelowania, która służy do zapewniania łączności protokołu IPv6 w trybie emisji pojedynczej w Internecie, w którym jest używany protokół IPv4.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będą używane ustawienia hosta lokalnego.

Jeśli to ustawienie zasad zostanie włączone, protokół Teredo będzie można skonfigurować przy użyciu jednego z następujących ustawień:

Domyślne: stan domyślny to „Klient”.

Wyłączone: brak interfejsów Teredo na hoście.

Klient: interfejs Teredo jest dostępny tylko wtedy, gdy host nie znajduje się w sieci zawierającej kontroler domeny.

Klient przedsiębiorstwa: interfejs Teredo jest zawsze dostępny, nawet jeśli host znajduje się w sieci zawierającej kontroler domeny.



</string>

      <string id="Teredo_Server_Name_Help">To ustawienie zasad umożliwia określenie nazwy serwera Teredo. Ta nazwa serwera będzie używana na komputerze klienckim protokołu Teredo, na którym zostanie zastosowane to ustawienie zasad.

Jeżeli to ustawienie zasad zostanie włączone, będzie można określić nazwę serwera Teredo stosowaną na kliencie Teredo.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, w celu ustalenia nazwy serwera Teredo będą używane ustawienia lokalne komputera.



</string>

      <string id="Teredo_Refresh_Rate_Help">To ustawienie zasad umożliwia skonfigurowanie szybkości odświeżania protokołu Teredo.

Uwaga: Każdy klient Teredo okresowo (domyślnie co 30 sekund) wysyła jeden pakiet żądania routera do serwera Teredo. Serwer Teredo w odpowiedzi wysyła pakiet anonsu routera. Ten okresowy pakiet odświeża mapowanie adresu IP i portu UDP w tabeli translacji urządzenia translatora adresów sieciowych klienta Teredo.

Jeżeli to ustawienie zasad zostanie włączone, będzie można określić szybkość odświeżania.  Wybranie szybkości odświeżania dłuższej niż czas trwania mapowania portu w tabeli translacji urządzenia translatora adresów sieciowych klienta Teredo może spowodować zatrzymanie działania protokołu Teredo lub problemy z łącznością.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, szybkość odświeżania zostanie skonfigurowana przy użyciu ustawień lokalnych komputera. Domyślna szybkość odświeżania to 30 sekund.



</string>

      <string id="Teredo_Client_Port_Help">To ustawienie zasad umożliwia wybranie portu UDP, którego klient Teredo będzie używał do wysyłania pakietów. Pozostawienie wartości domyślnej równej 0 spowoduje, że system operacyjny wybierze port (zalecane). Jeżeli zostanie wybrany port UDP, który jest już używany w systemie, inicjowania klienta Teredo zakończy się niepowodzeniem.

Jeżeli to ustawienie zasad zostanie włączone, będzie można dostosować port UDP klienta Teredo.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie używane ustawienie hosta lokalnego.



</string>

      <string id="Teredo_Default_Qualified_Help">To ustawienie zasad umożliwia ustawienie gotowości protokołu Teredo do komunikacji (ten proces nosi nazwę kwalifikacji). Domyślnie protokół Teredo przechodzi do stanu uśpienia, gdy nie jest używany. Proces kwalifikacji powoduje, że protokół Teredo wychodzi z tego stanu.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie używane ustawienie hosta lokalnego.

To ustawienie zasad zawiera tylko jeden stan:

Stan włączenia zasad: jeżeli jest włączone ustawienie Kwalifikowany domyślnie, protokół Teredo będzie natychmiast podejmował próbę kwalifikacji i pozostanie kwalifikowany, o ile proces kwalifikacji zakończy się pomyślnie.

</string>

      <string id="IPHTTPS_ClientState_Help">To ustawienie zasad umożliwia skonfigurowanie protokołu IP-HTTPS, czyli technologii tunelowania, w której protokół HTTPS jest używany do zapewniania łączności IP z siecią zdalną.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będą używane ustawienia hosta lokalnego.

Jeżeli to ustawienie zasad zostanie włączone, będzie można określić adres URL serwera IP-HTTPS. Protokół IP-HTTPS będzie można skonfigurować za pomocą jednego z następujących ustawień:

Stan domyślny zasad: interfejs IP-HTTPS jest używany, gdy nie są dostępne inne opcje łączności.

Stan włączenia zasad: interfejs IP-HTTPS jest zawsze dostępny, nawet gdy host ma inne opcje łączności.

Stan wyłączenia zasad: na hoście nie będą używane interfejsy IP-HTTPS.



</string>

      <string id="Parameters">Parametry</string>
      <string id="Windows_Scaling_Heuristics_State">Ustaw stan heurystyki skalowania okna</string>
      <string id="Windows_Scaling_Heuristics_Help">To ustawienie zasad pozwala skonfigurować heurystykę skalowania okna. Heurystyka skalowania okna to algorytm służący do identyfikowania problemów z łącznością i przepustowością powodowanych przez wiele zapór i inne programy pośredniczące, które niepoprawnie interpretują opcję Skalowanie okna.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, będą używane ustawienia hosta lokalnego.

Jeśli to ustawienie zasad zostanie włączone, heurystyka skalowania okna będzie włączona i system będzie próbował identyfikować problemy z łącznością i przepustowością oraz podejmować odpowiednie czynności naprawcze.

Jeśli to ustawienie zasad zostanie wyłączone, heurystyka skalowania okna będzie wyłączona i system nie będzie próbował identyfikować problemów z łącznością i przepustowością powodowanych przez zapory lub inne oprogramowanie pośredniczące.



</string>

      <string id="IP_Stateless_Autoconfiguration_Limits_State">Ustaw stan limitów bezstanowej autokonfiguracji protokołu IP</string>
      <string id="IP_Stateless_Autoconfiguration_Limits_Help">To ustawienie zasad pozwala skonfigurować limity bezstanowej autokonfiguracji protokołu IP.

Jeśli to ustawienie zasad zostanie włączone lub nie zostanie skonfigurowane, limity bezstanowej autokonfiguracji protokołu IP będą włączone i system będzie ograniczał liczbę automatycznie skonfigurowanych adresów i tras.

Jeśli to ustawienie zasad zostanie wyłączone, limity bezstanowej autokonfiguracji protokołu IP będą wyłączone i system nie będzie ograniczał liczby automatycznie skonfigurowanych adresów i tras.



</string>

</stringTable>

<presentationTable>

      <presentation id="StateSelection">
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Dokonaj wyboru spośród następujących stanów:</dropdownList>
      </presentation>

      <presentation id="RouterName">
        <textBox refId="RouterNameBox">
          <label>Wprowadź nazwę routera lub przekaźnika:</label>
        </textBox>
      </presentation>

      <presentation id="RouterNameResolutionInterval">
        <text>To ustawienie umożliwia dostosowanie interwału rozpoznawania nazwy przekaźnika 6to4</text>
        <decimalTextBox refId="RouterNameResolutionIntervalBox" defaultValue="1440">Minuty:  </decimalTextBox>
      </presentation>

      <presentation id="TeredoServerName">
        <textBox refId="TeredoServerNameBox">
          <label>Wprowadź nazwę serwera Teredo:</label>
        </textBox>
      </presentation>

      <presentation id="TeredoRefreshRate">
        <text>To ustawienie umożliwia dostosowanie szybkości odświeżania klienta Teredo</text>
        <text>Zakres wynosi od 1 do 86 400 sekund (1 dzień).</text>
        <decimalTextBox refId="TeredoRefreshRateBox" defaultValue="30">Sekundy:  </decimalTextBox>
      </presentation>

      <presentation id="TeredoClientPort">
        <text>To ustawienie umożliwia dostosowanie portu klienta Teredo</text>
        <text>Port musi należeć do zakresu od 0 do 65535. Wartość domyślna (zalecana) to 0; ta wartość umożliwia systemowi lokalnemu wybranie portu.</text>
        <decimalTextBox refId="TeredoClientPortBox" defaultValue="0"> </decimalTextBox>
      </presentation>

      <presentation id="IPHTTPS_ClientState">
        <textBox refId="IPHTTPSClientUrlBox">
          <label>Wprowadź adres URL serwera IP-HTTPS:</label>
        </textBox>
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Wybierz stan interfejsu spośród następujących opcji:</dropdownList>
      </presentation>

</presentationTable>

</resources>
</policyDefinitionResources>
