<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="P2P_Disabled">Wyłącz usługi sieci równorzędnej firmy Microsoft</string>
      <string id="P2P_Disabled_Explain">To ustawienie całkowicie wyłącza usługi sieci równorzędnej firmy Microsoft i w rezultacie powoduje zatrzymanie pracy wszystkich aplikacji zależnych.

Protokoły sieci równorzędnej zezwalają na aplikacje w obszarach RTC, współpracy, dystrybucji zawartości i przetwarzania rozproszonego.

Jeśli to ustawienie zostanie włączone, protokoły sieci równorzędnej zostaną wyłączone.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, protokoły sieci równorzędnej zostaną włączone.</string>
      <string id="P2P_Name">Usługi sieci równorzędnej firmy Microsoft</string>
      <string id="PNRP_DisableMulticastBootstrap">Wyłącz inicjowanie multiemisji</string>
      <string id="PNRP_DisableMulticastBootstrapGlobal_Explain">To ustawienie uniemożliwia protokołowi PNRP anonsowanie komputera lub wyszukiwanie innych komputerów w podsieci lokalnej w chmurze globalnej.

Protokół rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol) umożliwia rozpoznanie nazwy do adresu IPV6 i numeru portu. Jednym ze sposobów, których protokół PNRP używa do ładowania się, jest multiemisja w tej samej podsieci. Protokół PNRP publikuje się w podsieci lokalnej, aby inne komputery mogły go użyć w razie potrzeby.

Jeśli to ustawienie zostanie włączone, protokół PNRP nie użyje multiemisji do ładowania. Określenie tego klucza rejestru zapobiega scenariuszom, w których nie ma serwera referencyjnego dla ładowania, takim jak scenariusze sieciowe ad hoc.

Jeśli to ustawienie zostanie wyłączone, protokół PNRP użyje multiemisji do załadowania się w tej samej podsieci.

Jeśli to ustawienie nie zostanie skonfigurowane, protokół powróci do korzystania z publicznego klucza rejestru przy ustalaniu, czy ma publikować komputer lub użyć multiemisji do wyszukania innych komputerów w podsieci lokalnej.

Protokół multiemisji używany do ładowania to SSDP (Simple Service Discovery Protocol). Usługa SSDP musi być włączona (jest włączona domyślnie), aby ta zasada działała.</string>
      <string id="PNRP_Disabled">Wyłącz tworzenie chmur protokołu PNRP</string> 
      <string id="PNRP_SearchOnly">Ustaw opcję tylko rozpoznawania dla chmury protokołu PNRP</string> 
      <string id="PNRP_DisabledGlobal_Explain">To ustawienie zasad włącza lub wyłącza tworzenie chmur protokołu PNRP.

Protokół PNRP (Peer Name Resolution Protocol) jest rozproszonym protokołem rozpoznawania nazw, który umożliwia hostom internetowym publikowanie nazw elementów równorzędnych i odpowiadających im adresów protokołu Internet Protocol w wersji 6 (IPv6). Inne hosty mogą dzięki temu rozpoznawać nazwy, pobierać odpowiadające im adresy i ustanawiać połączenia równorzędne (P2P). Chmura protokołu PNRP to grupa połączonych węzłów PNRP, w której istnieją połączenia między elementami równorzędnymi, co umożliwia węzłom w chmurze PNRP rozpoznawanie nazw publikowanych przez inne węzły.

Protokół PNRP tworzy chmurę globalną, jeśli komputer ma globalny adres IPv6, a chmurę lokalną lokacji, jeśli komputer ma adres lokalny.

Jeżeli to ustawienie zasad zostanie włączone, protokół PNRP nie będzie tworzyć chmury, a aplikacje nie będą mogły jej używać do publikowania i rozpoznawania nazw, niezależnie od tego, czy komputer ma adres IPv6 pasujący do zakresu chmury.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, wszystkie chmury protokołu PNRP będą domyślnie włączone, a protokół PNRP będzie tworzyć chmurę, jeśli adres IPv6 komputera będzie zgodny z zakresem chmury.
</string>
      <string id="PNRP_SearchOnlyGlobal_Explain">To ustawienie zasad nakłada na węzeł ograniczenie zezwalające na rozpoznawanie nazw w określonej chmurze protokołu rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol), ale nie na publikowanie nazw.

To ustawienie zasad powoduje, że komputery działają jako klienci w scenariuszach komunikacji równorzędnej (P2P). Komputer kliencki może na przykład wykrywać inne komputery i inicjować sesje rozmów z nimi, ale inne komputery nie mogą wykrywać tego klienta ani inicjować z nim sesji.

Jeżeli to ustawienie zasad zostanie włączone, ten komputer nie będzie mógł rejestrować nazw protokołu PNRP ani pomagać innym komputerom w wyszukiwaniu nazw protokołu PNRP.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, ten komputer będzie mógł publikować nazwy protokołu PNRP i pomagać innym komputerom w wyszukiwaniu nazw protokołu PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapLinkLocal_Explain">To ustawienie uniemożliwia protokołowi PNRP anonsowanie komputera lub wyszukiwanie innych komputerów w podsieci lokalnej w chmurze połączenia lokalnego.

Protokół rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol) umożliwia rozpoznanie nazwy do adresu IPV6 i numeru portu. Jednym ze sposobów, których protokół PNRP używa do ładowania się, jest multiemisja w tej samej podsieci. Protokół PNRP publikuje się w podsieci lokalnej, aby inne komputery mogły go użyć w razie potrzeby.

Jeśli to ustawienie zostanie włączone, protokół PNRP nie użyje multiemisji do ładowania. Określenie tego klucza rejestru zapobiega scenariuszom, w których nie ma serwera referencyjnego dla ładowania, takim jak scenariusze sieciowe ad hoc.

Jeśli to ustawienie zostanie wyłączone, protokół PNRP użyje multiemisji do załadowania się w tej samej podsieci.

Jeśli to ustawienie nie zostanie skonfigurowane, protokół powróci do korzystania z publicznego klucza rejestru przy ustalaniu, czy ma publikować komputer lub użyć multiemisji do wyszukania innych komputerów w podsieci lokalnej.

Protokół multiemisji używany do ładowania to SSDP (Simple Service Discovery Protocol). Usługa SSDP musi być włączona (jest włączona domyślnie), aby ta zasada działała.</string>
      <string id="PNRP_DisabledLinkLocal_Explain">To ustawienie zasad włącza lub wyłącza tworzenie chmur protokołu PNRP.

Protokół PNRP (Peer Name Resolution Protocol) jest rozproszonym protokołem rozpoznawania nazw, który umożliwia hostom internetowym publikowanie nazw elementów równorzędnych i odpowiadających im adresów protokołu Internet Protocol w wersji 6 (IPv6). Inne hosty mogą dzięki temu rozpoznawać nazwy, pobierać odpowiadające im adresy i ustanawiać połączenia równorzędne (P2P). Chmura protokołu PNRP to grupa połączonych węzłów PNRP, w której istnieją połączenia między elementami równorzędnymi, co umożliwia węzłom w chmurze PNRP rozpoznawanie nazw publikowanych przez inne węzły.

Protokół PNRP tworzy chmurę globalną, jeśli komputer ma globalny adres IPv6, a chmurę lokalną lokacji, jeśli komputer ma adres lokalny.

Jeżeli to ustawienie zasad zostanie włączone, protokół PNRP nie będzie tworzyć chmury, a aplikacje nie będą mogły jej używać do publikowania i rozpoznawania nazw, niezależnie od tego, czy komputer ma adres IPv6 pasujący do zakresu chmury.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, wszystkie chmury protokołu PNRP będą domyślnie włączone, a protokół PNRP będzie tworzyć chmurę, jeśli adres IPv6 komputera będzie zgodny z zakresem chmury.
</string>
      <string id="PNRP_SearchOnlyLinkLocal_Explain">To ustawienie zasad nakłada na węzeł ograniczenie zezwalające na rozpoznawanie nazw w określonej chmurze protokołu rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol), ale nie na publikowanie nazw.

To ustawienie zasad powoduje, że komputery działają jako klienci w scenariuszach komunikacji równorzędnej (P2P). Komputer kliencki może na przykład wykrywać inne komputery i inicjować sesje rozmów z nimi, ale inne komputery nie mogą wykrywać tego klienta ani inicjować z nim sesji.

Jeżeli to ustawienie zasad zostanie włączone, ten komputer nie będzie mógł rejestrować nazw protokołu PNRP ani pomagać innym komputerom w wyszukiwaniu nazw protokołu PNRP.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, ten komputer będzie mógł publikować nazwy protokołu PNRP i pomagać innym komputerom w wyszukiwaniu nazw protokołu PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapSiteLocal_Explain">To ustawienie uniemożliwia protokołowi PNRP anonsowanie komputera lub wyszukiwanie innych komputerów w podsieci lokalnej w chmurze lokacji lokalnej.

Protokół rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol) umożliwia rozpoznanie nazwy do adresu IPV6 i numeru portu. Jednym ze sposobów, których protokół PNRP używa do ładowania się, jest multiemisja w tej samej podsieci. Protokół PNRP publikuje się w podsieci lokalnej, aby inne komputery mogły go użyć w razie potrzeby.

Jeśli to ustawienie zostanie włączone, protokół PNRP nie użyje multiemisji do ładowania. Określenie tego klucza rejestru zapobiega scenariuszom, w których nie ma serwera referencyjnego dla ładowania, takim jak scenariusze sieciowe ad hoc.

Jeśli to ustawienie zostanie wyłączone, protokół PNRP użyje multiemisji do załadowania się w tej samej podsieci.

Jeśli to ustawienie nie zostanie skonfigurowane, protokół powróci do korzystania z publicznego klucza rejestru przy ustalaniu, czy ma publikować komputer lub użyć multiemisji do wyszukania innych komputerów w podsieci lokalnej.

Protokół multiemisji używany do ładowania to SSDP (Simple Service Discovery Protocol). Usługa SSDP musi być włączona (jest włączona domyślnie), aby ta zasada działała.</string>
      <string id="PNRP_DisabledSiteLocal_Explain">To ustawienie zasad włącza lub wyłącza tworzenie chmur protokołu PNRP.

Protokół PNRP (Peer Name Resolution Protocol) jest rozproszonym protokołem rozpoznawania nazw, który umożliwia hostom internetowym publikowanie nazw elementów równorzędnych i odpowiadających im adresów protokołu Internet Protocol w wersji 6 (IPv6). Inne hosty mogą dzięki temu rozpoznawać nazwy, pobierać odpowiadające im adresy i ustanawiać połączenia równorzędne (P2P). Chmura protokołu PNRP to grupa połączonych węzłów PNRP, w której istnieją połączenia między elementami równorzędnymi, co umożliwia węzłom w chmurze PNRP rozpoznawanie nazw publikowanych przez inne węzły.

Protokół PNRP tworzy chmurę globalną, jeśli komputer ma globalny adres IPv6, a chmurę lokalną lokacji, jeśli komputer ma adres lokalny.

Jeżeli to ustawienie zasad zostanie włączone, protokół PNRP nie będzie tworzyć chmury, a aplikacje nie będą mogły jej używać do publikowania i rozpoznawania nazw, niezależnie od tego, czy komputer ma adres IPv6 pasujący do zakresu chmury.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, wszystkie chmury protokołu PNRP będą domyślnie włączone, a protokół PNRP będzie tworzyć chmurę, jeśli adres IPv6 komputera będzie zgodny z zakresem chmury.
</string>
      <string id="PNRP_SearchOnlySiteLocal_Explain">To ustawienie zasad nakłada na węzeł ograniczenie zezwalające na rozpoznawanie nazw w określonej chmurze protokołu rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol), ale nie na publikowanie nazw.

To ustawienie zasad powoduje, że komputery działają jako klienci w scenariuszach komunikacji równorzędnej (P2P). Komputer kliencki może na przykład wykrywać inne komputery i inicjować sesje rozmów z nimi, ale inne komputery nie mogą wykrywać tego klienta ani inicjować z nim sesji.

Jeżeli to ustawienie zasad zostanie włączone, ten komputer nie będzie mógł rejestrować nazw protokołu PNRP ani pomagać innym komputerom w wyszukiwaniu nazw protokołu PNRP.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, ten komputer będzie mógł publikować nazwy protokołu PNRP i pomagać innym komputerom w wyszukiwaniu nazw protokołu PNRP.
</string> 
      <string id="PNRP_Global">Chmury globalne</string>
      <string id="PNRP_LinkLocal">Chmury połączeń lokalnych</string>
      <string id="PNRP_Name">Protokół rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol)</string>
      <string id="PNRP_SeedServer">Ustaw serwer referencyjny</string>
      <string id="PNRP_SeedServerGlobal_Explain">To ustawienie ustawia serwer referencyjny dla chmury globalnej dla określonego węzła w przedsiębiorstwie. Protokół rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol) umożliwia rozpoznanie nazwy do adresu IPV6 i numeru portu. W niektórych przypadkach protokół wymaga serwera referencyjnego, z którego może się załadować.

Każdy węzeł sieci równorzędnej może być używany jako serwer referencyjny. Serwer referencyjny nie wymaga konfiguracji.

To ustawienie zapewnia większą elastyczność w umożliwianiu użytkownikom korzystania z ich aplikacji równorzędnych w domu. Istnieją cztery sposoby użycia tego ustawienia do sterowania serwerem referencyjnym w firmie.

1. Aby używać tylko globalnego, dobrze znanego serwera referencyjnego w Internecie, włącz ustawienie; pozostaw pustą listę serwera referencyjnego, pozostaw niezaznaczone pole wyboru.

2. Aby używać tylko serwera referencyjnego firmy, włącz ustawienie; wprowadź w pełni kwalifikowaną nazwę domeny lub adres IPV6 serwera referencyjnego firmy, a następnie zaznacz pole wyboru. Używanie tylko serwera referencyjnego firmy spowoduje, że użytkownicy mobilni nie będą mogli korzystać z aplikacji równorzędnych w domu.

3. Aby używać serwera referencyjnego firmy i globalnego serwera referencyjnego, włącz ustawienie; wprowadź w pełni kwalifikowaną nazwę domeny lub adres IPV6 serwera referencyjnego firmy i pozostaw niezaznaczone pole wyboru. To jest ustawienie, które umożliwi użytkownikom mobilnym korzystanie z aplikacji równorzędnych w domu i w pracy.

4. Aby nie używać żadnego serwera referencyjnego, włącz ustawienie; nie wprowadzaj nazwy serwera referencyjnego, a następnie zaznacz pole wyboru.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, protokół powróci do korzystania z publicznego klucza rejestru przy ustalaniu, z którego serwera referencyjnego ma się załadować.</string>
      <string id="PNRP_SeedServerLinkLocal_Explain">To ustawienie ustawia serwer referencyjny dla chmury połączenia lokalnego dla określonego węzła w przedsiębiorstwie.

Protokół rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol) umożliwia rozpoznanie nazwy do adresu IPV6 i numeru portu. W niektórych przypadkach protokół wymaga serwera referencyjnego, z którego może się załadować. To ustawienie pozwala ustawić serwer referencyjny dla określonego węzła w przedsiębiorstwie.

Aby użyć tego ustawienia, kliknij opcję Włącz, a następnie wprowadź rozdzielaną średnikami listę adresów IPV6 w dostępnym polu. Jeśli to ustawienie zostanie włączone i nie zostanie wprowadzony żaden adres, nie będzie używany żaden serwer referencyjny.

Jeśli to ustawienie nie zostanie skonfigurowane, protokół powróci do korzystania z publicznego klucza rejestru przy ustalaniu, z którego serwera referencyjnego ma się załadować.</string>
      <string id="PNRP_SeedServerSiteLocal_Explain">To ustawienie ustawia serwer referencyjny dla chmury lokacji lokalnej dla określonego węzła w przedsiębiorstwie.

Protokół rozpoznawania nazw równorzędnych (PNRP, Peer Name Resolution Protocol) umożliwia rozpoznanie nazwy do adresu IPV6 i numeru portu. W niektórych przypadkach protokół wymaga serwera referencyjnego, z którego może się załadować. To ustawienie pozwala ustawić serwer referencyjny dla określonego węzła w przedsiębiorstwie.

Aby użyć tego ustawienia, kliknij opcję Włącz, a następnie wprowadź rozdzielaną średnikami listę nazw DNS lub adresów IPV6 w dostępnym polu. Jeśli to ustawienie zostanie włączone i nie zostanie wprowadzony żaden adres, nie będzie używany żaden serwer referencyjny.

Jeśli to ustawienie nie zostanie skonfigurowane, protokół powróci do korzystania z publicznego klucza rejestru przy ustalaniu, z którego serwera referencyjnego ma się załadować.</string>
      <string id="PNRP_SiteLocal">Chmury lokacji lokalnych</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement">Wyłącz sprawdzanie poprawności siły hasła w grupach równorzędnych</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement_Explain">Po utworzeniu grupy równorzędnej, która zezwala na uwierzytelnianie przy użyciu hasła (lub po zmianie hasła dla takiej grupy), domyślnie jest sprawdzana zgodność tego hasła z wymaganiami dotyczącymi złożoności haseł obowiązującymi w systemie lokalnym. Dlatego dla grupy równorzędnej nie będzie można używać haseł słabszych niż te, które zostałyby dozwolone jako hasła logowania.

To ustawienie steruje zachowaniem sprawdzania poprawności hasła. Po ustawieniu wartości 1 sprawdzanie poprawności nie jest wykonywane i wszystkie hasła są dozwolone. Po ustawieniu wartości 0 sprawdzanie poprawności będzie wykonywane.
      </string>
    </stringTable>
    <presentationTable>
      <presentation id="PNRP_SeedServer_1">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Serwer referencyjny</label>
        </textBox>
        <checkBox refId="PNRP_Dont_Include_Microsoft_SeedServer">Nie używaj globalnego serwera referencyjnego PNRP firmy Microsoft</checkBox>
      </presentation>
      <presentation id="PNRP_SeedServer_2">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Serwer referencyjny</label>
        </textBox>
      </presentation>
      <presentation id="PNRP_SeedServer_3">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Serwer referencyjny</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
