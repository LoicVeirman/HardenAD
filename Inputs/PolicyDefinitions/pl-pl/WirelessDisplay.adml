<?xml version="1.0" encoding="utf-8"?>
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://www.microsoft.com/GroupPolicy/PolicyDefinitions">
  <displayName>Połącz</displayName>
  <description>Zaznacz element, aby wyświetlić jego opis</description>
  <resources>
    <stringTable>
      <!-- general -->
      <string id="Connect">Połącz</string>
      
      <!-- prevent file sync-->
      <string id="AllowProjectionToPC">Nie zezwalaj na projekcję na tym komputerze</string>
      <string id="AllowProjectionToPC_help">To ustawienie zasad umożliwia wyłączenie projekcji na komputerze.  

 

Jeśli to ustawienie zostanie włączone, nie będzie można wykryć komputera i wyświetlać na nim obrazu, o ile użytkownik nie uruchomi ręcznie aplikacji Ekran bezprzewodowy.  

 

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, będzie można wykryć komputer i wyświetlać na nim obraz tylko na powyższym ekranie blokady. Użytkownik może też na stałe włączyć lub wyłączyć to ustawienie z wyjątkiem ręcznego uruchomienia.</string>

      <string id="RequirePinForPairing">Wymagaj numeru PIN do parowania</string>
      <string id="RequirePinForPairing_help">To ustawienie zasad umożliwia włączenie wymagania kodu PIN podczas parowania.  

Jeśli określisz dla tego ustawienia wartość Nigdy, kod PIN nie będzie wymagany podczas parowania.

Jeśli określisz dla tego ustawienia wartość Za pierwszym razem, podczas parowania nowych urządzeń będzie wymagany kod PIN.

Jeśli określisz dla tego ustawienia wartość Zawsze, kod PIN będzie wymagany podczas każdego parowania.</string>
      <string id="RequirePinForPairing_0">Nigdy</string>
      <string id="RequirePinForPairing_1">Za pierwszym razem</string>
      <string id="RequirePinForPairing_2">Zawsze</string>
   </stringTable>
   <presentationTable>
      <presentation id="RequirePinForPairing">
        <dropdownList refId="RequirePinForPairingDropdown" noSort="true" defaultItem="0">Wybierz jedną z następujących akcji</dropdownList>
      </presentation>
   </presentationTable>
  </resources>
</policyDefinitionResources>
