<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Przenośny obszar roboczy</displayName>
  <description>Ten plik zawiera ustawienia zasad przenośnego obszaru roboczego.</description>
  <resources>
    <stringTable>
      <string id="PortableOperatingSystem">Przenośny system operacyjny</string>
      <string id="PortableOperatingSystem_Launcher_DisplayName">Domyślne opcje uruchamiania funkcji Windows To Go</string>
      <string id="PortableOperatingSystem_Launcher_Help">



To ustawienie zasad określa, czy rozruch komputera będzie następował przy użyciu funkcji Windows To Go, jeśli zostanie podłączone urządzenie USB zawierające obszar roboczy funkcji Windows To Go, oraz czy użytkownicy mogą wprowadzać zmiany przy użyciu elementu Opcje uruchamiania obszaru roboczego Windows To Go w Panelu sterowania.

Jeśli to ustawienie zostanie włączone, rozruch przy użyciu funkcji Windows To Go po podłączeniu urządzenia USB będzie włączony, a użytkownicy nie będą mogli wprowadzać zmian przy użyciu elementu Opcje uruchamiania obszaru roboczego Windows To Go w Panelu sterowania.

Jeśli to ustawienie zasad zostanie wyłączone, rozruch przy użyciu funkcji Windows To Go po podłączeniu urządzenia USB nie będzie możliwy, o ile użytkownik nie skonfiguruje tej opcji ręcznie w systemie BIOS lub w innej konfiguracji kolejności rozruchu.

Jeśli to ustawienie pozostanie nieskonfigurowane, użytkownicy należący do grupy Administratorzy będą mogli wprowadzać zmiany przy użyciu elementu Opcje uruchamiania obszaru roboczego Windows To Go w Panelu sterowania.</string>
      <string id="PortableOperatingSystem_Hibernate_DisplayName">Zezwalaj na hibernację (stan S4) przy uruchamianiu z obszaru roboczego funkcji Windows To Go</string>
      <string id="PortableOperatingSystem_Hibernate_Help">



Określa, czy komputer może używać stanu uśpienia hibernacji (S4), jeśli został uruchomiony z obszaru roboczego funkcji Windows To Go.

Jeśli to ustawienie zasad zostanie włączone, po uruchomieniu systemu Windows z obszaru roboczego funkcji Windows To Go będzie można zahibernować komputer.

Jeśli to ustawienie zasad zostanie wyłączone lub pozostanie nieskonfigurowane, w systemie Windows nie będzie można zahibernować komputera, jeśli został uruchomiony z obszaru roboczego funkcji Windows To Go.</string>
      <string id="PortableOperatingSystem_Sleep_DisplayName">Nie zezwalaj na stany uśpienia wstrzymania (S1–S3) przy uruchamianiu z obszaru roboczego funkcji Windows To Go</string>
      <string id="PortableOperatingSystem_Sleep_Help">



Określa, czy komputer może używać stanów uśpienia wstrzymania (S1–S3) przy uruchamianiu z obszaru roboczego funkcji Windows To Go.

Jeśli to ustawienie zostanie włączone, po uruchomieniu systemu Windows z obszaru roboczego funkcji Windows To Go nie będzie można używać stanów wstrzymania do uśpienia komputera.

Jeśli to ustawienie zostanie wyłączone lub pozostanie nieskonfigurowane, po uruchomieniu systemu Windows z obszaru roboczego funkcji Windows To Go będzie można używać stanów wstrzymania do uśpienia komputera.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
