<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź nazwę wyświetlaną</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="AM_AM">Menedżer załączników</string>
      <string id="AM_CallIOfficeAntiVirus">Powiadamiaj programy antywirusowe podczas otwierania załączników</string>
      <string id="AM_EstimateFileHandlerRisk">Logika zaufania dla plików załączników</string>
      <string id="AM_ExplainCallIOfficeAntiVirus">To ustawienie zasad umożliwia zarządzanie zachowaniem dotyczącym powiadamiania zarejestrowanych programów antywirusowych. Jeśli zarejestrowano wiele programów, wszystkie będą powiadamiane. Jeśli zarejestrowany program antywirusowy już przeprowadza testy dostępu lub skanuje pliki pojawiające się na serwerze poczty e-mail komputera, dodatkowe wywołania będą zbędne. 

Jeśli to ustawienie zasad zostanie włączone, system Windows zaleci zarejestrowanemu programowi antywirusowemu skanowanie pliku przy otwieraniu pliku załącznika przez użytkownika. Jeśli program antywirusowy zawiedzie, możliwość otwierania załącznika zostanie zablokowana.

Jeśli to ustawienie zasad zostanie wyłączone, w przypadku otwierania załączników system Windows nie będzie wywoływał zarejestrowanych programów antywirusowych.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, w przypadku otwierania załączników system Windows nie będzie wywoływał zarejestrowanych programów antywirusowych.</string>
      <string id="AM_ExplainEstimateFileHandlerRisk">To ustawienie zasad umożliwia skonfigurowanie logiki używanej przez system Windows do określania ryzyka dotyczącego plików załączników.

Funkcja Preferowanie obsługi plików instruuje system Windows, aby zostały użyte dane obsługi plików zamiast danych typów plików. Na przykład: ufaj programowi notepad.exe, ale nie ufaj plikom txt.

Funkcja Preferowanie typu pliku instruuje system Windows, aby zostały użyte dane typów plików zamiast danych obsługi plików. Na przykład: ufaj plikom txt bez względu na obsługę plików.

Użycie zarówno danych obsługi plików, jaki i danych typów plików jest opcją najbardziej ograniczającą. System Windows wybiera to zalecenie, które jest bardziej ograniczające, co powoduje wyświetlanie większej liczby monitów dotyczących zaufania niż w przypadku wybrania innych opcji.

Jeśli to ustawienie zasad zostanie włączone, będzie można wybrać kolejność przetwarzania przez system Windows danych dotyczących oceny ryzyka.

Jeśli to ustawienie zasad zostanie wyłączone, system Windows będzie korzystać z domyślnej logiki zaufania, w której preferowana jest obsługa plików, a nie typ plików.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, system Windows będzie korzystać z domyślnej logiki zaufania, w której preferowana jest obsługa plików, a nie typ plików.</string>
      <string id="AM_ExplainMarkZoneOnSavedAtttachments">To ustawienie zasad umożliwia zarządzanie procesem oznaczania przez system Windows plików załączników przy użyciu informacji o strefie pochodzenia (np. z ograniczeniami, Internet, sieć intranet, lokalna). Wymaga to systemu NTFS do poprawnego działania i w systemie FAT32 spowoduje błąd bez uprzedzenia. Bez zachowania informacji o strefie system Windows nie może dokonać właściwej oceny ryzyka.

Jeśli to ustawienie zasad zostanie włączone, system Windows nie będzie oznaczać plików załączników przy użyciu informacji o ich strefie.

Jeśli to ustawienie zasad zostanie wyłączone, system Windows będzie oznaczać pliki załączników przy użyciu informacji o ich strefie.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, system Windows będzie oznaczać pliki załączników przy użyciu informacji o ich strefie.</string>
      <string id="AM_ExplainRemoveZoneInfo">To ustawienie zasad umożliwia zarządzanie możliwością ręcznego usuwania przez użytkowników informacji o strefie z zapisanych plików załączników przez kliknięcie przycisku Odblokuj w arkuszu właściwości pliku lub użycie pola wyboru w oknie dialogowym ostrzeżenia o zabezpieczeniach. Usunięcie informacji o strefie pozwoli użytkownikom otwierać potencjalnie niebezpieczne pliki załączników, które zostały zablokowane przez system Windows przed otwarciem.

Jeśli to ustawienie zasad zostanie włączone, system Windows ukryje to pole wyboru oraz przycisk Odblokuj.

Jeśli to ustawienie zasad zostanie wyłączone, system Windows wyświetli to pole wyboru oraz przycisk Odblokuj.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, system Windows ukryje to pole wyboru oraz przycisk Odblokuj.</string>
      <string id="AM_ExplainSetFileRiskLevel">To ustawienie zasad umożliwia zarządzanie domyślnym poziomem ryzyka dla typów plików. Aby w pełni dostosować poziom ryzyka dla plików załączników, konieczne może być również skonfigurowanie logiki zaufania dla plików załączników.

Wysokie ryzyko: jeśli plik załącznika znajduje się na liście typów plików wysokiego ryzyka i pochodzi ze strefy z ograniczeniami, system Windows zablokuje użytkownikowi dostęp do pliku. Jeśli plik pochodzi ze strefy Internet, system Windows wyświetli monit przed udostępnieniem użytkownikowi tego pliku.

Średnie ryzyko: jeśli załącznik znajduje się na liście typów plików średniego ryzyka i pochodzi ze strefy z ograniczeniami lub strefy Internet, system Windows wyświetli monit przed udostępnieniem użytkownikowi tego pliku.

Niskie ryzyko: jeśli załącznik znajduje się na liście typów plików niskiego ryzyka, system Windows nie wyświetli monitu przed udostępnieniem użytkownikowi tego pliku, niezależnie od informacji o strefie pliku.

Jeśli to ustawienie zasad zostanie włączone, będzie można określić domyślny poziom ryzyka dla typów plików.

Jeśli to ustawienie zasad zostanie wyłączone, jako domyślny poziom ryzyka w systemie Windows zostanie ustawiony poziom średni.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, jako domyślny poziom ryzyka w systemie Windows zostanie ustawiony poziom średni.</string>
      <string id="AM_ExplainSetHighRiskInclusion">To ustawienie zasad umożliwia skonfigurowanie listy typów plików wysokiego ryzyka. Jeśli plik załącznika znajduje się na liście typów plików wysokiego ryzyka i pochodzi ze strefy z ograniczeniami, system Windows zablokuje użytkownikowi dostęp do pliku. Jeśli plik pochodzi ze strefy Internet, system Windows wyświetli monit przed udostępnieniem użytkownikowi tego pliku. Ta lista dołączania ma pierwszeństwo przed listami dołączania dotyczącymi średniego i niskiego ryzyka (gdzie rozszerzenie znajduje się na więcej niż jednej liście dołączania).

Jeśli to ustawienie zasad zostanie włączone, będzie można utworzyć niestandardową listę typów plików wysokiego ryzyka.

Jeśli to ustawienie zasad zostanie wyłączone, system Windows będzie korzystać z wbudowanej listy typów plików stwarzających wysokie ryzyko.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, system Windows będzie korzystać z wbudowanej listy typów plików wysokiego ryzyka.</string>
      <string id="AM_ExplainSetLowRiskInclusion">To ustawienie zasad umożliwia skonfigurowanie listy typów plików niskiego ryzyka. Jeśli załącznik znajduje się na liście typów plików niskiego ryzyka, system Windows nie wyświetli monitu przed udostępnieniem użytkownikowi tego pliku, niezależnie od informacji o strefie pliku. Ta lista dołączania zastępuje listę typów plików wysokiego ryzyka, która jest wbudowana w systemie Windows, i ma niższy priorytet niż listy dołączania dotyczące wysokiego lub średniego ryzyka (gdzie rozszerzenie znajduje się na więcej niż jednej liście dołączania).

Jeśli to ustawienie zasad zostanie włączone, będzie można określić typy plików, które stwarzają niskie ryzyko.

Jeśli to ustawienie zasad zostanie wyłączone, system Windows będzie korzystać z domyślnej logiki zaufania.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, system Windows będzie korzystać z domyślnej logiki zaufania.</string>
      <string id="AM_ExplainSetModRiskInclusion">To ustawienie zasad umożliwia skonfigurowanie listy typów plików średniego ryzyka. Jeśli załącznik znajduje się na liście typów plików średniego ryzyka i pochodzi ze strefy z ograniczeniami lub strefy Internet, system Windows wyświetli monit przed udostępnieniem użytkownikowi tego pliku. Ta lista dołączania zastępuje listę typów plików potencjalnie wysokiego ryzyka, która jest wbudowana w systemie Windows, i ma pierwszeństwo przed listą dołączania dotyczącą niskiego ryzyka, ale ma niższy priorytet niż lista dołączania dotycząca wysokiego ryzyka (gdzie rozszerzenie znajduje się na więcej niż jednej liście dołączania).

Jeśli to ustawienie zasad zostanie włączone, będzie można określić typy plików, które stwarzają średnie ryzyko.

Jeśli to ustawienie zasad zostanie wyłączone, system Windows będzie korzystać z domyślnej logiki zaufania.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, system Windows będzie korzystać z domyślnej logiki zaufania.</string>
      <string id="AM_HandlerBased">Preferowanie obsługi plików</string>
      <string id="AM_HandlerDisabled">Preferowanie typu pliku</string>
      <string id="AM_HandlerMostSecure">Sprawdzanie obsługi i typu pliku</string>
      <string id="AM_HighRisk">Wysokie ryzyko</string>
      <string id="AM_LowRisk">Niskie ryzyko</string>
      <string id="AM_MarkZoneOnSavedAtttachments">Nie zachowuj informacji o strefie w plikach załączników</string>
      <string id="AM_ModRisk">Średnie ryzyko</string>
      <string id="AM_RemoveZoneInfo">Ukryj mechanizmy do usuwania informacji o strefie</string>
      <string id="AM_SetFileRiskLevel">Domyślny poziom ryzyka dla plików załączników</string>
      <string id="AM_SetHighRiskInclusion">Lista dołączania dla typów plików wysokiego ryzyka</string>
      <string id="AM_SetLowRiskInclusion">Lista dołączania dla typów plików niskiego ryzyka</string>
      <string id="AM_SetModRiskInclusion">Lista dołączania dla typów plików średniego ryzyka</string>
    </stringTable>
    <presentationTable>
      <presentation id="AM_EstimateFileHandlerRisk">
        <dropdownList refId="AM_TrustLogicSetting" defaultItem="0">Określ ryzyko według</dropdownList>
      </presentation>
      <presentation id="AM_SetFileRiskLevel">
        <dropdownList refId="AM_RiskLevel" defaultItem="1">Ustaw domyślny poziom ryzyka</dropdownList>
      </presentation>
      <presentation id="AM_SetHighRiskInclusion">
        <textBox refId="AM_InstructHighRiskInclusionList">
          <label>Określ rozszerzenia wysokiego ryzyka (razem z kropką na początku, na przykład .cmd, .exe).</label>
        </textBox>
      </presentation>
      <presentation id="AM_SetLowRiskInclusion">
        <textBox refId="AM_InstructLowRiskInclusionList">
          <label>Określ rozszerzenia niskiego ryzyka (razem z kropką na początku, na przykład .bmp, .gif).</label>
        </textBox>
      </presentation>
      <presentation id="AM_SetModRiskInclusion">
        <textBox refId="AM_InstructModRiskInclusionList">
          <label>Określ rozszerzenia średniego ryzyka (razem z kropką na początku, na przykład .doc, .pdf).</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
