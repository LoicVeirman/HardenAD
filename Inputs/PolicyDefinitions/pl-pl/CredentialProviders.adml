<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź nazwę wyświetlaną</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="DefaultLogonDomain">Przypisz domyślną domenę logowania</string>
      <string id="DefaultLogonDomain_Help">To ustawienie zasad określa domyślną domenę logowania, która może się różnić od domeny, do której komputer jest przyłączony. Bez tego ustawienia zasad, jeśli podczas logowania użytkownik nie określi domeny logowania, jako domena domyślna zostanie przyjęta domena, do której należy komputer. Na przykład jeśli komputer należy do domeny Fabrikam, domyślną domeną logowania użytkownika będzie właśnie Fabrikam. 

Jeśli to ustawienie zasad zostanie włączone, domyślną domeną logowania będzie określona domena, która może różnić się od domeny, do której jest przyłączony komputer. 

Jeśli to ustawienie zasad zostanie wyłączone lub pozostanie nieskonfigurowane, domyślną domeną logowania będzie zawsze domena, do której jest przyłączony komputer.</string>
      <string id="ExcludedCredentialProviders">Wyklucz dostawców poświadczeń</string>
      <string id="ExcludedCredentialProviders_Help">To ustawienie zasad umożliwia administratorowi wykluczanie określonych dostawców poświadczeń z procedury uwierzytelniania.

Uwaga: dostawcy poświadczeń odpowiadają za wykonywanie operacji na poświadczeniach
użytkownika i ich weryfikowanie w trakcie logowania oraz w sytuacjach, gdy jest wymagane uwierzytelnianie.
System Windows Vista jest domyślnie wyposażony w dwa mechanizmy dostawców poświadczeń:
hasła i karty inteligentne. Administrator może zainstalować dodatkowych dostawców,
którzy będą odpowiadać za obsługę różnych innych zestawów poświadczeń
(np. związanych z uwierzytelnianiem przy użyciu biometrii).

Jeśli te zasady zostaną włączone, administrator może określić identyfikatory CLSID
zainstalowanych dostawców poświadczeń, którzy mają być wykluczeni z procesów uwierzytelniania.

Jeśli te zasady zostaną wyłączone lub pozostaną nieskonfigurowane, wszyscy dostawcy poświadczeń, zainstalowani lub włączeni w inny sposób, będą dostępni na potrzeby uwierzytelniania.</string>
      <string id="Logon">Logowanie</string>
      <string id="AllowDomainPINLogon">Włącz wygodne logowanie przy użyciu numeru PIN</string>
      <string id="AllowDomainPINLogon_Help">To ustawienie zasad umożliwia określenie, czy użytkownik domeny może się logować przy użyciu wygodnego numeru PIN.

Jeśli to ustawienie zasad zostanie włączone, użytkownik domeny będzie mógł skonfigurować wygodny numer PIN i logować się przy jego użyciu.

Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, użytkownik domeny nie będzie mógł skonfigurować i używać wygodnego numeru PIN.

Uwaga: W przypadku korzystania z tej funkcji hasło domeny użytkownika będzie buforowane w magazynie systemu.

Aby skonfigurować usługę Windows Hello dla firm, użyj zasad Szablon administracyjny w ramach funkcji Windows Hello dla firm.</string>
      <string id="BlockDomainPicturePassword">Wyłącz logowanie przy użyciu hasła obrazkowego</string>
      <string id="BlockDomainPicturePassword_Help">To ustawienie zasad umożliwia określenie, czy użytkownik domeny może się logować przy użyciu hasła obrazkowego.

Jeśli to ustawienie zasad zostanie włączone, użytkownik domeny nie będzie mógł używać hasła obrazkowego na potrzeby logowania i konfiguracji. 

Jeśli to ustawienie zasad zostanie wyłączone lub pozostanie nieskonfigurowane, użytkownik domeny będzie mógł używać hasła obrazkowego na potrzeby logowania i konfiguracji.

Należy pamiętać, że w przypadku korzystania z tej funkcji hasło domeny użytkownika będzie buforowane w magazynie systemu.</string>
      <string id="AllowDomainDelayLock">Zezwalaj użytkownikom na wybieranie czasu, po którym wznowienie pracy po wstrzymaniu przy zachowaniu połączenia będzie wymagać podania hasła</string>
      <string id="AllowDomainDelayLock_Help">To ustawienie zasad umożliwia określenie, czy użytkownik może zmienić wartość czasu po wyłączeniu ekranu urządzenia obsługującego wstrzymanie przy zachowaniu połączenia, po upływie którego trzeba podać hasło.

Jeśli włączysz to ustawienie zasad, użytkownik urządzenia obsługującego wstrzymanie przy zachowaniu połączenia będzie mógł zmienić czas, który upłynie po wyłączeniu ekranu urządzenia, zanim do wznowienia pracy urządzenia będzie wymagane hasło. Wartość czasu jest ograniczona przez ustawienia EAS lub zasady grupy, które określają maksymalny czas bezczynności przed zablokowaniem urządzenia. Ponadto jeśli po włączeniu wygaszacza ekranu jest wymagane hasło, opcje dostępne do wyboru przez użytkownika są ograniczone przez limit czasu wygaszacza ekranu.

Jeśli wyłączysz to ustawienie zasad, użytkownik nie będzie mógł zmienić czasu, który upłynie po wyłączeniu ekranu urządzenia, zanim do wznowienia pracy urządzenia będzie wymagane hasło. W takim przypadku podanie hasła będzie konieczne zaraz po wyłączeniu ekranu.

Jeśli nie skonfigurujesz tego ustawienia zasad na urządzeniu przyłączonym do domeny, użytkownik nie będzie mógł zmienić czasu, który upłynie po wyłączeniu ekranu urządzenia, zanim do wznowienia pracy urządzenia będzie wymagane hasło. W takim przypadku podanie hasła będzie konieczne zaraz po wyłączeniu ekranu.

Jeśli nie skonfigurujesz tego ustawienia zasad na urządzeniu grupy roboczej, użytkownik urządzenia obsługującego wstrzymanie przy zachowaniu połączenia będzie mógł zmienić czas, który upłynie po wyłączeniu ekranu urządzenia, zanim do wznowienia pracy urządzenia będzie wymagane hasło. Wartość czasu jest ograniczona przez ustawienia EAS lub zasady grupy, które określają maksymalny czas bezczynności przed zablokowaniem urządzenia. Ponadto jeśli po włączeniu wygaszacza ekranu jest wymagane hasło, opcje dostępne do wyboru przez użytkownika są ograniczone przez limit czasu wygaszacza ekranu.</string>
      <string id="DefaultCredentialProvider">Przypisz domyślnego dostawcę poświadczeń</string>
      <string id="DefaultCredentialProvider_Help">To ustawienie zasad umożliwia administratorowi przypisanie danego dostawcy poświadczeń jako domyślnego dostawcy poświadczeń.

Jeśli to ustawienie zasad zostanie włączone, dany dostawca poświadczeń zostanie wybrany na kafelku innego użytkownika.

Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, system wybierze domyślnego dostawcę poświadczeń na kafelku innego użytkownika.

Uwaga: lista zarejestrowanych dostawców poświadczeń i ich identyfikatorów GUID znajduje się w kluczu rejestru HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Authentication\Credential Providers.</string>
      <string id="AllowSecurityKeySignIn">Włącz logowanie przy użyciu klucza zabezpieczeń</string>
      <string id="AllowSecurityKeySignIn_Help">To ustawienie zasad umożliwia określenie, czy użytkownicy mogą logować się przy użyciu zewnętrznych kluczy zabezpieczeń.
      
Jeśli włączysz to ustawienie zasad, użytkownicy będą mogli logować się przy użyciu zewnętrznych kluczy zabezpieczeń.

Jeśli wyłączysz to ustawienie zasad albo go nie skonfigurujesz, użytkownicy nie będą mogli logować się przy użyciu zewnętrznych kluczy zabezpieczeń.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DefaultLogonDomain">
        <textBox refId="DefaultLogonDomain_Message">
          <label>Domyślna domena logowania:</label>
        </textBox>
        <text>Wprowadź nazwę domeny</text>
      </presentation>
      <presentation id="ExcludedCredentialProviders">
        <textBox refId="ExcludedCredentialProviders_Message">
          <label>Wyklucz następujących dostawców poświadczeń:</label>
        </textBox>
        <text>Wprowadź identyfikatory CLSID (rozdzielając je przecinkami) dostawców poświadczeń,
którzy mają być wykluczeni z procesu uwierzytelniania.

Na przykład: {ba0dd1d5-9754-4ba3-973c-40dce7901283},{383f1aa4-65dd-45bc-9f5a-ddd2f222f07d}</text>
      </presentation>
      <presentation id="DefaultCredentialProvider">
        <textBox refId="DefaultCredentialProvider_Message">
          <label>Przypisz następującego dostawcę poświadczeń jako domyślnego dostawcę poświadczeń:</label>
        </textBox>
        <text>Wprowadź identyfikator klasy dostawcy poświadczeń, który ma być domyślnym dostawcą poświadczeń.

Na przykład: {ba0dd1d5-9754-4ba3-973c-40dce7901283}</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
