<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="AllowBrowse">Zabroń przeglądania</string>
      <string id="AllowBrowseHelp">Ogranicza ostatnio zaplanowane elementy do elementów w menu Start użytkownika i uniemożliwia zmienianie przez użytkownika zaplanowanego programu dla istniejących zadań.

To ustawienie powoduje usunięcie przycisku Przeglądaj z Kreatora zaplanowanych zadań i z karty Zadanie okna dialogowego właściwości dla zadania. Użytkownicy nie mogą również edytować pól „Uruchom” i „Rozpocznij w” określających program i ścieżkę dla zadania.

W rezultacie użytkownicy tworzący zadanie muszą zaznaczyć program na liście w Kreatorze zaplanowanych zadań, gdzie wyświetlane są jedynie zadania pojawiające się w menu Start i w jego podmenu. Po utworzeniu zadania użytkownicy nie mogą zmienić programu, w którym jest uruchamiane zadanie.

Ważne: To ustawienie nie zapobiega tworzeniu przez użytkowników nowego zadania przez wklejanie lub przeciąganie programów do folderu Zaplanowane zadania. Aby zapobiec temu działaniu, należy użyć ustawienia „Zabroń stosowania metody przeciągnij i upuść”.

Uwaga: To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba ustawienia są skonfigurowane, ustawienie w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem w folderze Konfiguracja użytkownika.</string>
      <string id="DisableAdvanced">Ukryj pole wyboru Właściwości zaawansowane w Kreatorze dodawania zaplanowanego zadania</string>
      <string id="DisableAdvancedHelp">To ustawienie usuwa pole wyboru „Otwórz okno Zaawansowane właściwości dla tego zadania, kiedy kliknę przycisk Zakończ” z ostatniej strony Kreatora zaplanowanych zadań.  Ta zasada powstała wyłącznie w celu uproszczenia tworzenia zadań dla początkujących użytkowników.

Zaznaczenie pola wyboru powoduje przekazanie do Harmonogramu zadań instrukcji służącej do automatycznego otwarcia arkusza właściwości nowoutworzonego zadania po zakończeniu pracy kreatora „Dodaj zaplanowane zadanie”.  Arkusz właściwości zadania umożliwia użytkownikom zmienianie następujących właściwości zadania: programu, który to zadanie uruchamia, informacji szczegółowych o jego harmonogramie, ustawienia czasu bezczynności i zarządzania energią oraz jego kontekstu zabezpieczeń.  Początkujący użytkownicy mogą nie być zainteresowani lub mogą czuć się zdezorientowani przez automatycznie wyświetlający się arkusz właściwości.  Należy zauważyć, że pole wyboru nie jest zaznaczone domyślnie, nawet gdy to ustawienie jest wyłączone lub nieskonfigurowane.

Uwaga: To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba ustawienia są skonfigurowane, ustawienie w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem w folderze Konfiguracja użytkownika.</string>
      <string id="DragAndDrop">Zabroń stosowania metody przeciągnij i upuść</string>
      <string id="DragAndDropHelp">Uniemożliwia użytkownikom dodawanie lub usuwanie zadań przez przenoszenie lub kopiowanie programów w folderze Zaplanowane zadania.

To ustawienie wyłącza następujące elementy menu kontekstowego oraz menu Edycja w folderze Zaplanowane zadania: Wytnij, Kopiuj, Wklej i Wklej skrót. Wyłącza również funkcję przeciągnij i upuść w folderze Zaplanowane zadania.

W rezultacie użytkownicy nie mogą dodawać ostatnio zaplanowanych zadań do folderu Zaplanowane zadania przez przeciąganie, przenoszenie lub kopiowanie dokumentu lub programu.

To ustawienie nie uniemożliwia użycia innych metod tworzenia nowych zadań, nie chroni też przed usuwaniem zadań przez użytkowników.

Uwaga: To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba ustawienia są skonfigurowane, ustawienie w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem w folderze Konfiguracja użytkownika.</string>
      <string id="Execution">Zapobiegaj uruchamianiu i kończeniu zadań</string>
      <string id="ExecutionHelp">Uniemożliwia użytkownikom ręczne uruchamianie i zatrzymywanie zadań.

To ustawienie usuwa elementy Uruchom zadanie i Zakończ zadanie z menu kontekstowego wyświetlanego po kliknięciu zadania prawym przyciskiem myszy. W rezultacie użytkownicy nie mogą uruchamiać zadań ręcznie ani wymuszać ich zakończenia, zanim nie zostaną one ukończone.

Uwaga: To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba ustawienia są skonfigurowane, ustawienie w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem w folderze Konfiguracja użytkownika.</string>
      <string id="PropertyPages">Ukryj strony właściwości</string>
      <string id="PropertyPagesHelp">Uniemożliwia użytkownikom wyświetlanie i zmienianie właściwości istniejącego zadania.

To ustawienie usuwa element Właściwości z menu Plik w folderze Zaplanowane zadania i z menu kontekstowego wyświetlanego po kliknięciu zadania prawym przyciskiem myszy. W rezultacie użytkownicy nie mogą zmieniać żadnej właściwości zadania. Mogą jedynie zobaczyć właściwości pojawiające się w widoku Szczegół i w podglądzie zadania.

To ustawienie uniemożliwia wyświetlanie i zmienianie takich charakterystyk, jak program, który to zadanie uruchamia, informacje szczegółowe o jego harmonogramie, ustawienia czasu bezczynności i zarządzanie energią oraz jego kontekst zabezpieczeń.

Uwaga: To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba ustawienia są skonfigurowane, ustawienie w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem w folderze Konfiguracja użytkownika.

Porada: To ustawienie dotyczy tylko istniejących zadań. Aby zapobiec zmienianiu przez użytkowników właściwości nowoutworzonych zadań, użyj ustawienia „Usuń menu Zaawansowane”.</string>
      <string id="TaskCreation">Zabroń tworzenia nowych zadań</string>
      <string id="TaskCreationHelp">Uniemożliwia użytkownikom tworzenie nowych zadań.

To ustawienie powoduje usunięcie elementu Dodaj zaplanowane zadanie uruchamiającego Kreatora nowego zadania. System również nie odpowiada, gdy użytkownicy próbują przenieść, wkleić lub przeciągnąć programy czy dokumenty do folderu Zaplanowane zadania.

Uwaga: To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba ustawienia są skonfigurowane, ustawienie w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem w folderze Konfiguracja użytkownika.

Ważne: To ustawienie nie zapobiega używaniu przez administratora komputera programu At.exe do tworzenia nowych zadań i nie zapobiega przesyłaniu zadań przez administratorów z komputerów zdalnych.</string>
      <string id="TaskDeletion">Zabroń usuwania zadań</string>
      <string id="TaskDeletionHelp">Uniemożliwia użytkownikom usuwanie zadań z folderu Zaplanowane zadania.

To ustawienie usuwa polecenie Usuń z menu Edycja w folderze Zaplanowane zadania i z menu wyświetlanego po kliknięciu zadania prawym przyciskiem myszy. System również nie odpowiada, gdy użytkownicy próbują wyciąć lub przeciągnąć zadanie z folderu Zaplanowane zadania.

Uwaga: To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba ustawienia są skonfigurowane, ustawienie w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem w folderze Konfiguracja użytkownika.

Ważne: To ustawienie nie zapobiega używaniu przez administratora komputera programu At.exe do usuwania zadań.</string>
      <string id="TaskScheduler">Harmonogram zadań</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
