<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="Calculator">Kalkulator</string>
      <string id="AllowGraphingCalculator">Zezwalaj na używanie kalkulatora tworzącego wykresy</string>
      <string id="AllowGraphingCalculator_Help">To ustawienie zasad umożliwia określenie, czy funkcja tworzenia wykresów jest dostępna w aplikacji Kalendarz Windows. Jeśli wyłączysz to ustawienie zasad, funkcja tworzenia wykresów nie będzie dostępna w aplikacji Kalkulator Windows. Jeśli włączysz to ustawienie zasad albo go nie skonfigurujesz, użytkownicy będą mogli korzystać z funkcji tworzenia wykresów.</string>
      <string id="Programs">Programy</string>
      <string id="NoProgramsCPL">Ukryj aplet Programy w Panelu sterowania</string>
      <string id="NoProgramsCPL_Help">To ustawienie zapobiega korzystaniu przez użytkowników z apletu Programy w Panelu sterowania (widok kategorii) i strony Programy i funkcje (widok klasyczny).
 
Za pomocą apletu Programy znajdującego się w Panelu sterowania użytkownicy mogą odinstalowywać, modyfikować i naprawiać programy, włączać i wyłączać funkcje systemu Windows, konfigurować domyślne ustawienia programów, wyświetlać zainstalowane aktualizacje oraz kupować programy w witrynie Windows Marketplace. W oknie apletu są również wyświetlane programy opublikowane lub przypisane użytkownikowi przez administratora systemu.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, aplet Programy w Panelu sterowania (widok kategorii) i strona Programy i funkcje (widok klasyczny) będą dostępne dla wszystkich użytkowników.

Jeśli to ustawienie zostanie włączone, będzie miało priorytet względem innych ustawień w tym folderze.

To ustawienie nie zapobiega stosowaniu przez użytkowników innych narzędzi i metod do instalowania lub odinstalowywania programów.</string>

      <string id="NoProgramsAndFeatures">Ukryj stronę Programy i funkcje</string>
      <string id="NoProgramsAndFeatures_Help">To ustawienie zapobiega uzyskiwaniu przez użytkowników dostępu do strony Programy i funkcje, na której znajdują się opcje umożliwiające wyświetlanie, odinstalowywanie, modyfikowanie i naprawianie programów aktualnie zainstalowanych na komputerze.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, strona Programy i funkcje będzie dostępna dla wszystkich użytkowników.

To ustawienie nie zapobiega stosowaniu przez użytkowników innych narzędzi i metod do usuwania lub odinstalowywania programów.  Nie blokuje również możliwości dostępu do innych, pokrewnych stron apletu Programy w Panelu sterowania, takich jak Funkcje systemu Windows, Uzyskaj programy czy Windows Marketplace.</string>

      <string id="NoInstalledUpdates">Ukryj stronę Zainstalowane aktualizacje</string>
      <string id="NoInstalledUpdates_Help">To ustawienie zapobiega uzyskiwaniu przez użytkowników dostępu do strony Zainstalowane aktualizacje pokazywanej po użyciu opcji Wyświetl zainstalowane aktualizacje.

Opcje zawarte na tej stronie umożliwiają wyświetlanie i odinstalowywanie aktualizacji zainstalowanych obecnie na komputerze.  Aktualizacje te są często pobierane bezpośrednio z witryny Windows Update lub od różnych wydawców oprogramowania.

Jeśli to ustawienie zostanie wyłącznie lub nie zostanie skonfigurowane, funkcja Wyświetl zainstalowane aktualizacje oraz strona Zainstalowane aktualizacje będą dostępne dla wszystkich użytkowników.

To ustawienie nie zapobiega stosowaniu przez użytkowników innych narzędzi i metod do instalowania lub odinstalowywania programów.</string>

      <string id="NoDefaultPrograms">Ukryj stronę Określ dostęp do programów i ich ustawienia domyślne</string>
      <string id="NoDefaultPrograms_Help">To ustawienie powoduje usunięcie strony Określ dostęp do programów i ich ustawienia domyślne z apletu Programy w Panelu sterowania.  W rezultacie użytkownicy nie mogą wyświetlać ani zmieniać skojarzonej strony.

Opcje zawarte na stronie Określ dostęp do programów i ich ustawienia domyślne umożliwiają administratorom określenie programów używanych domyślnie do wykonywania niektórych działań, takich jak przeglądanie sieci Web, a także wskazanie programów, które będą dostępne z menu Start, pulpitu i innych lokalizacji.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, strona Określ dostęp do programów i ich ustawienia domyślne będzie dostępna dla wszystkich użytkowników.

To ustawienie nie zapobiega stosowaniu przez użytkowników innych narzędzi i metod do zmieniania opcji dostępu i ustawień domyślnych dotyczących programów.

Ustawienie nie blokuje wyświetlania ikony Programy domyślne w menu Start.</string>
      
      <string id="NoWindowsMarketplace">Ukryj witrynę Windows Marketplace</string>
      <string id="NoWindowsMarketplace_Help">To ustawienie zapobiega uzyskiwaniu przez użytkowników dostępu do funkcji Uzyskaj nowe programy z witryny Windows Marketplace z poziomu apletu Programy w Panelu sterowania (widok kategorii), strony Programy i funkcje (widok klasyczny) oraz strony Uzyskaj programy.

Korzystając z witryny Windows Marketplace, użytkownicy mogą kupować i/lub pobierać różne programy na swoje komputery w celu ich zainstalowania.

Włączenie tej funkcji nie uniemożliwia użytkownikom przechodzenia do witryny Windows Marketplace w inny sposób. 

Jeśli ta funkcja zostanie wyłączona lub nie zostanie skonfigurowana, łącze Uzyskaj nowe programy z witryny Windows Marketplace będzie dostępne dla wszystkich użytkowników.

Uwaga:  jeśli jest włączone ustawienie Ukryj aplet Programy w Panelu sterowania, to ustawienie jest ignorowane.</string>

      <string id="NoGetPrograms">Ukryj stronę Uzyskaj programy</string>
      <string id="NoGetPrograms_Help">Zapobiega wyświetlaniu lub instalowaniu przez użytkowników programów opublikowanych w sieci.  

To ustawienie zapobiega uzyskiwaniu przez użytkowników dostępu do strony Uzyskaj programy z poziomu apletu Programy w Panelu sterowania (widok kategorii), strony Programy i funkcje (widok klasyczny) oraz przy użyciu funkcji Zainstaluj program z sieci.  Na stronie Uzyskaj programy znajduje się lista opublikowanych programów wraz z opcjami umożliwiającymi ich łatwą instalację.

Publikowanymi programami są programy, które zostały jawnie udostępnione użytkownikowi przez administratora systemu przy użyciu takiego narzędzia jak Instalator Windows.  Administratorzy systemu zwykle publikują programy w celu powiadomienia użytkowników, że te programy są dostępne, że ich użycie jest zalecane lub aby umożliwić użytkownikom zainstalowanie ich bez konieczności wyszukiwania plików instalacyjnych.

Jeśli to ustawienie zostanie włączone, użytkownicy nie będą mogli wyświetlać programów opublikowanych przez administratora systemu i nie będą mogli korzystać ze strony Uzyskaj programy w celu ich zainstalowania.  Włączenie tego ustawienia nie powoduje zablokowania użytkownikom możliwości instalowania programów w inny sposób.  Użytkownicy w dalszym ciągu będą mogli wyświetlać oraz instalować przypisane (częściowo zainstalowane) programy zlokalizowane na pulpicie lub w menu Start.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, wszyscy użytkownicy będą mieli dostęp do strony Uzyskaj programy z poziomu funkcji Zainstaluj program z sieci.

Uwaga:  jeśli jest włączone ustawienie Ukryj aplet Programy w Panelu sterowania, to ustawienie jest ignorowane.</string>
    
      <string id="NoWindowsFeatures">Ukryj aplet Funkcje systemu Windows</string>
      <string id="NoWindowsFeatures_Help">To ustawienie zapobiega uzyskiwaniu przez użytkowników dostępu do funkcji Włącz lub wyłącz funkcje systemu Windows z poziomu apletu Programy w Panelu sterowania (widok kategorii), strony Programy i funkcje (widok klasyczny) oraz strony Uzyskaj programy.  W rezultacie użytkownicy nie mogą wyświetlać, włączać ani wyłączać różnych funkcji ani usług systemu Windows.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, opcja Włącz lub wyłącz funkcje systemu Windows będzie dostępna dla wszystkich użytkowników.

To ustawienie nie powoduje zablokowania użytkownikom możliwości konfigurowania usług czy włączania lub wyłączania składników programów za pomocą innych narzędzi i metod.</string>

    </stringTable>
    <presentationTable>
      <presentation id="DefaultCategory">
        <textBox refId="DefaultCategoryBox">
          <label>Kategoria:</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
