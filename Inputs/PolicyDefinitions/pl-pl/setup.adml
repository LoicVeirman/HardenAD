<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź nazwę wyświetlaną</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="ServicePackSourcePath">Określ lokalizację pliku instalacyjnego dodatku Service Pack systemu Windows</string>
      <string id="ServicePackSourcePath_Help">Określa alternatywną lokalizację plików instalacyjnych dodatku Service Pack dla systemu Windows.

Jeśli to ustawienie zasad zostanie włączone, w polu „Ścieżka do pliku Instalatora dodatku Windows Service Pack” należy wprowadzić w pełni kwalifikowaną ścieżkę do nowej lokalizacji.

Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, ścieżka źródłowa do Instalatora dodatku Service Pack dla systemu Windows będzie lokalizacją użytą podczas ostatniego uruchomienia w systemie Instalatora dodatku Service Pack dla systemu Windows.



</string>
      <string id="SetupSourcePath">Określ lokalizację pliku instalacyjnego systemu Windows</string>
      <string id="SetupSourcePath_Help">Określa alternatywną lokalizację plików instalacyjnych systemu Windows.

Jeśli to ustawienie zasad zostanie włączone, w polu „Ścieżka do pliku Instalatora systemu Windows” należy wprowadzić w pełni kwalifikowaną ścieżkę do nowej lokalizacji. 

Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, ścieżka źródłowa do Instalatora systemu Windows będzie lokalizacją użytą podczas ostatniego uruchomienia w systemie Instalatora systemu Windows.



</string>
    </stringTable>
    <presentationTable>
      <presentation id="ServicePackSourcePath">
        <textBox refId="ServicePackSourcePathBox">
          <label>Ścieżka do pliku Instalatora dodatku Windows Service Pack:</label>
        </textBox>
      </presentation>
      <presentation id="SetupSourcePath">
        <textBox refId="SetupSourcePathBox">
          <label>Ścieżka do pliku Instalatora systemu Windows:</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
