<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź nazwę wyświetlaną</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="DFSDiscoverDC">Skonfiguruj częstotliwość odnajdywania kontrolerów domeny przez klienta DFS</string>
      <string id="DFSDiscoverDC_Help">To ustawienie zasad umożliwia skonfigurowanie częstotliwości podejmowania przez klienta rozproszonego systemu plików (DFS, Distributed File System) próby odnalezienia kontrolerów domeny w sieci. Domyślnie klient DFS podejmuje próbę odnalezienia kontrolerów domeny co 15 minut.

Jeśli to ustawienie zasad zostanie włączone, będzie można skonfigurować częstotliwość podejmowania przez klienta DFS próby odnalezienia kontrolerów domeny. Wartość ta jest podawana w minutach.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie stosowana wartość domyślna równa 15 minut.

Uwaga: minimalna wartość, jaką można wybrać, to 15 minut. W przypadku próby skonfigurowania dla tego ustawienia wartości mniejszej niż 15 minut będzie stosowana wartość domyślna wynosząca 15 minut.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DFSDiscoverDC">
        <decimalTextBox refId="DFSDiscoverDialog" defaultValue="15">Czas w minutach:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
