<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź nazwę wyświetlaną</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">Na początku nie uruchamiaj automatycznie programu Windows Messenger</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">To ustawienie zasad zapobiega automatycznemu uruchamianiu programu Windows Messenger przy logowaniu. 

Jeśli to ustawienie zasad zostanie włączone, program Windows Messenger nie będzie automatycznie ładowany po zalogowaniu użytkownika.

Jeśli to ustawienie zasad zostanie wyłączone lub pozostanie nieskonfigurowane, program Windows Messenger będzie automatycznie ładowany po zalogowaniu.

Uwaga: to ustawienie zapobiega początkowemu uruchamianiu programu Windows Messenger. Jeśli użytkownik później wywoła program Windows Messenger i zacznie go używać, począwszy od tej chwili program Windows Messenger będzie ładowany.

Użytkownik może również skonfigurować takie zachowanie na karcie Preferencje w menu Narzędzia w interfejsie użytkownika programu Windows Messenger.

Uwaga: aby użytkownicy nie mogli używać programu Windows Messenger, należy włączyć ustawienie zasad „Nie zezwalaj na uruchamianie programu Windows Messenger”.

Uwaga: to ustawienie zasad jest dostępne w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli zostanie skonfigurowane w obu folderach, pierwszeństwo będzie miała wersja tego ustawienia zasad z folderu Konfiguracja komputera.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">Na początku nie uruchamiaj automatycznie programu Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Comp">Nie zezwalaj na uruchamianie programu Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">To ustawienie zasad pozwala zapobiec uruchamianiu programu Windows Messenger.

Jeśli to ustawienie zasad zostanie włączone, program Windows Messenger nie będzie mógł być uruchamiany.

Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, będzie można korzystać z programu Windows Messenger.

Uwaga: jeśli to ustawienie zasad zostanie włączone, użytkownicy Pomocy zdalnej również nie będą mogli korzystać z programu Windows Messenger.

Uwaga: to ustawienie zasad jest dostępne w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli zostanie skonfigurowane w obu folderach, pierwszeństwo będzie miała wersja tego ustawienia zasad z folderu Konfiguracja komputera.</string>
      <string id="WinMSG_NoWindowsMsg_User">Nie zezwalaj na uruchamianie programu Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
