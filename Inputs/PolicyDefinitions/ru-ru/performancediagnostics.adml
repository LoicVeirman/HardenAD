<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>введите выводимое имя</displayName>
  <description>введите описание</description>
  <resources>
    <stringTable>
      <string id="BootScenarioCategory">Диагностика быстродействия загрузки Windows</string>
      <string id="BootScenarioExecutionPolicyExplain">Определяет режим работы средства для диагностики быстродействия загрузки Windows.

При включении этого параметра политики необходимо выбрать режим работы из раскрывающегося меню. При выборе режима только выявления и установления причин ошибок служба политики диагностики будет выявлять проблемы быстродействия загрузки Windows и пытаться определить их основные причины. Если их удастся определить, причины будут отражены в журнале ошибок. При этом не будет предпринято никаких действий по устранению ошибок. Если выбран режим определения, установления причин ошибок и их устранения, служба политики диагностики будет определять проблемы быстродействия загрузки Windows и указывать  пользователю на доступные средства их устранения.

Если этот параметр локальной политики выключен, Windows не сможет обнаруживать проблемы быстродействия загрузки, устанавливать их причины и устранять ошибки, если они обрабатываются службой политики диагностики.

Если этот параметр политики не задан, служба политики диагностики будет по умолчанию использовать средство диагностики быстродействия загрузки Windows для устранения ошибок.

Этот параметр политики применяется только в том случае, если не настроена политика выполнения сценария диагностики.

Для вступления в силу изменений этого параметра не требуется перезапускать систему или службу: изменения вступают в силу немедленно.

Этот параметр политики применяется только в том случае, если запущена служба политики диагностики. Если эта служба остановлена или отключена, сценарии диагностики выполняться не будут.  Служба политики диагностики может быть настроена с помощью оснастки «Службы» в консоли управления Microsoft.</string>
      <string id="ResumeScenarioCategory">Диагностика производительности ждущего режима и возобновления работы Windows</string>
      <string id="ResumeScenarioExecutionPolicyExplain">Определяет режим работы средства для диагностики производительности ждущего режима и возобновления работы Windows.

При включении этого параметра политики необходимо выбрать режим работы из раскрывающегося меню. При выборе режима только определения и установления причин ошибок служба политики диагностики будет обнаруживать проблемы производительности ждущего режима и возобновления работы Windows и пытаться определить их основные причины. Если их удастся определить, причины будут отражены в журнале ошибок. При этом не будет предпринято никаких действий по устранению ошибок. Если выбран режим определения, установления причин ошибок и их устранения, служба политики диагностики будет определять проблемы производительности ждущего режима и возобновления работы Windows и указывать  пользователю на доступные средства их устранения.

Если отключить этот параметр политики, Windows не сможет обнаруживать проблемы производительности ждущего режима и возобновления работы, устанавливать их причины и устранять ошибки, если они обрабатываются службой политики диагностики.

Если не настраивать этот параметр политики, служба политики диагностики будет по умолчанию использовать средство диагностики производительности ждущего режима и возобновления работы Windows для устранения ошибок.

Этот параметр политики применяется только в том случае, если не настроена политика выполнения сценария диагностики.

Для вступления в силу изменений этого параметра не требуется перезапускать систему или службу: изменения вступают в силу немедленно.

Этот параметр политики применяется только в том случае, если запущена служба политики диагностики. Если эта служба остановлена или отключена, сценарии диагностики выполняться не будут.  Служба политики диагностики может быть настроена с помощью оснастки «Службы» в консоли управления Microsoft.</string>
      <string id="ShellScenarioCategory">Диагностика скорости отклика системы Windows</string>
      <string id="ShellScenarioExecutionPolicyExplain">Определяет режим работы средства для диагностики скорости отклика системы Windows.

При включении этого параметра политики необходимо выбрать режим работы из раскрывающегося меню. При выборе режима только определения и установления причин ошибок служба политики диагностики будет обнаруживать проблемы скорости отклика системы Windows и пытаться определить их основные причины. Если их удастся определить, причины будут отражены в журнале ошибок. При этом не будет предпринято никаких действий по устранению ошибок. Если выбран режим определения, установления причин ошибок и их устранения, служба политики диагностики будет определять проблемы скорости отклика системы Windows и указывать  пользователю на доступные средства их устранения.

Если отключить этот параметр политики, Windows не сможет обнаруживать проблемы скорости отклика системы, устанавливать их причины и устранять ошибки, если они обрабатываются службой политики диагностики.

Если не настраивать этот параметр локальной политики, служба диагностики локальной политики будет по умолчанию использовать средство диагностики скорости отклика системы Windows для устранения ошибок.

Этот параметр политики применяется только в том случае, если не настроена политика выполнения сценария диагностики.

Для вступления в силу изменений этого параметра не требуется перезапускать систему или службу: изменения вступают в силу немедленно.

Этот параметр политики применяется только в том случае, если запущена служба политики диагностики. Если эта служба остановлена или отключена, сценарии диагностики выполняться не будут.  Служба политики диагностики может быть настроена с помощью оснастки «Службы» в консоли управления Microsoft.</string>
      <string id="ShutdownScenarioCategory">Диагностика быстродействия завершения работы Windows</string>
      <string id="ShutdownScenarioExecutionPolicyExplain">Определяет режим работы средства для диагностики быстродействия завершения работы Windows.

При включении этого параметра политики необходимо выбрать режим работы из раскрывающегося меню. При выборе режима только определения и установления причин ошибок служба политики диагностики будет обнаруживать проблемы быстродействия завершения работы Windows и пытаться определить их основные причины. Если их удастся определить, причины будут отражены в журнале ошибок. При этом не будет предпринято никаких действий по устранению ошибок. Если выбран режим определения, установления причин ошибок и их устранения, служба диагностики локальной политики будет определять проблемы быстродействия завершения работы Windows и указывать  пользователю на доступные средства их устранения.

Если отключить этот параметр политики, Windows не сможет обнаруживать проблемы быстродействия завершения работы, устанавливать их причины и устранять ошибки, если они обрабатываются службой политики диагностики.

Если не настраивать этот параметр локальной политики, служба политики диагностики будет по умолчанию использовать средство диагностики быстродействия завершения работы Windows для устранения ошибок.

Этот параметр политики применяется только в том случае, если не настроена политика выполнения сценария диагностики.

Для вступления в силу изменений этого параметра не требуется перезапускать систему или службу: изменения вступают в силу немедленно.

Этот параметр политики применяется только в том случае, если запущена служба политики диагностики. Если эта служба остановлена или отключена, сценарии диагностики выполняться не будут.  Служба политики диагностики может быть настроена с помощью оснастки «Службы» в консоли управления Microsoft.</string>
      <string id="WdiScenarioExecutionPolicy">Настройки режима запуска сценария</string>
      <string id="WdiScenarioExecutionPolicyLevelResolution">Определение, установление причин ошибок и их устранение.</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Только определение и установление причин ошибок.</string>
    </stringTable>
    <presentationTable>
      <presentation id="WdiScenarioExecutionPolicy_1">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Режим запуска сценария</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_2">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Режим запуска сценария</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_3">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Режим запуска сценария</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_4">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Режим запуска сценария</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
