<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>여기에 표시 이름 입력</displayName>
  <description>여기에 설명 입력</description>
  <resources>
    <stringTable>
      <string id="Rpc">원격 프로시저 호출</string>
      <string id="RpcEEInfoOff">사용 안 함</string>
      <string id="RpcEEInfoOffWithExc">예외와 함께 사용 안 함</string>
      <string id="RpcEEInfoOn">사용</string>
      <string id="RpcEEInfoOnWithExc">예외와 함께 사용</string>
      <string id="RpcEnableAuthEpResolution">RPC 끝점 매퍼 클라이언트 인증 사용</string>
      <string id="RpcEnableAuthEpResolution_Help">이 정책 설정은 RPC 클라이언트가 수행하는 호출에 인증 정보가 포함되어 있을 때 끝점 매퍼 서비스에서 인증을 받을지 여부를 제어합니다.   Windows NT4(모든 서비스 팩 포함)가 실행 중인 컴퓨터의 끝점 매퍼 서비스는 이런 식으로 제공된 인증 정보를 처리할 수 없습니다. 

이 정책 설정을 사용하지 않으면 RPC 클라이언트는 끝점 매퍼 서비스의 인증을 받지 않으며 Windows NT4 Server의 끝점 매퍼 서비스와 통신할 수 있습니다.

이 정책 설정을 사용하면 RPC 클라이언트는 인증 정보를 포함하는 호출 시 끝점 매퍼 서비스의 인증을 받습니다.  이러한 호출을 수행하는 클라이언트는 Windows NT4 Server 끝점 매퍼 서비스와 통신할 수 없습니다.

이 정책 설정을 구성하지 않으면 비활성화되어 있습니다.  RPC 클라이언트는 끝점 매퍼 서비스의 인증을 받지 않지만 Windows NT4 Server 끝점 매퍼 서비스와 통신할 수 있습니다.

참고: 이 정책은 시스템을 다시 부팅해야 적용됩니다.</string>
      <string id="RpcExtendedErrorInformation">확장 오류 정보 전파</string>
      <string id="RpcExtendedErrorInformation_Help">이 정책 설정은 오류가 발생할 때 RPC 런타임이 확장 오류 정보를 생성할지 여부를 제어합니다.

확장 오류 정보에는 오류가 발생한 로컬 시간, RPC 버전, 그리고 오류가 발생했거나 전파된 컴퓨터의 이름이 포함됩니다. 프로그램은 표준 Windows API(응용 프로그래밍 인터페이스)를 사용하여 확장 오류 정보를 검색할 수 있습니다.

이 정책 설정을 사용하지 않으면 RPC 런타임에서 오류 조건을 나타내는 상태 코드만 생성합니다.

이 정책 설정을 구성하지 않으면 비활성화되어 있습니다.  또한 오류 조건을 나타내는 상태 코드만 생성합니다.

이 정책 설정을 사용하면 RPC 런타임은 확장 오류 정보를 생성하지 않습니다.  드롭다운 상자에서 오류 응답 유형을 선택해야 합니다.

--  "사용 안 함"은 모든 프로세스에 대한 확장 오류 정보를 사용하지 않도록 합니다. RPC는 오류 코드만 생성합니다.

--  "예외와 함께 사용"은 확장 오류 정보를 사용하도록 하지만 선택한 프로세스에 대해서는 사용할 수 없도록 합니다. 이 정책 설정이 적용되는 동안 프로세스에 대해 확장 오류 정보를 사용하지 않으려면 프로세스를 시작하는 명령이 확장 오류 정보의 예외 필드에 있는 문자열 중 하나로 시작해야 합니다.

--  "예외와 함께 사용 안 함"은 확장 오류 정보를 사용하지 않도록 하지만 선택한 프로세스에 대해서는 사용할 수 있도록 합니다. 이 정책 설정이 적용되는 동안 프로세스에 대해 확장 오류 정보를 사용하려면 프로세스를 시작하는 명령이 확장 오류 정보의 예외 필드에 있는 문자열 중 하나로 시작해야 합니다.

--  "사용"은 모든 프로세스에 대해 모든 확장 정보를 사용하도록 합니다.

참고: 확장 오류 정보의 예외 필드에 대한 자세한 내용은 Windows SDK(소프트웨어 개발 키트)를 참조하십시오.

참고: 확장 오류 정보는 다른 운영 체제 및 이전 버전의 Microsoft 운영 체제에서 호환되도록 구성되어 있지만 새 버전의 Microsoft 운영 체제에서만 정보를 읽고 응답할 수 있습니다.

참고: 기본 정책 설정값, "사용 안 함"은 확장 오류 정보가 중요하게 간주되거나, 원격으로 사용될 수 없는 시스템에 사용되도록 구성되었습니다.

참고: 이 정책 설정은 시스템을 다시 부팅해야 적용됩니다.</string>
      <string id="RpcIDFOff">사용 안 함</string>
      <string id="RpcIDFOn">사용</string>
      <string id="RpcIgnoreDelegationFailure">위임 실패 무시</string>
      <string id="RpcIgnoreDelegationFailure_Help">이 정책 설정은 위임이 요청될 때 RPC 런타임이 위임 오류를 무시할지 여부를 제어합니다.

Windows Server 2003에 포함된 제한된 위임 모델은 클라이언트가 서버에 연결할 때 위임이 보안 컨텍스트에서 사용하도록 설정되어 있는지를 보안 시스템이 보고하지 않습니다. RPC의 호출자 및 COM은 RPC_C_QOS_CAPABILITIES_IGNORE_DELEGATE_FAILURE 플래그를 사용하도록 권장되지만 Windows Server 2003 이전의 일반 위임 모델에 맞도록 작성된 일부 응용 프로그램은 이 플래그를 사용하지 않을 수도 있으며, 제한된 위임을 사용하는 서버에 연결할 경우 RPC_S_SEC_PKG_ERROR가 발생할 수도 있습니다.

이 정책 설정을 사용하지 않으면 RPC 런타임에서 위임을 요청하고 제한된 위임을 사용하여 서버에 연결하는 응용 프로그램에 대해 RPC_S_SEC_PKG_ERROR 오류를 생성합니다. 

이 정책 설정을 구성하지 않으면 비활성 상태를 유지하며 위임을 요청하고 제한된 위임을 사용하여 서버에 연결하는 응용 프로그램에 대해 RPC_S_SEC_PKG_ERROR 오류를 생성합니다. 

이 정책 설정을 사용할 경우 다음이 적용됩니다.

--  "사용 안 함"으로 설정하면 클라이언트가 위임을 요청할 경우 RPC 런타임에서 RPC_S_SEC_PKG_ERROR를 생성하지만 작성된 보안 컨텍스트는 위임을 지원하지 않습니다.

-- "사용"으로 설정하면 위임이 요청되더라도 RPC 런타임에서 위임을 지원하지 않는 보안 컨텍스트를 허용하도록 합니다.

참고: 이 정책 설정은 시스템을 다시 부팅해야 적용됩니다.</string>
      <string id="RpcMinimumHttpConnectionTimeout">RPC/HTTP 연결의 최소 유휴 연결 시간 제한 설정</string>
      <string id="RpcMinimumHttpConnectionTimeout_Help">이 정책 설정은 RPC/HTTP 연결의 유휴 연결 시간 제한을 제어합니다.  

이 정책 설정은 HTTP 프록시 또는 RPC/HTTP 프록시를 실행 중인 IIS 서버보다 낮은 유휴 연결 시간 제한을 사용하는 라우터와 같은 네트워크 에이전트가 있을 경우에 유용하게 사용됩니다. 이러한 경우 예상보다 빨리 시간 제한에 도달하므로 RPC/HTTP 클라이언트에 오류가 발생할 수 있습니다. 이 정책 설정을 사용하면 RPC 런타임 및 RPC/HTTP 프록시에서 낮은 연결 시간 제한을 사용하도록 할 수 있습니다.

이 정책 설정은 RPC 클라이언트, RPC 서버 및 RPC HTTP 프록시 모두가 Windows Server 2003 제품군/Windows XP SP1 이상 버전을 실행 중인 경우에만 적용됩니다. RPC 클라이언트, RPC 서버 또는 RPC HTTP 프록시가 이전 버전의 Windows에서 실행 중일 경우 이 정책 설정은 무시됩니다.

이 정책 설정에 허용되는 최소값은 90초이고 최대값은 7200(2시간)입니다.

이 정책 설정을 사용하지 않으면 RPC HTTP 프록시를 실행 중인 IIS 서버의 유휴 연결 시간 제한이 사용됩니다.

이 정책 설정을 구성하지 않으면 비활성화되어 있습니다.  RPC HTTP 프록시를 실행 중인 IIS 서버의 유휴 연결 시간 제한이 사용됩니다.

이 정책 설정을 사용하면 RPC HTTP 프록시를 실행 중인 IIS 서버가 낮은 유휴 연결 시간 제한으로 구성되어 있을 경우 IIS의 시간 제한이 사용됩니다. 그렇지 않은 경우 제공된 시간 제한이 사용됩니다.  시간 제한 단위는 초입니다.

참고: 이 정책 설정은 시스템을 다시 부팅해야 적용됩니다.</string>
      <string id="RpcRestrictRemoteClients">인증되지 않은 RPC 클라이언트 제한</string>
      <string id="RpcRestrictRemoteClients_Help">이 정책 설정은 RPC 서버 런타임이 RPC 서버에 대한 인증되지 않은 RPC 클라이언트 연결을 처리하는 방식을 제어합니다.

이 정책 설정은 모든 RPC 응용 프로그램에 영향을 미칩니다.  도메인 환경에서 이 정책 설정은 그룹 정책 처리 자체를 비롯하여 광범위한 기능에 영향을 미칠 수 있으므로 주의해서 사용해야 합니다.  이 정책 설정에 대한 변경 내용을 되돌리려면 영향을 받은 각 시스템에 수동으로 개입해야 할 수 있습니다.  도메인 컨트롤러에는 이 정책 설정을 적용하면 안 됩니다.

이 정책 설정을 사용하지 않으면 이 정책 설정을 지원하는 RPC 서버 런타임은 Windows 클라이언트에 대해서는 "인증됨" 값을, Windows Server에 대해서는 "없음" 값을 사용합니다. 

이 정책 설정을 구성하지 않으면 비활성화되어 있습니다.  RPC 서버 런타임은 마치 이 정책 설정을 지원하는 클라이언트에 대해 "인증됨" 값이, Server SKU에 대해 "없음" 값이 설정된 것처럼 동작합니다. 

이 정책 설정을 사용하면 RPC 서버 런타임이 인증되지 않은 RPC 클라이언트가 컴퓨터에 실행 중인 RPC 서버에 연결할 수 없도록 합니다. 클라이언트가 명명된 파이프로 서버와 통신하거나 RPC 보안을 사용하면 클라이언트는 인증된 것으로 여겨집니다. 인증되지 않은 클라이언트가 액세스할 수 있도록 명시적으로 요청한 RPC 인터페이스는 이 정책 설정의 선택한 값에 따라 이러한 제한에서 제외될 수 있습니다.

--  "없음"은 정책 설정이 적용된 컴퓨터에서 실행 중인 RPC 서버에 모든 RPC 클라이언트가 연결할 수 있도록 합니다.

--  "인증됨"은 위에 정의된 대로 인증된 RPC 클라이언트만 정책 설정이 적용된 컴퓨터에서 실행 중인 RPC 서버에 연결할 수 있도록 합니다. 요청한 인터페이스에 대해 예외가 허가됩니다.

-- "예외 없이 인증됨"은 위에 정의된 대로 인증된 RPC 클라이언트만 정책 설정이 적용된 컴퓨터에서 실행 중인 RPC 서버에 연결할 수 있도록 합니다.  예외가 허용되지 않습니다.

참고: 이 정책 설정은 시스템을 다시 부팅해야 적용됩니다.</string>
      <string id="RpcRestrictRemoteClientsAuth">인증됨</string>
      <string id="RpcRestrictRemoteClientsHigh">예외 없이 인증됨</string>
      <string id="RpcRestrictRemoteClientsNone">없음</string>
      <string id="RpcStateInfoAuto1">자동 1</string>
      <string id="RpcStateInfoAuto2">자동 2</string>
      <string id="RpcStateInfoBoth">최대</string>
      <string id="RpcStateInfoNone">없음</string>
      <string id="RpcStateInformation">Maintain RPC Troubleshooting State Information(RPC 문제 해결 상태 정보 유지 관리)</string>
      <string id="RpcStateInformation_Help">이 정책 설정은 시스템에 사용할 RPC 상태 정보를 RPC 런타임에서 유지하는지 여부와 유지하는 정보량을 결정합니다. 일반적으로 필요한 상태 데이터로만 구성된 기본 상태 정보는 RPC 문제 해결을 위해 필요합니다.

이 정책 설정을 사용하지 않으면 RPC 런타임의 기본 수준은 "자동 2"가 됩니다.

이 정책 설정을 구성하지 않으면 RPC의 기본 수준은 "자동 2"가 됩니다. 

이 정책 설정을 사용하면 드롭다운 상자에서 RPC 상태 정보를 유지하는 시스템을 선택할 수 있습니다.

-- "없음"은 시스템에서 RPC 상태 정보를 유지하지 않음을 나타냅니다. 참고: 문제 해결에 필요한 상태 정보는 성능에 거의 영향을 주지 않고 크기도 약 4K로 적기 때문에 대부분의 장치에서는 이 설정을 사용하지 않는 것이 좋습니다.

--   "자동 1"은 컴퓨터 메모리가 적어도 64MB인 경우에만 기본 상태 정보를 유지하도록 합니다.

-- "자동 2"는 컴퓨터 메모리가 적어도 128MB이고 Windows 2000 Server, Windows 2000 Advanced Server 또는 Windows 2000 Datacenter Server를 실행하는 경우에만 기본 상태 정보를 유지하도록 합니다. 

-- "서버"는 컴퓨터 용량에 상관없이 기본 상태 정보를 유지하도록 합니다.

-- "전체"는 컴퓨터 용량에 상관없이 완전한 RPC 상태 정보를 유지하도록 합니다. 이 수준은 성능을 저하시킬 수 있으므로 RPC 문제를 검사하는 경우에만 사용하는 것이 좋습니다.

참고: 시스템에 유지되는 RPC 상태 정보를 검색하려면 디버깅 도구를 사용해야 합니다.

참고: 이 정책 설정은 시스템을 다시 부팅해야 적용됩니다.</string>
      <string id="RpcStateInfoServer">서버</string>
    </stringTable>
    <presentationTable>
      <presentation id="RpcExtendedErrorInformation">
        <dropdownList refId="RpcExtendedErrorInformationList" defaultItem="0">확장 오류 정보의 전파:</dropdownList>
        <textBox refId="RpcExtErrorExceptions">
          <label>확장 오류 정보의 예외:</label>
        </textBox>
      </presentation>
      <presentation id="RpcIgnoreDelegationFailure">
        <dropdownList refId="RpcIgnoreDelegationFailureList" defaultItem="0">위임 실패 무시:</dropdownList>
      </presentation>
      <presentation id="RpcMinimumHttpConnectionTimeout">
        <decimalTextBox refId="RpcMinimumHttpConnectionTimeoutValue" defaultValue="900" spinStep="10">최소 유휴 연결 제한 시간(초):</decimalTextBox>
      </presentation>
      <presentation id="RpcRestrictRemoteClients">
        <dropdownList refId="RpcRestrictRemoteClientsList" defaultItem="1">인증되지 않은 RPC 런타임 클라이언트 제한 적용:</dropdownList>
      </presentation>
      <presentation id="RpcStateInformation">
        <dropdownList refId="RpcStateInformationList" defaultItem="2">유지할 RPC 런타임 상태 정보:</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
