<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>여기에 표시 이름 입력</displayName>
  <description>여기에 설명 입력</description>
  <resources>
    <stringTable>
      <string id="DFSDiscoverDC">Configure how often a DFS client discovers domain controllers(DFS 클라이언트가 도메인 컨트롤러를 검색하는 간격 구성)</string>
      <string id="DFSDiscoverDC_Help">이 정책 설정을 사용하면 DFS(분산 파일 시스템) 클라이언트가 네트워크에 있는 도메인 컨트롤러를 검색하는 간격을 구성할 수 있습니다. 기본적으로 DFS 클라이언트는 15분마다 도메인 컨트롤러를 검색합니다.

이 정책 설정을 사용하면 DFS 클라이언트가 도메인 컨트롤러를 검색하는 빈도를 구성할 수 있습니다. 이 값은 분 단위로 지정됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 기본값인 15분이 적용됩니다.

참고: 선택할 수 있는 최소값은 15분입니다. 이 설정을 15분보다 작은 값으로 설정하려고 할 경우 기본값인 15분이 적용됩니다.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DFSDiscoverDC">
        <decimalTextBox refId="DFSDiscoverDialog" defaultValue="15">시간(분):</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
