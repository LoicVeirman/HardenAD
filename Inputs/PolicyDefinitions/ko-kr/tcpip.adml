<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>TCPIP 그룹 정책 템플릿 파일</displayName>
  <description>이 admx 파일은 TCPIP 구성 요소에 대한 정책 템플릿을 설명합니다.</description>
  <resources>
    <stringTable>
      <string id="TCPIP">TCPIP 설정</string>
      <string id="Ipv6Transition">IPv6 변환 기술</string>

      <string id="ISATAP_State">ISATAP 상태 설정</string>
      <string id="ISATAP_Router_Name">ISATAP 라우터 이름 설정</string>
      <string id="6to4_State">6to4 상태 설정</string>
      <string id="6to4_Router_Name">6to4 릴레이 이름 설정</string>
      <string id="6to4_Router_Name_Resolution_Interval">6to4 릴레이 이름 확인 간격 설정</string>
      <string id="Teredo_State">Teredo 상태 설정</string>
      <string id="Teredo_Server_Name">Teredo 서버 이름 설정</string>
      <string id="Teredo_Refresh_Rate">Teredo 새로 고침 빈도 설정</string>
      <string id="Teredo_Client_Port">Teredo 클라이언트 포트 설정</string>
      <string id="Teredo_Default_Qualified">Teredo 기본 적격 설정</string>

      <string id="DefaultState">기본 상태</string>
      <string id="EnabledState">사용 상태</string>
      <string id="DisabledState">사용 안 함 상태</string>
      <string id="TeredoClient">클라이언트</string>
      <string id="TeredoEnterpriseClient">엔터프라이즈 클라이언트</string>
      <string id="IPHTTPS_ClientState">IP-HTTPS 상태 설정</string>
      <string id="IPHTTPS_ClientUrl">IP-HTTPS URL</string>

      <string id="ISATAP_State_Help">이 정책 설정으로 주소-라우터 및 호스트-호스트, 호스트-라우터 및 라우터-호스트 간의 자동 터널링 기술인 IPv4 인트라넷에서 IPv6 호스트 간에 유니캐스트 IPv6 연결을 제공하기 위해 사용되는 ISATAP(Intra-Site Automatic Tunnel Addressing Protocol)를 구성할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.

이 정책 설정을 사용하면 다음 설정 중 하나로 ISATAP를 구성할 수 있습니다.

정책 기본 상태: 호스트에 ISATAP 인터페이스가 없습니다.

정책 사용 상태: ISATAP 이름이 확인되면 호스트는 상태 비저장 주소 자동 구성을 통해 ISATAP 라우터로부터 받은 각 접두사에 대한 주소와 링크-로컬 주소로 구성된 ISATAP를 갖게 됩니다. ISATAP 이름이 확인되지 않으면 호스트는 링크-로컬 주소로 구성된 ISATAP 인터페이스를 갖게 됩니다.

정책 사용 안 함 상태: 호스트에 ISATAP 인터페이스가 없습니다.

</string>

      <string id="ISATAP_Router_Name_Help">ISATAP 라우터의 라우터 이름이나 IPv4(인터넷 프로토콜 버전 4) 주소를 지정할 수 있습니다.

이 정책 설정을 사용하면 ISATAP 라우터의 라우터 이름이나 IPv4 주소를 지정할 수 있습니다. 텍스트 상자에 ISATAP 라우터의 IPv4 주소를 입력할 경우 DNS 서비스가 필요하지 않습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.



</string>


      <string id="6to4_State_Help">이 정책 설정으로 IPv4 인터넷을 통해 IPv6 사이트와 호스트 간에 유니캐스트 IPv6 연결을 제공하기 위해 사용되는 주소 할당 및 라우터-라우터 간 자동 터널링 기술인 6to4를 구성할 수 있습니다. 6to4는 글로벌 주소 접두사인 2002:WWXX:YYZZ::/48을 사용하며 여기서 문자는 사이트에 할당된 글로벌 IPv4 주소(w.x.y.z)의 16진수 표현입니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.

이 정책 설정을 사용하면 다음 설정 중 하나로 6to4를 구성할 수 있습니다.

정책 기본 상태: 6to4가 꺼지고 6to4를 통한 연결을 사용할 수 없게 됩니다.

정책 사용 상태: 글로벌 IPv4 주소가 있으면 호스트는 6to4 인터페이스를 갖게 됩니다. 글로벌 IPv4 주소가 없으면 호스트는 6to4 인터페이스를 갖지 못합니다.

정책 사용 안 함 상태: 6to4가 꺼지고 6to4를 통한 연결을 사용할 수 없게 됩니다.

</string>

      <string id="6to4_Router_Name_Help">6to4 호스트에 대한 6to4 릴레이 이름을 지정할 수 있습니다. 6to4 릴레이는 6to4 호스트에서 보낸 IPv6 네트워크 트래픽에 대해 기본 게이트웨이로 사용됩니다. 호스트에서 6to4 연결을 사용할 수 없으면 6to4 릴레이 이름 설정이 적용되지 않습니다.

이 설정을 사용하면 6to4 호스트에 대한 릴레이 이름을 지정할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용되고 6to4 호스트에 대한 릴레이 이름을 지정할 수 없습니다.



</string>

      <string id="6to4_Router_Name_Resolution_Interval_Help">릴레이 이름을 확인하는 간격을 지정할 수 있습니다. 호스트에서 6to4 연결을 사용할 수 없으면 6to4 릴레이 이름 확인 간격 설정이 적용되지 않습니다.

이 정책 설정을 사용하면 릴레이 이름을 정기적으로 확인하는 시간 간격 값을 지정할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.



</string>

      <string id="Teredo_State_Help">이 정책 설정을 사용하면 IPv4 인터넷에서 유니캐스트 IPv6 연결을 제공하는 주소 할당 및 자동 터널링 기술인 Teredo를 구성할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.

이 정책 설정을 사용하면 다음 설정 중 하나로 Teredo를 구성할 수 있습니다.

기본: 기본 상태는 " 클라이언트" 입니다.

사용 안 함: 호스트에 Teredo 인터페이스가 없습니다.

클라이언트: 호스트가 도메인 컨트롤러를 포함하는 네트워크에 있지 않을 때만 Teredo 인터페이스가 나타납니다.

엔터프라이즈 클라이언트: Teredo 인터페이스가 항상 나타나며 호스트가 도메인 컨트롤러를 포함하는 네트워크에 있을 때에도 나타납니다



</string>

      <string id="Teredo_Server_Name_Help">이 정책 설정을 사용하면 Teredo 서버의 이름을 지정할 수 있습니다. 이 정책 설정이 적용되는 Teredo 클라이언트 컴퓨터에서 이 서버 이름이 사용됩니다.

이 정책 설정을 사용하면 Teredo 클라이언트에 적용되는 Teredo 서버 이름을 지정할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터의 로컬 설정을 사용하여 Teredo 서버 이름이 결정됩니다.



</string>

      <string id="Teredo_Refresh_Rate_Help">Teredo 새로 고침 빈도를 구성할 수 있습니다.

참고: Teredo 클라이언트는 정기적으로(기본적으로 30초마다) Teredo 서버에 하나의 라우터 요청 패킷을 보냅니다. Teredo 서버는 이에 대한 응답으로 라우터 알림 패킷을 보냅니다. 정기적으로 전송되는 이 패킷은 Teredo 클라이언트의 NAT 장치에 대한 변환 테이블에서 IP 주소와 UDP 포트 매핑을 새로 고칩니다.

이 정책 설정을 사용하면 새로 고침 빈도를 지정할 수 있습니다.  Teredo 클라이언트의 NAT 장치에 있는 포트 매핑보다 긴 새로 고침 빈도를 선택하면 Teredo에서 작동을 중지하거나 연결이 때때로 중단될 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터의 로컬 설정을 사용하여 새로 고침 빈도가 구성됩니다. 기본 새로 고침 빈도는 30초입니다.



</string>

      <string id="Teredo_Client_Port_Help">Teredo 클라이언트에서 패킷을 보내는 데 사용할 UDP 포트를 선택할 수 있습니다. 기본값인 0으로 두면 운영 체제에서 포트를 선택합니다(권장). 시스템에서 이미 사용하고 있는 UDP 포트를 선택하면 Teredo 클라이언트 초기화가 실패합니다.

이 정책 설정을 사용하면 Teredo 클라이언트에 대한 UDP 포트를 사용자 지정할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.



</string>

      <string id="Teredo_Default_Qualified_Help">이 정책 설정을 사용하면 Teredo에서 통신을 수행할 수 있도록 설정할 수 있습니다.이 절차를 자격 부여라고 합니다. 기본적으로 Teredo는 사용되지 않을 경우 휴면 상태로 들어 갑니다. 자격 부여 프로세스는 Teredo의 휴면 상태를 해제합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.

이 정책 설정에는 하나의 상태만 포함됩니다.

정책 사용 상태: 기본 적격을 사용하면 Teredo에서 자격 부여를 즉시 시도한 다음 프로세스가 성공하면 적격 상태를 유지합니다.

</string>

      <string id="IPHTTPS_ClientState_Help">이 정책 설정을 사용하면 HTTPS 프로토콜을 사용하여 원격 네트워크에 IP 연결을 제공하는 터널링 기술인 IP-HTTPS를 구성할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 호스트 설정이 사용됩니다.

이 정책 설정을 사용하면 IP-HTTPS 서버 URL을 지정할 수 있습니다. 다음 설정 중 하나로 IP-HTTPS를 구성할 수 있습니다.

정책 기본 상태: 다른 연결 옵션이 없을 경우 IP-HTTPS 인터페이스가 사용됩니다.

정책 사용 상태: 호스트에 다른 연결 옵션이 제공되어도 항상 IP-HTTPS 인터페이스가 사용됩니다.

정책 사용 안 함 상태: 호스트에 IP-HTTPS 인터페이스가 없습니다.



</string>

      <string id="Parameters">매개 변수</string>
      <string id="Windows_Scaling_Heuristics_State">창 크기 조정 추론 상태 설정</string>
      <string id="Windows_Scaling_Heuristics_Help">이 정책 설정을 사용하면 창 크기 추론을 구성할 수 있습니다. 창 크기 조정 추론은 많은 방화벽과 창 크기 조정 옵션을 올바르게 해석하지 않는 기타 중간 장비로 인해 발생한 연결 및 처리량 문제를 확인하는 알고리즘입니다.

이 정책 설정을 구성하지 않으면 로컬 호스트 설정이 사용됩니다.

이 정책 설정을 사용하면 창 크기 조정 추론이 사용되며 시스템에서 연결 및 처리량 문제를 확인하고 적절한 측정을 수행합니다.

이 정책 설정을 사용하지 않으면 창 크기 조정 추론이 사용되지 않으므로 시스템에서 방화벽이나 기타 중간 장비로 인한 연결 및 처리량 문제를 확인하지 않습니다.



</string>

      <string id="IP_Stateless_Autoconfiguration_Limits_State">Set IP Stateless Autoconfiguration Limits State(IP 상태 비저장 자동 구성 제한 상태 설정)</string>
      <string id="IP_Stateless_Autoconfiguration_Limits_Help">이 정책 설정을 사용하면 IP 상태 비저장 자동 구성 제한을 구성할 수 있습니다.

이 정책 설정을 사용하거나 구성하지 않으면 IP 상태 비저장 자동 구성 제한이 사용되고 시스템에서 자동 구성된 주소 및 경로 수를 제한합니다.

이 정책 설정을 사용하지 않으면 IP 상태 비저장 자동 구성 제한이 사용되지 않고 시스템에서 자동 구성된 주소 및 경로 수를 제한하지 않습니다.



</string>

</stringTable>

<presentationTable>

      <presentation id="StateSelection">
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">다음 상태 중에서 선택합니다.</dropdownList>
      </presentation>

      <presentation id="RouterName">
        <textBox refId="RouterNameBox">
          <label>라우터 또는 릴레이 이름을 입력합니다.</label>
        </textBox>
      </presentation>

      <presentation id="RouterNameResolutionInterval">
        <text>이 설정을 통해 6to4 릴레이 이름 확인 간격을 사용자 지정할 수 있습니다.</text>
        <decimalTextBox refId="RouterNameResolutionIntervalBox" defaultValue="1440">분:  </decimalTextBox>
      </presentation>

      <presentation id="TeredoServerName">
        <textBox refId="TeredoServerNameBox">
          <label>Teredo 서버 이름을 입력합니다.</label>
        </textBox>
      </presentation>

      <presentation id="TeredoRefreshRate">
        <text>이 설정을 통해 Teredo 클라이언트 새로 고침 빈도를 사용자 지정할 수 있습니다.</text>
        <text>범위는 1에서 86400초(1일)까지입니다.</text>
        <decimalTextBox refId="TeredoRefreshRateBox" defaultValue="30">초:  </decimalTextBox>
      </presentation>

      <presentation id="TeredoClientPort">
        <text>이 설정을 통해 Teredo 클라이언트 포트를 사용자 지정할 수 있습니다.</text>
        <text>범위는 0에서 65535까지입니다. 기본값(권장)은 0으로 로컬 시스템에서 포트를 선택하도록 합니다.</text>
        <decimalTextBox refId="TeredoClientPortBox" defaultValue="0"> </decimalTextBox>
      </presentation>

      <presentation id="IPHTTPS_ClientState">
        <textBox refId="IPHTTPSClientUrlBox">
          <label>IPHTTPS URL을 입력합니다.</label>
        </textBox>
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">다음 옵션 중에서 인터페이스 상태를 선택합니다.</dropdownList>
      </presentation>

</presentationTable>

</resources>
</policyDefinitionResources>
