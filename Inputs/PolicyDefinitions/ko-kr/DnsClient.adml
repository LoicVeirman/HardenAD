<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>여기에 표시 이름 입력</displayName>
  <description>여기에 설명 입력</description>
  <resources>
    <stringTable>
      <string id="DNS_Client">DNS 클라이언트</string>
      <string id="DNS_Domain">연결별 DNS 접미사</string>
      <string id="DNS_Domain_Help">연결에 따른 DNS 접미사를 지정합니다. 이 정책 설정은 로컬 연결별 DNS 접미사와 DHCP를 사용하여 구성된 DNS 접미사보다 우선합니다.

이 정책 설정을 사용하려면 사용을 클릭한 다음 해당 DNS 접미사를 나타내는 문자열 값을 입력하십시오.

이 정책 설정을 사용하면 입력한 DNS 접미사가 이 정책 설정을 수신하는 컴퓨터에 사용되는 모든 네트워크 연결에 적용됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 또는 DHCP 제공 연결별 DNS 접미사(구성된 경우)가 컴퓨터에서 사용됩니다.</string>
      <string id="DNS_NameServer">DNS 서버</string>
      <string id="DNS_NameServer_Help">이름 확인을 시도했을 때 컴퓨터에서 쿼리를 전송하는 DNS 서버를 정의합니다. 이 정책 설정은 로컬로 구성된 DNS 서버와 DHCP를 사용하여 구성된 DNS 서버의 목록보다 우선합니다.

이 정책 설정을 사용하려면 사용을 클릭하고 사용 가능한 필드에 공백으로 구분된 IP 주소 목록을 입력합니다. 이 정책 설정을 사용하려면 IP 주소를 하나 이상 입력해야 합니다.

이 정책 설정을 사용하면 DNS 서버 목록이 이 정책 설정을 수신하는 컴퓨터에 사용되는 모든 네트워크 연결에 적용됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터는 로컬 또는 DHCP 제공 DNS 서버 목록(구성된 경우)을 사용합니다.</string>
      <string id="DNS_PrimaryDnsSuffix">주 DNS 접미사</string>
      <string id="DNS_PrimaryDnsSuffix_Help">컴퓨터에서 DNS 이름 등록과 DNS 이름 확인에 사용되는 주 DNS 접미사를 지정합니다.

이 정책 설정을 사용하려면 사용을 클릭하고 할당하려는 전체 주 DNS 접미사를 입력하십시오 (예: microsoft.com).

중요: 이 정책 설정에 대한 변경 내용을 이 설정을 수신하는 컴퓨터에 적용하려면 Windows를 다시 시작해야 합니다.

이 정책 설정을 사용하면 이 정책 설정이 시스템 제어판을 사용하여 DNS 접미사 및 NetBIOS 컴퓨터 이름 대화 상자에서 구성한 주 DNS 접미사보다 우선합니다.

이 정책 설정을 사용하면 로컬 관리자를 포함한 사용자가 주 DNS 접미사를 변경하지 못하도록 할 수 있습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 각 컴퓨터는 자신이 가입되어 있는 Active Directory 도메인의 DNS 이름에 해당하는 로컬 주 DNS 접미사를 사용합니다.</string>
      <string id="DNS_RegisterAdapterName">연결별 DNS 접미사로 DNS 레코드 등록</string>
      <string id="DNS_RegisterAdapterName_Help">동적 DNS 등록을 수행하는 컴퓨터에서 A 및 PTR 리소스 레코드를 컴퓨터 이름 및 연결별 DNS 접미사의 연결로 등록하고 해당 컴퓨터 이름 및 주 DNS 접미사의 연결로 등록할지를 지정합니다.

기본적으로 동적 DNS 등록을 실행하는 DNS 클라이언트에서는 A 및 PTR 리소스 레코드를 컴퓨터 이름 및 주 DNS 접미사의 연결로 등록합니다. 예를 들어 컴퓨터 이름이 mycomputer이고 주 DNS 접미사가 microsoft.com이면 mycomputer.microsoft.com으로 등록됩니다.

이 정책 설정을 사용하면 컴퓨터는 A 및 PTR 리소스 레코드를 주 DNS 접미사로 등록하고 연결에 따른 DNS 접미사로 등록합니다. 이 내용은 이 정책 설정을 수신하는 컴퓨터에 사용되는 모든 네트워크 연결에 적용됩니다.

예를 들어 컴퓨터 이름이 mycomputer이고 주 DNS 접미사가 microsoft.com이고 연결별 DNS 접미사가 VPNconnection일 때 이 정책 설정을 사용하면 A 및 PTR 리소스 레코드가 mycomputer.VPNconnection 및 mycomputer.microsoft.com으로 등록됩니다.

중요: 동적 DNS 등록을 사용하지 않으면 DNS 클라이언트 컴퓨터에서는 이 정책 설정이 무시됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 DNS 클라이언트 컴퓨터는 연결에 따른 DNS 접미사를 사용하여 A 및 PTR 리소스 레코드를 등록하지 않습니다.</string>
      <string id="DNS_RegisterReverseLookup">PTR 레코드 등록</string>
      <string id="DNS_RegisterReverseLookup_DoNotRegister">등록 안 함</string>
      <string id="DNS_RegisterReverseLookup_Help">DNS 클라이언트 컴퓨터에서 PTR 리소스 레코드를 등록할지를 지정합니다.

기본적으로 동적 DNS 등록에서 해당 A 리소스 레코드를 성공적으로 등록했을 때만 PTR 리소스 레코드 등록을 시도하도록 DNS 클라이언트가 구성됩니다.

이 정책 설정을 사용하면 PTR 레코드 등록에서 선택한 옵션에 의해 PTR 레코드 등록이 결정됩니다.

이 정책 설정을 사용하려면 사용을 클릭한 다음 드롭다운 목록에서 다음 옵션 중 하나를 선택합니다.

등록 안 함:  컴퓨터에서 PTR 리소스 레코드 등록을 시도하지 않습니다.

등록: A 레코드 등록의 성공 여부에 관계없이 컴퓨터에서 PTR 리소스 레코드 등록을 시도합니다.

A 레코드 등록이 성공한 때에만 등록: A 레코드 등록이 성공한 때에만 컴퓨터에서 PTR 리소스 레코드 등록을 시도합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬로 구성된 설정이 사용됩니다.</string>
      <string id="DNS_RegisterReverseLookup_Register">등록</string>
      <string id="DNS_RegisterReverseLookup_RegisterOnlyIfASucceeds">A 레코드 등록이 성공한 때에만 등록</string>
      <string id="DNS_RegistrationEnabled">동적 업데이트</string>
      <string id="DNS_RegistrationEnabled_Help">DNS 동적 업데이트가 사용하도록 설정되었는지를 지정합니다. DNS 동적 업데이트가 구성된 컴퓨터는 자동으로 DNS 리소스 레코드를 DNS 서버에 등록 및 업데이트합니다.

이 정책 설정을 사용하거나 구성하지 않으면 컴퓨터는 연결별 동적 DNS 등록을 사용하도록 설정된 모든 네트워크 연결에서 동적 DNS 등록을 사용하려고 시도합니다. 네트워크 연결에 대해 동적 DNS 등록을 사용하려면 연결별 구성에서 동적 DNS 등록을 허용하고 이 정책 설정을 사용하지 않도록 설정해야 합니다.

이 정책 설정을 사용하지 않으면 컴퓨터는 개별 네트워크 연결의 구성에 관계없이 모든 네트워크 연결에 동적 DNS 등록을 사용하지 않습니다.</string>
      <string id="DNS_RegistrationOverwritesInConflict">충돌이 있는 주소 대체</string>
      <string id="DNS_RegistrationOverwritesInConflict_Help">동적 업데이트에서 충돌하는 IP 주소를 포함하는 기존 리소스 레코드를 덮어쓸지 여부를 지정합니다.

이 정책 설정은 보안 동적 업데이트를 사용하지 않는 DNS 영역에 주소 (A) 리소스 레코드를 등록하는 컴퓨터에 사용됩니다. 보안 동적 업데이트는 리소스 레코드의 소유권을 보존하고 DNS 클라이언트가 다른 컴퓨터에서 등록한 레코드를 덮어쓰지 않도록 합니다.

보안 동적 업데이트를 사용하지 않는 영역에서 리소스 레코드를 동적 업데이트하는 동안 클라이언트의 호스트 이름을 클라이언트에 현재 사용 중인 것과 다른 IP 주소에 연결하는 A 리소스 레코드가 존재할 수 있습니다. 기본적으로 DNS 클라이언트에서는 기존 A 리소스 레코드를 클라이언트의 현재 IP 주소를 사용하는 A 리소스 레코드와 바꾸려고 시도합니다.

이 정책 설정을 사용하거나 구성하지 않으면 DNS 클라이언트에서는 기본 동작을 유지하고 동적 업데이트 중에 충돌하는 A 리소스 레코드를 바꾸려고 시도합니다.

이 정책 설정을 사용하지 않으면 충돌하는 IP 주소를 포함하는 기존 A 리소스 레코드는 동적 업데이트 중에 바뀌지 않고 이벤트 뷰어에 오류가 기록됩니다.</string>
      <string id="DNS_RegistrationRefreshInterval">등록 새로 고침 간격</string>
      <string id="DNS_RegistrationRefreshInterval_Help">DNS 클라이언트에서 A 및 PTR 리소스의 등록을 새로 고치는 데 사용되는 간격을 지정합니다. 이 정책 설정은 동적 DNS 업데이트를 수행하는 컴퓨터에만 적용됩니다.

A 및 PTR 리소스 레코드의 동적 DNS 등록을 수행하도록 구성된 컴퓨터에서는 레코드가 변경되지 않더라도 정기적으로 해당 레코드를 DNS 서버에 등록합니다. 이렇게 등록하려면 부실 레코드를 삭제하도록 구성된 DNS 서버에 레코드가 최신의 상태임을 알려서 자동으로 제거(청소)되지 않도록 해야 합니다.

경고: 영역에서 레코드 청소를 사용하는 경우 이 정책 설정 값이 DNS 영역 새로 고침 간격 값보다 짧아야 합니다. 등록 새로 고침 간격을 DNS 영역의 새로 고침 간격보다 크게 설정하면 A 및 PTR 리소스 레코드가 삭제될 수도 있습니다.

등록 새로 고침 간격을 지정하려면 사용을 클릭한 다음 1800보다 큰 값을 입력합니다. 지정하는 값은 등록 새로 고침 간격으로 사용되는 시간(초)입니다. 예를 들어 1800초는 30분입니다.

이 정책 설정을 사용하면 지정한 등록 새로 고침 간격이 이 정책 설정을 수신하는 컴퓨터에 사용되는 모든 네트워크 연결에 적용됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터는 로컬 또는 DHCP 제공 설정을 사용합니다. 기본적으로 고정 IP 주소로 구성된 클라이언트 컴퓨터는 24시간마다 한 번씩 DNS 리소스 레코드를 업데이트하려고 시도하고 DHCP 클라이언트는 DHCP 임대가 허용되거나 갱신될 때마다 DNS 리소스 레코드를 업데이트하려고 시도합니다.</string>
      <string id="DNS_RegistrationTtl">A 및 PTR 레코드의 TTL 값</string>
      <string id="DNS_RegistrationTtl_Help">

이 정책 설정이 적용되는 컴퓨터에서 등록한 A 및 PTR 리소스 레코드에 있는 TTL(Time-To-Live) 필드의 값을 지정합니다.

TTL을 지정하려면 사용을 클릭하고 값(초)을 입력하십시오(예를 들어 900은 15분과 같음).

이 정책 설정을 사용하면 지정한 TTL 값이 이 정책 설정을 수신하는 컴퓨터에 사용되는 모든 네트워크 연결에 대해 등록된 DNS 리소스 레코드에 적용됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터는 DNS에 지정된 TTL 설정을 사용합니다. 기본적으로 TTL은 1200초(20분)입니다.</string>
      <string id="DNS_SearchList">DNS 접미사 검색 목록</string>
      <string id="DNS_SearchList_Help">해당 이름에 대한 DNS 쿼리를 제출하기 전에 정식 이름이 아닌 단일 이름에 DNS 접미사를 덧붙일지를 지정합니다.

정식 이름이 아닌 단일 레이블 이름에는 점이 포함되어 있지 않습니다. 이름 "example"은 단일 레이블 이름입니다. 이 이름은 정규화된 도메인 이름(예: "example.microsoft.com")과 다릅니다.

이 정책 설정을 수신하는 컴퓨터는 단일 레이블 이름에 대한 DNS 쿼리에 접미사를 하나 이상 첨부합니다. 예를 들어 이 정책 설정을 "microsoft.com" 접미사와 함께 사용하도록 설정한 경우 단일 레이블 이름 "example"에 대한 DNS 쿼리는 DNS 서버로 전송되기 전에 "example.microsoft.com"으로 수정됩니다.

이 정책 설정을 사용하려면 사용을 클릭한 다음 단일 레이블 이름에 추가할 DNS 접미사를 나타내는 문자열 값을 입력합니다. 접미사를 하나 이상 지정해야 합니다. 접미사를 여러 개 지정하려면 쉼표로 구분된 문자열(예: "microsoft.com,serverua.microsoft.com,office.microsoft.com")을 사용합니다.

이 정책 설정을 사용하면 각 쿼리에 대해 DNS 접미사가 한 번에 하나씩 첨부됩니다. 쿼리가 실패하면 실패한 접미사 위치에 새 DNS 접미사를 추가해서 이 새 쿼리를 제출합니다. 왼쪽에 있는 값부터 시작하여 쿼리가 성공하거나 접미사를 모두 시도할 때까지 문자열에 나타나는 순서대로 값을 사용합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 주 DNS 접미사 및 네트워크 연결별 DNS 접미사가 비정규화된 쿼리에 첨부됩니다.</string>
      <string id="DNS_UpdateSecurityLevel">보안 수준 업데이트</string>
      <string id="DNS_UpdateSecurityLevel_Help">동적 DNS 업데이트에 대한 보안 수준을 지정합니다.

이 정책 설정을 사용하려면 사용을 클릭하고 다음 값 중 하나를 선택합니다.

보안 후 보안 해제 - 컴퓨터에서는 비보안 동적 업데이트가 거부된 경우에만 보안 동적 업데이트를 전송합니다.

보안 해제 - 컴퓨터에서는 비보안 동적 업데이트만 전송합니다.

보안 - 컴퓨터에서는 보안 동적 업데이트만 전송합니다.

이 정책 설정을 사용하면 동적 DNS 업데이트를 전송하려고 시도하는 컴퓨터에서는 이 정책 설정에 지정된 보안 수준을 사용합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 로컬 설정이 사용됩니다. 기본적으로 DNS 클라이언트는 먼저 비보안 동적 업데이트를 시도합니다. 비보안 업데이트가 거부되면 클라이언트는 보안 업데이트를 시도합니다.</string>
      <string id="DNS_UpdateSecurityLevel_OnlySecure">보안</string>
      <string id="DNS_UpdateSecurityLevel_OnlyUnsecure">보안 해제</string>
      <string id="DNS_UpdateSecurityLevel_UnsecureFollowedBySecure">보안 후 보안 해제</string>
      <string id="DNS_UpdateTopLevelDomainZones">최상위 도메인 영역 업데이트</string>
      <string id="DNS_UpdateTopLevelDomainZones_Help">단일 레이블 이름을 가진 영역에 동적 업데이트를 전송할 수 있는지를 지정합니다. 이러한 영역을 최상위 도메인 영역(예: "com")이라고 합니다.

기본적으로 동적 DNS 업데이트를 수행하도록 구성된 DNS 클라이언트는 권한 있는 영역이 최상위 도메인 또는 루트 영역이 아닌 이상 DNS 리소스 레코드에 대해 권한이 있는 DNS 영역을 업데이트합니다.

이 정책 설정을 사용하면 컴퓨터에서는 루트 영역을 제외하고 업데이트해야 하는 컴퓨터의 리소스 레코드에 대해 권한이 있는 모든 영역으로 동적 업데이트를 전송합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터에서는 업데이트해야 하는 컴퓨터의 리소스 레코드에 대해 권한이 있는 루트 또는 최상위 도메인 영역으로 동적 업데이트를 전송하지 않습니다.</string>
      <string id="DNS_UseDomainNameDevolution">주 DNS 접미사 계승</string>
      <string id="DNS_UseDomainNameDevolution_Help">DNS 클라이언트에서 이름 확인 프로세스 중에 주 DNS 접미사 계승을 수행할지를 지정합니다.

계승을 수행하는 경우 DNS 클라이언트에서는 정식 이름이 아닌 단일 레이블 도메인 이름에 주 DNS 접미사 이름의 부모 접미사, 해당 접미사의 부모, 등의 순서에 따라 이름이 확인되거나 계승 설정에 지정된 수준까지 접미사를 추가하여 쿼리를 만듭니다. 사용자 또는 응용 프로그램에서 단일 레이블 도메인 이름에 대한 쿼리를 제출할 때 계승이 사용될 수 있습니다.

DNS 클라이언트는 인터넷 프로토콜(TCP/IP) 속성 대화 상자의 고급 TCP/IP 설정에 있는 DNS 탭의 주 DNS 도메인 접미사 및 연결별 DNS 접미사 추가 라디오 단추 및 주 DNS 접미사의 부모 접미사 추가 확인란의 상태를 기반으로 정식 이름이 아닌 단일 레이블 도메인 이름에 DNS 접미사를 추가합니다.

그룹 정책을 사용하여 전역 접미사 검색 목록을 구성한 경우에는 계승이 사용되지 않습니다.

전역 접미사 검색 목록을 구성하지 않고 주 DNS 도메인 접미사 및 연결별 DNS 접미사 추가 라디오 단추를 선택하면 DNS 클라이언트에서는 DNS 쿼리를 전송할 때 단일 레이블 이름에 다음 이름을 추가합니다.

주 DNS 접미사: 시스템 제어판의 컴퓨터 이름 탭에서 지정합니다.

각 연결별 DNS 접미사: DHCP를 통해 할당되거나 각 연결에 대한 고급 TCP/IP 설정 대화 상자의 DNS 탭에 있는 이 연결 설정의 DNS 접미사 상자에서 지정됩니다.

예를 들어 사용자가 "example"과 같은 단일 레이블 이름에 대한 쿼리를 제출하면 로컬 DNS 클라이언트에서 "microsoft.com"과 같은 접미사를 덧붙인(결과: "example.microsoft.com") 다음 쿼리를 DNS 서버로 보냅니다.

DNS 접미사 검색 목록이 지정되어 있지 않으면 DNS 클라이언트에서 단일 레이블 이름에 주 DNS 접미사를 붙입니다. 이 쿼리가 실패하면 새 쿼리를 위해 해당 연결의 DNS 접미사가 붙습니다. 쿼리가 하나도 확인되지 않으면 클라이언트는 컴퓨터의 주 DNS 접미사를 계승하고(주 DNS 접미사의 가장 왼쪽 레이블 삭제) 해당 DNS 접미사를 단일 레이블 이름에 덧붙인 후에 DNS 서버에 이 새 쿼리를 제출합니다.

예를 들어 주 DNS 접미사 ooo.aaa.microsoft.com을 점이 없는 단일 레이블인 "example"에 덧붙인 상태에서 example.ooo.aaa.microsoft.com에 대한 DNS 쿼리가 실패하면 DNS 클라이언트에서는 주 DNS 접미사를 계승(가장 왼쪽 레이블 삭제)한 후 example.aaa.microsoft.com에 대한 쿼리를 제출합니다. 이 쿼리도 실패하면 주 DNS 접미사를 한 번 더 계승하고 example.microsoft.com 쿼리를 제출합니다. 이 쿼리도 실패한 경우 계승을 계속하면 계승 수준이 두 개인 example.microsoft.com 쿼리가 제출됩니다. DNS 접미사에 레이블이 두 개("microsoft.com") 있으므로 주 DNS 접미사를 더 이상 계승하지 않습니다. 주 DNS 접미사 계승 수준 정책 설정을 사용하여 계승 수준을 구성할 수 있습니다. 기본 계승 수준은 2입니다.

이 정책 설정을 사용하거나 구성하지 않으면 DNS 클라이언트에서는 확인할 단일 레이블 이름과 계승된 주 DNS 접미사로 연결된 단일 레이블 이름을 확인하려고 시도합니다.

이 정책 설정을 사용하지 않으면 DNS 클라이언트에서는 확인할 단일 레이블 이름과 계승된 주 DNS 접미사로 연결된 이름을 확인하지 않습니다.</string>
      <string id="DNS_DomainNameDevolutionLevel">주 DNS 접미사 계승 수준</string>
      <string id="DNS_DomainNameDevolutionLevel_Help">DNS 클라이언트에서 이름 확인 프로세스 중에 주 DNS 접미사 계승을 수행할 때 사용할 계승 수준을 지정합니다.

계승을 수행하는 경우 DNS 클라이언트에서는 정식 이름이 아닌 단일 레이블 도메인 이름에 주 DNS 접미사 이름의 부모 접미사, 해당 접미사의 부모, 등의 순서에 따라 이름이 확인되거나 계승 설정에 지정된 수준까지 접미사를 추가하여 쿼리를 만듭니다. 사용자 또는 응용 프로그램에서 단일 레이블 도메인 이름에 대한 쿼리를 제출할 때 계승이 사용될 수 있습니다.

DNS 클라이언트는 인터넷 프로토콜(TCP/IP) 속성 대화 상자의 고급 TCP/IP 설정에 있는 DNS 탭의 주 DNS 도메인 접미사 및 연결별 DNS 접미사 추가 라디오 단추 및 주 DNS 접미사의 부모 접미사 추가 확인란의 상태를 기반으로 정식 이름이 아닌 단일 레이블 도메인 이름에 DNS 접미사를 추가합니다.

그룹 정책을 사용하여 전역 접미사 검색 목록을 구성한 경우에는 계승이 사용되지 않습니다.

전역 접미사 검색 목록을 구성하지 않고 주 DNS 도메인 접미사 및 연결별 DNS 접미사 추가 라디오 단추를 선택하면 DNS 클라이언트에서는 DNS 쿼리를 전송할 때 단일 레이블 이름에 다음 이름을 추가합니다.

주 DNS 접미사: 시스템 제어판의 컴퓨터 이름 탭에서 지정합니다.

각 연결별 DNS 접미사: DHCP를 통해 할당되거나 각 연결에 대한 고급 TCP/IP 설정 대화 상자의 DNS 탭에 있는 이 연결 설정의 DNS 접미사 상자에서 지정됩니다.

예를 들어 사용자가 "example"과 같은 단일 레이블 이름에 대한 쿼리를 제출하면 로컬 DNS 클라이언트에서 "microsoft.com"과 같은 접미사를 덧붙인(결과: "example.microsoft.com") 다음 쿼리를 DNS 서버로 보냅니다.

DNS 접미사 검색 목록이 지정되어 있지 않으면 DNS 클라이언트에서 단일 레이블 이름에 주 DNS 접미사를 붙입니다. 이 쿼리가 실패하면 새 쿼리를 위해 해당 연결의 DNS 접미사가 붙습니다. 쿼리가 하나도 확인되지 않으면 클라이언트는 컴퓨터의 주 DNS 접미사를 계승하고(주 DNS 접미사의 가장 왼쪽 레이블 삭제) 해당 DNS 접미사를 단일 레이블 이름에 덧붙인 후에 DNS 서버에 이 새 쿼리를 제출합니다.

예를 들어 주 DNS 접미사 ooo.aaa.microsoft.com을 점이 없는 단일 레이블인 "example"에 덧붙인 상태에서 example.ooo.aaa.microsoft.com에 대한 DNS 쿼리가 실패하면 DNS 클라이언트에서는 주 DNS 접미사를 계승(가장 왼쪽 레이블 삭제)한 후 example.aaa.microsoft.com에 대한 쿼리를 제출합니다. 이 쿼리도 실패하면 주 DNS 접미사를 한 번 더 계승하고 example.microsoft.com 쿼리를 제출합니다. 이 쿼리도 실패한 경우 계승을 계속하면 계승 수준이 두 개인 example.microsoft.com 쿼리가 제출됩니다. DNS 접미사에 레이블이 두 개("microsoft.com") 있으므로 주 DNS 접미사를 더 이상 계승하지 않습니다. 이 정책 설정을 사용하여 계승 수준을 구성할 수 있습니다. 기본 계승 수준은 2입니다.

이 정책 설정과 DNS 계승을 모두 사용하면 DNS 클라이언트에서는 지정된 DNS 계승 수준을 사용합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 DNS 클라이언트에서는 기본 계승 수준 2를 사용합니다(DNS 계승을 사용하는 경우).</string>
      <string id="DNS_TurnOffMulticast">멀티캐스트 이름 확인 사용 안 함</string>
      <string id="DNS_TurnOffMulticast_Help">클라이언트 컴퓨터에서 LLMNR(로컬 링크 멀티캐스트 이름 확인)을 사용하지 않도록 지정합니다.

LLMNR은 보조 이름 확인 프로토콜입니다. LLMNR을 사용하면 단일 서브넷의 로컬 네트워크 링크를 통해 멀티캐스트를 사용하여 클라이언트 컴퓨터에서 LLMNR을 사용하는 동일한 서브넷의 다른 클라이언트 컴퓨터로 쿼리를 전송합니다. LLMNR은 DNS 서버 또는 DNS 클라이언트 구성이 필요하지 않으며 기존의 DNS 이름 확인이 불가능한 시나리오에서도 이름 확인을 가능하게 합니다.

이 정책 설정을 사용하면 클라이언트 컴퓨터의 모든 사용 가능한 네트워크 어댑터에서 LLMNR이 사용되지 않습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 모든 사용 가능한 네트워크 어댑터에서 LLMNR이 사용됩니다.</string>
      <string id="DNS_AppendToMultiLabelName">비정규화된 다중 레이블 이름 쿼리에 DNS 접미사 추가 허용</string>
      <string id="DNS_AppendToMultiLabelName_Help">원래 이름 쿼리에 실패한 경우 컴퓨터가 후속 DNS 쿼리를 보내기 전에 비정규화된 다중 레이블 이름에 접미사를 추가할 수 있도록 지정합니다.

점을 포함하지만 점으로 종결되지 않은 이름을 비정규화된 다중 레이블 이름이라고 합니다. 예를 들어 "server.corp"는 비정규화된 다중 레이블 이름이고, 이름 "server.corp.contoso.com."은 종결점이 있으므로 정규화된 이름입니다.

예를 들어 접미사 추가가 허용되는 경우 "server.corp"에 대한 비정규화된 다중 레이블 이름 쿼리를 먼저 DNS 클라이언트가 쿼리합니다. 쿼리에 성공하면 응답이 클라이언트에게 반환됩니다. 쿼리에 실패하면 비정규화된 다중 레이블 이름이 DNS 접미사와 함께 추가됩니다. 이러한 접미사는 로컬 DNS 클라이언트의 주 도메인 접미사, 연결별 도메인 접미사 및 DNS 접미사 검색 목록의 조합에서 파생될 수 있습니다.

접미사 추가가 허용되고 주 도메인 접미사 "contoso.com"을 포함하는 DNS 클라이언트가 "server.corp"를 쿼리하는 경우 DNS 클라이언트는 먼저 "server.corp"에 대한 쿼리를 전송한 다음 첫 번째 쿼리가 실패하면 "server.corp.contoso.com."에 대한 쿼리를 전송합니다.

이 정책 설정을 사용하면 원래 이름 쿼리에 실패한 경우 비정규화된 다중 레이블 이름에 접미사를 추가할 수 있습니다.

이 정책 설정을 사용하지 않으면 원래 이름 쿼리에 실패하더라도 비정규화된 다중 레이블 이름 쿼리에 접미사가 추가되지 않습니다.

이 정책 설정을 구성하지 않으면 컴퓨터는 로컬 DNS 클라이언트 설정을 사용하여 비정규화된 다중 레이블 이름의 쿼리 동작을 확인합니다.</string>
      <string id="DNS_SmartMultiHomedNameResolution">스마트 멀티홈 이름 확인 사용 안 함</string>
      <string id="DNS_SmartMultiHomedNameResolution_Help">멀티홈 DNS 클라이언트에서 네트워크를 통해 이름 확인을 최적화하도록 지정합니다.  이 설정을 사용하면 모든 네트워크를 통해 병렬 DNS, LLMNR(로컬 링크 멀티캐스트 이름 확인) 및 NetBT(NetBIOS over TCP/IP) 쿼리를 실행하여 성능이 향상됩니다. 긍정적인 응답이 여러 개 수신되는 경우 네트워크 바인딩 순서를 사용하여 적용할 응답을 결정합니다.

이 정책 설정을 사용하면 DNS 클라이언트는 최적화를 수행하지 않습니다.  모든 네트워크에서 DNS 쿼리가 먼저 실행됩니다. 그런 다음 DNS 쿼리가 실패하면 LLMNR 쿼리가 실행되고, LLMNR 쿼리가 실패하면 NetBT 쿼리가 실행됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 DNS, LLMNR 및 NetBT 쿼리를 실행할 때 이름 확인이 최적화됩니다.</string>
      <string id="DNS_SmartProtocolReorder">스마트 프로토콜 다시 정렬 사용 안 함</string>
      <string id="DNS_SmartProtocolReorder_Help">DNS 클라이언트에서 일반 이름에 대한 쿼리를 실행할 때 도메인이 아닌 네트워크의 로컬 링크 이름 확인 프로토콜 응답을 DNS 응답보다 우선 적용하도록 지정합니다. 로컬 링크 이름 확인 프로토콜의 예로는 LLMNR(로컬 링크 멀티캐스트 이름 확인), NetBT(NetBIOS over TCP/IP) 등을 들 수 있습니다.

이 정책 설정을 사용하면 DNS 클라이언트에서는 모든 네트워크에 대해 DNS 응답, LLMNR, NetBT의 순서로 적용합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 DNS 클라이언트에서는 도메인이 아닌 네트워크의 일반 이름 쿼리에 대한 로컬 링크 응답을 우선적으로 적용합니다.

참고: 이 정책 설정은 스마트 멀티홈 이름 확인 사용 안 함 정책 설정을 사용하지 않거나 구성하지 않은 경우에만 적용됩니다.</string>
      <string id="DNS_AllowFQDNNetBiosQueries">정규화된 도메인 이름에 대한 NetBT 쿼리 허용</string>
      <string id="DNS_AllowFQDNNetBiosQueries_Help">정규화된 도메인 이름에 대해 NetBT(NetBIOS over TCP/IP) 쿼리를 실행하도록 지정합니다. 

이 정책 설정을 사용하면 단일 레이블 이름 이외에 다중 레이블 및 정규화된 도메인 이름(예: "www.example.com")에 대해서도 NetBT 쿼리가 실행됩니다.  

이 정책 설정을 사용하지 않거나 구성하지 않으면 NetBT 쿼리가 단일 레이블 이름(예: "example")에 대해서만 실행되고 다중 레이블 및 정규화된 도메인 이름에 대해서는 실행되지 않습니다.</string>
      <string id="DNS_PreferLocalResponsesOverLowerOrderDns">우선 순위가 더 높은 네트워크를 통해 수신하는 경우 로컬 링크 응답이 DNS보다 우선</string>
      <string id="DNS_PreferLocalResponsesOverLowerOrderDns_Help">바인딩 순서가 더 높은 네트워크 인터페이스를 통해 수신되는 로컬 링크 이름 확인 프로토콜의 응답이 바인딩 순서가 더 낮은 네트워크 인터페이스의 DNS 응답보다 우선하도록 지정합니다. 로컬 링크 이름 확인 프로토콜의 예로는 LLMNR(로컬 링크 멀티캐스트 이름 확인), NetBT(NetBIOS over TCP/IP) 등을 들 수 있습니다.

이 정책 설정을 사용하면 바인딩 순서가 더 높은 로컬 링크 프로토콜의 응답이 DNS 응답보다 우선합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 바인딩 순서가 더 낮은 네트워크의 DNS 응답이 바인딩 순서가 더 높은 네트워크에서 수신되는 로컬 링크 프로토콜의 응답보다 우선합니다.

참고: 이 정책 설정은 스마트 멀티홈 이름 확인 사용 안 함 정책 설정을 사용하지 않거나 구성하지 않은 경우에만 적용됩니다.</string>
      <string id="DNS_IdnEncoding">IDN 인코딩 사용 안 함</string>
      <string id="DNS_IdnEncoding_Help">컴퓨터가 WINS 서버를 구성하지 않은 도메인이 아닌 네트워크에 있을 때 DNS 클라이언트에서 IDN(Internationalized Domain Name)을 Punycode로 변환해야 하는지 여부를 지정합니다.

이 정책 설정을 사용하면 IDN이 Punycode로 변환되지 않습니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터가 WINS 서버를 구성하지 않은 도메인이 아닌 네트워크에 있을 때 IDN이 Punycode로 변환됩니다.</string>
      <string id="DNS_IdnMapping">IDN 매핑</string>
      <string id="DNS_IdnMapping_Help">DNS 클라이언트에서 IDN(Internationalized Domain Name)을 문자열의 정식 유니코드 표현인 Nameprep 형태로 변환해야 하는지 여부를 지정합니다.

이 정책 설정을 사용하면 IDN이 Nameprep 형태로 변환됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 IDN이 Nameprep 형태로 변환되지 않습니다.</string>
      <string id="DNS_Doh">HTTPS(DoH) 이름 해결 방법을 통한 DNS 구성</string>
      <string id="DNS_Doh_Disabled">DoH 금지</string>
      <string id="DNS_Doh_Auto">DoH 허용</string>
      <string id="DNS_Doh_Force">DoH 필요</string>
      <string id="DNS_Doh_Help">DNS 클라이언트가 HTTPS(DoH)를 통해 DNS 이름 확인을 수행할지를 지정합니다.

기본적으로 DNS 클라이언트는 UDP 또는 TCP를 통한 클래식 DNS 이름 확인을 수행합니다. 이 설정을 사용하면 DNS 클라이언트가 DoH 프로토콜을 사용하여 도메인 이름을 확인할 수 있습니다.

이 정책 설정을 사용하려면 [사용]을 클릭한 다음 드롭다운 목록에서 다음 옵션 중 하나를 선택합니다.

DoH 금지: DoH 이름 확인이 수행되지 않습니다.

DoH 허용: DNS 서버가 지원하는 경우 DoH 쿼리를 수행합니다. 지원하지 않는 경우 클래식 이름 확인을 사용합니다.

DoH 필수: DoH 이름 확인만 허용합니다. 구성된 DoH 지원 DNS 서버가 없다면 이름 확인에 실패합니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 컴퓨터에서 로컬로 구성된 설정을 사용합니다. DoH 설정 자동 검색에 필요하므로 DDR(Discovery of Designated Resolvers) 일반 텍스트 트래픽이 허용됩니다.</string>
      <string id="DNS_Netbios">NetBIOS 설정 구성</string>
      <string id="DNS_Netbios_Disable">NetBIOS 이름 확인 비활성화</string>
      <string id="DNS_Netbios_Allow">NetBIOS 이름 확인 허용</string>
      <string id="DNS_Netbios_Disable_Public_Networks">공용 네트워크에서 NetBIOS 이름 확인 비활성화</string>
      <string id="DNS_Netbios_Learning_Mode">NetBIOS 학습 모드</string>
      <string id="DNS_Netbios_Help">DNS 클라이언트가 NetBIOS를 통해 이름 확인을 수행할지 지정합니다.

기본적으로 DNS 클라이언트는 보안상의 이유로 공용 네트워크에서 NetBIOS 이름 확인을 사용하지 않도록 설정합니다.

이 정책 설정을 사용하려면 ‘사용’을 클릭한 다음 드롭다운 목록에서 다음 옵션 중 하나를 선택합니다.

NetBIOS 이름 확인 비활성화: NetBIOS 이름 확인을 허용하지 않습니다.

NetBIOS 이름 확인 허용: 항상 NetBIOS 이름 확인을 허용합니다. 

공용 네트워크에서 NetBIOS 이름 확인 비활성화: 공용 네트워크에 연결되지 않은 네트워크 어댑터에서만 NetBIOS 이름 확인을 허용합니다.

NetBIOS 학습 모드: 항상 NetBIOS 이름 확인을 허용하고 mDNS/LLMNR 쿼리가 실패한 후 대체(fallback)로 사용합니다.

이 정책 설정을 사용하지 않도록 설정하거나 이 정책 설정을 구성하지 않으면 컴퓨터에서 로컬로 구성된 설정을 사용합니다.</string>
      <string id="DNS_Ddr">지정된 확인 프로그램(DDR) 프로토콜 검색 구성</string>
      <string id="DNS_Ddr_Help">DNS 클라이언트가 DDR 프로토콜을 사용할지 여부를 지정합니다.

지정된 확인 프로그램 검색(DDR) 프로토콜을 사용하면 확인자의 IP 주소만 알려진 경우 Windows가 암호화되지 않은 DNS에서 암호화된 DNS로 이동할 수 있습니다.

이 정책을 사용하면 DNS 클라이언트가 DDR 프로토콜을 사용합니다.

이 정책 설정을 사용하지 않거나 이 정책 설정을 구성하지 않으면 컴퓨터에서 로컬로 구성된 설정을 사용합니다.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DNS_Domain">
        <textBox refId="DNS_DomainLabel">
          <label>DNS 접미사:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_NameServer">
        <textBox refId="DNS_NameServerLabel">
          <label>IP 주소:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_PrimaryDnsSuffix">
        <textBox refId="DNS_PrimaryDnsSuffixBox">
          <label>기본 DNS 접미사를 입력하십시오.</label>
        </textBox>
      </presentation>
      <presentation id="DNS_RegisterReverseLookup">
        <dropdownList refId="DNS_RegisterReverseLookup_Box" defaultItem="0">PTR 레코드 등록:</dropdownList>
      </presentation>
      <presentation id="DNS_RegistrationRefreshInterval">
        <decimalTextBox refId="DNS_RegistrationRefreshIntervalLabel" defaultValue="1800" spinStep="100">초:</decimalTextBox>
      </presentation>
      <presentation id="DNS_DomainNameDevolutionLevel">
        <decimalTextBox refId="DNS_DomainNameDevolutionLevelLabel" defaultValue="2" spinStep="1">주 DNS 접미사 계승 수준 설정</decimalTextBox>
      </presentation>
      <presentation id="DNS_RegistrationTtl">
        <decimalTextBox refId="DNS_RegistrationTtlLabel" defaultValue="600" spinStep="100">초:</decimalTextBox>
      </presentation>
      <presentation id="DNS_SearchList">
        <textBox refId="DNS_SearchListLabel">
          <label>DNS 접미사:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_UpdateSecurityLevel">
        <dropdownList refId="DNS_UpdateSecurityLevel_Box" defaultItem="2">보안 수준 업데이트:</dropdownList>
      </presentation>
      <presentation id="DNS_Doh">
        <dropdownList refId="DNS_Doh_Box" defaultItem="1">DoH 옵션 구성:</dropdownList>
      </presentation>
      <presentation id="DNS_Netbios">
        <dropdownList refId="DNS_Netbios_Box" defaultItem="1">NetBIOS 옵션 구성:</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
