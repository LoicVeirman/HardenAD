<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.2" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>여기에 표시 이름 입력</displayName>
  <description>여기에 설명 입력</description>
  <resources>
    <stringTable>
      <!-- Component name -->
      <string id="Cat_FileShareShadowCopyAgent">파일 공유 섀도 복사본 에이전트</string>
      <!-- Component name -->

       <string id="Pol_MaximumAge">Configure maximum age of file server shadow copies(파일 서버 섀도 복사본 최대 사용 기간 구성)</string>
       <string id="Pol_MaximumAge_Help">이 정책 설정은 파일 공유 섀도 복사본의 최대 사용 기간을 구성합니다. 이 기간보다 오래된 파일 공유 섀도 복사본은 파일 공유 섀도 복사본 에이전트에 의해 삭제됩니다. 표시된 섀도 복사본 공유와 해당 볼륨 섀도 복사본이 모두 삭제됩니다.

이 정책 설정을 사용하면 지정된 값에 따라 파일 공유 섀도 복사본의 최대 사용 기간이 결정됩니다.

이 정책 설정을 사용하지 않거나 구성하지 않으면 24시간보다 오래된 파일 공유 섀도 복사본이 삭제됩니다.

       

       </string>
     </stringTable>
     <presentationTable> 
       <presentation id="Pol_MaximumAge">
         <decimalTextBox refId="Lbl_MaximumShadowCopyAgeInHours" defaultValue="24" spinStep="1">Maximum Age of Shadow Copies to be Garbage Collected in Hours:(가비지 수집될 섀도 복사본의 최대 사용 기간(시간):)  </decimalTextBox>
       </presentation> 
    </presentationTable>
   </resources>
</policyDefinitionResources>
