<?xml version="1.0" encoding="utf-8"?>
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>라이선싱 그룹 정책</displayName>
  <description>라이선싱 그룹 정책</description>
  <resources>
    <stringTable>
        <string id="NoAcquireGT">KMS 클라이언트 온라인 AVS 유효성 검사 끄기</string>
        <string id="NoAcquireGT_Help"> 
    이 정책 설정을 통해 KMS 클라이언트 정품 인증 데이터를 자동으로 Microsoft에 전송하도록 옵트아웃(opt out)할 수 있습니다. 이 설정을 사용하면 이 컴퓨터가 Microsoft에 해당 정품 인증 상태에 대한 데이터를 전송하지 않습니다.
    이 정책 설정을 사용하지 않거나 구성하지 않으면 이 장치가 정품 인증할 때 KMS 클라이언트 정품 인증 데이터를 Microsoft 서비스로 전송합니다. 
    정책 옵션: 
        -   구성 안 함(기본값 -- 데이터를 자동으로 Microsoft로 전송)
        -   사용 안 함(데이터를 자동으로 Microsoft로 전송)
        -   사용(데이터를 Microsoft로 전송하지 않음)
        </string>
        <string id="AllowWindowsEntitlementReactivation">정품 디바이스에 대한 디바이스 다시 정품 인증 제어</string>
        <string id="AllowWindowsEntitlementReactivation_Help"> 
            이 정책 설정은 OS 다시 정품 인증이 디바이스에서 차단되었는지 여부를 제어합니다. 
            정책 옵션: 
                -   구성되지 않음(기본값 -- Windows 등록 및 다시 정품 인증이 허용됨)
                -   사용 안 함(Windows 등록 및 다시 정품 인증이 허용되지 않음)
                -   사용(Windows 등록이 허용됨)
        </string>
      <string id="SoftwareProtectionPlatform">Software Protection Platform</string>
   </stringTable>
  </resources>
</policyDefinitionResources>
