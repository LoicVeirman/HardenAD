<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>여기에 표시 이름 입력</displayName>
  <description>여기에 설명 입력</description>
  <resources>
    <stringTable>
      <string id="NoEncryptOnMove">암호화된 폴더로 이동한 파일을 자동으로 암호화 안 함</string>
      <string id="NoEncryptOnMove_Help">이 정책 설정은 파일 탐색기에서 암호화된 폴더로 이동된 파일을 암호화하지 않도록 합니다.

이 정책 설정을 사용하면 파일 탐색기에서 암호화된 폴더로 이동된 파일을 자동으로 암호화하지 않습니다.

이 설정을 사용하지 않거나 구성하지 않으면 파일 탐색기에서 암호화된 폴더로 이동된 파일을 자동으로 암호화합니다.

이 설정은 같은 볼륨 안에서 이동된 파일에만 적용됩니다. 파일이 다른 볼륨으로 이동되거나 암호화된 폴더에 새 파일을 만들면 파일 탐색기는 그 파일을 자동으로 암호화합니다.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
