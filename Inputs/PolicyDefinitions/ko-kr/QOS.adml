<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>여기에 표시 이름 입력</displayName>
  <description>여기에 설명 입력</description>
  <resources>
    <stringTable>
      <string id="QosDBMC_BestEffort_Help">최상의 서비스 종류(ServiceTypeBestEffort)의 패킷에 대한 대체 레이어-3 DSCP(Differentiated Services Code Point) 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 최상의 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMC_ControlledLoad_Help">제어된 로드 서비스 종류(ServiceTypeControlledLoad)의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 제어된 로드 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 24(0x18)가 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMC_Guaranteed_Help">보장된 서비스 종류(ServiceTypeGuaranteed)의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 보장된 서비스 종류에 연결되는 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 40(0x28)이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMC_NetworkControl_Help">네트워크 컨트롤 서비스 종류(ServiceTypeNetworkControl)의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 네트워크 컨트롤 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 48(0x30)이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMC_Qualitative_Help">질적인 서비스 종류(ServiceTypeQualitative)의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 질적인 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMNC_BestEffort_Help">최상의 서비스 종류의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞지 않는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 최상의 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMNC_ControlledLoad_Help">제어된 로드 서비스 종류의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞지 않는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 제어된 로드 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMNC_Guaranteed_Help">보장된 서비스 종류의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞지 않는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 보장된 서비스 종류에 연결되는 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMNC_NetworkControl_Help">네트워크 컨트롤 서비스 종류의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞지 않는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 네트워크 컨트롤 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDBMNC_Qualitative_Help">질적인 서비스 종류의 패킷에 대한 대체 레이어-3 DSCP 값을 지정합니다. 패킷 스케줄러는 패킷의 IP 헤더에 해당 DSCP 값을 삽입합니다.

흐름 사양에 맞지 않는 패킷에만 이 설정이 적용됩니다.

이 설정을 사용하면 질적인 서비스 종류에 연결된 기본 DSCP 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 DSCP 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 DSCP 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosDiffservByteMappingConforming">알맞은 패킷의 DSCP 값</string>
      <string id="QosDiffservByteMappingNonConforming">맞지 않는 패킷의 DSCP 값</string>
      <string id="QosMaxOutstandingSends">처리되지 않은 패킷 제한</string>
      <string id="QosMaxOutstandingSends_Help">시스템에서 허용하는 처리되지 않은 패킷의 최대 수를 지정합니다. 처리되지 않은 패킷 수가 이 한계에 도달하면 패킷 스케줄러는 해당 수가 이 제한 아래로 떨어질 때까지 네트워크 어댑터에 대한 모든 제출을 연기합니다.

"처리되지 않은 패킷"은 패킷 스케줄러가 전송을 위해 네트워크 어댑터로 제출했지만 아직 전송되지 않은 패킷입니다.

이 설정을 사용하면 처리되지 않은 패킷 수를 제한할 수 있습니다.

이 설정을 사용하지 않거나 구성하지 않으면 시스템에 아무 영향이 없습니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 최대 처리되지 않은 패킷 수를 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosNonBestEffortLimit">예약 대역폭 제한</string>
      <string id="QosNonBestEffortLimit_Help">시스템에서 예약할 수 있는 연결 대역폭의 백분율을 결정합니다. 이 값은 시스템에서 실행되는 모든 프로그램의 결합된 대역폭 예약을 제한합니다.

기본적으로 패킷 스케줄러는 연결 대역폭의 80%로 시스템을 제한하지만 사용자는 이 설정을 사용하여 기본값을 무시할 수 있습니다.

이 설정을 사용하면 "대역폭 제한" 상자를 사용하여 시스템에서 예약할 수 있는 대역폭의 양을 조정할 수 있습니다.

이 설정을 사용하지 않거나 구성하지 않으면 기본값으로 80%가 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 대역폭 제한을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosPsched">QoS 패킷 스케줄러</string>
      <string id="QosServiceTypeBestEffort_C">최상의 서비스 종류</string>
      <string id="QosServiceTypeBestEffort_NC">최상의 서비스 종류</string>
      <string id="QosServiceTypeBestEffort_PV">최상의 서비스 종류</string>
      <string id="QosServiceTypeControlledLoad_C">제어된 로드 서비스 종류</string>
      <string id="QosServiceTypeControlledLoad_NC">제어된 로드 서비스 종류</string>
      <string id="QosServiceTypeControlledLoad_PV">제어된 로드 서비스 종류</string>
      <string id="QosServiceTypeGuaranteed_C">보장된 서비스 종류</string>
      <string id="QosServiceTypeGuaranteed_NC">보장된 서비스 종류</string>
      <string id="QosServiceTypeGuaranteed_PV">보장된 서비스 종류</string>
      <string id="QosServiceTypeNetworkControl_C">네트워크 컨트롤 서비스 종류</string>
      <string id="QosServiceTypeNetworkControl_NC">네트워크 컨트롤 서비스 종류</string>
      <string id="QosServiceTypeNetworkControl_PV">네트워크 컨트롤 서비스 종류</string>
      <string id="QosServiceTypeNonConforming">맞지 않는 패킷</string>
      <string id="QosServiceTypeQualitative_C">질적인 서비스 종류</string>
      <string id="QosServiceTypeQualitative_NC">질적인 서비스 종류</string>
      <string id="QosServiceTypeQualitative_PV">질적인 서비스 종류</string>
      <string id="QosTimerResolution">타이머 확인 설정</string>
      <string id="QosTimerResolution_Help">패킷 스케줄러에서 패킷 전송의 스케줄링에 사용하는 가장 작은 시간 단위를 결정합니다. 패킷 스케줄러는 이 항목 값에서 허용하는 것보다 자주 전송하도록 패킷을 예약할 수 없습니다.

이 설정을 사용하면 시스템에서 사용하는 기본 타이머(일반적으로 10마이크로초 단위)를 변경할 수 있습니다.

이 설정을 사용하지 않거나 구성하지 않으면 시스템에 아무 영향이 없습니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 타이머 확인을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosUPM_BestEffort_Help">최상의 서비스 종류의 패킷에 대한 대체 링크 레이어(레이어-2) 우선 순위 값을 지정합니다. 패킷 스케줄러는 패킷의 계층-2 헤더에 해당 우선 순위 값을 삽입합니다.

이 설정을 사용하면 최상의 서비스 종류에 연결된 기본 우선 순위 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 우선 순위 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 레이어-2 우선 순위 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosUPM_ControlledLoad_Help">제어된 로드 서비스 종류의 패킷에 대한 대체 링크 레이어(레이어-2) 우선 순위 값을 지정합니다. 패킷 스케줄러는 패킷의 계층-2 헤더에 해당 우선 순위 값을 삽입합니다.

이 설정을 사용하면 제어된 로드 서비스 종류에 연결된 기본 우선 순위 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 우선 순위 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 레이어-2 우선 순위 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosUPM_Guaranteed_Help">보장된 서비스 종류의 패킷에 대한 대체 링크 레이어(레이어-2) 우선 순위 값을 지정합니다. 패킷 스케줄러는 패킷의 계층-2 헤더에 해당 우선 순위 값을 삽입합니다.

이 설정을 사용하면 보장된 서비스 종류에 연결된 기본 우선 순위 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 우선 순위 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 레이어-2 우선 순위 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosUPM_NetworkControl_Help">네트워크 컨트롤 서비스 종류의 패킷에 대한 대체 링크 레이어(레이어-2) 우선 순위 값을 지정합니다. 패킷 스케줄러는 패킷의 계층-2 헤더에 해당 우선 순위 값을 삽입합니다.

이 설정을 사용하면 네트워크 컨트롤 서비스 종류에 연결된 기본 우선 순위 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 우선 순위 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 레이어-2 우선 순위 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosUPM_NonConforming_Help">흐름 사양에 맞지 않는 패킷에 대한 대체 링크 레이어(레이어-2) 우선 순위 값을 지정합니다. 패킷 스케줄러는 패킷의 계층-2 헤더에 해당 우선 순위 값을 삽입합니다.

이 설정을 사용하면 맞지 않는 패킷에 연결된 기본 우선 순위 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 우선 순위 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 맞지 않는 패킷의 레이어-2 우선 순위 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosUPM_Qualitative_Help">질적인 서비스 종류의 패킷에 대한 대체 링크 레이어(레이어-2) 우선 순위 값을 지정합니다. 패킷 스케줄러는 패킷의 계층-2 헤더에 해당 우선 순위 값을 삽입합니다.

이 설정을 사용하면 질적인 서비스 종류에 연결된 기본 우선 순위 값을 변경할 수 있습니다.

이 설정을 사용하지 않으면 기본 우선 순위 값으로 0이 사용됩니다.

중요: 레지스트리에 특정 네트워크 어댑터에 대한 이 서비스 종류의 레이어-2 우선 순위 값을 지정하면 해당 네트워크 어댑터 구성 시 이 설정이 무시됩니다.</string>
      <string id="QosUserPriorityMapping">계층-2 우선 순위 값</string>
    </stringTable>
    <presentationTable>
      <presentation id="QosMaxOutstandingSends">
        <decimalTextBox refId="QosMaxOutstandingSends_Box" defaultValue="65535">패킷 수:    </decimalTextBox>
      </presentation>
      <presentation id="QosNonBestEffortLimit">
        <decimalTextBox refId="QosNonBestEffortLimit_Box" defaultValue="80" spinStep="10">대역폭 제한 (%):    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeBestEffort_C">
        <text>다음 순응 패킷에 DSCP 값 입력  </text>
        <text>최상의 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeBestEffort_NC">
        <text>다음 비순응 패킷에 DSCP 값 입력  </text>
        <text>최상의 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeBestEffort_PV">
        <text>다음 패킷에 우선 순위 값 입력  </text>
        <text>최상의 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="0">우선 순위 값    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeControlledLoad_C">
        <text>다음 순응 패킷에 DSCP 값 입력  </text>
        <text>제어된 로드 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="24">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeControlledLoad_NC">
        <text>다음 비순응 패킷에 DSCP 값 입력  </text>
        <text>제어된 로드 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeControlledLoad_PV">
        <text>다음 패킷에 우선 순위 값 입력  </text>
        <text>제어된 로드 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="4">우선 순위 값    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeGuaranteed_C">
        <text>다음 순응 패킷에 DSCP 값 입력  </text>
        <text>보장된 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="40">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeGuaranteed_NC">
        <text>다음 비순응 패킷에 DSCP 값 입력  </text>
        <text>보장된 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeGuaranteed_PV">
        <text>다음 패킷에 우선 순위 값 입력  </text>
        <text>보장된 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="5">우선 순위 값    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNetworkControl_C">
        <text>다음 순응 패킷에 DSCP 값 입력  </text>
        <text>네트워크 컨트롤 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="48">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNetworkControl_NC">
        <text>다음 비순응 패킷에 DSCP 값 입력  </text>
        <text>네트워크 컨트롤 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNetworkControl_PV">
        <text>다음 패킷에 우선 순위 값 입력  </text>
        <text>네트워크 컨트롤 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="7">우선 순위 값    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNonConforming">
        <text>맞지 않는 패킷에 대한 우선 순위 값 입력</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue">우선 순위 값    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeQualitative_C">
        <text>다음 순응 패킷에 DSCP 값 입력  </text>
        <text>질적인 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeQualitative_NC">
        <text>다음 비순응 패킷에 DSCP 값 입력  </text>
        <text>질적인 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 값:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeQualitative_PV">
        <text>다음 패킷에 우선 순위 값 입력  </text>
        <text>질적인 서비스 종류</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="0">우선 순위 값    </decimalTextBox>
      </presentation>
      <presentation id="QosTimerResolution">
        <decimalTextBox refId="QosTimerResolution_Box" defaultValue="10">타이머 단위(마이크로초):    </decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
