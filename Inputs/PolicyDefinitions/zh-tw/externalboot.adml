<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>可攜式工作區</displayName>
  <description>這個檔案包含可攜式工作區原則設定。</description>
  <resources>
    <stringTable>
      <string id="PortableOperatingSystem">可攜式作業系統</string>
      <string id="PortableOperatingSystem_Launcher_DisplayName">Windows To Go 預設啟動選項</string>
      <string id="PortableOperatingSystem_Launcher_Help">



這個原則設定可以控制當連接的 USB 裝置包含 Windows To Go 工作區時，電腦是否將開機進入 Windows To Go，並控制使用者是否能夠使用 [Windows To Go 啟動選項] 控制台項目來進行變更。

如果您啟用這個設定，連接 USB 裝置時將開機進入 Windows To Go，但使用者無法使用 [Windows To Go 啟動選項] 控制台項目進行變更。

如果您停用這個設定，則連接 USB 裝置時不會開機進入 Windows To Go，除非使用者手動在 BIOS 或其他開機順序設定中進行設定。

如果您未設定這個設定，則隸屬於 Administrators 群組的使用者可使用 [Windows To Go 啟動選項] 控制台項目進行變更。</string>
      <string id="PortableOperatingSystem_Hibernate_DisplayName">從 Windows To Go 工作區啟動時允許休眠 (S4)</string>
      <string id="PortableOperatingSystem_Hibernate_Help">



指定從 Windows To Go 工作區啟動時，電腦是否可以使用休眠狀態 (S4)。

如果您啟用這個設定，從 Windows To Go 工作區啟動時 Windows 就可以讓電腦休眠。

如果您停用或未設定這個設定，從 Windows To Go 工作區啟動時 Windows 就無法讓電腦休眠。</string>
      <string id="PortableOperatingSystem_Sleep_DisplayName">從 Windows To Go 工作區啟動時不允許待命睡眠狀態 (S1-S3)</string>
      <string id="PortableOperatingSystem_Sleep_Help">



指定從 Windows To Go 工作區啟動時，電腦是否可以使用待命睡眠狀態 (S1-S3)。

如果您啟用這個設定，從 Windows To Go 工作區啟動時，Windows 不能使用待命狀態讓電腦睡眠。

如果您停用或未設定這個設定，從 Windows To Go 工作區啟動時，Windows 就可以使用待命狀態讓電腦睡眠。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
