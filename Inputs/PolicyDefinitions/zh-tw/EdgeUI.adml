<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2011 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="EdgeUI">邊緣 UI</string>
      <string id="EdgeUI_Help">包含與附加至螢幕邊緣之系統使用者介面相關的設定。</string>
      <string id="TurnOffBackstack">關閉在最近使用的應用程式之間切換</string>
      <string id="TurnOffBackstack_Help">如果您啟用這個設定，則使用者無法在最近使用的應用程式之間切換。[電腦設定] 應用程式中的 [應用程式切換] 選項也會同時停用。

如果您停用或未設定這個原則設定，使用者將可以在最近使用的應用程式之間切換。</string>
      <string id="DisableMFUTracking">關閉追蹤應用程式使用量</string>
      <string id="ExplainDisableMFUTracking">這個原則設定可以防止 Windows 追蹤最常使用和搜尋的應用程式。如果您啟用這個原則設定，系統將在下列位置依英文字母順序排序應用程式:
      - 搜尋結果
      - [搜尋] 和 [共用] 窗格
      - 選擇器中的下拉式應用程式清單

      如果您停用或未設定這個原則設定，Windows 將追蹤最常使用和搜尋的應用程式。最常使用的應用程式會顯示在最頂端。</string>
      <string id="DisableRecentApps">當滑鼠指到螢幕左上角時，不要顯示最近使用的應用程式</string>
      <string id="DisableRecentApps_Help">這個原則設定可讓您在滑鼠指到螢幕左上角時，不要顯示上次使用的應用程式及最近使用應用程式的清單。

如果您啟用這個原則設定，使用者將不能再使用滑鼠切換到最近使用的應用程式。 使用者仍然可以使用觸控手勢、鍵盤快速鍵及 [開始] 畫面切換應用程式。

如果您停用或未設定這個原則設定，根據預設，將可使用最近使用的應用程式，而且使用者可以設定這個設定。</string>
      <string id="DisableCharms">當滑鼠指到螢幕右上角時，不要顯示 [搜尋]、[分享]、[開始]、[裝置] 及 [設定]</string>
      <string id="DisableCharms_Help">這個原則設定可讓您在滑鼠指到螢幕右上角時，不要顯示 [搜尋]、[分享]、[開始]、[裝置] 及 [設定]。

如果您啟用這個原則設定，當滑鼠指到右上角時，將不會顯示 [搜尋]、[分享]、[開始]、[裝置] 及 [設定]。如果滑鼠指到右下角，仍然可以使用它們。

如果您停用或未設定這個原則設定，根據預設，將可使用 [搜尋]、[分享]、[開始]、[裝置] 及 [設定]，而且使用者可以設定這個設定。</string>
      <string id="ShowCommandPromptOnWinX">當使用者用滑鼠右鍵按一下左下角或按 Windows 標誌鍵+X 時，防止使用者在看到的功能表中以 Windows PowerShell 取代命令提示字元</string>
      <string id="ShowCommandPromptOnWinX_Help">這個原則設定可讓您在使用者用滑鼠右鍵按一下左下角或按 Windows 標誌鍵 + X 時，防止他們在看到的功能表中以 Windows PowerShell 取代命令提示字元。

如果您啟用這個原則設定，該功能表中一律會列出命令提示字元，而且使用者無法用 Windows PowerShell 取代它。使用者仍然能夠存取 Windows PowerShell，但不是從該功能表。

如果您停用或未設定這個原則設定，根據預設，功能表中將會列出命令提示字元，而且使用者可以設定這個設定。</string>

      <string id="DisableHelpSticker">停用說明秘訣</string>
      <string id="DisableHelpSticker_Help">停用 Windows 對使用者顯示的說明秘訣。

根據預設，Windows 會對使用者顯示說明秘訣，直到使用者順利完成案例為止。

如果啟用這個設定，Windows 不會對使用者顯示任何說明秘訣。
      
      </string>
      <string id="AllowEdgeSwipe">允許邊緣撥動</string>
      <string id="AllowEdgeSwipe_Help">若停用此原則設定，使用者將無法透過從任何螢幕邊緣向內撥動以叫用任何系統 UI。

若啟用或未設定此原則設定，使用者將能透過從螢幕邊緣向內撥動以叫用系統 UI。
      </string>
    </stringTable>
  </resources>
</policyDefinitionResources>
