<?xml version="1.0" encoding="utf-8"?>
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://www.microsoft.com/GroupPolicy/PolicyDefinitions">
  <displayName>連線</displayName>
  <description>選取一個項目來檢視它的描述</description>
  <resources>
    <stringTable>
      <!-- general -->
      <string id="Connect">連線</string>
      
      <!-- prevent file sync-->
      <string id="AllowProjectionToPC">不允許使用此電腦做為投影目標</string>
      <string id="AllowProjectionToPC_help">此原則設定可讓您關閉投影到電腦的功能。

 

若將它開啟，您的電腦將無法被探索，也無法做為投影目標，除非使用者手動啟動 [無線顯示器] 應用程式。

 

若將它關閉或未設定，您的電腦將可被探索，而且也可以做為投影目標 (但只能投影在鎖定畫面之上)。除了手動啟動之外，使用者可以選擇一直開啟或關閉。</string>

      <string id="RequirePinForPairing">要求提供 PIN 以進行配對</string>
      <string id="RequirePinForPairing_help">此原則設定可讓您要求 PIN 以進行配對。 

若設為 [永不]，則配對時不需要 PIN。

若設為 [第一次]，新裝置的配對程序將一律要求 PIN。

若設為 [一律]，則配對時一律需要 PIN。</string>
      <string id="RequirePinForPairing_0">永不</string>
      <string id="RequirePinForPairing_1">第一次</string>
      <string id="RequirePinForPairing_2">一律</string>
   </stringTable>
   <presentationTable>
      <presentation id="RequirePinForPairing">
        <dropdownList refId="RequirePinForPairingDropdown" noSort="true" defaultItem="0">選擇下列其中一個動作</dropdownList>
      </presentation>
   </presentationTable>
  </resources>
</policyDefinitionResources>
