<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="Channel_Log_AutoBackup">記錄檔已滿時自動備份</string>
      <string id="Channel_Log_AutoBackup_Help">這個原則設定可以控制記錄檔達到大小上限時的事件記錄檔行為，而且只有在 [保留舊事件] 原則設定啟用時才會生效。

如果您啟用這個原則設定且 [保留舊事件] 原則設定已啟用，則會自動在事件記錄檔已滿時加以關閉並重新命名。然後開始一個新檔案。

如果您停用這個原則設定且 [保留舊事件] 原則設定已啟用，則會丟棄新事件並保留舊事件。

如果您未設定這個原則設定且 [保留舊事件] 原則設定也已啟用，則會丟棄新事件並保留舊事件。</string>
      <string id="Channel_Log_FileLogAccess">設定記錄檔存取</string>
      <string id="Channel_Log_FileLogAccess_Legacy">設定記錄檔存取 (傳統)</string>
      <string id="Channel_Log_FileLogAccessApplication_Help">這個原則設定使用「安全性描述元定義語言」(SDDL) 字串指定記錄檔使用的安全性描述元。

如果您啟用這個原則設定，則只有符合安全性描述元的使用者可以存取記錄檔。

如果您停用或未設定這個原則設定，則所有已驗證的使用者和系統服務都可以寫入、讀取或清除這個記錄檔。

注意: 如果您啟用這個原則設定，某些工具和 API 可能會忽略它。應該對「設定記錄檔存取 (傳統)」原則設定做相同的變更，以強制所有工具和 API 進行此變更。</string>
      <string id="Channel_Log_FileLogAccessApplication_Help_Legacy">這個原則設定使用「安全性描述元定義語言」(SDDL) 字串指定記錄檔使用的安全性描述元。您必須為這個記錄檔設定兩個「設定記錄檔存取」原則設定，才能對現代和傳統工具都生效。

如果您啟用這個原則設定，則只有符合安全性描述元的使用者可以存取記錄檔。

如果您停用這個原則設定，則所有已驗證的使用者和系統服務都可以寫入、讀取或清除這個記錄檔。

如果您未設定這個原則設定，則先前的原則設定設定仍然有效。</string>
      <string id="Channel_Log_FileLogAccessSecurity_Help">這個原則設定使用「安全性描述元定義語言」(SDDL) 字串指定記錄檔使用的安全性描述元。您無法設定這個記錄檔的寫入權限。您必須為這個記錄檔設定兩個「設定記錄檔存取」原則設定，才能對現代和傳統工具都生效。

如果您啟用這個原則設定，則只有安全性描述元符合設定之指定值的使用者可以存取記錄檔。

如果您停用或未設定這個原則設定，則只有系統軟體和系統管理員可以讀取或清除這個記錄檔。

注意: 如果您啟用此原則設定，某些工具和 API 可能會忽略它。應該對「設定記錄檔存取 (傳統)」原則設定做相同的變更，以強制所有工具和 API 進行此變更。</string>
      <string id="Channel_Log_FileLogAccessSecurity_Help_Legacy">這個原則設定使用「安全性描述元定義語言」(SDDL) 字串指定記錄檔使用的安全性描述元。您無法設定這個記錄檔的寫入權限。

如果您啟用這個原則設定，則只有安全性描述元符合設定之指定值的使用者可以存取記錄檔。

如果您停用這個原則設定，則只有系統軟體和系統管理員可以讀取或清除這個記錄檔。

如果您未設定這個原則設定，則先前的原則設定設定仍然有效。</string>
      <string id="Channel_Log_FileLogAccessSystem_Help">這個原則設定使用「安全性描述元定義語言」(SDDL) 字串指定記錄檔使用的安全性描述元。您必須為這個記錄檔設定兩個「設定記錄檔存取」原則設定，才能對現代和傳統工具都生效。

如果您啟用這個原則設定，則只有安全性描述元符合設定值的使用者可以存取記錄檔。

如果您停用或未設定這個原則設定，則只有系統軟體和系統管理員可以寫入或清除這個記錄檔，且任何已驗證的使用者均可讀取其中的事件。

注意: 如果您啟用這個原則設定，某些工具和 API 可能會忽略它。應該對「設定記錄檔存取 (傳統)」原則設定做相同的變更，以強制所有工具和 API 進行此變更。</string>
      <string id="Channel_Log_FileLogAccessSystem_Help_Legacy">這個原則設定使用「安全性描述元定義語言」(SDDL) 字串指定記錄檔使用的安全性描述元。

如果您啟用這個原則設定，則只有安全性描述元符合設定之指定值的使用者可以存取記錄檔。

如果您停用這個原則設定，則只有系統軟體和系統管理員可以寫入或清除這個記錄檔，且任何已驗證的使用者均可讀取其中的事件。

如果您未設定這個原則設定，則先前的原則設定設定仍然有效。</string>
      <string id="Channel_Log_Retention">控制記錄檔達到其大小上限時的事件記錄檔行為</string>
      <string id="Channel_Log_Retention_Help">這個原則設定可以控制記錄檔達到其大小上限時的事件記錄檔行為。

如果您啟用這個原則設定，且記錄檔達到其大小上限時，新事件將不會寫入記錄檔且會遺失。

如果您停用或未設定這個原則設定，且記錄檔達到其大小上限，則新事件會覆寫舊事件。

注意: 舊事件的保留與否，是根據 [記錄檔已滿時自動備份] 原則設定所決定。</string>
      <string id="Channel_LogEnabled">開啟記錄</string>
      <string id="Channel_LogEnabled_Help">這個原則設定會開啟記錄。

如果您啟用或未設定這個原則設定，則事件將寫入此記錄檔。

如果這個原則設定已停用，則不會記錄新事件。無論這個原則設定為何，一律可以從記錄檔讀取事件。</string>
      <string id="Channel_LogFilePath">控制記錄檔的位置</string>
      <string id="Channel_LogFilePath_Help">此原則設定可以控制記錄檔的位置。檔案位置必須可以讓事件記錄服務寫入，而且只有系統管理員可以存取。

若啟用此原則設定，事件記錄檔會使用此原則設定中指定的路徑。

若停用或未設定此原則設定，事件記錄檔會使用資料夾 %SYSTEMROOT%\System32\winevt\Logs。</string>
      <string id="Channel_LogMaxSize">指定記錄檔大小上限 (KB)</string>
      <string id="Channel_LogMaxSize_Help">這個原則設定可指定記錄檔的大小上限 (以 KB 為單位)。

如果您啟用這個原則設定，即可將記錄檔大小上限設定為介於 1 MB (1024 KB) 至 2 TB (2147483647 KB) 之間 (以 KB 為遞增單位)。

如果您停用或未設定這個原則設定，則記錄檔大小上限將會設定為本機設定值。本機系統管理員可使用 [記錄內容] 對話方塊來變更這個值，此值預設為 1 MB。</string>
      <string id="Channel_LogMaxSizeSecurity_Help">這個原則設定可指定記錄檔的大小上限 (以 KB 為單位)。

如果您啟用這個原則設定，即可將記錄檔大小上限設定為介於 20 MB (20480 KB) 至 2 TB (2147483647 KB) 之間 (以 KB 為遞增單位)。

如果您停用或未設定這個原則設定，則記錄檔大小上限將會設定為本機設定值。本機系統管理員可使用 [記錄內容] 對話方塊來變更這個值，此值預設為 20 MB。</string>
      <string id="ChannelName_Help">記錄檔的組態設定</string>
      <string id="EventLog_Application">應用程式</string>
      <string id="EventLog_Security">安全性</string>
      <string id="EventLog_Setup">安裝</string>
      <string id="EventLog_System">系統</string>
      <string id="EventLogCategory">事件記錄服務</string>
    </stringTable>
    <presentationTable>
      <presentation id="Channel_Log_FileLogAccess_1">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_Log_FileLogAccess_2">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_Log_FileLogAccess_3">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_Log_FileLogAccess_4">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_Log_FileLogAccess_5">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_Log_FileLogAccess_6">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_Log_FileLogAccess_7">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_Log_FileLogAccess_8">
        <textBox refId="Channel_Log_FileLogAccess">
          <label>記錄檔存取</label>
        </textBox>
      </presentation>
      <presentation id="Channel_LogFilePath_1">
        <textBox refId="Channel_LogFilePath">
          <label>記錄檔路徑</label>
        </textBox>
      </presentation>
      <presentation id="Channel_LogFilePath_2">
        <textBox refId="Channel_LogFilePath">
          <label>記錄檔路徑</label>
        </textBox>
      </presentation>
      <presentation id="Channel_LogFilePath_3">
        <textBox refId="Channel_LogFilePath">
          <label>記錄檔路徑</label>
        </textBox>
      </presentation>
      <presentation id="Channel_LogFilePath_4">
        <textBox refId="Channel_LogFilePath">
          <label>記錄檔路徑</label>
        </textBox>
      </presentation>
      <presentation id="Channel_LogMaxSize_1">
        <decimalTextBox refId="Channel_LogMaxSize" defaultValue="1024">記錄檔大小上限 (KB)</decimalTextBox>
      </presentation>
      <presentation id="Channel_LogMaxSize_2">
        <decimalTextBox refId="Channel_LogMaxSize" defaultValue="20480">記錄檔大小上限 (KB)</decimalTextBox>
      </presentation>
      <presentation id="Channel_LogMaxSize_3">
        <decimalTextBox refId="Channel_LogMaxSize" defaultValue="1024">記錄檔大小上限 (KB)</decimalTextBox>
      </presentation>
      <presentation id="Channel_LogMaxSize_4">
        <decimalTextBox refId="Channel_LogMaxSize" defaultValue="1024">記錄檔大小上限 (KB)</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
