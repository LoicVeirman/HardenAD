<?xml version="1.0" encoding="utf-8"?>
<!--  (c) Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>執行指令的診斷</displayName>
  <description>執行指令的診斷</description>
  <resources>
    <stringTable>
      <string id="ScriptedDiagnosticsCategory">執行指令的診斷</string>
      <string id="ScriptedDiagnosticsSecurityPolicy">為執行指令的診斷設定安全性原則</string>
      <string id="ScriptedDiagnosticsSecurityPolicyExplain">這個原則設定會決定執行指令的診斷是否執行不受信任的發行者所簽署的診斷套件。

如果您啟用這個原則設定，執行指令的診斷執行引擎將驗證所有診斷套件的簽署者，並且只執行由受信任的發行者所簽署的診斷套件。

如果您停用或未設定這個原則設定，則執行指令的診斷執行引擎將執行所有數位簽署的套件。</string>

      <string id="ScriptedDiagnosticsExecutionPolicy">疑難排解: 允許使用者存取和執行疑難排解精靈</string>
      <string id="ScriptedDiagnosticsExecutionPolicyExplain">這個原則設定可讓使用者存取和執行 [疑難排解控制台] 中可用的疑難排解工具，並執行疑難排解精靈對電腦的問題進行疑難排解。

如果您啟用或未設定這個原則設定，使用者可以存取和執行 [疑難排解控制台] 中的疑難排解工具。

如果您停用這個原則設定，則使用者無法存取或執行 [控制台] 中的疑難排解工具。

注意，這個設定也會控制使用者能否啟動獨立的疑難排解套件，例如 .diagcab 檔案中的疑難排解套件。
    
    </string>

    <string id="BetterWhenConnected">疑難排解: 允許使用者從 [疑難排解控制台] 存取 Microsoft 伺服器上的線上疑難排解內容 (經由 Windows 線上疑難排解服務 - WOTS)</string>
    <string id="BetterWhenConnectedExplain">這個原則設定可讓連線到網際網路的使用者存取和搜尋裝載在 Microsoft 內容伺服器上的疑難排解內容。使用者在 [疑難排解控制台] UI 中看到 [您要取得可供疑難排解使用的最新內容嗎?] 提示訊息時，只要按一下 [是]，就可以存取線上疑難排解內容。

如果您啟用或未設定這個原則設定，連線到網際網路的使用者可以從 [疑難排解控制台] 使用者介面存取和搜尋裝載在 Microsoft 內容伺服器上的疑難排解內容。

如果您停用這個原則設定，則使用者即使已連線到網際網路，也只能存取和搜尋本機電腦上可用的疑難排解內容。他們無法連線到裝載 Windows 線上疑難排解服務的 Microsoft 伺服器。
    
    </string>

    </stringTable>
  </resources>
</policyDefinitionResources>
