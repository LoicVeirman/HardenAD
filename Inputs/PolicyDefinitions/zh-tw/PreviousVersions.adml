<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="DisableBackupRestore">防止從備份還原先前的版本</string>
      <string id="DisableBackupRestore_Help">這個原則設定可以讓您在使用者選取舊版的本機檔案時 (舊版儲存於備份中)，抑制舊版內容頁上的 [還原] 按鈕。

如果您啟用這個原則設定，當使用者選取對應至備份中的舊版時，會停用 [還原] 按鈕。

如果您停用這個原則設定，則對應至備份中舊版的 [還原] 按鈕可以使用。如果使用者按一下 [還原] 按鈕，Windows 會嘗試從備份媒體還原檔案。

如果您未設定這個原則設定，預設會停用。舊版是本機檔案且儲存在備份時，可以使用 [還原] 按鈕。</string>
      <string id="DisableLocalPage">隱藏舊版本機檔案的清單</string>
      <string id="DisableLocalPage_Help">這個原則設定可以讓您隱藏本機磁碟上的舊版檔案清單。舊版可能來自磁碟還原點或備份媒體。

如果您啟用這個原則設定，使用者無法列出或還原本機磁碟上的舊版檔案。

如果您停用這個原則設定，使用者可以列出和還原本機磁碟上的舊版檔案。

如果您未設定這個原則設定，預設為停用。</string>
      <string id="DisableLocalRestore">防止還原本機舊版</string>
      <string id="DisableLocalRestore_Help">這個原則設定可以讓您在使用者選取舊版本機檔案時，抑制舊版內容頁上的 [還原] 按鈕。

如果您啟用這個原則設定，當使用者選取對應至本機檔案的舊版時，會停用 [還原] 按鈕。

如果您停用這個原則設定，則對應至舊版本機檔案的 [還原] 按鈕可以使用。如果使用者按一下 [還原] 按鈕，Windows 會嘗試從本機磁碟還原檔案。

如果您未設定這個原則設定，預設會停用。舊版是本機檔案時，可以使用 [還原] 按鈕。</string>
      <string id="DisableRemotePage">隱藏舊版遠端檔案的清單</string>
      <string id="DisableRemotePage_Help">這個原則設定可以讓您隱藏檔案共用上的舊版檔案清單。舊版是來自檔案共用上的磁碟還原點。

如果您啟用這個原則設定，使用者無法列出或還原檔案共用上的舊版檔案。

如果您停用這個原則設定，使用者可以列出和還原檔案共用上的舊版檔案。

如果您未設定這個原則設定，預設會停用。</string>
      <string id="DisableRemoteRestore">防止還原遠端舊版</string>
      <string id="DisableRemoteRestore_Help">這個設定可讓您在使用者選取檔案共用上的舊版檔案時，抑制舊版內容頁上的 [還原] 按鈕。

如果您啟用這個原則設定，當使用者選取對應至檔案共用上的舊版檔案時，會停用 [還原] 按鈕。

如果您停用這個原則設定，則對應至檔案共用上舊版檔案的 [還原] 按鈕可以使用。如果使用者按一下 [還原] 按鈕，Windows 會嘗試從檔案共用還原檔案。

如果您未設定這個原則設定，預設會停用。舊版是檔案共用上的檔案時，可以使用 [還原] 按鈕。</string>
      <string id="HideBackupEntries">隱藏備份位置上的舊版檔案</string>
      <string id="HideBackupEntries_Help">這個原則設定可以讓您隱藏舊版檔案清單中的項目，該清單中的舊版檔案都位於備份媒體上。舊版可能來自磁碟還原點或備份媒體。

如果您啟用這個原則設定，使用者不會看到任何對應至備份複本的舊版，只會看到對應至磁碟還原點的舊版。

如果您停用這個原則設定，使用者可以看到對應至備份複本的舊版，以及對應至磁碟還原點的舊版。

如果您未設定這個原則設定，預設會停用。</string>
      <string id="PreviousVersions">舊版</string>
      <string id="SUPPORTED_Vista_through_Win7">支援 Windows Vista 到 Windows 7</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
