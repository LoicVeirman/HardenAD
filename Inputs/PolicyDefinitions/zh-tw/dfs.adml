<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="DFSDiscoverDC">設定 DFS 用戶端搜尋網域控制站的頻率</string>
      <string id="DFSDiscoverDC_Help">這個原則設定可讓您設定「分散式檔案系統」(DFS) 用戶端嘗試搜尋網路中網域控制站的頻率。預設狀況下，DFS 用戶端會每隔 15 分鐘嘗試搜尋網域控制站。

如果您啟用這個原則設定，可以設定 DFS 用戶端嘗試搜尋網域控制站的頻率。這個值是以分鐘為單位來指定的。

如果您停用或未設定這個原則設定，將套用 15 分鐘的預設值。

注意: 可選取的最小值是 15 分鐘。如果您嘗試將這個設定設成低於 15 分鐘的值，將會套用預設值 15 分鐘。</string>
    </stringTable>
    <presentationTable>
      <presentation id="DFSDiscoverDC">
        <decimalTextBox refId="DFSDiscoverDialog" defaultValue="15">時間 (分鐘):</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
