<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="BootScenarioCategory">Diagnostik för startprestanda i Windows</string>
      <string id="BootScenarioExecutionPolicyExplain">Bestämmer körningsnivån för diagnostik för startprestanda i Windows.

Om du aktiverar den här principinställningen måste du välja en körningsnivå i listrutan. Om du väljer att endast hitta problem och felsöka upptäcker Diagnostic Policy Service (DPS) startprestandaproblem och försöker ta reda på vad de beror på. Orsakerna loggas i händelseloggen när de upptäcks, men inga åtgärder vidtas. Om du väljer upptäckt, felsökning och lösning upptäcker DPS startprestandaproblem och visar för användaren att det finns hjälp att få att lösa problemet.

Om du inaktiverar den här principinställningen kan Windows inte hitta, felsöka eller lösa några startprestandaproblem som hanteras av DPS.

Om du inte konfigurerar den här principinställningen löser DPS startprestandaproblem i Windows om inget annat anges.

Den här principinställningen börjar endast gälla om scenariokörningsprincipen för all diagnostik inte har konfigurerats.

Varken datorn eller några tjänster behöver startas om för att den här principen ska börja gälla: ändringarna börjar gälla omedelbart.

Den här principinställningen börjar endast gälla om DPS (Diagnostic Policy Service) körs. När tjänsten har stoppats eller inaktiverats körs inga diagnostiska scenarier. DPS kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <string id="ResumeScenarioCategory">Diagnostik för vänteläges-/återupptagningsprestanda i Windows</string>
      <string id="ResumeScenarioExecutionPolicyExplain">Bestämmer körningsnivån för diagnostik för vänteläges-/återupptagningsprestanda i Windows.

Om du aktiverar den här principinställningen måste du välja en körningsnivå i listrutan. Om du väljer att endast hitta problem och felsöka upptäcker Diagnostic Policy Service (DPS) prestandaproblem för vänteläge/återupptagning i Windows och försöker ta reda på vad de beror på. Orsakerna loggas i händelseloggen när de upptäcks, men inga åtgärder vidtas. Om du väljer upptäckt, felsökning och lösning upptäcker DPS att det finns problem med viloläget och återgången från viloläget och visar för användaren att det finns hjälp att få att lösa problemet.

Om du inaktiverar den här principinställningen kan Windows inte hitta, felsöka eller lösa några problem med viloläget och återgången från viloläget som hanteras av DPS.

Om du inte konfigurerar den här principinställningen löser DPS problem med viloläget och återgången från viloläget i Windows om inget annat anges.

Den här principinställningen börjar endast gälla om scenariokörningsprincipen för all diagnostik inte har konfigurerats.

Varken datorn eller några tjänster behöver startas om för att den här principen ska börja gälla: ändringarna börjar gälla omedelbart.

Den här principinställningen börjar endast gälla om DPS (Diagnostic Policy Service) körs. När tjänsten har stoppats eller inaktiverats körs inga diagnostiska scenarier. DPS kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <string id="ShellScenarioCategory">Diagnostik för systemtillgänglighetsprestanda i Windows</string>
      <string id="ShellScenarioExecutionPolicyExplain">Bestämmer körningsnivån för diagnostik för systemtillgänglighetsprestanda i Windows.

Om du aktiverar den här principinställningen måste du välja en körningsnivå i listrutan. Om du väljer att endast hitta problem och felsöka upptäcker Diagnostic Policy Service (DPS) problem med systemets svarsförmåga och försöker ta reda på vad de beror på. Orsakerna loggas i händelseloggen när de upptäcks, men inga åtgärder vidtas. Om du väljer upptäckt, felsökning och lösning upptäcker DPS problem med systemets svarsförmåga och visar för användaren att det finns hjälp att få att lösa problemet.

Om du inaktiverar den här principinställningen kan Windows inte hitta, felsöka eller lösa några problem med systemets svarsförmåga som hanteras av DPS.

Om du inte konfigurerar den här principinställningen löser DPS problem med systemets svarsförmåga om inget annat anges.

Den här principinställningen börjar endast gälla om scenariokörningsprincipen för all diagnostik inte har konfigurerats.

Varken datorn eller några tjänster behöver startas om för att den här principen ska börja gälla: ändringarna börjar gälla omedelbart.

Den här principinställningen börjar endast gälla om DPS (Diagnostic Policy Service) körs. När tjänsten har stoppats eller inaktiverats körs inga diagnostiska scenarier. DPS kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <string id="ShutdownScenarioCategory">Diagnostik för avstängningsprestanda i Windows</string>
      <string id="ShutdownScenarioExecutionPolicyExplain">Bestämmer körningsnivån för diagnostik för avstängningsprestanda i Windows.

Om du aktiverar den här principinställningen måste du välja en körningsnivå i listrutan. Om du väljer att endast hitta problem och felsöka upptäcker Diagnostic Policy Service (DPS) avstängningsprestandaproblem och försöker ta reda på vad de beror på. Orsakerna loggas i händelseloggen när de upptäcks, men inga åtgärder vidtas. Om du väljer upptäckt, felsökning och lösning upptäcker DPS avstängningsprestandaproblem och visar för användaren att det finns hjälp att få att lösa problemet.

Om du inaktiverar den här principinställningen kan Windows inte hitta, felsöka eller lösa några avstängningsprestandaproblem som hanteras av DPS.

Om du inte konfigurerar den här principinställningen löser DPS avstängningsprestandaproblem i Windows om inget annat anges.

Den här principinställningen börjar endast gälla om scenariokörningsprincipen för all diagnostik inte har konfigurerats.

Varken datorn eller några tjänster behöver startas om för att den här principen ska börja gälla: ändringarna börjar gälla omedelbart.

Den här principinställningen börjar endast gälla om DPS (Diagnostic Policy Service) körs. När tjänsten har stoppats eller inaktiverats körs inga diagnostiska scenarier. DPS kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <string id="WdiScenarioExecutionPolicy">Konfigurera körningsnivå för scenario</string>
      <string id="WdiScenarioExecutionPolicyLevelResolution">Identifiering, felsökning och lösning</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Endast identifiering och felsökning</string>
    </stringTable>
    <presentationTable>
      <presentation id="WdiScenarioExecutionPolicy_1">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_2">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_3">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_4">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
