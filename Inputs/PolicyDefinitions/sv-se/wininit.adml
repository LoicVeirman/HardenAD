<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="DisableNamedPipeShutdownPolicyDescription">Inaktivera tidigare gränssnitt för fjärravstängning</string>
      <string id="DisableNamedPipeShutdownPolicyDescription_Help">Den här principinställningen styr det tidigare gränssnittet för fjärravstängning (namngiven pipe). Detta gränssnitt används för att stänga av den här datorn från en fjärrdator med Windows XP eller Windows Server 2003.

Om du aktiverar den här principinställningen skapar systemet inte den namngivna pipens gränssnitt för fjärravstängning.

Om du inaktiverar eller inte konfigurerar den här principinställningen skapar systemet den namngivna pipens gränssnitt för fjärravstängning.</string>
      <string id="ShutdownOptions">Avslutningsalternativ</string>
      <string id="ShutdownTimeoutHungSessionsDescription">Timeout för låsta inloggningssessioner under avstängning</string>
      <string id="ShutdownTimeoutHungSessionsDescription_Help">Den här principinställningen bestämmer hur många minuter systemet väntar på låsta inloggningssessioner innan datorn stängs av.

Om du aktiverar den här principinställningen väntar systemet på låsta inloggningssessioner i det angivna antalet minuter.

Om du inaktiverar eller inte konfigurerar den här principinställningen används standardvärdet på tre minuter för arbetsstationer och 15 minuter för servrar.</string>
       <string id="Shutdown">Stäng av</string>
       <string id="Hiberboot">Kräv användning av snabbstart (Require use of fast startup)</string>
       <string id="Hiberboot_Help">Den här principinställningen styr bruket av snabbstart.  

Om du aktiverar den här principinställningen måste viloläget vara aktiverat.

Om du inaktiverar eller inte konfigurerar den här principinställningen används den lokala inställningen.</string>
    </stringTable>
    <presentationTable>
      <presentation id="ShutdownTimeoutHungSessionsDescription">
        <decimalTextBox refId="ShutdownSessionTimeout_Time" defaultValue="9999">Timeout i minuter för låst session:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
