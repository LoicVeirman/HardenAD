<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2012 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <!-- General -->
      <string id="SettingSyncCategory">Synkronisera dina inställningar</string>

      <!-- Main policy -->
      <string id="DisableSettingSync">Synkronisera inte</string>
      <string id="DisableSettingSync_Help">Förhindra synkronisera till och från den här datorn.  Detta stänger av och inaktiverar växeln Synkronisera dina inställningar på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen stängs Synkronisera dina inställningar av och ingen av grupperna Synkronisera dina inställningar kommer att synkroniseras på den här datorn.

Använd alternativet Tillåt användarna att aktivera synkronisering så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är Synkronisera dina inställningar aktiverat som standard och användaren kan konfigurera.</string>

      <!-- Application Settings -->
      <string id="DisableApplicationSettingSync">Synkronisera inte appinställningar</string>
      <string id="DisableApplicationSettingSync_Help">Förhindra gruppen Appinställningar från att synkroniseras till och från den här datorn.  Detta stänger av och inaktiverar gruppen Appinställningar på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras gruppen Appinställningar inte.

Använd alternativet Tillåt användarna att aktivera synkronisering av appinställningar så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Appinställningar aktiverat som standard och användaren kan konfigurera.</string>

      <!-- Credentials -->
      <string id="DisableCredentialsSettingSync">Synkronisera inte lösenord</string>
      <string id="DisableCredentialsSettingSync_Help">Förhindra gruppen Lösenord från att synkroniseras till och från den här datorn.  Detta stänger av och inaktiverar gruppen Lösenord på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras gruppen Lösenord inte.

Använd alternativet Tillåt användarna att aktivera synkronisering av lösenord så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Lösenord aktiverat som standard och användaren kan konfigurera.</string>

      <!-- Personalization -->
      <string id="DisablePersonalizationSettingSync">Synkronisera inte Anpassa</string>
      <string id="DisablePersonalizationSettingSync_Help">Förhindra gruppen Anpassa från att synkroniseras till och från den här datorn.  Detta stänger av och inaktiverar gruppen Anpassa på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras gruppen Anpassa inte.

Använd alternativet Tillåt användarna att aktivera synkronisering av Anpassa så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Anpassa aktiverat som standard och användaren kan konfigurera.</string>

      <!-- AppSync -->
      <string id="DisableAppSyncSettingSync">Synkronisera inte appar</string>
      <string id="DisableAppSyncSettingSync_Help">
        
        Förhindra att gruppen AppSync synkroniseras till och från den här datorn.  Detta stänger av och inaktiverar gruppen AppSync på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras gruppen AppSync inte.

Använd alternativet Tillåt användarna att aktivera synkronisering av appar så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen AppSync aktiverat som standard och användaren kan konfigurera den.
      
      </string>

      <!-- Windows -->
      <string id="DisableWindowsSettingSync">Synkronisera inte Andra Windows-inställningar</string>
      <string id="DisableWindowsSettingSync_Help">Förhindra gruppen Andra Windows-inställningar från att synkroniseras till och från den här datorn.  Detta stänger av och inaktiverar gruppen Andra Windows-inställningar på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras inte gruppen Andra Windows-inställningar.

Använd alternativet Tillåt användarna att aktivera synkronisering av andra Windows-inställningar så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Andra Windows-inställningar aktiverat som standard och användaren kan konfigurera.</string>

      <!-- Desktop Theme -->
      <string id="DisableDesktopThemeSettingSync">Synkronisera inte Anpassa skrivbordet</string>
      <string id="DisableDesktopThemeSettingSync_Help">Förhindra gruppen Anpassa skrivbordet från att synkroniseras till och från den här datorn.  Detta stänger av och inaktiverar gruppen Anpassa skrivbordet på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras inte gruppen Anpassa skrivbordet.

Använd alternativet Tillåt användarna att aktivera synkronisering av Anpassa skrivbordet så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Anpassa skrivbordet aktiverat som standard och användaren kan konfigurera.
      
      </string>

      <!-- WebBrowser -->
      <string id="DisableWebBrowserSettingSync">Synkronisera inte Webbläsarinställningar</string>
      <string id="DisableWebBrowserSettingSync_Help">Förhindra gruppen Webbläsare från att synkroniseras till och från den här datorn.  Detta stänger av och inaktiverar gruppen Webbläsare på sidan Synkronisera dina inställningar i Datorinställningar.  Gruppen Webbläsare innehåller inställningar och information som historiken och favoriterna.

Om du aktiverar den här principinställningen synkroniseras inte gruppen Webbläsare och information som historiken och favoriterna.

Använd alternativet Tillåt användarna att aktivera synkronisering av webläsare så att synkronisering är avstängt som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Webbläsare aktiverat som standard och användaren kan konfigurera den.
      
      </string>

      <!-- Paid Networks -->
      <string id="DisableSyncOnPaidNetwork">Synkronisera inte anslutningar med datapriser</string>
      <string id="DisableSyncOnPaidNetwork_Help">Förhindra synkronisering till och från den här datorn på Internetanslutningar med datapriser.  Detta stänger av och inaktiverar växeln Synkronisera dina inställningar med anslutningar med datapriser på sidan Synkronisera dina inställningar i Datorinställningar.

Om du aktiverar den här principinställningen stängs synkronisering vid anslutningar med datapriser av och ingen synkronisering sker när datorn använder sådana anslutningar med datapriser.

Om du inte anger eller inaktiverar den här inställningen kan användaren välja att synkronisera vid anslutningar med datapriser.
      
      </string>

      <!-- StartLayout -->
      <string id="DisableStartLayoutSettingSync">Synkronisera inte startinställningar</string>
      <string id="DisableStartLayoutSettingSync_Help">
        
        Förhindra att gruppen Startlayout synkroniseras till och från den här datorn. Detta stänger av och inaktiverar gruppen Startlayout på sidan Synkronisera dina inställningar i Datorinställningar.

        Om du aktiverar den här principinställningen synkroniseras gruppen Startlayout inte.

        Använd alternativet Tillåt användarna att starta synkronisering så att synkronisering är avstängt som standard men inte inaktiverat.

        Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Startlayout aktiverat som standard och kan konfigureras av användaren.
      
      </string>

      <!-- Accessibility -->
      <string id="DisableAccessibilitySettingSync">Synkronisera inte hjälpmedelsinställningar</string>
      <string id="DisableAccessibilitySettingSync_Help">
        Förhindra gruppen Hjälpmedel från att synkroniseras till och från den här datorn. Detta stänger av och inaktiverar gruppen Hjälpmedel på inställningssidan för Windows säkerhetskopiering i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras gruppen hjälpmedel inte.

Använd alternativet Tillåt användare att aktivera hjälpmedelssynkronisering så att synkronisering är inaktiverat som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Hjälpmedel aktiverat som standard och användaren kan konfigurera.
      </string>

      <!-- Language -->
      <string id="DisableLanguageSettingSync">Synkronisera inte inställningar för språkinställningar</string>
      <string id="DisableLanguageSettingSync_Help">
        Förhindra gruppen Språkinställningar från att synkroniseras till och från den här datorn. Detta stänger av och inaktiverar gruppen Språkinställningar på inställningssidan för Windows Säkerhetskopiering i Datorinställningar.

Om du aktiverar den här principinställningen synkroniseras inte gruppen Språkinställningar.

Använd alternativet Tillåt användare att aktivera synkronisering av språkinställningar så att synkronisering är inaktiverat som standard men inte inaktiverat.

Om du inte anger eller inaktiverar den här inställningen är synkronisering av gruppen Språkinställningar aktiverat som standard och användaren kan konfigurera.
      </string>

    </stringTable>
    <presentationTable>

      <!-- Main policy -->
      <presentation id="DisableSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering.</checkBox>
      </presentation>

      <!-- Application Settings -->
      <presentation id="DisableApplicationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering av appinställningar.</checkBox>
      </presentation>

      <!-- Credentials -->
      <presentation id="DisableCredentialsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering av lösenord.</checkBox>
      </presentation>

      <!-- Personalization -->
      <presentation id="DisablePersonalizationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering av Anpassa.</checkBox>
      </presentation>

      <!-- AppSync -->
      <presentation id="DisableAppSyncSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera AppSync-synkronisering.</checkBox>
      </presentation>

      <!-- Windows -->
      <presentation id="DisableWindowsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering av Andra Windows-inställningar.</checkBox>
      </presentation>

      <!-- Desktop Theme -->
      <presentation id="DisableDesktopThemeSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering av Anpassa skrivbordet.</checkBox>
      </presentation>

      <!-- WebBrowser -->
      <presentation id="DisableWebBrowserSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering av webbläsare.</checkBox>
      </presentation>
      
      <!-- StartLayout -->
      <presentation id="DisableStartLayoutSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användarna att aktivera synkronisering av startlayout.</checkBox>
      </presentation>

      <!-- AccessibilitySync -->
      <presentation id="DisableAccessibilitySettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användare att aktivera synkronisering av "acessibility".</checkBox>
      </presentation>

      <!-- LanguageSync -->
      <presentation id="DisableLanguageSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Tillåt användare att aktivera synkronisering av "språkinställningar".</checkBox>
      </presentation>

    </presentationTable>
  </resources>
</policyDefinitionResources>
