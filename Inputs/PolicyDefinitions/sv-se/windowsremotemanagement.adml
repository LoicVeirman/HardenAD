<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="AllowAutoConfig">Tillåt fjärrserverhantering via WinRM</string>
      <string id="AllowBasic">Tillåt grundläggande autentisering</string>
      <string id="AllowBasicClientHelp">Om du aktiverar den här principinställningen kan du ange om grundläggande autentisering ska användas i WinRM-klienten (Windows Remote Management).

Om du aktiverar den här principinställningen används grundläggande autentisering i WinRM-klienten. Om WinRM har konfigurerats för användning av HTTP-transport skickas användarnamnet och lösenordet i klartext via nätverket.

Om du inaktiverar eller inte konfigurerar den här principinställningen används inte grundläggande autentisering i WinRM-klienten.</string>
      <string id="AllowBasicServiceHelp">Om du aktiverar den här principinställningen kan du ange om grundläggande autentisering från en fjärrklient ska godkännas i WinRM-klienten (Windows Remote Management).

        Om du aktiverar den här principinställningen godkänner WinRM-tjänsten grundläggande autentisering från en fjärrklient.

        Om du inaktiverar eller inte konfigurerar den här principinställningen godkänner WinRM-tjänsten inte grundläggande autentisering från en fjärrklient.</string>
      <string id="AllowUnencrypted">Tillåt okrypterad trafik</string>
      <string id="AllowUnencryptedMessagesClientHelp">Om du aktiverar den här principinställningen kan du ange om okrypterade meddelanden ska användas i WinRM-klienten (Windows Remote Management) vid kommunikation i nätverket.

Om du aktiverar den här principinställningen används okrypterade meddelanden i WinRM-klienten vid kommunikation i nätverket.

Om du inaktiverar eller låter bli att konfigurera den här principinställningen skickas endast krypterade meddelanden via nätverket i WinRM-klienten.</string>
      <string id="AllowUnencryptedMessagesServiceHelp">Om du aktiverar den här principinställningen kan du ange om okrypterade meddelanden ska användas i WinRM-tjänsten (Windows Remote Management) vid kommunikation i nätverket.

Om du aktiverar den här principinställningen används okrypterade meddelanden i WinRM-klienten vid kommunikation i nätverket.

Om du inaktiverar eller låter bli att konfigurera den här principinställningen skickas endast krypterade meddelanden via nätverket i WinRM-klienten.</string>
      <string id="DisableRunAs">Tillåt inte att WinRM sparar RunAs-uppgifter</string>      
      <string id="DisableRunAsMessagesServiceHelp">Med den här principinställningen kan du ange huruvida WinRM-tjänsten (Windows Remote Management) tillåter att RunAs-uppgifter lagras för några insticksprogram.

Om du aktiverar den här principinställningen tillåter WinRM-tjänsten inte att konfigurationsvärdena RunAsUser eller RunAsPassword används för något insticksprogram.  Om ett insticksprogram redan har angett konfigurationsvärdena för RunAsUser och RunAsPassword tas konfigurationsvärdet för RunAsPassword bort från arkivet för autentiseringsuppgifter på den här datorn.

Om du inaktiverar eller inte konfigurerar den här principinställningen tillåter WinRM-tjänsten att konfigurationsvärdena RunAsUser eller RunAsPassword anges för insticksprogram och värdet på RunAsPassword lagras säkert.

Om du aktiverar och sedan inaktiverar den här principinställningen måste alla värden som tidigare har konfigurerats för RunAsPassword återställas.</string>
      <string id="AutoConfigHelp">Om du aktiverar den här principinställningen kan du ange om WinRM-tjänsten (Windows Remote Management) automatiskt ska lyssna i nätverket efter HTTP-begäranden via standardporten för HTTP.

Om du aktiverar den här principinställningen lyssnar WinRM-tjänsten automatiskt i nätverket efter HTTP-begäranden via standardporten för HTTP.

Om du vill tillåta att begäranden ska kunna tas emot i WinRM-tjänsten via nätverket ska du konfigurera Windows-brandväggsprincipen med undantag för port 5985 (standardport för HTTP).

Om du inaktiverar eller inte konfigurerar den här principinställningen svarar WinRM-tjänsten inte på begäranden från en fjärrdator, oavsett om några WinRM-lyssnare är konfigurerade eller inte.

Tjänsten lyssnar på de adresser som anges i IPv4- och IPv6-filtren. I IPv4-filtret anges ett eller flera intervall med IPv4-adresser och i IPv6-filtret anges ett eller flera intervall med IPv6-adresser. Om så anges räknas datorns tillgängliga IP-adresser upp i tjänsten, och endast adresser i filterintervallen används.

Använd en asterisk (*) för att ange att tjänsten ska lyssna på datorns alla tillgängliga IP-adresser. Om asterisk används ignoreras andra intervall i filtret. Om filtret lämnas tomt lyssnar tjänsten inte på någon adress.

Om du t ex vill att tjänsten endast ska lyssna på IPv4-adresser ska du låta IPv6-filtret vara tomt.

Intervall anges med syntaxen IP1-IP2. Flera intervall avgränsas med "," (komma).

Exempel på IPv4-filter:\n2.0.0.1-2.0.0.20, 24.0.0.1-24.0.0.22
Exempel på IPv6-filter:\n3FFE:FFFF:7654:FEDA:1245:BA98:0000:0000-3FFE:FFFF:7654:FEDA:1245:BA98:3210:4562</string>
      <string id="DisallowDigest">Tillåt inte Digest-autentisering</string>
      <string id="DisallowDigestClientHelp">Om du aktiverar den här principinställningen kan du ange om sammanfattad autentisering ska användas i WinRM-klienten (Windows Remote Management).

Om du aktiverar den här principinställningen används inte sammanfattad autentisering i WinRM-klienten.

Om du inaktiverar eller inte konfigurerar den här principinställningen används sammanfattad autentisering i WinRM-klienten.</string>
      <string id="DisallowNegotiate">Tillåt inte förhandlingsautentisering</string>
      <string id="DisallowKerberos">Tillåt inte Kerberos-autentisering</string>
      <string id="DisallowNegotiateClientHelp">Med den här principinställningen kan du ange om Negotiate-autentisering ska användas i WinRM-klienten (Windows Remote Management).

Om du aktiverar den här principinställningen används inte Negotiate-autentisering i WinRM-klienten.

Om du inaktiverar eller inte konfigurerar den här principinställningen används Negotiate-autentisering i WinRM-klienten.</string>
      <string id="DisallowNegotiateServiceHelp">Med den här principinställningen kan du ange om Negotiate-autentisering från en fjärrklient ska godkännas i WinRM-tjänsten (Windows Remote Management).

        Om du aktiverar den här principinställningen godkänns inte Negotiate-autentisering från en fjärrklient av WinRM-tjänsten.

        Om du inaktiverar eller inte konfigurerar den här principinställningen godkänns Negotiate-autentisering från en fjärrklient av WinRM-tjänsten.</string>
      <string id="DisallowKerberosClientHelp">Med den här principinställningen kan du ange om Kerberos-autentisering ska användas direkt i WinRM-klienten (Windows Remote Management).

Om du aktiverar den här principinställningen används inte Kerberos-autentisering direkt i WinRM-klienten (Windows Remote Management). Kerberos kan dock användas om Negotiate-autentisering används i WinRM-klienten och Kerberos är valt.

Om du inaktiverar eller inte konfigurerar den här principinställningen används Kerberos-autentisering direkt i WinRM-klienten.</string>
      <string id="DisallowKerberosServiceHelp">Med den här principinställningen kan du ange huruvida Kerberos-autentiseringsuppgifter ska godkännas i WinRM-tjänsten (Windows Remote Management) vid kommunikation i nätverket.

        Om du aktiverar den här principinställningen godkänns inte Kerberos-autentiseringsuppgifter vid kommunikation i nätverket av WinRM-tjänsten.

        Om du inaktiverar eller inte konfigurerar den här principinställningen godkänns Kerberos-autentisering från en fjärrklient av WinRM-tjänsten.</string>
      <string id="AllowCredSSP">Tillåt CredSSP-autentisering</string>
      <string id="AllowCredSSPClientHelp">Om du aktiverar den här principinställningen kan du ange om CredSSP-autentisering ska användas i WinRM-klienten (Windows Remote Management).

Om du aktiverar den här principinställningen används CredSSP-autentisering i WinRM-klienten.

Om du inaktiverar eller inte konfigurerar den här principinställningen används inte CredSSP-autentisering i WinRM-klienten.</string>
      <string id="AllowCredSSPServiceHelp">Om du aktiverar den här principinställningen kan du ange om CredSSP-autentisering från en fjärrklient ska godkännas i WinRM-tjänsten (Windows Remote Management).

        Om du aktiverar den här principinställningen accepterar WinRM-tjänsten CredSSP-autentisering från en fjärrklient.

        Om du inaktiverar eller inte konfigurerar den här principinställningen accepterar WinRM-tjänsten inte CredSSP-autentisering från en fjärrklient.
      
      </string>
      <string id="CBTHardeningLevel">Ange härdningsnivå för kanalbindningstoken</string>
      <string id="CBTHardeningLevelHelp">Du kan använda den här principinställningen för att ange härdningsnivå för kanalbindningstoken, för WinRM-tjänsten (Windows Remote Management).

        Om du aktiverar den här principinställningen använder WinRM-tjänsten den nivå som anges i HardeningLevel för att avgöra om en mottagen begäran ska accepteras, baserat på ett angivet kanalbindningstoken.

        Om du inaktiverar eller inte konfigurerar den här principinställningen kan du konfigurera härdningsnivån lokalt på varje dator.

        Om HardeningLevel har värdet Strikt nekas alla begäranden som saknar en giltig kanalbindningstoken.

        Om HardeningLevel har värdet Avslappnad (standardvärdet) nekas alla begäranden som innehåller en ogiltig kanalbindningstoken. Däremot accepteras en begäran som saknar kanalbindningstoken, men den skyddas inte från attacker som utnyttjar vidarebefordrade autentiseringsuppgifter.

        Om HardeningLevel har värdet Ingen accepteras alla begäranden, men de skyddas inte från attacker som utnyttjar vidarebefordrade autentiseringsuppgifter.
      
      </string>
        <string id="None">Ingen</string>
        <string id="Strict">Strikt</string>
        <string id="Relaxed">Avslappnad</string>
        <string id="WinRM">Windows Remote Management (WinRM)</string>
      <string id="WinRMClient">WinRM-klienten</string>
      <string id="WinRMService">WinRM-tjänsten</string>
      <string id="TrustedHosts">Betrodda värdar</string>
      <string id="TrustedHostsHelp">Med den här principinställningen kan du ange att WinRM-klienten (Windows Remote Management) ska använda listan TrustedHostsList för att avgöra om målvärden är betrodd.

Om du aktiverar den här principinställningen använder WinRM-klienten listan som anges i "Betrodda värdar" för att avgöra om målvärden är betrodd. Listan används i klienten när varken Kerberos eller HTTPS används för autentisering av värdens identitet.

Om du inaktiverar eller inte konfigurerar principinställningen och listan med betrodda värdar måste användas i WinRM-klienten, måste listan konfigureras lokalt på alla datorer.</string>
      <string id="HttpCompatibilityListener">Aktivera HTTP-lyssnare för kompatibilitet</string>
      <string id="HttpsCompatibilityListener">Aktivera HTTPS-lyssnare för kompatibilitet</string>
      <string id="HttpCompatibilityListenerHelp">Den här principinställningen aktiverar eller inaktiverar en HTTP-lyssnare som har skapats för bakåtkompatibilitet i WinRM-tjänsten (Windows Remote Management).

        Om du aktiverar den här principinställningen visas HTTP-lyssnaren alltid.

        Om du inaktiverar eller inte konfigurerar den här principinställningen visas HTTP-lyssnaren aldrig.

        När vissa lyssnare på port 80 migreras till WinRM 2.0 ändras lyssnarportnumret till 5985.

        En lyssnare kan skapas automatiskt på port 80 för att garantera bakåtkompatibilitet.</string>
      <string id="HttpsCompatibilityListenerHelp">Den här principinställningen aktiverar eller inaktiverar en HTTPS-lyssnare som har skapats för bakåtkompatibilitet i WinRM-tjänsten (Windows Remote Management).

        Om du aktiverar den här principinställningen visas HTTPS-lyssnaren alltid.

        Om du inaktiverar eller inte konfigurerar den här principinställningen visas HTTPS-lyssnaren aldrig.

        När vissa lyssnare på port 443 migreras till WinRM 2.0 ändras lyssnarportnumret till 5986.

        En lyssnare kan skapas automatiskt på port 443 för att garantera bakåtkompatibilitet.</string>
    </stringTable>
    <presentationTable>
      <presentation id="AllowAutoConfig">
        <textBox refId="AllowAutoConfig_IPv4Filter">
          <label>IPv4-filter:</label>
        </textBox>
        <textBox refId="AllowAutoConfig_IPv6Filter">
          <label>IPv6-filter:</label>
        </textBox>
        <text>Syntax:</text>
        <text>Skriv "*" om du vill tillåta meddelanden från alla IP-adresser, eller</text>
        <text>låt fältet vara tomt om tjänsten inte ska lyssna på någon IP-adress. Du kan ange ett</text>
        <text>eller flera intervall med IP-adresser.</text>
        <text/>
        <text>Exempel på IPv4-filter:</text>
        <text>2.0.0.1-2.0.0.20, 24.0.0.1-24.0.0.22</text>
        <text>*</text>
        <text/>
        <text>Exempel på IPv6-filter:</text>
        <text>3FFE:FFFF:7654:FEDA:1245:BA98:0000:0000-3FFE:FFFF:7654:FEDA:1245:BA98:3210:4562</text>
        <text>*</text>
      </presentation>
      <presentation id="TrustedHosts">
        <textBox refId="TrustedHosts_List">
          <label>Betrodda värdar:</label>
        </textBox>
        <text>Syntax:</text>
        <text>Konfigurera betrodda värdar med en kommaavgränsad lista</text>
        <text>med värdnamn. Du kan använda jokertecken (*), men</text>
        <text>det går endast att använda ett jokertecken per värdnamnsmönster.</text>
        <text>Använd "&lt;local&gt;" (skiftlägesokänsligt) för att ange</text>
        <text>alla värdnamn som inte innehåller punkt (.).</text>
        <text>Du kan låta listan vara tom för att ange att ingen värd är betrodd.</text>
        <text>Använd asterisk (*) för att ange att alla värdar är betrodda.</text>
        <text>Om du använder asterisk kan inget annat mönster visas i listan.</text>
        <text/>
        <text/>
        <text>Exempel:</text>
        <text>*.domän.se anger att alla datorer i domän.se är betrodda</text>
        <text>2.0.* anger att alla IP-adresser som börjar med 2.0. är betrodda</text>
      </presentation>
        <presentation id="CBTHardeningLevel">
            <dropdownList refId="HardeningLevelCombo" defaultItem="1">Härdningsnivå:</dropdownList>
        </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
