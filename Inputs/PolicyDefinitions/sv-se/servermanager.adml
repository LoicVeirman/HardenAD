<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="SUPPORTED_WindowsServer2008OrWindowsServer2008R2Only">Gäller endast operativsystemen Windows Server 2008 och Windows Server 2008 R2</string>
      <string id="DoNotLaunchServerManager">Visa inte Serverhanteraren automatiskt vid inloggning</string>
      <string id="DoNotLaunchServerManagerHelp">Med hjälp av den här principinställningen kan du hindra Serverhanteraren från att visas automatiskt vid inloggning.

Om du aktiverar den här principinställningen visas Serverhanteraren inte automatiskt när en användare loggar in på servern.

Om du inaktiverar den här principinställningen visas Serverhanteraren automatiskt när en användare loggar in på servern.

Om du inte konfigurerar den här principinställningen visas Serverhanteraren när en användare loggar in på servern. Om alternativet Visa inte denna konsol vid inloggning (Windows Server 2008 och Windows Server 2008 R2) eller alternativet Starta inte Serverhanteraren automatiskt vid inloggning (Windows Server 2012) markeras visas emellertid konsolen inte vid inloggning.

Obs! Oavsett hur principinställningen är konfigurerad är Serverhanteraren alltid tillgänglig på Start-menyn i Aktivitetsfältet i Windows.

</string>
      <string id="ServerManagerAutoRefreshRate">Konfigurera Serverhanterarens uppdateringsintervall</string>
      <string id="ServerManagerAutoRefreshRateHelp">Med hjälp av den här principinställningen kan du ange Serverhanterarens uppdateringsintervall. Vid varje uppdatering får Serverhanteraren uppdaterad information om vilka roller och funktioner som är installerade på de servrar som du hanterar med Serverhanteraren. Serverhanteraren övervakar även status för roller och funktioner som är installerade på hanterade servrar.

Om du aktiverar den här principinställningen använder Serverhanteraren de uppdateringsintervall som anges i principinställningen i stället för inställningen Konfigurera uppdatering (i Windows Server 2008 och Windows Server 2008 R2) eller Uppdatera data som visas i serverhanteraren efter [x] [minuter/timmar/dagar] (i Windows Server 2012) som konfigureras i serverhanterarkonsolen.

Om du inaktiverar den här principinställningen uppdateras inte Serverhanteraren automatiskt. Om du inte konfigurerar den här principinställningen använder Serverhanteraren uppdateringsinställningarna i serverhanterarkonsolen.

Obs! Uppdateringsintervallet för Serverhanteraren är som standard två minuter i Windows Server 2008 och Windows Server 2008 R2 samt 10 minuter i Windows Server 2012.

</string>
      <string id="Do_not_display_Manage_Your_Server_page">Visa inte Hantera servern vid inloggning</string>
      <string id="Do_not_display_Manage_Your_Server_page_Explain">Med hjälp av den här inställningen kan du inaktivera automatisk visning av sidan Hantera servern. 

Om den här inställningen är aktiverad visas inte sidan Hantera servern varje gång en administratör loggar in på servern. 

Om den här inställningen är inaktiverad eller inte konfigurerad visas sidan Hantera servern varje gång en administratör loggar in på servern. Om administratören har markerat kryssrutan Visa inte den här sidan vid inloggning längst ned på sidan Hantera servern visas sidan inte.

</string>
      <string id="DoNotLaunchInitialConfigurationTasks">Visa inte Åtgärder vid inledande konfiguration automatiskt vid inloggning</string>
      <string id="DoNotLaunchInitialConfigurationTasksHelp">Med den här principinställningen kan du hindra att fönstret Åtgärder vid inledande konfiguration visas automatiskt vid inloggning i Windows Server 2008 och Windows Server 2008 R2. 

Om du aktiverar den här principinställningen visas inte fönstret Åtgärder vid inledande konfiguration när en administratör loggar in på servern. 

Om du inaktiverar den här principinställningen visas fönstret Åtgärder vid inledande konfiguration automatiskt när en administratör loggar in på servern.

Om du inte konfigurerar den här principinställningen visas fönstret Åtgärder vid inledande konfiguration när en administratör loggar in på servern. Om en administratör markerar kryssrutan Visa inte det här fönstret vid inloggning, öppnas inte fönstret vid påföljande inloggningar.

</string>
      <string id="ServerManager">Serverhanteraren</string>
    </stringTable>
    <presentationTable>
      <presentation id="ServerManagerAutoRefreshRate">
        <decimalTextBox refId="RefreshRate" defaultValue="2">Minuter:</decimalTextBox>
        <text>Värdet kan vara mellan 1 och 34 560</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
