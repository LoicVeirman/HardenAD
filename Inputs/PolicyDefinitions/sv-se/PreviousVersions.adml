<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="DisableBackupRestore">Förhindra återställning av tidigare versioner från säkerhetskopior</string>
      <string id="DisableBackupRestore_Help">Den här principinställningen innebär att du kan ignorera knappen Återställ på tidigare versioners egenskapssida när användaren har valt en tidigare version av en lokal fil som finns på säkerhetskopia.

Om du aktiverar den här principinställningen inaktiveras knappen Återställ om användaren har valt en tidigare version som finns på säkerhetskopia.

Om du inaktiverar den här principinställningen förblir knappen Återställ aktiv för en tidigare version som finns på säkerhetskopia. Om du klickar på knappen Återställ försöker Windows återställa filen från säkerhetskopieringsmediet.

Om du inte konfigurerar den här principinställningen inaktiveras den som standard. Knappen Återställ är aktiv när den tidigare versionen gäller en lokal fil och när den lagras på säkerhetskopian.</string>
      <string id="DisableLocalPage">Dölj listan med tidigare versioner av lokala filer</string>
      <string id="DisableLocalPage_Help">Du kan använda den här principinställningen för att dölja listan med tidigare versioner av filer som finns på lokala diskar. De tidigare versionerna kan komma från återställningspunkter på disken eller från ett säkerhetskopieringsmedium.

Om du aktiverar den här principinställningen kan inte användarna visa eller återställa tidigare versioner av filer på lokala diskar.

Om du inaktiverar den här principinställningen kan inte användarna visa och återställa tidigare versioner av filer på lokala diskar.

Om du inte konfigurerar den här principinställningen inaktiveras den som standard.</string>
      <string id="DisableLocalRestore">Förhindra återställning av tidigare lokala versioner</string>
      <string id="DisableLocalRestore_Help">Den här principinställningen innebär att du kan ignorera knappen Återställ på tidigare versioners egenskapssida när användaren har valt en tidigare version av en lokal fil.

Om du aktiverar den här principinställningen inaktiveras knappen Återställ om användaren har valt en tidigare version som finns på en lokal fil.

Om du inaktiverar den här principinställningen förblir knappen Återställ aktiv för en tidigare version som finns på en lokal fil. Om användaren klickar på knappen Återställ försöker Windows återställa filen från den lokala disken.

Om du inte konfigurerar den här principinställningen inaktiveras den som standard. Knappen Återställ är aktiv när den tidigare versionen gäller en lokal fil.</string>
      <string id="DisableRemotePage">Dölj listan med tidigare versioner av fjärrlagrade filer</string>
      <string id="DisableRemotePage_Help">Du kan använda den här principinställningen för att dölja listan med tidigare versioner av filer som finns på filresurser. De tidigare versionerna kan komma från återställningspunkter på disken eller från filresursen.

Om du aktiverar den här principinställningen kan inte användarna visa eller återställa tidigare versioner av filer på filresurser.

Om du inaktiverar den här principinställningen kan användarna visa och återställa tidigare versioner av filer på filresurser.

Om du inte konfigurerar den här principinställningen inaktiveras den som standard.</string>
      <string id="DisableRemoteRestore">Förhindra återställning av tidigare fjärrlagrade versioner</string>
      <string id="DisableRemoteRestore_Help">I den här principinställningen kan du ignorera knappen Återställ på tidigare versioners egenskapssida när användaren har valt en tidigare version av en fil på en filresurs.

Om du aktiverar den här principinställningen inaktiveras knappen Återställ om användaren har valt en tidigare version som motsvarar en fil på en filresurs.

Om du inaktiverar den här principinställningen förblir knappen Återställ aktiv för en tidigare version som motsvarar en fil på en filresurs. Om användaren klickar på knappen Återställ försöker Windows återställa filen från filresursen.

Om du inte konfigurerar den här principinställningen inaktiveras den som standard. Knappen Återställ är aktiv när den tidigare versionen gäller en fil på en filresurs.</string>
      <string id="HideBackupEntries">Dölj tidigare versioner av filer på säkerhetskopieringsmedier</string>
      <string id="HideBackupEntries_Help">Den här principinställningen innebär att du kan dölja de poster i listan med tidigare versioner av en fil som finns på ett säkerhetskopieringsmedium. De tidigare versionerna kan komma från återställningspunkter på disken eller från säkerhetskopieringsmediet.

Om du aktiverar den här principinställningen visas inga tidigare versioner som finns på säkerhetskopior för användarna. De kan bara se tidigare versioner som finns på återställningspunkter på disken.

Om du inaktiverar den här principinställningen visas både tidigare versioner som finns på säkerhetskopior och tidigare versioner som finns på återställningspunkter på disken för användarna.

Om du inte konfigurerar den här principinställningen inaktiveras den som standard.</string>
      <string id="PreviousVersions">Tidigare versioner</string>
      <string id="SUPPORTED_Vista_through_Win7">Stöds från Windows Vista till Windows 7</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
