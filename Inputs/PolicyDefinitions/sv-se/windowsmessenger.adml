<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">Starta inte Windows Messenger automatiskt vid inloggning</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">Den här principinställningen hindrar Windows Messenger från att startas automatiskt vid inloggning. 

Om du aktiverar den här principinställningen startas Windows Messenger inte automatiskt när användaren loggar in.

Om du inaktiverar eller inte konfigurerar den här principinställningen startas Windows Messenger automatiskt vid inloggning.

Obs! Den här principinställningen hindrar endast Windows Messenger från att startas automatiskt vid inloggning. Om användaren försöker starta och använda Windows Messenger efter inloggning, startas programmet som vanligt.

Användaren kan också konfigurera detta på fliken "Inställningar" på "Verktyg"-menyn i användargränssnittet för Windows Messenger.

Obs! Om du inte vill att användarna ska använda Windows Messenger kan du aktivera principinställningen Tillåt inte att Windows Messenger körs.

Obs! Den här principinställningen är tillgänglig både i Datorkonfiguration och i Användarkonfiguration. Om båda inställningarna konfigureras gäller inställningen i Datorkonfiguration.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">Starta inte Windows Messenger automatiskt vid inloggning</string>
      <string id="WinMSG_NoWindowsMsg_Comp">Tillåt inte att Windows Messenger körs</string>
      <string id="WinMSG_NoWindowsMsg_Help">Du kan använda den här principinställningen för att hindra Windows Messenger från att köras.

Om du aktiverar den här principinställningen går Windows Messenger inte att köra.

Om du inaktiverar eller inte konfigurerar den här principinställningen går det att köra Windows Messenger.

Obs! Om du aktiverar den här principinställningen kan Fjärrhjälp inte heller användas med Windows Messenger.

Obs! Den här principinställningen är tillgänglig både i Datorkonfiguration och i Användarkonfiguration. Om båda inställningarna konfigureras gäller inställningen i Datorkonfiguration.</string>
      <string id="WinMSG_NoWindowsMsg_User">Tillåt inte att Windows Messenger körs</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
