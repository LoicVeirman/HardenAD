<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="PublishDfsRoots">Tillåt publicering av DSF-rötter</string>
      <string id="PublishDfsRoots_Help">Den här principinställningen bestämmer om användaren kan publicera DFS-rötter i AD DS (Active Directory Domain Services).

Om den här principinställningen aktiveras eller inte konfigureras kan användare använda alternativet Publicera i Active Directory för att publicera DFS-rötter i AD DS.

Om den här principinställningen inaktiveras kan användare inte publicera DFS-rötter i AD DS och alternativet Publicera Active Directory är inaktiverat. Obs! Som standard tillåts att delade mappar publiceras när den här inställningen inte är konfigurerad.</string>
      <string id="PublishSharedFolders">Tillåt publicering av delade mappar</string>
      <string id="PublishSharedFolders_Help">Den här principinställningen bestämmer om användaren kan publicera delade mappar i AD DS (Active Directory Domain Services).

Om den här principinställningen aktiveras eller inte konfigureras kan användare använda alternativet i snapin-modulen Delade mappar för att publicera delade mappar i AD DS.

Om den här principinställningen inaktiveras kan användare inte publicera delade mappar i AD DS och alternativet Publicera Active Directory är inaktiverat. Obs! Som standard tillåts att delade mappar publiceras när den här inställningen inte är konfigurerad.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
