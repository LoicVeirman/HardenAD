<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>introduzir o nome a apresentar aqui</displayName>
  <description>introduzir a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="P2P_Disabled">Desligar os Serviços de Funcionamento em Rede Ponto a Ponto da Microsoft</string>
      <string id="P2P_Disabled_Explain">Esta definição desliga totalmente os Serviços de Funcionamento em Rede Ponto a Ponto da Microsoft, fazendo com que todas as aplicações dependentes deixem de funcionar.

Os protocolos ponto a ponto permitem a utilização de aplicações nas áreas de RTC, colaboração, distribuição de conteúdo e processamento distribuído.

Se ativar esta definição, os protocolos ponto a ponto serão desligados.

Se desativar esta definição ou não a configurar, os protocolos ponto a ponto serão ligados.</string>
      <string id="P2P_Name">Serviços de Funcionamento em Rede Ponto a Ponto da Microsoft</string>
      <string id="PNRP_DisableMulticastBootstrap">Desligar Arranque de Sistema Multicast</string>
      <string id="PNRP_DisableMulticastBootstrapGlobal_Explain">Esta definição desativa o protocolo PNRP impedindo-o de anunciar o computador ou de efetuar pesquisas noutros computadores na sub-rede local no cloud global.

O Protocolo de Resolução de Nome Peer (PNRP) permite a resolução distribuída de um nome para um endereço e número de porta IPV6. Uma das formas que o PNRP utiliza para efetuar o próprio arranque de sistema é através de um multicast na mesma sub-rede. Isto é, o PNRP publica-se automaticamente na sub-rede local, de modo a que outros computadores o consigam localizar, se for necessário.

Se ativar esta definição, o PNRP não utilizará o multicast para efetuar o arranque do sistema. A especificação desta chave de registo abrirá cenários onde não existem servidores seed para efetuar o arranque do sistema (tais como cenários de funcionamento em rede ad-hoc).

Se desativar esta definição, o PNRP utilizará o multicast para efetuar o arranque do sistema na mesma sub-rede.

Se esta definição não estiver configurada, o protocolo voltará a utilizar uma chave de registo pública para determinar se irá publicar o computador ou irá utilizar o multicast para procurar outros computadores na sub-rede local.

O protocolo multicast utilizado para o arranque do sistema é o Protocolo de Serviço de Identificação Simples (SSDP). O serviço SSDP tem de estar ativado (está por predefinição) para esta política produzir efeito.</string>
      <string id="PNRP_Disabled">Desativar criação de nuvens PNRP</string> 
      <string id="PNRP_SearchOnly">Definir nuvem PNRP como Apenas resolução</string> 
      <string id="PNRP_DisabledGlobal_Explain">Esta definição de política ativa ou desativa a criação de nuvens PNRP.

O PNRP é um protocolo de resolução de nomes distribuído que permite que os anfitriões da Internet publiquem nomes de elementos com um endereço IPv6 (Internet Protocol versão 6) correspondente. Em seguida, outros anfitriões podem resolver o nome, obter o endereço correspondente e estabelecer ligações ponto a ponto (P2P). A nuvem PNRP consiste num grupo de nós PNRP ligados, em que existem ligações entre elementos da rede, de modo a que um nó na nuvem PNRP possa resolver nomes publicados por outros nós.

O PNRP cria uma nuvem global se o computador tiver um endereço IPv6 global, mas cria uma nuvem de local de site se o computador tiver um endereço de local de site.

Se ativar esta definição de política, o PNRP não cria uma nuvem e as aplicações não conseguem utilizar esta nuvem para publicar ou resolver nomes, independentemente de o computador ter um endereço IPv6 correspondente ao âmbito da nuvem.

Se desativar ou não configurar esta definição de política, todas as nuvens PNRP são ativadas por predefinição e o PNRP cria uma nuvem se o computador tiver um endereço IPv6 compatível com o âmbito da nuvem.
</string>
      <string id="PNRP_SearchOnlyGlobal_Explain">Esta definição de política limita um nó à resolução, mas não à publicação, de nomes numa nuvem PNRP (Peer Name Resolution Protocol) específica.

Esta definição de política força os computadores a agirem como clientes em cenários ponto a ponto (P2P). Por exemplo, um computador cliente pode detetar outros computadores para iniciar sessões de chat, mas outros computadores não conseguem detetar esse cliente e iniciar sessões com ele.

Se ativar esta definição de política, este computador não consegue registar nomes PNRP e não consegue ajudar outros computadores a executarem pesquisas PNRP.

Se desativar ou não configurar esta definição de política, este computador pode publicar nomes PNRP e ajudar outros computadores a executar pesquisas PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapLinkLocal_Explain">Esta definição desativa o protocolo PNRP impedindo-o de anunciar o computador ou de efetuar pesquisas noutros computadores na sub-rede local no cloud global de ligação.

O Protocolo de Resolução de Nome Peer (PNRP) permite a resolução distribuída de um nome para um endereço e número de porta IPV6. Uma das formas que o PNRP utiliza para efetuar o próprio arranque de sistema é através de um multicast na mesma sub-rede. Isto é, o PNRP publica-se automaticamente na sub-rede local, de modo a que outros computadores o consigam localizar, se for necessário.

Se ativar esta definição, o PNRP não utilizará o multicast para efetuar o arranque do sistema. A especificação desta chave de registo abrirá cenários onde não existem servidores seed para efetuar o arranque do sistema (tais como cenários de funcionamento em rede ad-hoc).

Se desativar esta definição, o PNRP utilizará o multicast para efetuar o arranque do sistema na mesma sub-rede.

Se esta definição não estiver configurada, o protocolo voltará a utilizar uma chave de registo pública para determinar se irá publicar o computador ou irá utilizar o multicast para procurar outros computadores na sub-rede local.

O protocolo multicast utilizado para o arranque do sistema é o Protocolo de Serviço de Identificação Simples (SSDP). O serviço SSDP tem de estar ativado (está por predefinição) para esta política produzir efeito.</string>
      <string id="PNRP_DisabledLinkLocal_Explain">Esta definição de política ativa ou desativa a criação de nuvens PNRP.

O PNRP é um protocolo de resolução de nomes distribuído que permite que os anfitriões da Internet publiquem nomes de elementos com um endereço IPv6 (Internet Protocol versão 6) correspondente. Em seguida, outros anfitriões podem resolver o nome, obter o endereço correspondente e estabelecer ligações ponto a ponto (P2P). A nuvem PNRP consiste num grupo de nós PNRP ligados, em que existem ligações entre elementos da rede, de modo a que um nó na nuvem PNRP possa resolver nomes publicados por outros nós.

O PNRP cria uma nuvem global se o computador tiver um endereço IPv6 global, mas cria uma nuvem de local de site se o computador tiver um endereço de local de site.

Se ativar esta definição de política, o PNRP não cria uma nuvem e as aplicações não conseguem utilizar esta nuvem para publicar ou resolver nomes, independentemente de o computador ter um endereço IPv6 correspondente ao âmbito da nuvem.

Se desativar ou não configurar esta definição de política, todas as nuvens PNRP são ativadas por predefinição e o PNRP cria uma nuvem se o computador tiver um endereço IPv6 compatível com o âmbito da nuvem.
</string>
      <string id="PNRP_SearchOnlyLinkLocal_Explain">Esta definição de política limita um nó à resolução, mas não à publicação, de nomes numa nuvem PNRP (Peer Name Resolution Protocol) específica.

Esta definição de política força os computadores a agirem como clientes em cenários ponto a ponto (P2P). Por exemplo, um computador cliente pode detetar outros computadores para iniciar sessões de chat, mas outros computadores não conseguem detetar esse cliente e iniciar sessões com ele.

Se ativar esta definição de política, este computador não consegue registar nomes PNRP e não consegue ajudar outros computadores a executarem pesquisas PNRP.

Se desativar ou não configurar esta definição de política, este computador pode publicar nomes PNRP e ajudar outros computadores a executar pesquisas PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapSiteLocal_Explain">Esta definição desativa o protocolo PNRP impedindo-o de anunciar o computador ou de efetuar pesquisas noutros computadores na sub-rede local no cloud global de site.

O Protocolo de Resolução de Nome Peer (PNRP) permite a resolução distribuída de um nome para um endereço e número de porta IPV6. Uma das formas que o PNRP utiliza para efetuar o próprio arranque de sistema é através de um multicast na mesma sub-rede. Isto é, o PNRP publica-se automaticamente na sub-rede local, de modo a que outros computadores o consigam localizar, se for necessário.

Se ativar esta definição, o PNRP não utilizará o multicast para efetuar o arranque do sistema. A especificação desta chave de registo abrirá cenários onde não existem servidores seed para efetuar o arranque do sistema (tais como cenários de funcionamento em rede ad-hoc).

Se desativar esta definição, o PNRP utilizará o multicast para efetuar o arranque do sistema na mesma sub-rede.

Se esta definição não estiver configurada, o protocolo voltará a utilizar uma chave de registo pública para determinar se irá publicar o computador ou irá utilizar o multicast para procurar outros computadores na sub-rede local.

O protocolo multicast utilizado para o arranque do sistema é o Protocolo de Serviço de Identificação Simples (SSDP). O serviço SSDP tem de estar ativado (está por predefinição) para esta política produzir efeito.</string>
      <string id="PNRP_DisabledSiteLocal_Explain">Esta definição de política ativa ou desativa a criação de nuvens PNRP.

O PNRP é um protocolo de resolução de nomes distribuído que permite que os anfitriões da Internet publiquem nomes de elementos com um endereço IPv6 (Internet Protocol versão 6) correspondente. Em seguida, outros anfitriões podem resolver o nome, obter o endereço correspondente e estabelecer ligações ponto a ponto (P2P). A nuvem PNRP consiste num grupo de nós PNRP ligados, em que existem ligações entre elementos da rede, de modo a que um nó na nuvem PNRP possa resolver nomes publicados por outros nós.

O PNRP cria uma nuvem global se o computador tiver um endereço IPv6 global, mas cria uma nuvem de local de site se o computador tiver um endereço de local de site.

Se ativar esta definição de política, o PNRP não cria uma nuvem e as aplicações não conseguem utilizar esta nuvem para publicar ou resolver nomes, independentemente de o computador ter um endereço IPv6 correspondente ao âmbito da nuvem.

Se desativar ou não configurar esta definição de política, todas as nuvens PNRP são ativadas por predefinição e o PNRP cria uma nuvem se o computador tiver um endereço IPv6 compatível com o âmbito da nuvem.
</string>
      <string id="PNRP_SearchOnlySiteLocal_Explain">Esta definição de política limita um nó à resolução, mas não à publicação, de nomes numa nuvem PNRP (Peer Name Resolution Protocol) específica.

Esta definição de política força os computadores a agirem como clientes em cenários ponto a ponto (P2P). Por exemplo, um computador cliente pode detetar outros computadores para iniciar sessões de chat, mas outros computadores não conseguem detetar esse cliente e iniciar sessões com ele.

Se ativar esta definição de política, este computador não consegue registar nomes PNRP e não consegue ajudar outros computadores a executarem pesquisas PNRP.

Se desativar ou não configurar esta definição de política, este computador pode publicar nomes PNRP e ajudar outros computadores a executar pesquisas PNRP.
</string> 
      <string id="PNRP_Global">Clouds Globais</string>
      <string id="PNRP_LinkLocal">Clouds Locais de Ligação</string>
      <string id="PNRP_Name">Protocolo de Resolução de Nome Peer</string>
      <string id="PNRP_SeedServer">Definir o Servidor Seed</string>
      <string id="PNRP_SeedServerGlobal_Explain">Esta definição define o servidor seed para o cloud global como um nó especificado na empresa. O Protocolo de Resolução de Nome Peer (PNRP) permite a resolução distribuída de um nome para um endereço e número de porta IPv6. O protocolo, nalguns casos, requer um servidor seed a partir do qual possa efetuar o arranque do sistema.

Qualquer nó executado ponto a ponto pode ser utilizado como um servidor seed. Não é necessária qualquer configuração para o próprio servidor seed.

Esta definição fornece a flexibilidade acrescida de permitir aos utilizadores utilizar as respetivas aplicações ponto a ponto igualmente em casa. Seguem-se as quatro formas em que pode utilizar esta definição para controlar o servidor seed utilizado na empresa.

1. Para utilizar apenas o servidor seed global bem conhecido na Internet; ative a definição, deixe a lista de servidores seed em branco, deixe a caixa de verificação desmarcada.

2. Para utilizar apenas um servidor seed da empresa, ative a definição; insira o nome de domínio completamente qualificado ou o endereço IPv6 do servidor seed da empresa; e selecione a caixa de verificação. A utilização apenas do servidor seed da empresa impedirá que os utilizadores móveis consigam utilizar as respetivas aplicações ponto a ponto em casa.

3. Para utilizar um servidor seed da empresa e o servidor seed global, ative a definição; insira o nome de domínio completamente qualificado ou o endereço IPv6 do servidor seed da empresa e deixe a caixa de verificação desmarcada. Esta é a definição que permitirá aos utilizadores móveis utilizar as aplicações ponto a ponto, tanto no trabalho como em casa, sem interrupções.

4. Para não utilizar qualquer servidor seed, ative a definição; não insira um nome de servidor seed; e selecione a caixa de verificação.

Se esta definição estiver desativada ou não configurada, o protocolo voltará a utilizar uma chave de registo pública para determinar o servidor seed a partir do qual efetua o arranque do sistema.</string>
      <string id="PNRP_SeedServerLinkLocal_Explain">Esta definição define o servidor seed para o cloud global de ligação como um nó especificado na empresa.

O Protocolo de Resolução de Nome Peer (PNRP) permite a resolução distribuída de um nome para um endereço e número de porta IPV6. O protocolo, nalguns casos, requer um servidor seed a partir do qual possa efetuar o arranque do sistema. Esta definição permite a definição do servidor seed para um nó especificado numa empresa.

Para utilizar esta definição, clique em Ativar e, em seguida, introduza uma lista delimitada por ponto e vírgula de endereços IPV6 no campo disponível. Se ativar esta definição e não introduzir qualquer endereço, não será utilizado qualquer servidor seed.

Se esta definição não for configurada, o protocolo voltará a utilizar uma chave de registo pública para determinar o servidor seed a partir do qual efetua o arranque do sistema.</string>
      <string id="PNRP_SeedServerSiteLocal_Explain">Esta definição define o servidor seed para o cloud global de site como um nó especificado na empresa.

O Protocolo de Resolução de Nome Peer (PNRP) permite a resolução distribuída de um nome para um endereço e número de porta IPV6. O protocolo, nalguns casos, requer um servidor seed a partir do qual possa efetuar o arranque do sistema. Esta definição permite a definição do servidor seed para um nó especificado numa empresa.

Para utilizar esta definição, clique em Ativar e, em seguida, introduza uma lista delimitada por ponto e vírgula de nomes DNS ou de endereços IPV6 no campo disponível. Se ativar esta definição e não introduzir qualquer endereço, não será utilizado qualquer servidor seed.

Se esta definição não for configurada, o protocolo voltará a utilizar uma chave de registo pública para determinar o servidor seed a partir do qual efetua o arranque do sistema.</string>
      <string id="PNRP_SiteLocal">Clouds Locais de Site</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement">Desativar a validação da força da palavra-passe para Agrupamento ponto a ponto</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement_Explain">Por predefinição, quando é criado um Grupo ponto a ponto que permite a autenticação por palavra-passe (ou a palavra-passe desse Grupo é alterada), o Agrupamento ponto a ponto valida que a palavra-passe satisfaz aos requisitos de complexidade para palavras-passe para o sistema local. Desta forma, não permitirá a utilização de palavras-passe para um Grupo ponto a ponto que sejam mais permeáveis do que seria permitido para uma palavra-passe de início de sessão.

Esta definição controla este comportamento de validação. Se o valor for definido para 1, esta validação não será efetuada e serão permitidas todas as palavras-passe. Se o valor for definido para 0, a validação será efetuada.
      </string>
    </stringTable>
    <presentationTable>
      <presentation id="PNRP_SeedServer_1">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Servidor Seed</label>
        </textBox>
        <checkBox refId="PNRP_Dont_Include_Microsoft_SeedServer">Não utilizar o servidor seed de PNRP global da Microsoft</checkBox>
      </presentation>
      <presentation id="PNRP_SeedServer_2">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Servidor Seed</label>
        </textBox>
      </presentation>
      <presentation id="PNRP_SeedServer_3">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Servidor Seed</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
