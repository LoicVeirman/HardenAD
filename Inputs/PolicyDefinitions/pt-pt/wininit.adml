<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>introduza o nome a apresentar aqui</displayName>
  <description>introduza a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="DisableNamedPipeShutdownPolicyDescription">Desativar a interface de encerramento remota legada</string>
      <string id="DisableNamedPipeShutdownPolicyDescription_Help">Esta definição de política controla a interface de encerramento remota legada (pipe nomeado). A interface de encerramento remota de pipe nomeado é necessária para encerrar este sistema a partir de um sistema remoto Windows XP ou Windows Server 2003.

Se esta definição estiver ativada, o sistema não cria a interface de encerramento remota de pipe nomeado.

Se desativar ou não configurar esta definição de política, o sistema cria a interface de encerramento remota de pipe nomeado.</string>
      <string id="ShutdownOptions">Opções de Encerramento</string>
      <string id="ShutdownTimeoutHungSessionsDescription">Tempo limite para sessões de início de sessão paradas durante o encerramento</string>
      <string id="ShutdownTimeoutHungSessionsDescription_Help">Esta definição de política configura o número de minutos que o sistema aguardará pelas sessões de início de sessão paradas antes de avançar com o encerramento do sistema.

Se ativar esta definição de política, o sistema aguardará pelas sessões de início de sessão paradas durante o número de minutos especificado.

Se desativar ou não configurar esta definição de política, o valor de tempo limite predefinido será de 3 minutos para estações de trabalho e de 15 minutos para servidores.</string>
       <string id="Shutdown">Encerramento</string>
       <string id="Hiberboot">Exigir utilização de arranque rápido</string>
       <string id="Hiberboot_Help">Esta definição de política controla a utilização do arranque rápido.  

Se ativar esta definição de política, o sistema necessitará que a hibernação esteja ativada.

Se desativar ou não configurar esta definição de política, será utilizada a definição local.</string>
    </stringTable>
    <presentationTable>
      <presentation id="ShutdownTimeoutHungSessionsDescription">
        <decimalTextBox refId="ShutdownSessionTimeout_Time" defaultValue="9999">Tempo limite de sessão parada em Minutos:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
