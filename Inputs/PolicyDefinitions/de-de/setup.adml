<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Anzeigenamen hier eingeben</displayName>
  <description>Beschreibung hier eingeben</description>
  <resources>
    <stringTable>
      <string id="ServicePackSourcePath">Windows Service Pack-Installationsdateipfad angeben</string>
      <string id="ServicePackSourcePath_Help">Gibt einen alternativen Speicherort für die Windows Service Pack-Installationsdateien an.

Wenn Sie diese Richtlinieneinstellung aktivieren, geben Sie den vollqualifizierten Pfad zum neuen Speicherort im Feld "Windows Service Pack Setup-Dateipfad" ein.

Wenn Sie diese Richtlinieneinstellung deaktivieren oder nicht konfigurieren, wird als Quellpfad für Windows Service Pack Setup der Speicherort verwendet, der bei der letzten Ausführung von Windows Service Pack Setup auf dem System verwendet wurde.



</string>
      <string id="SetupSourcePath">Windows-Installationsdateipfad angeben</string>
      <string id="SetupSourcePath_Help">Gibt einen alternativen Speicherort für die Windows-Installationsdateien an.

Wenn Sie diese Richtlinieneinstellung aktivieren, geben Sie den vollqualifizierten Pfad zum neuen Speicherort im Feld "Windows Setup-Dateipfad" ein. 

Wenn Sie diese Richtlinieneinstellung deaktivieren oder nicht konfigurieren, wird als Windows Setup-Quellpfad der Speicherort verwendet, der bei der letzten Ausführung von Windows Setup auf dem System verwendet wurde.



</string>
    </stringTable>
    <presentationTable>
      <presentation id="ServicePackSourcePath">
        <textBox refId="ServicePackSourcePathBox">
          <label>Windows Service Pack Setup-Dateipfad:</label>
        </textBox>
      </presentation>
      <presentation id="SetupSourcePath">
        <textBox refId="SetupSourcePathBox">
          <label>Windows Setup-Dateipfad:</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
