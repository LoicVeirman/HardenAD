<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Anzeigename hier eingeben</displayName>
  <description>Beschreibung hier eingeben</description>
  <resources>
    <stringTable>
      <string id="Snis">Server für NIS</string>
      <string id="Snis_LoggingLevel">Erweiterte Protokollierung für Domänencontroller der Active Directory-Domänendienste aktivieren, auf denen Server für NIS ausgeführt wird</string>
      <string id="Snis_LoggingLevel_Help">Diese Richtlinieneinstellung ermöglicht es Administratoren, die erweiterte Protokollierung für Computer zu konfigurieren, auf denen Server für NIS (Network Information Service, Netzwerkinformationsdienst) ausgeführt wird.

Wenn Sie diese Richtlinieneinstellung aktivieren, werden für alle betroffenen Computer, auf denen Server für NIS ausgeführt wird, Zwischenschritte von NIS-Zuordnungsaktualisierungen und NIS-Zuordnungspropagierungen sowie der Erfolgsstatus von Zuordnungsaktualisierungen protokolliert.

Wenn Sie diese Richtlinieneinstellung deaktivieren oder nicht konfigurieren, protokollieren Computer, auf denen Server für NIS ausgeführt wird, Zuordnungspropagierungsschritte gemäß der Einstellung "Protokollierung für NIS-Zuordnungspropagierung" auf der Registerkarte "Protokollierung" des Dialogfelds "Server für NIS - Eigenschaften".</string>
      <string id="Snis_PushInterval">Zuordnungsaktualisierungsintervall für untergeordnete NIS-Server festlegen</string>
      <string id="Snis_PushInterval_Help">Diese Richtlinieneinstellung ermöglicht Administratoren von Server für NIS, ein Aktualisierungsintervall zum Übertragen von Netzwerkinformationsdienst-Zuordnungen (NIS-Zuordnungen) an untergeordnete NIS-Server zu konfigurieren.

Wenn Sie diese Richtlinieneinstellung aktivieren, gilt das in dieser Richtlinieneinstellung angegebene Zuordnungsaktualisierungsintervall für alle betroffenen Domänencontroller, auf denen Server für NIS ausgeführt wird.

Wenn Sie diese Richtlinieneinstellung deaktivieren oder nicht konfigurieren, verwenden Computer, auf denen Server für NIS ausgeführt wird, das auf der Registerkarte "Allgemein" des Dialogfelds "Server für NIS - Eigenschaften" angegebene Zuordnungsaktualisierungsintervall.

Hinweis: Als Werte für Intervalle sind ganze Zahlen in den folgenden Bereichen gültig: 0 bis 99999 Tage, 0 bis 23 Stunden, 0 bis 59 Minuten. Wenn die Richtlinieneinstellung aktiviert ist, ist der Standardwert ein Tag.</string>
    </stringTable>
    <presentationTable>
      <presentation id="Snis_PushInterval">
        <decimalTextBox refId="Snis_PushInterval" defaultValue="1440">Aktualisierungsintervall für untergeordnete NIS-Server festlegen</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
