<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Přenosný pracovní prostor</displayName>
  <description>Tento soubor obsahuje nastavení zásad Přenosný pracovní prostor.</description>
  <resources>
    <stringTable>
      <string id="PortableOperatingSystem">Přenosný operační systém</string>
      <string id="PortableOperatingSystem_Launcher_DisplayName">Výchozí možnosti spuštění balíčku Windows To Go</string>
      <string id="PortableOperatingSystem_Launcher_Help">



Toto nastavení zásad určuje, zda se počítač spustí v režimu balíčku Windows To Go, když je připojeno zařízení USB obsahující pracovní prostor Windows To Go, a určuje, zda může uživatel provádět změny pomocí položky ovládacího panelu Možnosti spuštění balíčku Windows To Go.

Pokud toto nastavení povolíte, bude povoleno spuštění balíčku Windows To Go, když bude připojeno zařízení USB, a uživatelé nebudou moci provádět změny pomocí položky ovládacího panelu Možnosti spuštění balíčku Windows To Go.

Pokud toto nastavení zakážete, nebude povoleno spuštění balíčku Windows To Go, když je připojeno zařízení USB, pokud uživatel tuto možnost nenakonfiguruje ručně v systému BIOS nebo jiné konfiguraci pořadí spouštění.

Pokud toto nastavení nenakonfigurujete, uživatelé, kteří jsou členy skupiny Administrators, mohou provádět změny pomocí položky ovládacího panelu Možnosti spouštěcího balíčku Windows To Go.</string>
      <string id="PortableOperatingSystem_Hibernate_DisplayName">Povolit hibernaci (S4) při spuštění z pracovního prostoru Windows To Go</string>
      <string id="PortableOperatingSystem_Hibernate_Help">



Určuje, zda může počítač při spuštění z pracovního prostoru Windows To Go používat stav hibernace (S4).

Pokud toto nastavení povolíte, může systém Windows při spuštění z pracovního prostoru Windows To Go přepnout počítač do režimu hibernace.

Pokud toto nastavení zakážete nebo nenakonfigurujete, nemůže systém Windows při spuštění z pracovního prostoru Windows To Go přepnout počítač do režimu hibernace.</string>
      <string id="PortableOperatingSystem_Sleep_DisplayName">Nepovolit stavy spánku (S1–S3) v úsporném režimu při spuštění z pracovního prostoru Windows To Go</string>
      <string id="PortableOperatingSystem_Sleep_Help">



Určuje, zda počítač může při spuštění z pracovního prostoru Windows To Go používat stavy spánku (S1–S3) v úsporném režimu.

Pokud toto nastavení povolíte, nemůže systém Windows při spuštění z pracovního prostoru Windows To Go přepnout počítač do režimu spánku pomocí stavů úsporného režimu.

Pokud toto nastavení zakážete nebo nenakonfigurujete, může systém Windows při spuštění z pracovního prostoru Windows To Go přepnout počítač do režimu spánku pomocí stavů úsporného režimu.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
