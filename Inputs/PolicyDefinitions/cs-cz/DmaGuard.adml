<?xml version="1.0" encoding="utf-8"?>
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://www.microsoft.com/GroupPolicy/PolicyDefinitions">

  <displayName>Ochrana přímého přístupu do paměti (DMA) u jádra</displayName>
  <description>Tento soubor obsahuje možnosti konfigurace pro ochranu přímého přístupu do paměti (DMA) u jádra.</description>
  <resources>

    <stringTable>
      <string id="DmaGuard">Ochrana přímého přístupu do paměti (DMA) u jádra</string>
      <string id="DmaGuard_Explain">Zásady konfigurace ochrany přímého přístupu do paměti (DMA) u jádra</string>
      <string id="DmaGuardEnumerationPolicy">Zásada pro výčet externích zařízení nekompatibilních s ochranou přímého přístupu do paměti (DMA) u jádra</string>
      <string id="DmaGuardEnumerationPolicy_Explain">Zásada pro výčet externích zařízení podporujících přímý přístup do paměti (DMA) a nekompatibilních s přemapováním DMA. Tyto zásady se projeví, pouze pokud je ochrana přímého přístupu do paměti jádra povolena a podporována systémem. Poznámka: Tato zásada neplatí pro zařízení 1394, PCMCIA a ExpressCard.</string>
      <string id="DmaGuardEnumerationPolicy_Options_1">Povolit vše</string>
      <string id="DmaGuardEnumerationPolicy_Options_2">Pouze při přihlášení (výchozí)</string>
      <string id="DmaGuardEnumerationPolicy_Options_3">Blokovat vše</string>
    </stringTable>

    <presentationTable>
      <presentation id="DmaGuardEnumerationPolicy">
        <dropdownList refId="DmaGuardEnumerationPolicy_Options" noSort="true" defaultItem="1">Zásada pro výčet</dropdownList>
      </presentation>
    </presentationTable>

  </resources>
</policyDefinitionResources>
