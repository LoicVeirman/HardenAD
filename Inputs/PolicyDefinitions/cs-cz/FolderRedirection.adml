<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Sem zadejte zobrazovaný název.</displayName>
  <description>Sem zadejte popis.</description>
  <resources>
    <stringTable>
      <string id="Fdeploy_Cat">Přesměrování složky</string>
      <string id="LocalizeXPRelativePaths">Při přesměrování složek Nabídka Start a Dokumenty použít lokalizované názvy podsložek</string>
      <string id="LocalizeXPRelativePaths_Help">Toto nastavení zásad umožňuje správci určit, zda má funkce Přesměrování složky při přesměrování složky Nabídka Start nebo starší složky Dokumenty použít pro podsložky Všechny programy, Po spuštění, Hudba, Obrázky a Filmy lokalizované názvy.

Pokud toto nastavení zásad povolíte, systém Windows Vista, Windows 7, Windows 8 a Windows Server 2012 při přesměrování složky Nabídka Start nebo starší složky Dokumenty použije pro tyto podsložky lokalizované názvy.

Pokud toto nastavení zásad zakážete nebo nenakonfigurujete, systém Windows Vista, Windows 7, Windows 8 a Windows Server 2012 při přesměrování složky Nabídka Start nebo starší složky Dokumenty použije pro tyto podsložky standardní anglické názvy.

Poznámka: Tyto zásady jsou pro systémy Windows Vista, Windows 7, Windows 8 a Windows Server 2012 platné pouze při zpracování starších zásad přesměrování, které pro tyto složky již byly nasazeny ve stávajícím lokalizovaném prostředí.</string>

      <string id="DisableFRAdminPinByFolder">Nepovolit automaticky zpřístupnění přesměrovaných složek offline</string>
      <string id="DisableFRAdminPinByFolder_Help">Toto nastavení zásad umožňuje určit, zda jsou jednotlivé přesměrované složky prostředí ve výchozím nastavení přístupné offline.

U složek ovlivněných tímto nastavením musejí uživatelé ručně vybrat soubory, které chtějí zpřístupnit offline.

Pokud toto nastavení zásad zakážete nebo nenakonfigurujete, jsou všechny přesměrované složky prostředí automaticky zpřístupněny offline. Všechny podsložky v přesměrovaných složkách jsou také zpřístupněny offline.

Poznámka: Toto nastavení zásad nebrání automatickému ukládání souborů do mezipaměti, pokud je síťová sdílená složka nakonfigurována na automatické ukládání do mezipaměti, ani v nabídce uživatelského rozhraní neovlivní dostupnost příkazu Vždy přístupné offline.

Poznámka: Konfigurace této zásady u libovolné složky přepíše nakonfigurovanou hodnotu Nepovolit automaticky zpřístupnění všech přesměrovaných složek offline.</string>

      <string id="DisableFRAdminPin">Nepovolit automaticky zpřístupnění všech přesměrovaných složek offline</string>
      <string id="DisableFRAdminPin_Help">Toto nastavení zásad umožňuje určit, zda všechny přesměrované složky prostředí, například Kontakty, Dokumenty, Plocha, Oblíbené položky, Hudba, Obrázky, Filmy, Nabídka Start a Data aplikací\Roaming, mají být ve výchozím nastavení k dispozici offline.

Pokud toto nastavení zásad povolíte, budou uživatelé muset ručně vybrat soubory, které chtějí zpřístupnit offline.  

Pokud toto nastavení zásad zakážete nebo nenakonfigurujete, budou přesměrované složky prostředí automaticky zpřístupněny offline. Všechny podsložky v přesměrovaných složkách budou také zpřístupněny offline.  

Poznámka: Toto nastavení zásad nebrání automatickému ukládání souborů do mezipaměti, pokud je síťová sdílená složka nakonfigurována na automatické ukládání do mezipaměti, ani v nabídce uživatelského rozhraní neovlivní dostupnost příkazu Vždy přístupné offline.  

Poznámka: Toto nastavení zásad nepovolujte, pokud budou uživatelé potřebovat přistupovat k přesměrovaným souborům, když budou síť nebo server s přesměrovanými soubory nedostupné.

Poznámka: Pokud v nastavení zásad Nepovolit automaticky zpřístupnění přesměrovaných složek offline je určen jeden nebo více platných identifikátorů GUID, toto nastavení přepíše nakonfigurovanou hodnotu nastavení zásad Nepovolit automaticky zpřístupnění přesměrovaných složek offline.
      
      </string>  
      <string id="FolderRedirectionEnableCacheRename">Povolit optimalizované přesouvání obsahu v mezipaměti Offline soubory při změně cesty serveru přesměrování složky</string>
      <string id="FolderRedirectionEnableCacheRename_Help">Toto nastavení zásad určuje, zda se obsah přesměrovaných složek při přesměrování složky do nového umístění zkopíruje ze starého umístění do nového, nebo jen přejmenuje v mezipaměti offline souborů.

Pokud toto nastavení zásad povolíte, bude chování následující: Pokud se změní cesta k přesměrované složce z jednoho umístění v síti na jiné a funkce Přesměrování složky je nakonfigurována na přesunutí obsahu do nového umístění, nikoli na jeho zkopírování do nového umístění, obsah uložený v mezipaměti bude přejmenován v místní mezipaměti a nebude zkopírován do nového umístění. Chcete-li toto nastavení zásad použít, je nutné obsah serveru přesunout nebo obnovit do nového umístění v síti způsobem, který zachová stav souborů včetně časových razítek, a teprve potom aktualizovat umístění ve funkci Přesměrování složky.

Pokud toto nastavení zásad zakážete nebo nenakonfigurujete, bude chování následující: Pokud se změní cesta k přesměrované složce a funkce Přesměrování složky je nakonfigurována na přesunutí obsahu do nového umístění, systém Windows zkopíruje obsah místní mezipaměti do nového umístění v síti a pak odstraní obsah ze starého umístění v síti.
      
      </string>
      <string id="PrimaryComputerFr">Přesměrovat složky pouze v primárních počítačích</string>
      <string id="PrimaryComputerFr_Help">Toto nastavení zásad řídí, zda budou složky přesměrovány pouze v primárních počítačích uživatele. Toto nastavení zásad je užitečné ke zvýšení výkonu při přihlašování a ke zlepšení zabezpečení uživatelských dat v počítačích, do kterých uživatel nechce stahovat osobní údaje, jako je počítač v zasedací místnosti nebo ve vzdálené kanceláři.

Pro určení primárních počítačů uživatele musí správce pomocí softwaru pro správu nebo pomocí skriptu přidat atributy primárního počítače k účtu uživatele ve službě AD DS (Active Directory Domain Services). Toto nastavení zásad také vyžaduje, aby fungovala verze schématu služby Active Directory pro systém Windows Server 2012.

Pokud toto nastavení zásad povolíte a uživatel má přesměrované složky, například Dokumenty a Obrázky, jsou složky přesměrovány pouze v primárním počítači uživatele.

Pokud toto nastavení zásad zakážete nebo nenakonfigurujete a uživatel má přesměrované složky, složky budou přesměrovány v každém počítači, ke kterému se uživatel přihlašuje.

Poznámka: Pokud toto nastavení zásad povolíte v Konfiguraci počítače i v Konfiguraci uživatele, bude mít přednost nastavení zásad v Konfiguraci počítače.
      
      </string>
    </stringTable>
    <presentationTable>    
      <presentation id="DisableFRAdminPinByFolder">
        <text>Zaškrtněte políčko u všech přesměrovaných složek prostředí, které by neměly být automaticky zpřístupněny offline.</text>
        <checkBox refId="DisablePinAppDataRoaming">AppData(Roaming)</checkBox>
        <checkBox refId="DisablePinDesktop">Plocha</checkBox>
        <checkBox refId="DisablePinStartMenu">Nabídka Start</checkBox>
        <checkBox refId="DisablePinDocuments">Dokumenty</checkBox>
        <checkBox refId="DisablePinPictures">Obrázky</checkBox>
        <checkBox refId="DisablePinMusic">Hudba</checkBox>
        <checkBox refId="DisablePinVideos">Videa</checkBox>
        <checkBox refId="DisablePinFavorites">Oblíbené položky</checkBox>
        <checkBox refId="DisablePinContacts">Kontakty</checkBox>
        <checkBox refId="DisablePinDownloads">Stažené soubory</checkBox>
        <checkBox refId="DisablePinLinks">Odkazy</checkBox>
        <checkBox refId="DisablePinSearches">Hledání</checkBox>
        <checkBox refId="DisablePinSavedGames">Uložené hry</checkBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
