<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>表示名をここに入力する</displayName>
  <description>説明をここに入力する</description>
  <resources>
    <stringTable>


      <!-- Overall category text -->
      <string id="PcaScenarioCategory">アプリケーションの互換性の診断</string>
      

      <!-- Generic WDI text -->
      <string id="WdiScenarioExecutionPolicyLevelResolution">検出、トラブルシューティング、および解決</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">検出とトラブルシューティングのみ</string>


      <!-- Individual scenario text -->
      <string id="DetectBlockedDriversText">ブロックされたドライバーを通知する</string>
      <string id="DetectDeprecatedComponentFailuresText">使用されていない Windows DLL によって発生したアプリケーション エラーを検出する</string>
      <string id="DetectDeprecatedCOMComponentFailuresText">使用されていない COM オブジェクトによって発生したアプリケーション エラーを検出する</string> 
      <string id="DetectInstallFailuresText">アプリケーション インストールの失敗を検出する</string>
      <string id="DetectUpdateFailuresText">UAC で管理されたインストーラーを起動できないアプリケーションを検出する</string>
      <string id="DetectUndetectedInstallersText">管理者として実行する必要があるアプリケーション インストーラーを検出する</string>
      <string id="DisablePcaUIText">アプリケーションとドライバーの互換性の問題を検出する</string>      
      <!-- End individual scenario text -->

      <!-- Start detailed explain text -->
      <string id="DetectBlockedDriversExplain">この設定は旧バージョンとの互換性のためだけに存在するため、このバージョンの Windows では有効ではありません。プログラム互換性アシスタントを構成するには、[コンピューターの構成]、[管理用テンプレート]、[Windows コンポーネント]、[アプリケーションの互換性] の順に展開して、[プログラム互換性アシスタントを終了する] 設定を使用します。</string>
      <string id="DetectDeprecatedComponentFailuresExplain">この設定は旧バージョンとの互換性のためだけに存在するため、このバージョンの Windows では有効ではありません。プログラム互換性アシスタントを構成するには、[コンピューターの構成]、[管理用テンプレート]、[Windows コンポーネント]、[アプリケーションの互換性] の順に展開して、[プログラム互換性アシスタントを終了する] 設定を使用します。</string>
      <string id="DetectDeprecatedCOMComponentFailuresExplain">この設定は旧バージョンとの互換性のためだけに存在するため、このバージョンの Windows では有効ではありません。プログラム互換性アシスタントを構成するには、[コンピューターの構成]、[管理用テンプレート]、[Windows コンポーネント]、[アプリケーションの互換性] の順に展開して、[プログラム互換性アシスタントを終了する] 設定を使用します。</string>
      <string id="DetectInstallFailuresExplain">この設定は旧バージョンとの互換性のためだけに存在するため、このバージョンの Windows では有効ではありません。プログラム互換性アシスタントを構成するには、[コンピューターの構成]、[管理用テンプレート]、[Windows コンポーネント]、[アプリケーションの互換性] の順に展開して、[プログラム互換性アシスタントを終了する] 設定を使用します。</string>
      <string id="DetectUpdateFailuresExplain">この設定は旧バージョンとの互換性のためだけに存在するため、このバージョンの Windows では有効ではありません。プログラム互換性アシスタントを構成するには、[コンピューターの構成]、[管理用テンプレート]、[Windows コンポーネント]、[アプリケーションの互換性] の順に展開して、[プログラム互換性アシスタントを終了する] 設定を使用します。</string>
      <string id="DetectUndetectedInstallersExplain">この設定は旧バージョンとの互換性のためだけに存在するため、このバージョンの Windows では有効ではありません。プログラム互換性アシスタントを構成するには、[コンピューターの構成]、[管理用テンプレート]、[Windows コンポーネント]、[アプリケーションの互換性] の順に展開して、[プログラム互換性アシスタントを終了する] 設定を使用します。</string>

      <string id="DisablePcaUIExplain">このポリシー設定では、プログラム互換性アシスタント (PCA) が構成され、アプリケーションおよびドライバーの互換性で発生した失敗が診断されます。

このポリシー設定を有効にすると、PCA はアプリケーションのインストールの失敗、アプリケーションの実行時の失敗、および互換性の問題によりブロックされたドライバーを検出するように構成されます。失敗が検出された場合、互換性モードでアプリケーションを実行するか、Microsoft の Web サイトでヘルプをオンラインで参照することができます。

このポリシー設定を無効にすると、アプリケーションとドライバーの互換性の問題は検出されません。

このポリシー設定を構成しない場合、PCA はアプリケーションのインストールの失敗、アプリケーションの実行時の失敗、および互換性の問題によりブロックされたドライバーを検出するように構成されます。

注: [プログラム互換性アシスタントを終了する] ポリシー設定が有効な場合、このポリシー設定は無効になります。PCA を実行するには、診断ポリシー サービス (DPS) およびプログラム互換性アシスタント サービスが実行されている必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインで構成することができます。</string>
      <!-- End detailed explain text -->
      
    </stringTable>

    <presentationTable>
      <presentation id="DetectUpdateFailuresTable">
        <dropdownList refId="DetectUpdateFailuresLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedComponentFailuresLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedCOMComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedCOMComponentFailuresLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
      <presentation id="DetectUndetectedInstallersTable">
        <dropdownList refId="DetectUndetectedInstallersLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
