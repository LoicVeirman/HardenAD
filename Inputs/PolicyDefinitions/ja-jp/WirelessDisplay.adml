<?xml version="1.0" encoding="utf-8"?>
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://www.microsoft.com/GroupPolicy/PolicyDefinitions">
  <displayName>接続</displayName>
  <description>項目を選択すると説明が表示されます</description>
  <resources>
    <stringTable>
      <!-- general -->
      <string id="Connect">接続</string>
      
      <!-- prevent file sync-->
      <string id="AllowProjectionToPC">この PC にプロジェクションを許可しない</string>
      <string id="AllowProjectionToPC_help">このポリシー設定を使用すると、PC への投影をオフにすることができます。

 

このポリシー設定を有効にすると、PC は検出不可になり、ユーザーが手動で接続アプリを起動した場合を除いて投影先になりません。 

 

このポリシー設定を無効にした場合、または構成しなかった場合、PC が検出可能になり、ロック画面の上にのみ投影できます。ユーザーには手動起動を除き、常にオンまたはオフにするオプションがあります。</string>

      <string id="RequirePinForPairing">ペアリングに暗証番号 (PIN) を要求する</string>
      <string id="RequirePinForPairing_help">このポリシー設定では、ペアリングに PIN を要求できます。  

[なし] に設定すると、ペアリングに PIN は必要ありません。

[初回] に設定すると、新しいデバイスのペアリング手順で常に PIN が要求されます。 

[常時] に設定すると、すべてのペアリングに PIN が要求されます。</string>
      <string id="RequirePinForPairing_0">なし</string>
      <string id="RequirePinForPairing_1">初回</string>
      <string id="RequirePinForPairing_2">常時</string>
   </stringTable>
   <presentationTable>
      <presentation id="RequirePinForPairing">
        <dropdownList refId="RequirePinForPairingDropdown" noSort="true" defaultItem="0">次の操作から 1 つ選択してください</dropdownList>
      </presentation>
   </presentationTable>
  </resources>
</policyDefinitionResources>
