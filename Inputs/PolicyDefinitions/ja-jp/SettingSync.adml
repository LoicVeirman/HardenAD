<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2012 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>表示名をここに入力する</displayName>
  <description>説明をここに入力する</description>
  <resources>
    <stringTable>
      <!-- General -->
      <string id="SettingSyncCategory">PC 設定の同期</string>

      <!-- Main policy -->
      <string id="DisableSettingSync">同期しない</string>
      <string id="DisableSettingSync_Help">この PC との相互同期を禁止します。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [PC 設定の同期] スイッチをオフにし、無効にします。

このポリシー設定を有効にすると、[PC 設定の同期] はオフになり、[PC 設定の同期] グループはこの PC 上で同期されなくなります。

[ユーザーが同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。

このポリシー設定を設定しなかった場合、または無効にした場合、[PC 設定の同期] は既定でオンになっており、ユーザーが構成できます。</string>

      <!-- Application Settings -->
      <string id="DisableApplicationSettingSync">アプリの設定を同期しない</string>
      <string id="DisableApplicationSettingSync_Help">[アプリ設定] グループがこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [アプリ設定] グループをオフにし、無効にします。

このポリシー設定を有効にすると、[アプリ設定] グループは同期されなくなります。

[ユーザーがアプリ設定の同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。

このポリシー設定を設定しなかった場合、または無効にした場合、[アプリ設定] グループの同期は既定でオンになり、ユーザーが構成できます。</string>

      <!-- Credentials -->
      <string id="DisableCredentialsSettingSync">パスワードを同期しない</string>
      <string id="DisableCredentialsSettingSync_Help">[パスワード] グループがこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [パスワード] グループをオフにし、無効にします。

このポリシー設定を有効にすると、[パスワード] グループは同期されなくなります。

[ユーザーがパスワードの同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。

このポリシー設定を設定しなかった場合、または無効にした場合は、[パスワード] グループの同期が既定でオンになり、ユーザーが構成できます。</string>

      <!-- Personalization -->
      <string id="DisablePersonalizationSettingSync">個人用設定を同期しない</string>
      <string id="DisablePersonalizationSettingSync_Help">[個人用設定] グループがこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [個人用設定] グループをオフにし、無効にします。 

 このポリシー設定を有効にすると、[個人用設定] グループは同期されなくなります。 

 [ユーザーが個人用設定の同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。 

 このポリシー設定を設定しなかった場合、または無効にした場合は、[個人用設定] グループの同期が既定でオンになり、ユーザーが構成できます。</string>

      <!-- AppSync -->
      <string id="DisableAppSyncSettingSync">アプリを同期しない</string>
      <string id="DisableAppSyncSettingSync_Help">
        
        [AppSync] グループがこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [AppSync] グループをオフにし、無効にします。

このポリシー設定を有効にした場合、[AppSync] グループは同期されなくなります。

[ユーザーがアプリの同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。

このポリシー設定を構成しなかった場合、または無効にした場合、[AppSync] グループの同期が既定でオンになり、ユーザーが構成できます。
      
      </string>

      <!-- Windows -->
      <string id="DisableWindowsSettingSync">その他の Windows 設定を同期しない</string>
      <string id="DisableWindowsSettingSync_Help">[その他の Windows 設定] グループがこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [その他の Windows 設定] グループをオフにし、無効にします。

このポリシー設定を有効にすると、[その他の Windows 設定] グループは同期されなくなります。

[ユーザーがその他の Windows 設定の同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。

このポリシー設定を設定しなかった場合、または無効にした場合は、[その他の Windows 設定] グループの同期は既定でオンになり、ユーザーが構成できます。</string>

      <!-- Desktop Theme -->
      <string id="DisableDesktopThemeSettingSync">デスクトップの個人用設定を同期しない</string>
      <string id="DisableDesktopThemeSettingSync_Help">[デスクトップの個人用設定] グループがこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [デスクトップの個人用設定] グループをオフにし、無効にします。 

 このポリシー設定を有効にすると、[デスクトップの個人用設定] グループは同期されなくなります。 

 [ユーザーがデスクトップの個人用設定の同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。 

 このポリシー設定を設定しなかった場合、または無効にした場合は、[デスクトップの個人用設定] グループの同期は既定でオンになり、ユーザーが構成できます。
      </string>

      <!-- WebBrowser -->
      <string id="DisableWebBrowserSettingSync">ブラウザーの設定を同期しない</string>
      <string id="DisableWebBrowserSettingSync_Help">[ブラウザー] グループがこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [ブラウザー] グループをオフにし、無効にします。 [ブラウザー] グループには、履歴やお気に入りなどの設定および情報が含まれています。

このポリシー設定を有効にすると、履歴やお気に入りなどの情報が含まれている [ブラウザー] グループは同期されなくなります。

[ユーザーがブラウザーの同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。

このポリシー設定を設定しなかった場合、または無効にした場合は、[ブラウザー] グループの同期が既定でオンになり、ユーザーが構成できます。
      
      </string>

      <!-- Paid Networks -->
      <string id="DisableSyncOnPaidNetwork">従量制課金接続で同期しない</string>
      <string id="DisableSyncOnPaidNetwork_Help">従量制課金接続の使用時にこの PC と相互に同期しないようにします。 このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [従量制課金接続で設定を同期する] スイッチをオフにし、無効にします。

このポリシー設定を有効にすると、従量制課金接続での同期はオフになり、この PC で従量制課金接続を使用している場合は同期が行われなくなります。

このポリシー設定を設定しなかった場合、または無効にした場合は、従量制課金接続での同期をユーザーが構成できます。
      
      </string>

      <!-- StartLayout -->
      <string id="DisableStartLayoutSettingSync">スタート設定を同期しない</string>
      <string id="DisableStartLayoutSettingSync_Help">
        
        [スタート画面のレイアウト] グループがこの PC と相互に同期しないようにします。このポリシー設定は、PC 設定の [PC 設定の同期] ページにある [スタート画面のレイアウト] グループをオフにし、無効にします。

        このポリシー設定を有効にすると、[スタート画面のレイアウト] グループは同期されなくなります。

        [ユーザーがスタート画面の同期をオンにできるようにする] オプションを使用すると、同期は既定でオフになりますが、無効にはなりません。

        このポリシー設定を構成しなかった場合、または無効にした場合、[スタート画面のレイアウト] グループの同期は既定でオンになり、ユーザーが構成できます。
      
      </string>

      <!-- Accessibility -->
      <string id="DisableAccessibilitySettingSync">アクセシビリティ設定を同期しないでください</string>
      <string id="DisableAccessibilitySettingSync_Help">
        "accessibility" グループがこの PC と同期しないようにします。これにより、PC 設定の [Windows バックアップ] 設定ページの [アクセシビリティ] グループがオフになり、無効になります。

このポリシー設定を有効にした場合、[アクセシビリティ] グループは同期されません。

[ユーザーがアクセシビリティの同期をオンにできるようにする] オプションを使用すると、同期は既定で無効になりますが、無効になりません。

この設定を設定しなかった場合、または無効にした場合、"accessibility" グループの同期は既定でオンになり、ユーザーが構成できます。
      </string>

      <!-- Language -->
      <string id="DisableLanguageSettingSync">言語設定を同期しない</string>
      <string id="DisableLanguageSettingSync_Help">
        [言語設定] グループがこの PC と同期しないようにします。これにより、PC 設定の [Windows バックアップ] 設定ページの [言語の基本設定] グループがオフになり、無効になります。

このポリシー設定を有効にした場合、[言語設定] グループは同期されません。

[ユーザーが言語設定の同期をオンにできるようにする] オプションを使用すると、同期は既定で無効になりますが、無効になりません。

この設定を設定しなかった場合、または無効にした場合、既定では "言語設定" グループの同期が有効になり、ユーザーが構成できます。
      </string>

    </stringTable>
    <presentationTable>

      <!-- Main policy -->
      <presentation id="DisableSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- Application Settings -->
      <presentation id="DisableApplicationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [アプリ設定] の同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- Credentials -->
      <presentation id="DisableCredentialsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [パスワード] の同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- Personalization -->
      <presentation id="DisablePersonalizationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [個人用設定] の同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- AppSync -->
      <presentation id="DisableAppSyncSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [AppSync] の同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- Windows -->
      <presentation id="DisableWindowsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [その他の Windows 設定] の同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- Desktop Theme -->
      <presentation id="DisableDesktopThemeSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [デスクトップの個人用設定] の同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- WebBrowser -->
      <presentation id="DisableWebBrowserSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [ブラウザー] の同期をオンにできるようにします。</checkBox>
      </presentation>
      
      <!-- StartLayout -->
      <presentation id="DisableStartLayoutSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが [スタート画面のレイアウト] の同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- AccessibilitySync -->
      <presentation id="DisableAccessibilitySettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが "アクセシビリティ" 同期をオンにできるようにします。</checkBox>
      </presentation>

      <!-- LanguageSync -->
      <presentation id="DisableLanguageSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">ユーザーが "言語設定" の同期をオンにすることを許可します。</checkBox>
      </presentation>

    </presentationTable>
  </resources>
</policyDefinitionResources>
