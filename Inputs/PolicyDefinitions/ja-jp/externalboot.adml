<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ポータブル ワークスペース</displayName>
  <description>このファイルには、ポータブル ワークスペースのポリシー設定が含まれています。</description>
  <resources>
    <stringTable>
      <string id="PortableOperatingSystem">ポータブル オペレーティング システム</string>
      <string id="PortableOperatingSystem_Launcher_DisplayName">Windows To Go の既定のスタートアップ オプション</string>
      <string id="PortableOperatingSystem_Launcher_Help">



このポリシー設定では、Windows To Go ワークスペースを含む USB デバイスが接続された場合に PC で Windows To Go を起動するかどうかを制御します。また、ユーザーが [コントロール パネル] の [Windows To Go スタートアップ オプション] を使用して変更を行うことができるかどうかを制御します。

この設定を有効にした場合、USB デバイスを接続したときの Windows To Go の起動が有効になり、ユーザーは [コントロール パネル] の [Windows To Go スタートアップ オプション] を使用して変更を行うことができなくなります。

この設定を無効にした場合、USB デバイスを接続したときの Windows To Go の起動は無効になります。ただし、ユーザーが BIOS などのブート順序の構成でこのオプションを手動で構成した場合は除きます。

この設定を構成しなかった場合、Administrators グループのメンバーであるユーザーは、[コントロール パネル] の [Windows To Go スタートアップ オプション] を使用して変更を行うことができます。</string>
      <string id="PortableOperatingSystem_Hibernate_DisplayName">Windows To Go ワークスペースから起動したときに休止状態 (S4) を許可する</string>
      <string id="PortableOperatingSystem_Hibernate_Help">



Windows To Go ワークスペースから起動したときに、PC で休止状態 (S4) を使用できるかどうかを指定します。

この設定を有効にした場合、Windows To Go ワークスペースから起動したときに PC を休止状態にすることができます。

この設定を無効にした場合、または構成しなかった場合、Windows To Go ワークスペースから起動したときに PC を休止状態にすることはできません。</string>
      <string id="PortableOperatingSystem_Sleep_DisplayName">Windows To Go ワークスペースから起動したときにスタンバイ状態 (S1-S3) を禁止する</string>
      <string id="PortableOperatingSystem_Sleep_Help">



Windows To Go ワークスペースから起動するときに、PC でスタンバイ状態 (S1 から S3) を使用できるかどうかを指定します。

この設定を有効にした場合、Windows To Go ワークスペースから起動したときに、スタンバイ状態を使用して PC をスリープ状態にすることはできません。

この設定を無効にした場合、または構成しなかった場合、Windows To Go ワークスペースから起動したときに、スタンバイ状態を使用して PC をスリープ状態にすることができます。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
