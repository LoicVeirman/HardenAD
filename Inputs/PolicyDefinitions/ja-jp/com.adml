<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>表示名をここに入力する</displayName>
  <description>説明をここに入力する</description>
  <resources>
    <stringTable>
      <string id="AppMgmt_COM_SearchForCLSID">COM コンポーネントが存在しない場合ダウンロードする</string>
      <string id="AppMgmt_COM_SearchForCLSID_Help">このポリシー設定では、Active Directory を検索して、プログラムで必要だが存在しないコンポーネント オブジェクト モデル (COM) コンポーネントを見つけるよう、システムに指示します。

MMC スナップインなど、多くの Windows プログラムが、COM コンポーネントによって提供されるインターフェイスを使用します。これらのプログラムは、必要なコンポーネントを Windows が内部登録していない場合、その機能のすべてを実行することはできません。

このポリシー設定が有効で、コンポーネントの登録が存在しない場合、システムは Active Directory 内を検索し、見つかった場合、それをダウンロードします。こうした検索により、一部のプログラムでは、起動または動作が遅くなる場合があります。

このポリシー設定を無効にした場合、または構成しなかった場合、プログラムは登録しなくても続行します。その結果、プログラムは、一部の機能を実行しなかったり、停止する場合があります。

この設定は [コンピューターの構成] および [ユーザーの構成] フォルダーにあります。両方で設定が構成されている場合、[コンピューターの構成] での設定が [ユーザーの構成] での設定より優先されます。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
