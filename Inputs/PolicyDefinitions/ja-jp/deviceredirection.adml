<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>表示名をここに入力する</displayName>
  <description>説明をここに入力する</description>
  <resources>
    <stringTable>
      <string id="DeviceRedirect_Category">デバイス リダイレクト</string>
      <string id="DeviceRedirect_Restrictions_Category">デバイス リダイレクトの制限</string>
      <string id="DeviceRedirect_Restrictions_Help">これらのポリシー設定では、USB デバイスの代替ドライバーを読み込めるかどうかを指定できます。</string>
      <string id="USBRedirect_Prevent">USB デバイスのリダイレクトを禁止する</string>
      <string id="USBRedirect_Prevent_Help">このポリシー設定では、USB デバイスをリダイレクトできないようにします。

この設定を有効にすると、USB デバイスの代替ドライバーを読み込めなくなります。

この設定を無効にした場合、または構成しなかった場合は、USB デバイスの代替ドライバーを読み込めます。
</string>
      <string id="DeviceRedirect_IDS_Prevent">これらのデバイス ID と一致するデバイスのリダイレクトを禁止する</string>
      <string id="DeviceRedirect_IDS_Prevent_Help">このポリシー設定では、特定の USB デバイスをリダイレクトできないようにします。

この設定を有効にすると、USB デバイスの代替ドライバーを読み込めなくなります。

この設定を無効にした場合、または構成しなかった場合は、USB デバイスの代替ドライバーを読み込めます。
      </string>
    </stringTable>
    <presentationTable>
      <presentation id="DeviceRedirect_IDS_Prevent">
        <listBox refId="Redirect_IDS_Deny_List">これらのデバイス ID と一致するデバイスのリダイレクトを禁止する:</listBox>
        <text>デバイスの一覧を作成するには、[表示] をクリックして USB デバイスのハードウェア ID を指定し、[OK] をクリックします。</text>
        <text>たとえば、VID が 058F、PID が 6387、リビジョンが 0142 のデバイスを指定するには、"VID_058F&amp;&amp;PID_6387&amp;&amp;REV_0142" という形式で値を入力します。デバイス マネージャーを使用して、USB デバイスの VID、PID、およびリビジョンを確認できます。[詳細] タブで [ハードウェア ID] を選択すると、コンピューターに接続されているすべての USB デバイスの ID が一覧表示されます。</text>
        <text>または、クラスとプロトコルの情報を使用して USB デバイスをブロックすることもできます。クラスが 08、サブクラスが 06、プロトコルが 50 のデバイスを指定するには、"Class_08&amp;&amp;SubClass_06&amp;&amp;Prot_50" という形式で値を入力します。この情報もデバイス マネージャーで確認できます。[詳細] タブで [互換性 ID] を選択すると、コンピューターに接続されているすべての USB デバイスのクラスとプロトコルの値が一覧表示されます。</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
