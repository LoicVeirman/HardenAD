<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">Non avviare automaticamente Windows Messenger all'accesso</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">Questa impostazione dei criteri impedisce l'esecuzione automatica di Windows Messenger all'accesso. 

Se si abilita questa impostazione dei criteri, Windows Messenger non verrà caricato automaticamente quando un utente esegue l'accesso.

Se si disabilita o non si configura questa impostazione dei criteri, Windows Messenger verrà caricato automaticamente all'accesso.

Nota: questa impostazione dei criteri impedisce semplicemente l'esecuzione di Windows Messenger all'accesso. Se l'utente richiama e utilizza Windows Messenger dopo aver eseguito l'accesso, Windows Messenger verrà caricato.

L'utente può inoltre configurare questo comportamento nella scheda Preferenze visualizzata attraverso il menu Strumenti dell'interfaccia di Windows Messenger.

Nota: per evitare che gli utenti utilizzino Windows Messenger, abilitare l'impostazione dei criteri "Non consentire l'esecuzione di Windows Messenger".

Nota: questa impostazione dei criteri è disponibile sia in Configurazione computer che in Configurazione utente. Se entrambe le impostazioni sono presenti, la versione dell'impostazione dei criteri in Configurazione computer ha la priorità.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">Non avviare automaticamente Windows Messenger all'accesso</string>
      <string id="WinMSG_NoWindowsMsg_Comp">Non consentire l'esecuzione di Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">Questa impostazione dei criteri consente di impedire l'esecuzione di Windows Messenger.

Se si abilita questa impostazione dei criteri, Windows Messenger non verrà eseguito.

Se si disabilita o non si configura questa impostazione dei criteri, sarà possibile utilizzare Windows Messenger.

Nota: se si abilita questa impostazione dei criteri, Windows Messenger non potrà essere utilizzato nemmeno in Assistenza remota.

Nota: questa impostazione dei criteri è disponibile sia in Configurazione computer che in Configurazione utente. Se entrambe le impostazioni sono presenti, la versione dell'impostazione dei criteri in Configurazione computer ha la priorità.</string>
      <string id="WinMSG_NoWindowsMsg_User">Non consentire l'esecuzione di Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
