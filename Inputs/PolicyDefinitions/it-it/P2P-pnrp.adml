<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="P2P_Disabled">Disattiva Servizi di rete peer-to-peer Microsoft</string>
      <string id="P2P_Disabled_Explain">Questa impostazione consente di disabilitare completamente Servizi di rete peer-to-peer Microsoft e comporta l'interruzione del funzionamento di tutte le applicazioni che dipendono da tali servizi.

I protocolli peer-to-peer consentono il funzionamento di applicazioni di RTC, collaborazione, distribuzione di contenuti ed elaborazione distribuita.

Se si abilita questa impostazione, i protocolli peer-to-peer verranno disabilitati.

Se si disabilita o non si configura questa impostazione, i protocolli peer-to-peer verranno abilitati.</string>
      <string id="P2P_Name">Servizi di rete peer-to-peer Microsoft</string>
      <string id="PNRP_DisableMulticastBootstrap">Disattiva bootstrap multicast</string>
      <string id="PNRP_DisableMulticastBootstrapGlobal_Explain">Questa impostazione consente di impedire al protocollo PNRP di visualizzare annunci relativi al computer o cercare altri computer nella subnet locale dell'area globale.

Il protocollo PNRP (Peer Name Resolution Protocol) consente la risoluzione distribuita dei nomi in indirizzi e numeri porta IPV6. Una delle modalità di avvio di PNRP è tramite multicast nella stessa subnet. In altre parole, PNRP pubblica se stesso nella subnet locale in modo che altri computer possano trovarlo in caso di necessità.

Se si abilita questa impostazione, PNRP non utilizzerà il multicast per il bootstrap. Se si specifica questa chiave di registro, verrà interrotto il funzionamento di scenari in cui non è disponibile un server di inizializzazione per il bootstrap, ad esempio nel caso di reti ad hoc.

Se si disabilita questa impostazione, PNRP utilizzerà il multicast per il bootstrap nella stessa subnet.

Se non si configura questa impostazione, il protocollo tornerà a utilizzare una chiave del Registro di sistema pubblica per determinare se rendere pubblico il computer oppure utilizzare il multicast per cercare altri computer nella subnet locale.

Per il bootstrap viene utilizzato il protocollo di multicast SSDP (Simple Service Discovery Protocol). Perché questo criterio abbia effetto, il servizio SSDP deve essere abilitato, come previsto dall'impostazione predefinita.</string>
      <string id="PNRP_Disabled">Disattiva creazione area PNRP</string> 
      <string id="PNRP_SearchOnly">Imposta area PNRP come di sola risoluzione</string> 
      <string id="PNRP_DisabledGlobal_Explain">Questa impostazione consente di abilitare o disabilitare la creazione di un'area PNRP.

PNRP è un protocollo per la risoluzione distribuita dei nomi che consente agli host Internet di pubblicare nomi peer con un indirizzo IP versione 6 (IPv6) corrispondente. Altri host potranno quindi risolvere il nome, recuperare l'indirizzo corrispondente e stabilire connessioni peer-to-peer (P2P). L'area PNRP è un gruppo di nodi PNRP connessi in cui sussistono connessioni tra peer in modo tale che un nodo dell'area PNRP possa risolvere i nomi pubblicati dagli altri nodi.

Il protocollo PNRP crea un'area globale se il computer ha un indirizzo IPv6 globale, mentre crea un'area locale al sito se il computer ha un indirizzo locale rispetto al sito.

Se si abilita questa impostazione, PNRP non crea un'area e le applicazioni non possono pertanto utilizzarla per pubblicare o risolvere nomi, indipendentemente dal fatto che il computer abbia o meno un indirizzo IPv6 corrispondente all'ambito dell'area.

Se si disabilita o non si configura questa impostazione, tutte le aree PNRP vengono abilitate per impostazione predefinita e PNRP crea un'area se il computer ha un indirizzo IPv6 compatibile con l'ambito dell'area.
</string>
      <string id="PNRP_SearchOnlyGlobal_Explain">Questa impostazione consente di limitare un nodo alla risoluzione dei nomi in un'area Peer Name Resolution Protocol (PNRP) specifica, senza pubblicarli.

Questo comportamento impone ai computer di operare come client in scenari peer-to-peer (P2P). Un computer client, ad esempio, potrà rilevare altri computer per iniziare sessioni chat senza che venga a sua volta rilevato da tali computer.

Se si abilita questa impostazione, il computer non sarà in grado di registrare nomi PNRP o di aiutare gli altri computer a eseguire ricerche PNRP.

Se si disabilita o non si configura questa impostazione, il computer potrà pubblicare nomi PNRP e aiutare gli altri computer a eseguire ricerche PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapLinkLocal_Explain">Questa impostazione consente di impedire al protocollo PNRP di visualizzare annunci relativi al computer o cercare altri computer nella subnet locale dell'area locale al collegamento.

Il protocollo PNRP (Peer Name Resolution Protocol) consente la risoluzione distribuita dei nomi in indirizzi e numeri porta IPV6. Una delle modalità di avvio di PNRP è tramite multicast nella stessa subnet. In altre parole, PNRP pubblica se stesso nella subnet locale in modo che altri computer possano trovarlo in caso di necessità.

Se si abilita questa impostazione, PNRP non utilizzerà il multicast per il bootstrap. Se si specifica questa chiave di registro, verrà interrotto il funzionamento di scenari in cui non è disponibile un server di inizializzazione per il bootstrap, ad esempio nel caso di reti ad hoc.

Se si disabilita questa impostazione, PNRP utilizzerà il multicast per il bootstrap nella stessa subnet.

Se non si configura questa impostazione, il protocollo tornerà a utilizzare una chiave del Registro di sistema pubblica per determinare se rendere pubblico il computer oppure utilizzare il multicast per cercare altri computer nella subnet locale.

Per il bootstrap viene utilizzato il protocollo di multicast SSDP (Simple Service Discovery Protocol). Perché questo criterio abbia effetto, il servizio SSDP deve essere abilitato, come previsto dall'impostazione predefinita.</string>
      <string id="PNRP_DisabledLinkLocal_Explain">Questa impostazione consente di abilitare o disabilitare la creazione di un'area PNRP.

PNRP è un protocollo per la risoluzione distribuita dei nomi che consente agli host Internet di pubblicare nomi peer con un indirizzo IP versione 6 (IPv6) corrispondente. Altri host potranno quindi risolvere il nome, recuperare l'indirizzo corrispondente e stabilire connessioni peer-to-peer (P2P). L'area PNRP è un gruppo di nodi PNRP connessi in cui sussistono connessioni tra peer in modo tale che un nodo dell'area PNRP possa risolvere i nomi pubblicati dagli altri nodi.

Il protocollo PNRP crea un'area globale se il computer ha un indirizzo IPv6 globale, mentre crea un'area locale al sito se il computer ha un indirizzo locale rispetto al sito.

Se si abilita questa impostazione, PNRP non crea un'area e le applicazioni non possono pertanto utilizzarla per pubblicare o risolvere nomi, indipendentemente dal fatto che il computer abbia o meno un indirizzo IPv6 corrispondente all'ambito dell'area.

Se si disabilita o non si configura questa impostazione, tutte le aree PNRP vengono abilitate per impostazione predefinita e PNRP crea un'area se il computer ha un indirizzo IPv6 compatibile con l'ambito dell'area.
</string>
      <string id="PNRP_SearchOnlyLinkLocal_Explain">Questa impostazione consente di limitare un nodo alla risoluzione dei nomi in un'area Peer Name Resolution Protocol (PNRP) specifica, senza pubblicarli.

Questo comportamento impone ai computer di operare come client in scenari peer-to-peer (P2P). Un computer client, ad esempio, potrà rilevare altri computer per iniziare sessioni chat senza che venga a sua volta rilevato da tali computer.

Se si abilita questa impostazione, il computer non sarà in grado di registrare nomi PNRP o di aiutare gli altri computer a eseguire ricerche PNRP.

Se si disabilita o non si configura questa impostazione, il computer potrà pubblicare nomi PNRP e aiutare gli altri computer a eseguire ricerche PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapSiteLocal_Explain">Questa impostazione consente di impedire al protocollo PNRP di visualizzare annunci relativi al computer o cercare altri computer nella subnet locale dell'area locale al sito.

Il protocollo PNRP (Peer Name Resolution Protocol) consente la risoluzione distribuita dei nomi in indirizzi e numeri porta IPV6. Una delle modalità di avvio di PNRP è tramite multicast nella stessa subnet. In altre parole, PNRP pubblica se stesso nella subnet locale in modo che altri computer possano trovarlo in caso di necessità.

Se si abilita questa impostazione, PNRP non utilizzerà il multicast per il bootstrap. Se si specifica questa chiave di registro, verrà interrotto il funzionamento di scenari in cui non è disponibile un server di inizializzazione per il bootstrap, ad esempio nel caso di reti ad hoc.

Se si disabilita questa impostazione, PNRP utilizzerà il multicast per il bootstrap nella stessa subnet.

Se non si configura questa impostazione, il protocollo tornerà a utilizzare una chiave del Registro di sistema pubblica per determinare se rendere pubblico il computer oppure utilizzare il multicast per cercare altri computer nella subnet locale.

Per il bootstrap viene utilizzato il protocollo di multicast SSDP (Simple Service Discovery Protocol). Perché questo criterio abbia effetto, il servizio SSDP deve essere abilitato, come previsto dall'impostazione predefinita.</string>
      <string id="PNRP_DisabledSiteLocal_Explain">Questa impostazione consente di abilitare o disabilitare la creazione di un'area PNRP.

PNRP è un protocollo per la risoluzione distribuita dei nomi che consente agli host Internet di pubblicare nomi peer con un indirizzo IP versione 6 (IPv6) corrispondente. Altri host potranno quindi risolvere il nome, recuperare l'indirizzo corrispondente e stabilire connessioni peer-to-peer (P2P). L'area PNRP è un gruppo di nodi PNRP connessi in cui sussistono connessioni tra peer in modo tale che un nodo dell'area PNRP possa risolvere i nomi pubblicati dagli altri nodi.

Il protocollo PNRP crea un'area globale se il computer ha un indirizzo IPv6 globale, mentre crea un'area locale al sito se il computer ha un indirizzo locale rispetto al sito.

Se si abilita questa impostazione, PNRP non crea un'area e le applicazioni non possono pertanto utilizzarla per pubblicare o risolvere nomi, indipendentemente dal fatto che il computer abbia o meno un indirizzo IPv6 corrispondente all'ambito dell'area.

Se si disabilita o non si configura questa impostazione, tutte le aree PNRP vengono abilitate per impostazione predefinita e PNRP crea un'area se il computer ha un indirizzo IPv6 compatibile con l'ambito dell'area.
</string>
      <string id="PNRP_SearchOnlySiteLocal_Explain">Questa impostazione consente di limitare un nodo alla risoluzione dei nomi in un'area Peer Name Resolution Protocol (PNRP) specifica, senza pubblicarli.

Questo comportamento impone ai computer di operare come client in scenari peer-to-peer (P2P). Un computer client, ad esempio, potrà rilevare altri computer per iniziare sessioni chat senza che venga a sua volta rilevato da tali computer.

Se si abilita questa impostazione, il computer non sarà in grado di registrare nomi PNRP o di aiutare gli altri computer a eseguire ricerche PNRP.

Se si disabilita o non si configura questa impostazione, il computer potrà pubblicare nomi PNRP e aiutare gli altri computer a eseguire ricerche PNRP.
</string> 
      <string id="PNRP_Global">Aree globali</string>
      <string id="PNRP_LinkLocal">Aree locali al collegamento</string>
      <string id="PNRP_Name">Peer Name Resolution Protocol</string>
      <string id="PNRP_SeedServer">Imposta il server di inizializzazione</string>
      <string id="PNRP_SeedServerGlobal_Explain">Questa impostazione consente di impostare su un nodo specificato dell'organizzazione il server di inizializzazione per l'area globale. Il protocollo PNRP (Peer Name Resolution Protocol) consente la risoluzione distribuita dei nomi in indirizzi e numeri porta IPv6. In alcuni casi, per il bootstrap del protocollo è necessario un server di inizializzazione.

È possibile utilizzare come server di inizializzazione qualsiasi nodo che esegue il protocollo peer-to-peer. Non è necessaria nessuna configurazione per il server di inizializzazione.

Questa impostazione garantisce una maggior flessibilità perché consente agli utenti di utilizzare le applicazioni peer-to-peer anche a casa. È possibile utilizzare questa impostazione per controllare il server di inizializzazione utilizzato nell'organizzazione in quattro modi diversi elencati di seguito.

1. Per utilizzare il server di inizializzazione noto e globale solo su Internet, abilitare questa impostazione, lasciare vuoto l'elenco dei server di inizializzazione e deselezionata la casella di controllo.

2. Per utilizzare solo un server di inizializzazione aziendale, abilitare l'impostazione, immettere il nome di dominio completo o l'indirizzo IPv6 del server di inizializzazione aziendale e selezionare la casella di controllo. L'utilizzo del solo server di inizializzazione aziendale impedirà agli utenti mobili di utilizzare le applicazioni peer-to-peer da casa.

3. Per utilizzare sia il server di inizializzazione aziendale che quello globale, abilitare l'impostazione, immettere il nome di dominio completo o l'indirizzo IPv6 del server di inizializzazione aziendale e non selezionare la casella di controllo. Questa impostazione consentirà agli utenti mobili di utilizzare le applicazioni peer-to-peer sia al lavoro che a casa.

4. Per non utilizzare nessun server di inizializzazione, abilitare l'impostazione, non immettere un nome per il server di inizializzazione e selezionare la casella di controllo.

Se si disabilita o non si configura questa impostazione, il protocollo tornerà a utilizzare una chiave del Registro di sistema pubblica per determinare il server di inizializzazione da cui eseguire il bootstrap.</string>
      <string id="PNRP_SeedServerLinkLocal_Explain">Questa impostazione consente di impostare su un nodo specificato dell'organizzazione il server di inizializzazione per l'area locale al collegamento.

Il protocollo PNRP (Peer Name Resolution Protocol) consente la risoluzione distribuita dei nomi in indirizzi e numeri porta IPV6. In alcuni casi, per il bootstrap del protocollo è necessario un server di inizializzazione. Questa impostazione consente di impostare il server di inizializzazione su un nodo specificato di un'organizzazione.

Per utilizzare questa impostazione, fare clic su Abilita e quindi immettere nel campo disponibile un indirizzi IPV6 separati da punti e virgola. Se si abilita questa impostazione e non si immette nessun indirizzo, il server di inizializzazione non verrà utilizzato.

Se non si configura questa impostazione, il protocollo tornerà a utilizzare una chiave del Registro di sistema pubblica per determinare il server di inizializzazione da cui eseguire il bootstrap.</string>
      <string id="PNRP_SeedServerSiteLocal_Explain">Questa impostazione consente di impostare su un nodo specificato dell'organizzazione il server di inizializzazione per l'area locale al sito.

Il protocollo PNRP (Peer Name Resolution Protocol) consente la risoluzione distribuita dei nomi in indirizzi e numeri porta IPV6. In alcuni casi, per il bootstrap del protocollo è necessario un server di inizializzazione. Questa impostazione consente di impostare il server di inizializzazione su un nodo specificato di un'organizzazione.

Per utilizzare questa impostazione, fare clic su Abilita e quindi immettere nel campo disponibile un elenco di nomi DNS o indirizzi IPV6 separati da punti e virgola. Se si abilita questa impostazione e non si immette nessun indirizzo, il server di inizializzazione non verrà utilizzato.

Se non si configura questa impostazione, il protocollo tornerà a utilizzare una chiave del Registro di sistema pubblica per determinare il server di inizializzazione da cui eseguire il bootstrap.</string>
      <string id="PNRP_SiteLocal">Aree locali al sito</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement">Disabilita convalida complessità password per i Gruppi peer</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement_Explain">Per impostazione predefinita, quando si crea un Gruppo peer che consente l'autenticazione password (o se si modifica la password di questo gruppo), i Gruppi peer controllano che la password rispetti i requisiti di complessità della password per il sistema locale. Non sarà quindi consentito a un Gruppo peer l'utilizzo di password con una complessità inferiore a quella consentita per una password di accesso.

Questa impostazione controlla le modalità di convalida. Se impostato su 1, la convalida non verrà eseguita e sarà consentita qualsiasi password. Se impostato su 0, la convalida verrà eseguita.
      </string>
    </stringTable>
    <presentationTable>
      <presentation id="PNRP_SeedServer_1">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Server di inizializzazione</label>
        </textBox>
        <checkBox refId="PNRP_Dont_Include_Microsoft_SeedServer">Non usare il server di inizializzazione PNRP globale Microsoft</checkBox>
      </presentation>
      <presentation id="PNRP_SeedServer_2">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Server di inizializzazione</label>
        </textBox>
      </presentation>
      <presentation id="PNRP_SeedServer_3">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Server di inizializzazione</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
