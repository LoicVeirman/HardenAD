<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="DisableThumbnails">Disattiva la visualizzazione delle anteprime visualizzando solo le icone.</string>
      <string id="DisableThumbnails_Help">Questa impostazione dei criteri consente di configurare la visualizzazione delle immagini di anteprima o delle icone in Esplora file nel computer locale.

Per impostazione predefinita, in Esplora file vengono visualizzate le immagini di anteprima. 

Se si abilita questa impostazione dei criteri, in Esplora file verranno visualizzate solo le icone e non le immagini di anteprima.

Se si disabilita o non si configura questa impostazione dei criteri, in Esplora file verranno visualizzate solo le immagini di anteprima.</string>
      <string id="DisableThumbnailsOnNetworkFolders">Disattiva la visualizzazione delle anteprime visualizzando solo le icone nelle cartelle di rete</string>
      <string id="DisableThumbnailsOnNetworkFolders_Help">Questa impostazione dei criteri consente di configurare la visualizzazione delle immagini di anteprima o delle icone nelle cartelle di rete in Esplora file.

Per impostazione predefinita, in Esplora file vengono visualizzate le immagini di anteprima nelle cartelle di rete.

Se si abilita questa impostazione dei criteri, in Esplora file verranno visualizzate solo le icone e non le immagini di anteprima nelle cartelle di rete.

Se si disabilita o non si configura questa impostazione dei criteri, in Esplora file verranno visualizzate solo le immagini di anteprima nelle cartelle di rete.</string>
      <string id="DisableThumbsDBOnNetworkFolders">Disattiva memorizzazione nella cache di anteprime in file thumbs.db nascosti</string>
      <string id="DisableThumbsDBOnNetworkFolders_Help">Disabilita la memorizzazione nella cache delle anteprime nei file thumbs.db nascosti.

Questa impostazione dei criteri consente di configurare Esplora file per la memorizzazione nella cache delle anteprime di elementi presenti nelle cartelle di rete in file thumbs.db nascosti.

Se si abilita questa impostazione dei criteri, Esplora file non sarà in grado di creare, leggere o scrivere nei file thumbs.db.

Se si disabilita o non si configura questa impostazione dei criteri, Esplora file sarà in grado di creare, leggere e scrivere nei file thumbs.db.

</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
