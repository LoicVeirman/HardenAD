<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="BootScenarioCategory">Diagnostica prestazioni avvio Windows</string>
      <string id="BootScenarioExecutionPolicyExplain">Determina il livello di esecuzione di Diagnostica prestazioni avvio Windows.

Se si abilita questa impostazione, è necessario selezionare un livello di esecuzione dal menu a discesa. Se si seleziona l'opzione Solo rilevamento e identificazione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni all'avvio di Windows e tenterà di determinarne le principali cause. Le cause principali verranno registrate nel registro eventi al momento del rilevamento, tuttavia non verrà intrapresa nessuna azione correttiva. Se si seleziona l'opzione Rilevamento, identificazione e risoluzione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni all'avvio di Windows e indicherà all'utente che è disponibile una soluzione assistita.

Se si disabilita questa impostazione, Windows non sarà in grado di rilevare, individuare o risolvere tutti i problemi di prestazioni all'avvio di Windows gestiti dal Servizio criteri di diagnostica.

Se non si configura questa impostazione, per impostazione predefinita, il Servizio criteri di diagnostica abiliterà la risoluzione dei problemi di prestazioni all'avvio di Windows.

Questa impostazione avrà effetto solo se non sono configurati i criteri di esecuzione dello scenario a livello di diagnostica.

Perché questi criteri abbiano effetto, non è necessario riavviare il sistema o il servizio. Le modifiche diventano effettive immediatamente.

Questa impostazione avrà effetto solo se il Servizio criteri di diagnostica è in esecuzione. Se il servizio viene arrestato o disabilitato, gli scenari di diagnostica non verranno eseguiti. Il Servizio criteri di diagnostica può essere configurato con lo snap-in Servizi di Microsoft Management Console.</string>
      <string id="ResumeScenarioCategory">Diagnostica prestazioni standby o riattivazione Windows</string>
      <string id="ResumeScenarioExecutionPolicyExplain">Determina il livello di esecuzione di Diagnostica prestazioni standby o riabilitazione Windows.

Se si abilita questa impostazione, è necessario selezionare un livello di esecuzione dal menu a discesa. Se si seleziona l'opzione Solo rilevamento e identificazione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni standby o riabilitazione di Windows e tenterà di determinarne le principali cause. Le cause principali verranno registrate nel registro eventi al momento del rilevamento, tuttavia non verrà intrapresa nessuna azione correttiva. Se si seleziona l'opzione Rilevamento, identificazione e risoluzione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni standby o riabilitazione di Windows e indicherà all'utente che è disponibile una soluzione assistita.

Se si disabilita questa impostazione, Windows non sarà in grado di rilevare, individuare o risolvere tutti i problemi di prestazioni standby o riabilitazione di Windows gestiti dal Servizio criteri di diagnostica.

Se non si configura questa impostazione, per impostazione predefinita, il Servizio criteri di diagnostica abiliterà la risoluzione dei problemi di prestazioni standby o riabilitazione di Windows.

Questa impostazione avrà effetto solo se non sono configurati i criteri di esecuzione dello scenario a livello di diagnostica.

Perché questi criteri abbiano effetto, non è necessario riavviare il sistema o il servizio. Le modifiche diventano effettive immediatamente.

Questa impostazione avrà effetto solo se il Servizio criteri di diagnostica è in esecuzione. Se il servizio viene arrestato o disabilitato, gli scenari di diagnostica non verranno eseguiti. Il Servizio criteri di diagnostica può essere configurato con lo snap-in Servizi di Microsoft Management Console.</string>
      <string id="ShellScenarioCategory">Diagnostica prestazioni velocità di risposta sistema Windows</string>
      <string id="ShellScenarioExecutionPolicyExplain">Determina il livello di esecuzione di Diagnostica prestazioni velocità di risposta sistema Windows.

Se si abilita questa impostazione, è necessario selezionare un livello di esecuzione dal menu a discesa. Se si seleziona l'opzione Solo rilevamento e identificazione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni velocità di risposta sistema Windows e tenterà di determinarne le principali cause. Le cause principali verranno registrate nel registro eventi al momento del rilevamento, tuttavia non verrà intrapresa nessuna azione correttiva. Se si seleziona l'opzione Rilevamento, identificazione e risoluzione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni velocità di risposta sistema Windows e indicherà all'utente che è disponibile una soluzione assistita.

Se si disabilita questa impostazione, Windows non sarà in grado di rilevare, individuare o risolvere tutti i problemi di prestazioni velocità di risposta sistema Windows gestiti dal Servizio criteri di diagnostica.

Se non si configura questa impostazione, per impostazione predefinita, il Servizio criteri di diagnostica abiliterà Velocità di risposta shell di Windows.

Questa impostazione avrà effetto solo se non sono configurati i criteri di esecuzione dello scenario a livello di diagnostica.

Perché questi criteri abbiano effetto, non è necessario riavviare il sistema o il servizio. Le modifiche diventano effettive immediatamente.

Questa impostazione avrà effetto solo se il Servizio criteri di diagnostica è in esecuzione. Se il servizio viene arrestato o disabilitato, gli scenari di diagnostica non verranno eseguiti. Il Servizio criteri di diagnostica può essere configurato con lo snap-in Servizi di Microsoft Management Console.</string>
      <string id="ShutdownScenarioCategory">Diagnostica prestazioni arresto Windows</string>
      <string id="ShutdownScenarioExecutionPolicyExplain">Determina il livello di esecuzione di Diagnostica prestazioni arresto Windows.

Se si abilita questa impostazione, è necessario selezionare un livello di esecuzione dal menu a discesa. Se si seleziona l'opzione Solo rilevamento e identificazione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni all'arresto di Windows e tenterà di determinarne le principali cause. Le cause principali verranno registrate nel registro eventi al momento del rilevamento, tuttavia non verrà intrapresa nessuna azione correttiva. Se si seleziona l'opzione Rilevamento, identificazione e risoluzione dei problemi, il Servizio criteri di diagnostica rileverà i problemi di prestazioni all'arresto di Windows e indicherà all'utente che è disponibile una soluzione assistita.

Se si disabilita questa impostazione, Windows non sarà in grado di rilevare, individuare o risolvere tutti i problemi di prestazioni all'arresto di Windows gestiti dal Servizio criteri di diagnostica.

Se non si configura questa impostazione, per impostazione predefinita, il Servizio criteri di diagnostica abiliterà la risoluzione dei problemi di prestazioni all'arresto di Windows.

Questa impostazione avrà effetto solo se non sono configurati i criteri di esecuzione dello scenario a livello di diagnostica.

Perché questi criteri abbiano effetto, non è necessario riavviare il sistema o il servizio. Le modifiche diventano effettive immediatamente.

Questa impostazione avrà effetto solo se il Servizio criteri di diagnostica è in esecuzione. Se il servizio viene arrestato o disabilitato, gli scenari di diagnostica non verranno eseguiti. Il Servizio criteri di diagnostica può essere configurato con lo snap-in Servizi di Microsoft Management Console.</string>
      <string id="WdiScenarioExecutionPolicy">Configura livello di esecuzione scenari</string>
      <string id="WdiScenarioExecutionPolicyLevelResolution">Rilevamento, identificazione e risoluzione dei problemi</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Solo rilevamento e identificazione dei problemi</string>
    </stringTable>
    <presentationTable>
      <presentation id="WdiScenarioExecutionPolicy_1">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Livello di esecuzione dello scenario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_2">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Livello di esecuzione dello scenario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_3">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Livello di esecuzione dello scenario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_4">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Livello di esecuzione dello scenario</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
