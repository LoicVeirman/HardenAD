<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="UseOEMBackground">Usa sempre sfondo di accesso personalizzato</string> 
      <string id="UseOEMBackground_Help">Questa impostazione dei criteri ignora lo sfondo di accesso di Windows.

Questa impostazione dei criteri può essere utilizzata per fare in modo che in Windows venga data preferenza a uno sfondo di accesso personalizzato. 

Se si abilita questa impostazione dei criteri, per la schermata di accesso verrà sempre tentato il caricamento di uno sfondo personalizzato anziché dello sfondo di accesso di Windows. 

Se si disabilita o non si configura questa impostazione dei criteri, verrà utilizzato lo sfondo di accesso di Windows predefinito o lo sfondo personalizzato.</string> 
      <string id="DisableExplorerRunLegacy">Non elaborare l'elenco di esecuzione precedente</string>
      <string id="DisableExplorerRunLegacy_Help">Questa impostazione dei criteri ignora l'elenco di esecuzione personalizzato.

È possibile creare un elenco personalizzato di programmi e documenti aggiuntivi che il sistema avvia automaticamente se eseguito su Windows Vista, Windows XP Professional e Windows 2000 Professional. Questi programmi vengono aggiunti all'elenco di esecuzione standard dei programmi e servizi avviati dal sistema.

Se si abilita questa impostazione dei criteri, il sistema ignorerà l'elenco di esecuzione per Windows Vista, Windows XP Professional e Windows 2000 Professional.

Se si disabilita o non si configura questa impostazione dei criteri, Windows Vista aggiungerà qualsiasi elenco di esecuzione personalizzato configurato al proprio elenco di esecuzione.

Questa impostazione dei criteri si trova nelle cartelle Configurazione computer e Configurazione utente. Se entrambe le impostazioni del criterio sono configurate, l'impostazione in Configurazione computer ha la priorità.

Nota: per creare un elenco di esecuzione personalizzato tramite un criterio, utilizzare l'impostazione "Esegui queste applicazioni all'avvio".

Vedere anche l'impostazione "Non elaborare l'elenco di esecuzione unica".</string>

        <string id="DontDisplayNetworkSelectionUI">Non visualizzare l'interfaccia utente di selezione rete</string>
        <string id="DontDisplayNetworkSelectionUI_Help">Questa impostazione dei criteri consente di specificare se chiunque può interagire con l'interfaccia utente per le reti disponibili nella schermata di accesso.

Se si abilita questa impostazione dei criteri, lo stato di connettività di rete del computer non potrà essere modificato senza eseguire l'accesso a Windows.

Se si disabilita o non si configura questa impostazione dei criteri, qualsiasi utente potrà disconnettere il computer dalla rete o connetterlo ad altre reti disponibili senza eseguire l'accesso a Windows.</string>

        <string id="DisableExplorerRunOnceLegacy">Non elaborare l'elenco di esecuzione unica</string>
      <string id="DisableExplorerRunOnceLegacy_Help">Questa impostazione dei criteri ignora l'elenco di esecuzione personalizzato.

È possibile creare un elenco personalizzato di programmi e documenti aggiuntivi avviati automaticamente solo al successivo avvio del sistema, ma non in seguito. Questi programmi vengono aggiunti all'elenco standard dei programmi e servizi avviati dal sistema.

Se si abilita questa impostazione dei criteri, il sistema ignorerà l'elenco di esecuzione unica.

Se si disabilita o non si configura questa impostazione dei criteri, verranno eseguiti i programmi inclusi nell'elenco di esecuzione unica.

Questa impostazione si trova nelle cartelle Configurazione computer e Configurazione utente. Se entrambe le impostazioni del criterio sono configurate, l'impostazione in Configurazione computer ha la priorità su quella in Configurazione utente.

Nota: gli elenchi di esecuzione unica personalizzati sono archiviati nella chiave del Registro di sistema HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\RunOnce.

Vedere anche l'impostazione dei criteri "Non elaborare l'elenco di esecuzione precedente".</string>
      <string id="DisableStatusMessages">Disattiva messaggi di stato relativi ad Avvio/Arresto del sistema/Accesso/Fine sessione</string>
      <string id="DisableStatusMessages_Help">Questa impostazione dei criteri elimina i messaggi di stato del sistema.

Se si abilita questa impostazione, non verranno visualizzati i messaggi che indicano che è in corso l'avvio o l'arresto del sistema oppure la connessione o la disconnessione.

Se si disabilita o non si configura questa impostazione dei criteri, verranno visualizzati i messaggi che ricordano all'utente di aspettare che il sistema venga avviato o arrestato, o che vengano effettuate la connessione o la disconnessione.</string>
      <string id="Logon">Accesso</string>
      <string id="LogonType">Usa sempre schermata di accesso classica</string>
      <string id="LogonType_Help">Questo criterio non è disponibile in questa versione di Windows.</string>
      <string id="NoWelcomeTips">Non visualizzare la schermata iniziale della Guida introduttiva all'accesso</string>
      <string id="NoWelcomeTips_Help">Questa impostazione dei criteri nasconde la schermata iniziale visualizzata ogni volta che l'utente accede a Windows 2000 Professional.

Se si abilita questa impostazione dei criteri, la schermata iniziale verrà nascosta all'utente connesso a un computer su cui viene applicato il criterio.

Per visualizzare la schermata iniziale, è possibile selezionarla dal menu Start oppure digitare "Welcome" nella finestra di dialogo Esegui.

Se si disabilita o non si configura questa impostazione dei criteri, la schermata iniziale verrà visualizzata ogni volta che un utente al computer.

Questa impostazione si applica solo a Windows 2000 Professional. Non ha effetto sulla schermata "Configurazione server Windows 2000" di Windows 2000 Server.

Nota: Questa impostazione si trova nelle cartelle Configurazione computer e Configurazione utente. Se vengono configurate entrambe le impostazioni, l'impostazione di Configurazione computer risulterà prioritaria rispetto a quella di Configurazione utente.

Suggerimento: per visualizzare la schermata iniziale, fare clic sul pulsante Start, scegliere Programmi, Accessori, Utilità di sistema e quindi "Guida introduttiva". Per disattivare la visualizzazione della schermata iniziale senza specificare un'impostazione, deselezionare la casella di controllo "Mostra questa schermata all'avvio" nella schermata iniziale.</string>
      <string id="Run">Esegui questi programmi all'accesso dell'utente</string>
      <string id="Run_Help">Questa impostazione dei criteri specifica programmi o documenti aggiuntivi avviati automaticamente da Windows quando un utente accede al sistema.

Se si abilita questa impostazione dei criteri, sarà possibile specificare i programmi che possono essere eseguiti quando l'utente accede al computer su cui è applicato il criterio.

Per specificare valori per questa impostazione dei criteri, fare clic su Mostra. Nella colonna Valore della finestra di dialogo Mostra contenuto digitare il nome del file eseguibile del programma (con estensione exe) o del file del documento. Per specificare un altro nome, premere INVIO e digitare il nome. Se il file non si trova nella directory %Systemroot%, sarà necessario specificare il percorso completo del file.

Se l'impostazione dei criteri viene disabilitata o non viene configurata, l'utente dovrà avviare i programmi appropriati dopo l'accesso.

Nota: questa impostazione si trova nelle cartelle Configurazione computer e Configurazione utente. Se vengono configurate entrambe le impostazioni, il sistema avvierà i programmi specificati nell'impostazione Configurazione computer prima di quelli specificati nell'impostazione Configurazione utente.

Vedere anche le impostazioni "Non elaborare l'elenco di esecuzione precedente" e "Non elaborare l'elenco di esecuzione unica".</string>
      <string id="SyncForegroundPolicy">Attendi sempre disponibilità rete all'avvio e all'accesso</string>
      <string id="SyncForegroundPolicy_Help">Questa impostazione dei criteri determina se l'elaborazione dei criteri di gruppo viene eseguita in modo sincrono, ovvero se i computer attendono l'inizializzazione completa della rete all'avvio e all'accesso utente. Per impostazione predefinita, l'elaborazione dei criteri di gruppo non è sincrona in quanto i computer client in genere non attendono l'inizializzazione completa della rete all'avvio e all'accesso. Gli utenti esistenti si connettono utilizzando le credenziali memorizzate nella cache, con tempi di accesso più brevi. I criteri di gruppo vengono applicati in background quando la rete diventa disponibile. 

Si noti che a causa dell'aggiornamento in background, per estensioni quali Installazione software e Reindirizzamento cartelle sono necessari due accessi per applicare le modifiche. Per operare in modo sicuro, tali estensioni richiedono che nessun utente sia connesso. È pertanto necessario elaborarle in primo piano prima che gli utenti utilizzino attivamente il computer. È inoltre possibile che il rilevamento di modifiche apportate all'oggetto utente, ad esempio l'aggiunta di un percorso del profilo mobile, di una home directory o di uno script di accesso oggetto utente, richieda fino a due accessi.

Se un utente con un profilo mobile, una home directory o uno script di accesso oggetto utente accede a un computer, il computer attenderà sempre l'inizializzazione della rete prima di consentire l'accesso dell'utente. Se un utente accede al computer per la prima volta, il computer attenderà sempre l'inizializzazione della rete.

Se si abilita questa impostazione dei criteri, i computer attenderanno l'inizializzazione completa della rete prima di consentire l'accesso degli utenti. I criteri di gruppo verranno applicati in primo piano in modo sincrono. 

Nei server che eseguono Windows Server 2008 o versioni successive questa impostazione dei criteri verrà ignorata durante l'elaborazione dei criteri di gruppo all'avvio del computer e l'elaborazione dei criteri di gruppo sarà sincrona, ovvero i server attenderanno l'inizializzazione della rete durante l'avvio del computer. 

Se il server è configurato come descritto di seguito, questa impostazione dei criteri verrà applicata durante l'elaborazione dei criteri di gruppo all'accesso dell'utente:
•  Il server è configurato come Terminal Server, ovvero il servizio ruolo Terminal Server è stato installato e configurato nel server) e
•  L'impostazione dei criteri "Consenti elaborazione asincrona Criteri di gruppo utente quando l'accesso viene effettuato tramite Servizi terminal" è abilitata. Questa impostazione è disponibile in Configurazione computer\Criteri\Modelli amministrativi\Sistema\Criteri gruppo\.

Se questa configurazione non è implementata nel server, l'impostazione dei criteri verrà ignorata. In questo caso, l'elaborazione dei criteri di gruppo all'accesso dell'utente sarà sincrona, ovvero i server attenderanno l'inizializzazione della rete durante l'accesso dell'utente.

Se si disabilita o non si configura questa impostazione dei criteri e gli utenti accedono a un computer client o a un server che esegue Windows Server 2008 o versione successiva configurato come illustrato in precedenza, il computer non attenderà l'inizializzazione completa della rete. In questo caso, per l'accesso degli utenti vengono utilizzate le credenziali memorizzate nella cache. I criteri di gruppo vengono applicati in background in modo asincrono.

Note: 
-se si desidera garantire che l'applicazione di Reindirizzamento cartelle, Installazione software o delle impostazioni del profilo utente mobile avvenga con un solo accesso, abilitare questa impostazione dei criteri in modo che Windows attenda che la rete sia disponibile prima di applicare i criteri. 
-se il criterio di reindirizzamento delle cartelle viene applicato durante il successivo accesso, i criteri di sicurezza verranno applicati in modo asincrono durante il ciclo di aggiornamento successivo, se è disponibile la connettività di rete.</string>
      <string id="VerboseStatus">Visualizza messaggi di stato molto dettagliati</string>
      <string id="VerboseStatus_Help">Questa impostazione dei criteri indica al sistema di visualizzare messaggi di stato molto dettagliati.

Questa impostazione dei criteri è destinata a utenti esperti che necessitano di tale tipo di informazioni.

Se si abilita questa impostazione dei criteri, verranno visualizzati messaggi di stato che includono informazioni dettagliate su tutti passaggi del processo di avvio, chiusura, connessione e disconnessione del sistema.

Se si disabilita o non si configura questa impostazione dei criteri, durante questi processi verranno visualizzati solo i messaggi di stato predefiniti.

Nota: quest'impostazione dei criteri viene ignorata se è stata abilitata l'impostazione "Disattiva messaggi di stato relativi ad Avvio/Arresto del sistema/Accesso/Fine sessione".</string>
      <string id="HideFastUserSwitching">Nascondi punti di ingresso per Cambio rapido utente</string>
      <string id="HideFastUserSwitching_Help">Questa impostazione dei criteri consente di nascondere l'interfaccia Cambia utente nell'interfaccia utente di accesso, nel menu Start e in Gestione attività.

Se si abilita questa impostazione dei criteri, l'interfaccia Cambia utente verrà nascosta all'utente che sta tentando di effettuare l'accesso o che ha effettuato l'accesso al computer su cui è applicato questo criterio. 

L'interfaccia Cambia utente viene visualizzata nell'interfaccia utente di accesso, nel menu Start e in Gestione attività.

Se si disabilita o non si configura questa impostazione dei criteri, l'interfaccia Cambia utente sarà accessibile all'utente nei tre punti.</string>
      <string id="DisableStartupSound">Disattiva il suono di avvio di Windows</string>
      <string id="DisableStartupSound_Help">Questo criterio non è disponibile in questa versione di Windows.</string>
      <string id="EnableFirstLogonAnimation">Mostra animazione primo accesso  </string>
      <string id="EnableFirstLogonAnimation_Help">Questa impostazione dei criteri consente di controllare se l'utente visualizza l'animazione del primo accesso quando accede al computer per la prima volta.  Ciò è valido sia per il primo utente del computer che completa la configurazione iniziale sia per gli utenti che vengono aggiunti in seguito al computer.  L'impostazione determina inoltre se agli utenti con account Microsoft verrà richiesto il consenso esplicito per i servizi durante il primo accesso.

Se si abilita questa impostazione dei criteri, agli utenti con account Microsoft verrà mostrata una richiesta di consenso esplicito per i servizi mentre agli utenti con altri account verrà visualizzata l'animazione del primo accesso.

Se si disabilita questa impostazione, agli utenti non verrà visualizzata l'animazione e agli utenti con account Microsoft non verrà visualizzata la richiesta di consenso esplicito per i servizi.

Se questa impostazione dei criteri non viene configurata, l'utente che completa la configurazione iniziale di Windows vedrà l'animazione al momento del primo accesso. Se il primo utente ha già completato la configurazione iniziale e questa impostazione dei criteri non è configurata, i nuovi utenti del computer non visualizzeranno l'animazione.

        Nota: l'animazione del primo accesso non verrà visualizzata su server, pertanto questa impostazione dei criteri non avrà effetto.
      
      </string>
      <string id="DontEnumerateConnectedUsers">Non enumerare utenti connessi su computer di dominio</string>
      <string id="DontEnumerateConnectedUsers_Help">Questa impostazione dei criteri impedisce che gli utenti connessi su computer che fanno parte di un dominio vengano enumerati. 

Se si abilita questa impostazione dei criteri, l'interfaccia utente di accesso non enumererà gli utenti connessi sui computer che fanno parte di un dominio.

Se si disabilita o non si configura questa impostazione dei criteri, gli utenti connessi su computer che fanno parte di un dominio verranno enumerati.</string>
      <string id="EnumerateLocalUsers">Enumera utenti locali su computer di dominio</string>
      <string id="EnumerateLocalUsers_Help">Questa impostazione dei criteri consente che gli utenti locali di computer che fanno parte di un dominio vengano enumerati.  

Se si abilita questa impostazione dei criteri, l'interfaccia utente di accesso enumererà tutti gli utenti locali di computer che fanno parte di un dominio.

Se si disabilita o non si configura questa impostazione dei criteri, l'interfaccia utente di accesso non enumererà gli utenti locali di computer che fanno parte di un dominio.</string>
      <string id="BlockUserFromShowingAccountDetailsOnSignin">Impedisci all'utente di visualizzare i dettagli dell'account nella schermata di accesso</string>
      <string id="BlockUserFromShowingAccountDetailsOnSignin_Help">Questa impostazione dei criteri impedisce all'utente di visualizzare i dettagli dell'account (indirizzo e-mail o nome utente) nella schermata di accesso.

Se si abilita questa impostazione dei criteri, l'utente non può scegliere di visualizzare i dettagli dell'account nella schermata di accesso.

Se si disabilita o non si configura questa impostazione dei criteri, l'utente può scegliere di visualizzare i dettagli dell'account nella schermata di accesso.</string>
      <string id="DisableLockScreenAppNotifications">Disattiva notifiche app nella schermata di blocco</string>
      <string id="DisableLockScreenAppNotifications_Help">Questa impostazione dei criteri consente di impedire la visualizzazione delle notifiche di app sulla schermata di blocco.

Se si abilita questa impostazione dei criteri, sulla schermata di blocco non verrà visualizzata alcuna notifica di app.

Se si disabilita o non si configura questa impostazione, gli utenti potranno scegliere quali notifiche di app visualizzare sulla schermata di blocco.</string>
      <string id="DisableAcrylicBackgroundOnLogon">Mostra sfondo di accesso chiaro</string>
      <string id="DisableAcrylicBackgroundOnLogon_Help">Questa impostazione dei criteri disabilita l'effetto di sfocatura acrilica sull'immagine di sfondo dell'accesso.

        Se abiliti questo criterio, l'immagine di sfondo di accesso sarà visualizzata senza sfocatura.
        Se disabiliti o non configura questo criterio, l'immagine di sfondo di accesso adotterà l'effetto di sfocatura acrilica.
      </string>
    </stringTable>
    <presentationTable>
      <presentation id="Run_1">
        <listBox refId="RunListBox1">Elementi da eseguire all'accesso</listBox>
      </presentation>
      <presentation id="Run_2">
        <listBox refId="RunListBox2">Elementi da eseguire all'accesso</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
