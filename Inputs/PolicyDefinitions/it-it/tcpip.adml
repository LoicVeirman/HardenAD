<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>File del modello Criteri di gruppo TCPIP</displayName>
  <description>Questo file admx descrive il modello dei criteri per i componenti TCPIP</description>
  <resources>
    <stringTable>
      <string id="TCPIP">Impostazioni TCPIP</string>
      <string id="Ipv6Transition">Tecnologie di transizione IPv6</string>

      <string id="ISATAP_State">Imposta stato ISATAP</string>
      <string id="ISATAP_Router_Name">Imposta Nome router ISATAP</string>
      <string id="6to4_State">Imposta stato 6to4</string>
      <string id="6to4_Router_Name">Imposta nome relay 6to4</string>
      <string id="6to4_Router_Name_Resolution_Interval">Imposta intervallo di risoluzione nome relay 6to4</string>
      <string id="Teredo_State">Imposta stato Teredo</string>
      <string id="Teredo_Server_Name">Imposta nome server Teredo</string>
      <string id="Teredo_Refresh_Rate">Imposta frequenza di aggiornamento Teredo</string>
      <string id="Teredo_Client_Port">Imposta porta client Teredo</string>
      <string id="Teredo_Default_Qualified">Imposta qualificato predefinito Teredo</string>

      <string id="DefaultState">Stato predefinito</string>
      <string id="EnabledState">Stato abilitato</string>
      <string id="DisabledState">Stato disabilitato</string>
      <string id="TeredoClient">Client</string>
      <string id="TeredoEnterpriseClient">Enterprise Client</string>
      <string id="IPHTTPS_ClientState">Imposta stato IP-HTTPS</string>
      <string id="IPHTTPS_ClientUrl">URL IP-HTTPS</string>

      <string id="ISATAP_State_Help">Questa impostazione dei criteri consente di configurare l'indirizzo IP (ISATAP) del tunnel Automatic (IP-to-Point) per il router e la tecnologia di tunneling automatico da host a host e router a host e router da cui deve essere utilizzata per garantire la connettività IPv6 unicast tra gli host IPv6 tra una Intranet IPv4.

Se si disabilita o non si configura questa impostazione dei criteri, viene utilizzata l'impostazione dell'host locale.

Se si abilita questa impostazione dei criteri, sarà possibile configurare ISATAP con una delle impostazioni seguenti:

Stato predefinito del criterio: nell'host non sono presenti interfacce ISATAP.

Stato abilitato del criterio: se il nome del ISATAP viene risolto, l'host avrà ISATAP configurato con un indirizzo locale rispetto al collegamento e un indirizzo per ciascun prefisso ricevuto dal router ISATAP tramite la configurazione automatica degli indirizzi senza stato. Se il nome del ISATAP non viene risolto correttamente, l'host avrà un'interfaccia ISATAP configurata con un indirizzo locale rispetto al collegamento.

Stato disabilitato del criterio: nell'host non è presente alcuna interfaccia ISATAP.

</string>

      <string id="ISATAP_Router_Name_Help">Questa impostazione consente di specificare un nome del router o un indirizzo IPv4 (Internet Protocol version 4) per un router ISATAP.

Se si abilita questa impostazione, sarà possibile specificare un nome del router o un indirizzo IPv4 per un router ISATAP. Se si immette un indirizzo IPv4 del router ISATAP nella casella di testo, i servizi DNS non sono richiesti.

Se si disabilita o non si configura questa impostazione, verrà utilizzata l'impostazione dell'host locale.



</string>


      <string id="6to4_State_Help">Questa impostazione dei criteri consente di configurare 6to4, un'assegnazione di indirizzi e una tecnologia di tunneling automatico router-to-router usata per fornire la connettività IPv6 unicast tra i siti IPv6 e gli host nell'Internet IPv4. 6to4 usa il prefisso di indirizzo globale: 2002: WWXX: YYZZ::/48 in cui le lettere sono una rappresentazione esadecimale dell'indirizzo IPv4 globale (con estensione z) assegnato a un sito.

se si disabilita o non si configura questa impostazione dei criteri, viene usata l'impostazione host locale.

se si abilita questa impostazione dei criteri, è possibile configurare 6to4 con una delle impostazioni seguenti:

criterio predefinito: 6to4 è disattivato e la connettività con 6to4 non sarà disponibile.

stato abilitato per i criteri: se è presente un indirizzo IPv4 globale, l'host avrà un'interfaccia 6to4. Se non è presente alcun indirizzo IPv4 globale, l'host non avrà un'interfaccia 6to4.

criteri disabilitati: 6to4 è disattivato e la connettività con 6to4 non sarà disponibile.

</string>

      <string id="6to4_Router_Name_Help">Questa impostazione consente di specificare un nome di relay 6to4 per un host 6to4. Un relay 6to4 viene utilizzato come gateway predefinito per il traffico di rete IPv6 inviato dall'host 6to4. L'impostazione del nome di relay 6to4 non ha effetto se nell'host non è disponibile la connettività 6to4.

Se si abilita questa impostazione, sarà possibile specificare un nome di relay per un host 6to4.

Se si disabilita o non si configura questa impostazione, verrà utilizzata l'impostazione dell'host locale e non sarà possibile specificare un nome di relay per un host 6to4.



</string>

      <string id="6to4_Router_Name_Resolution_Interval_Help">Questa impostazione consente di specificare l'intervallo di risoluzione del nome del relay. L'impostazione relativa all'intervallo di risoluzione del nome del relay 6to4 non ha effetto se nell'host non è disponibile la connettività 6to4.

Se si abilita questa impostazione, sarà possibile specificare il valore della durata della risoluzione periodica del nome del relay.

Se si disabilita o non si configura questa impostazione, verrà utilizzata l'impostazione dell'host locale.



</string>

      <string id="Teredo_State_Help">Questa impostazione dei criteri consente di configurare Teredo, una tecnologia di assegnazione degli indirizzi e di tunneling automatico che fornisce connettività IPv6 unicast sulla rete Internet IPv4.

Se si disabilita o non si configura questa impostazione dei criteri, verranno utilizzate le impostazioni dell'host locale.

Se si abilita questa impostazione dei criteri, sarà possibile configurare Teredo con una delle impostazioni seguenti:

Predefinito: lo stato predefinito è "Client."

Disabilitato: nell'host non sono presenti interfacce Teredo.

Client: l'interfaccia Teredo è presente solo quando l'host non si trova su una rete che include un controller di dominio.

Enterprise Client: l'interfaccia Teredo è sempre presente, anche quando l'host si trova in una rete che include un controller di dominio.



</string>

      <string id="Teredo_Server_Name_Help">Questa impostazione dei criteri consente di specificare il nome del server Teredo. Il nome del server verrà utilizzato nel computer client Teredo a cui è applicata l'impostazione dei criteri.

Se si abilita questa impostazione dei criteri, sarà possibile specificare un nome del server Teredo che si applica a un client Teredo.

Se si disabilita o non si configura questa impostazione dei criteri, il nome del server Teredo verrà determinato in base alle impostazioni locali del computer.



</string>

      <string id="Teredo_Refresh_Rate_Help">Questa impostazione consente di configurare la frequenza di aggiornamento dei client Teredo.

Nota: i client Teredo inviano periodicamente (per impostazione predefinita, ogni 30 secondi) un unico pacchetto di richieste router al server Teredo. Il server Teredo invia in risposta un pacchetto di annunci router. Tale pacchetto periodico aggiorna l'indirizzo IP e il mapping delle porte UDP nella tabella di conversione del dispositivo NAT del client Teredo.

Se si abilita questa impostazione, sarà possibile specificare la frequenza di aggiornamento.  Se si sceglie una frequenza di aggiornamento superiore al mapping delle porte nel dispositivo NAT del client Teredo, è possibile che il servizio Teredo smetta di funzionare o che la connettività sia intermittente.

Se si disabilita o non si configura questa impostazione, la frequenza di aggiornamento verrà configurata in base alle impostazioni locali del computer. La frequenza di aggiornamento predefinita è 30 secondi.



</string>

      <string id="Teredo_Client_Port_Help">Questa impostazione consente di selezionare la porta UDP che verrà utilizzata dal client Teredo per inviare pacchetti. Se si mantiene il valore predefinito 0, a porta verrà selezionata automaticamente (scelta consigliata). Se si seleziona una porta UDP già utilizzata dal sistema, l'inizializzazione del client Teredo non verrà completata.

Se si abilita questa impostazione, sarà possibile personalizzare la porta UDP per il client Teredo.

Se si disabilita o non si configura questa impostazione, verrà utilizzata l'impostazione dell'host locale.



</string>

      <string id="Teredo_Default_Qualified_Help">Questa impostazione consente di predisporre il servizio Teredo alle comunicazioni, un processo definito come qualificazione. Per impostazione predefinita, Teredo assume lo stato inattivo quando non è in uso. Il processo di qualificazione produce l'uscita del servizio dallo stato inattivo.

Se si disabilita o non si configura questa impostazione, verrà utilizzata l'impostazione dell'host locale.

Questa impostazione contiene un solo stato:

Stato abilitato del criterio: se l'impostazione Qualificato predefinito Teredo è abilitata, Teredo tenterà immediatamente di eseguire la qualificazione e, se tale processo verrà completato, rimarrà qualificato.

</string>

      <string id="IPHTTPS_ClientState_Help">Questa impostazione dei criteri consente di configurare IP-HTTPS, una tecnologia di tunneling che utilizza il protocollo HTTPS per fornire connettività IP a una rete remota.

Se si disabilita o non si configura questa impostazione dei criteri, verranno utilizzate le impostazioni dell'host locale.

Se si abilita questa impostazione dei criteri, sarà possibile specificare un URL del server IP-HTTPS. Sarà possibile configurare IP-HTTPS con una delle impostazioni seguenti:

Stato predefinito del criterio: l'interfaccia IP-HTTPS viene utilizzata quando non sono disponibili altre opzioni di connettività.

Stato abilitato del criterio: l'interfaccia IP-HTTPS è sempre presente, anche quando per l'host sono disponibili altre opzioni di connettività.

Stato disabilitato del criterio: nell'host non sono presenti interfacce IP-HTTPS.



</string>

      <string id="Parameters">Parametri</string>
      <string id="Windows_Scaling_Heuristics_State">Imposta stato Euristica ridimensionamento finestre</string>
      <string id="Windows_Scaling_Heuristics_Help">Questa impostazione dei criteri consente di configurare l'euristica di ridimensionamento delle finestre. Euristica ridimensionamento finestre è un algoritmo utilizzato per identificare i problemi di connettività e di velocità causati da numerosi firewall e da altre middle box che non interpretano correttamente l'opzione di ridimensionamento delle finestre.

Se non si configura questa impostazione dei criteri, verranno utilizzate le impostazioni dell'host locale.

Se si abilita questa impostazione dei criteri, Euristica ridimensionamento finestre verrà abilitata e verrà tentata l'identificazione dei problemi di connettività e di velocità nonché l'adozione delle misure opportune.

Se si disabilita questa impostazione dei criteri, Euristica ridimensionamento finestre verrà disabilitata e non verrà tentata l'identificazione dei problemi di connettività e di velocità causati dai firewall o da altre middle box.



</string>

      <string id="IP_Stateless_Autoconfiguration_Limits_State">Imposta stato limiti configurazione automatica senza stato per gli indirizzi IP</string>
      <string id="IP_Stateless_Autoconfiguration_Limits_Help">Questa impostazione consente di configurare i limiti della configurazione automatica senza stato per gli indirizzi IP.

Se si abilita o non si configura questa impostazione dei criteri, i limiti della configurazione automatica senza stato per gli indirizzi IP saranno abilitati e verrà limitato il numero di indirizzi e route configurati automaticamente.

Se si disabilita questa impostazione dei criteri, i limiti della configurazione automatica senza stato per gli indirizzi IP saranno disabilitati e non verrà limitato il numero di indirizzi e route configurati automaticamente.



</string>

</stringTable>

<presentationTable>

      <presentation id="StateSelection">
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Scegliere uno degli stati seguenti:</dropdownList>
      </presentation>

      <presentation id="RouterName">
        <textBox refId="RouterNameBox">
          <label>Immettere il nome di un router o di un relay:</label>
        </textBox>
      </presentation>

      <presentation id="RouterNameResolutionInterval">
        <text>Questa impostazione consente di personalizzare l'intervallo di risoluzione del nome di relay 6to4</text>
        <decimalTextBox refId="RouterNameResolutionIntervalBox" defaultValue="1440">Minuti:  </decimalTextBox>
      </presentation>

      <presentation id="TeredoServerName">
        <textBox refId="TeredoServerNameBox">
          <label>Immettere un nome di server Teredo:</label>
        </textBox>
      </presentation>

      <presentation id="TeredoRefreshRate">
        <text>Questa impostazione consente di personalizzare la frequenza di aggiornamento del client Teredo</text>
        <text>L'intervallo è compreso tra 1 e 86400 secondi (1 giorno).</text>
        <decimalTextBox refId="TeredoRefreshRateBox" defaultValue="30">Secondi:  </decimalTextBox>
      </presentation>

      <presentation id="TeredoClientPort">
        <text>Questa impostazione consente di personalizzare la porta del client Teredo</text>
        <text>L'intervallo è compreso tra 0 e 65535. Il valore predefinito è 0 (scelta consigliata) e comporta la selezione automatica della porta nel sistema locale.</text>
        <decimalTextBox refId="TeredoClientPortBox" defaultValue="0"> </decimalTextBox>
      </presentation>

      <presentation id="IPHTTPS_ClientState">
        <textBox refId="IPHTTPSClientUrlBox">
          <label>Immettere l'URL IPHTTPS:</label>
        </textBox>
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Selezionare lo stato dell'interfaccia tra le due seguenti opzioni:</dropdownList>
      </presentation>

</presentationTable>

</resources>
</policyDefinitionResources>
