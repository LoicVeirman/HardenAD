<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="DisableBackupRestore">Impedisci ripristino di versioni precedenti da backup</string>
      <string id="DisableBackupRestore_Help">Questa impostazione dei criteri consente di non visualizzare il pulsante Ripristina nella pagina delle proprietà delle versioni precedenti quando un utente ha selezionato una versione precedente di un file locale e tale versione è archiviata in un backup.

Se si abilita questa impostazione dei criteri, il pulsante Ripristina verrà disabilitato quando l'utente seleziona una versione precedente corrispondente a un backup.

Se si disabilita questa impostazione dei criteri, il pulsante Ripristina rimarrà attivo per una versione precedente corrispondente a un backup. Se si fa clic sul pulsante Ripristina, verrà eseguito un tentativo di ripristino del file dal supporto di backup.

Se non si configura questa impostazione dei criteri, il pulsante sarà disabilitato per impostazione predefinita. Il pulsante Ripristina è attivo quando la versione precedente corrisponde a un file locale ed è archiviata nel backup.</string>
      <string id="DisableLocalPage">Nascondi elenco versioni precedenti per file locali</string>
      <string id="DisableLocalPage_Help">Questa impostazione dei criteri consente di nascondere l'elenco delle versioni precedenti dei file che si trovano nei dischi locali. Le versioni precedenti possono essere disponibili in punti di ripristino su disco o supporti di backup.

Se si abilita questa impostazione dei criteri, gli utenti non potranno elencare o ripristinare versioni precedenti di file nei dischi locali.

Se si disabilita questa impostazione dei criteri, gli utenti potranno elencare o ripristinare versioni precedenti di file nei dischi locali.

Se non si configura questa impostazione dei criteri, rimarrà disabilitata per impostazione predefinita.</string>
      <string id="DisableLocalRestore">Impedisci ripristino di versioni precedenti locali</string>
      <string id="DisableLocalRestore_Help">Questa impostazione dei criteri consente di non visualizzare il pulsante Ripristina nella pagina delle proprietà delle versioni precedenti quando un utente ha selezionato una versione precedente di un file locale.

Se si abilita questa impostazione dei criteri, il pulsante Ripristina verrà disabilitato quando l'utente seleziona una versione precedente corrispondente a un file locale.

Se si disabilita questa impostazione dei criteri, il pulsante Ripristina rimarrà attivo per una versione precedente corrispondente a un file locale. Se l'utente fa clic sul pulsante Ripristina, verrà eseguito un tentativo di ripristino del file dal disco locale.

Se non si configura questa impostazione dei criteri, il pulsante sarà disabilitato per impostazione predefinita. Il pulsante Ripristina è attivo quando la versione precedente corrisponde a un file locale.</string>
      <string id="DisableRemotePage">Nascondi elenco versioni precedenti per file remoti</string>
      <string id="DisableRemotePage_Help">Questa impostazione dei criteri consente di nascondere l'elenco delle versioni precedenti dei file che si trovano in condivisioni file. Le versioni precedenti provengono da punti di ripristino su disco presenti nella condivisione file.

Se si abilita questa impostazione dei criteri, gli utenti non potranno elencare o ripristinare versioni precedenti di file in condivisioni file.

Se si disabilita questa impostazione dei criteri, gli utenti potranno elencare o ripristinare versioni precedenti di file in condivisioni file.

Se non si configura questa impostazione dei criteri, il pulsante sarà disabilitato per impostazione predefinita.</string>
      <string id="DisableRemoteRestore">Impedisci ripristino di versioni precedenti remote</string>
      <string id="DisableRemoteRestore_Help">Questa impostazione consente di non visualizzare il pulsante Ripristina nella pagina delle proprietà delle versioni precedenti quando un utente ha selezionato una versione precedente di un file presente in una condivisione file.

Se si abilita questa impostazione dei criteri, il pulsante Ripristina verrà disabilitato quando l'utente seleziona una versione precedente corrispondente a un file in una condivisione file.

Se si disabilita questa impostazione dei criteri, il pulsante Ripristina rimarrà attivo per una versione precedente corrispondente a un file in una condivisione file. Se l'utente fa clic sul pulsante Ripristina, verrà eseguito un tentativo di ripristino del file dalla condivisione file.

Se non si configura questa impostazione dei criteri, il pulsante sarà disabilitato per impostazione predefinita. Il pulsante Ripristina è attivo quando la versione precedente corrisponde a un file in una condivisione file.</string>
      <string id="HideBackupEntries">Nascondi versioni precedenti di file in percorso di backup</string>
      <string id="HideBackupEntries_Help">Questa impostazione dei criteri consente di escludere dall'elenco delle versioni precedenti di un file quelle archiviate su supporti di backup. Le versioni precedenti possono provenire da punti di ripristino su disco o supporti di backup.

Se si abilita questa impostazione dei criteri, gli utenti potranno visualizzare solo le versioni precedenti corrispondenti a punti di ripristino su disco, ma non quelle corrispondenti a copie di backup.

Se si disabilita questa impostazione dei criteri, gli utenti potranno visualizzare sia le versioni precedenti corrispondenti a copie di backup che quelle corrispondenti a punti di ripristino su disco.

Se non si configura questa impostazione dei criteri, il pulsante sarà disabilitato per impostazione predefinita.</string>
      <string id="PreviousVersions">Versioni precedenti</string>
      <string id="SUPPORTED_Vista_through_Win7">Supporto completo da Windows Vista a Windows 7</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
