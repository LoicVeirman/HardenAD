<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>digite o nome para exibição aqui</displayName>
  <description>digite a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="P2P_Disabled">Desativar os serviços de rede ponto a ponto da Microsoft</string>
      <string id="P2P_Disabled_Explain">Esta opção desativa totalmente os serviços de rede ponto a ponto da Microsoft, e todos os aplicativos que dependem dele deixarão de funcionar.

Os protocolos ponto a ponto auxiliam aplicativos nas áreas de RTC, colaboração, distribuição de conteúdo processamento distribuído.

Se você habilitar esta opção, os protocolos ponto a ponto serão desativados.

Se você desabilitar essa opção ou não a configurar, os protocolos ponto a ponto serão ativados.</string>
      <string id="P2P_Name">Serviços de rede ponto a ponto da Microsoft</string>
      <string id="PNRP_DisableMulticastBootstrap">Desativar Inicialização por Multicast</string>
      <string id="PNRP_DisableMulticastBootstrapGlobal_Explain">Esta opção impede que o protocolo PNRP anuncie o computador ou procure por outros computadores na sub-rede local, na nuvem global.

O protocolo PNRP (Peer Name Resolution Protocol) possibilita a resolução distribuída de um nome em um endereço IPv6 e número de porta. Uma das maneiras como o PNRP se inicializa e utilizando multicast na mesma sub-rede. Ou seja, o PNRP se anuncia na sub-rede local, para que outros computadores possam localizá-lo quando necessário.

Se você habilitar esta opção, o PNRP não utilizará multicast para sua inicialização. Especificar essa chave do registro causará problemas onde não houver servidor de roteamento para inicialização (como cenários de rede ad hoc).

Se você desabilitar essa opção, o PNRP empregará multicast para se inicializar na mesma sub-rede.

Se esta configuração não for definida, o protocolo voltará a usar uma chave pública do registro para determinar se anunciará o computador, ou se usará multicast para procurar por outros computadores na sub-rede local.

O protocolo multicast usado para inicialização é o SSDP (Simple Service Discovery Protocol). O serviço SSDP deverá estar habilitado (e está, por definição) para que esta diretiva tenha efeito.</string>
      <string id="PNRP_Disabled">Desativar a criação de nuvem PNRP</string> 
      <string id="PNRP_SearchOnly">Definir nuvem PNRP apenas como resolver</string> 
      <string id="PNRP_DisabledGlobal_Explain">Essa configuração de diretiva habilita ou desabilita a criação de nuvem PNRP.

PNRP é um protocolo de resolução de nome distribuído que permite que hosts de Internet publiquem nomes de mesmo nível com um endereço IPv6 correspondente. Outros hosts poderão então resolver o nome, recuperar o endereço correspondente e estabelecer conexões P2P (ponto a ponto). A nuvem PNRP é um grupo de nós PNRP conectados, nos quais existem conexões entre os mesmos níveis para que, desse modo, um nó na nuvem PNRP possa resolver nomes publicados por outros nós.

PNRP cria uma nuvem global se o computador possui um endereço IPv6 global, mas cria uma nuvem local no site se o computador possui um endereço local no site.

Se essa configuração de diretiva for habilitada, o PNRP não cria uma nuvem e os aplicativos não poderão usar essa nuvem para publicar ou resolver nomes, independentemente de se o computador possui um endereço IPv6 que corresponde ao escopo da nuvem.

Se essa configuração for desabilitada ou não for definida, todas as nuvens PNRP serão ativadas por padrão e o PNRP criará uma nuvem se o computador possuir um endereço IPv6 compatível com o escopo da nuvem.
</string>
      <string id="PNRP_SearchOnlyGlobal_Explain">Essa configuração de diretiva limita um nó na resolução, mas não na publicação, de nomes em uma nuvem PNRP específica.

Essa configuração de diretiva força os computadores a agir como clientes em cenários P2P (ponto a ponto). Por exemplo, um computador cliente pode detectar outros computadores para iniciar sessões de bate-papo, mas outros computadores não podem detectar esse cliente e iniciar sessões com ele.

Se essa configuração de diretiva for habilitada, esse computador não poderá registrar nomes PNRP e não poderá auxiliar outros computadores na execução de pesquisas PNRP.

Se essa configuração de diretiva for desabilitada ou não for configurada, este computador poderá publicar nomes PNRP e ajudar outros computadores nas pesquisas PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapLinkLocal_Explain">Esta opção impede que o protocolo PNRP anuncie o computador ou procure por outros computadores na sub-rede local, na nuvem de vínculo local.

O protocolo PNRP (Peer Name Resolution Protocol) possibilita a resolução distribuída de um nome em um endereço IPv6 e número de porta. Uma das maneiras como o PNRP se inicializa e utilizando multicast na mesma sub-rede. Ou seja, o PNRP se anuncia na sub-rede local, para que outros computadores possam localizá-lo quando necessário.

Se você habilitar esta opção, o PNRP não utilizará multicast para sua inicialização. Especificar essa chave do registro causará problemas onde não houver servidor de roteamento para inicialização (como cenários de rede ad hoc).

Se você desabilitar essa opção, o PNRP empregará multicast para se inicializar na mesma sub-rede.

Se esta configuração não for definida, o protocolo voltará a usar uma chave pública do registro para determinar se anunciará o computador, ou se usará multicast para procurar por outros computadores na sub-rede local.

O protocolo multicast usado para inicialização é o SSDP (Simple Service Discovery Protocol). O serviço SSDP deverá estar habilitado (e está, por definição) para que esta diretiva tenha efeito.</string>
      <string id="PNRP_DisabledLinkLocal_Explain">Essa configuração de diretiva habilita ou desabilita a criação de nuvem PNRP.

PNRP é um protocolo de resolução de nome distribuído que permite que hosts de Internet publiquem nomes de mesmo nível com um endereço IPv6 correspondente. Outros hosts poderão então resolver o nome, recuperar o endereço correspondente e estabelecer conexões P2P (ponto a ponto). A nuvem PNRP é um grupo de nós PNRP conectados, nos quais existem conexões entre os mesmos níveis para que, desse modo, um nó na nuvem PNRP possa resolver nomes publicados por outros nós.

PNRP cria uma nuvem global se o computador possui um endereço IPv6 global, mas cria uma nuvem local no site se o computador possui um endereço local no site.

Se essa configuração de diretiva for habilitada, o PNRP não cria uma nuvem e os aplicativos não poderão usar essa nuvem para publicar ou resolver nomes, independentemente de se o computador possui um endereço IPv6 que corresponde ao escopo da nuvem.

Se essa configuração for desabilitada ou não for definida, todas as nuvens PNRP serão ativadas por padrão e o PNRP criará uma nuvem se o computador possuir um endereço IPv6 compatível com o escopo da nuvem.
</string>
      <string id="PNRP_SearchOnlyLinkLocal_Explain">Essa configuração de diretiva limita um nó na resolução, mas não na publicação, de nomes em uma nuvem PNRP específica.

Essa configuração de diretiva força os computadores a agir como clientes em cenários P2P (ponto a ponto). Por exemplo, um computador cliente pode detectar outros computadores para iniciar sessões de bate-papo, mas outros computadores não podem detectar esse cliente e iniciar sessões com ele.

Se essa configuração de diretiva for habilitada, esse computador não poderá registrar nomes PNRP e não poderá auxiliar outros computadores na execução de pesquisas PNRP.

Se essa configuração de diretiva for desabilitada ou não for configurada, este computador poderá publicar nomes PNRP e ajudar outros computadores nas pesquisas PNRP.
</string> 
      <string id="PNRP_DisableMulticastBootstrapSiteLocal_Explain">Esta opção impede que o protocolo PNRP anuncie o computador ou procure por outros computadores na sub-rede local, na nuvem de site local.

O protocolo PNRP (Peer Name Resolution Protocol) possibilita a resolução distribuída de um nome em um endereço IPv6 e número de porta. Uma das maneiras como o PNRP se inicializa e utilizando multicast na mesma sub-rede. Ou seja, o PNRP se anuncia na sub-rede local, para que outros computadores possam localizá-lo quando necessário.

Se você habilitar esta opção, o PNRP não utilizará multicast para sua inicialização. Especificar essa chave do registro causará problemas onde não houver servidor de roteamento para inicialização (como cenários de rede ad hoc).

Se você desabilitar essa opção, o PNRP empregará multicast para se inicializar na mesma sub-rede.

Se esta configuração não for definida, o protocolo voltará a usar uma chave pública do registro para determinar se anunciará o computador, ou se usará multicast para procurar por outros computadores na sub-rede local.

O protocolo multicast usado para inicialização é o SSDP (Simple Service Discovery Protocol). O serviço SSDP deverá estar habilitado (e está, por definição) para que esta diretiva tenha efeito.</string>
      <string id="PNRP_DisabledSiteLocal_Explain">Essa configuração de diretiva habilita ou desabilita a criação de nuvem PNRP.

PNRP é um protocolo de resolução de nome distribuído que permite que hosts de Internet publiquem nomes de mesmo nível com um endereço IPv6 correspondente. Outros hosts poderão então resolver o nome, recuperar o endereço correspondente e estabelecer conexões P2P (ponto a ponto). A nuvem PNRP é um grupo de nós PNRP conectados, nos quais existem conexões entre os mesmos níveis para que, desse modo, um nó na nuvem PNRP possa resolver nomes publicados por outros nós.

PNRP cria uma nuvem global se o computador possui um endereço IPv6 global, mas cria uma nuvem local no site se o computador possui um endereço local no site.

Se essa configuração de diretiva for habilitada, o PNRP não cria uma nuvem e os aplicativos não poderão usar essa nuvem para publicar ou resolver nomes, independentemente de se o computador possui um endereço IPv6 que corresponde ao escopo da nuvem.

Se essa configuração for desabilitada ou não for definida, todas as nuvens PNRP serão ativadas por padrão e o PNRP criará uma nuvem se o computador possuir um endereço IPv6 compatível com o escopo da nuvem.
</string>
      <string id="PNRP_SearchOnlySiteLocal_Explain">Essa configuração de diretiva limita um nó na resolução, mas não na publicação, de nomes em uma nuvem PNRP específica.

Essa configuração de diretiva força os computadores a agir como clientes em cenários P2P (ponto a ponto). Por exemplo, um computador cliente pode detectar outros computadores para iniciar sessões de bate-papo, mas outros computadores não podem detectar esse cliente e iniciar sessões com ele.

Se essa configuração de diretiva for habilitada, esse computador não poderá registrar nomes PNRP e não poderá auxiliar outros computadores na execução de pesquisas PNRP.

Se essa configuração de diretiva for desabilitada ou não for configurada, este computador poderá publicar nomes PNRP e ajudar outros computadores nas pesquisas PNRP.
</string> 
      <string id="PNRP_Global">Nuvens Globais</string>
      <string id="PNRP_LinkLocal">Nuvens de Vínculo Local</string>
      <string id="PNRP_Name">Protocolo PNRP (Peer Name Resolution Protocol)</string>
      <string id="PNRP_SeedServer">Definir o Servidor Semente</string>
      <string id="PNRP_SeedServerGlobal_Explain">Esta opção atribui o servidor semente da nuvem global a um nó especificado na empresa. O protocolo PNRP (Peer Name Resolution Protocol) possibilita a resolução distribuída de um nome em um endereço IPv6 e número de porta. O protocolo, em alguns casos, necessita de um servidor semente pelo qual ele consiga se inicializar.

Qualquer nó que execute ponto a ponto pode ser usado como servidor de roteamento. Nenhuma configuração é exigida para o servidor semente propriamente dito.

Esta configuração oferece a flexibilidade adicional de permitir que os usuários usem seus aplicativos ponto a ponto também em casa. São apresentadas a seguir as quatro maneiras como você pode usar esta opção para controlar o servidor semente usado na sua empresa.

1. Para usar somente na Internet o servidor semente global bem conhecido: habilite a configuração, deixe a lista de servidores semente vazia e a caixa de seleção, desmarcada.

2. Para usar somente um servidor semente corporativo: habilite a configuração, insira o nome totalmente qualificado do domínio ou o endereço IPv6 do servidor semente corporativo, e marque a caixa de seleção. Usar apenas o servidor semente corporativo impedirá usuários em trânsito de utilizar aplicativos ponto a ponto em casa.

3. Para usar um servidor semente corporativo e o servidor semente global: habilite a opção, insira o nome totalmente qualificado do domínio ou o endereço IPv6 do servidor semente corporativo, e desmarque a caixa de seleção. Essa é a configuração que permitirá que usuários em trânsito utilizem aplicativos ponto a ponto em casa e no escritório sem interrupção.

4. Para não usar nenhum servidor semente: habilite a opção, não forneça o nome de um servidor semente e marque a caixa de seleção.

Se esta opção for desabilitada ou não for configurada, o protocolo voltará a usar uma chave pública de registro para determinar o servidor semente a partir do qual deverá se inicializar.</string>
      <string id="PNRP_SeedServerLinkLocal_Explain">Esta opção atribui o servidor semente da nuvem local vinculada a um nó especificado na empresa.

O protocolo PNRP (Peer Name Resolution Protocol) possibilita a resolução distribuída de um nome em um endereço IPv6 e número de porta. O protocolo, em alguns casos, necessita de um servidor semente pelo qual ele consiga se inicializar. Esta opção permite atribuir o servidor semente a um nó especificado na empresa.

Para usar esta configuração, clique em Habilitar e forneça a lista de endereços IPV6 separados por ponto-e-vírgula no campo disponível. Se você habilitar esta configuração e não fornecer um endereço, nenhum servidor semente será usado.

Se esta opção não for configurada, o protocolo voltará a usar uma chave pública de registro para determinar o servidor semente a partir do qual deverá se inicializar.</string>
      <string id="PNRP_SeedServerSiteLocal_Explain">Esta opção atribui o servidor semente da nuvem local do site a um nó especificado na empresa.

O protocolo PNRP (Peer Name Resolution Protocol) possibilita a resolução distribuída de um nome em um endereço IPv6 e número de porta. O protocolo, em alguns casos, necessita de um servidor semente pelo qual ele consiga se inicializar. Esta opção permite atribuir o servidor semente a um nó especificado na empresa.

Para usar esta configuração, clique em Habilitar e forneça a lista de nomes DNS ou endereços IPV6 separados por ponto-e-vírgula no campo disponível. Se você habilitar esta configuração e não fornecer um endereço, nenhum servidor semente será usado.

Se esta opção não for configurada, o protocolo voltará a usar uma chave pública de registro para determinar o servidor semente a partir do qual deverá se inicializar.</string>
      <string id="PNRP_SiteLocal">Nuvens de Área Local</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement">Desabilitar validação de força de senha para Agrupamento de Mesmo Nível</string>
      <string id="GROUPING_DisablePasswordPolicyEnforcement_Explain">Por padrão, quando um Grupo de Mesmo Nível é criado e permite a autenticação de senha (ou quando a senha desse Grupo é alterada), o Agrupamento de Mesmo Nível valida a senha se ela atender aos requisitos de complexidade de senha do sistema local.  Assim, ele não permitirá que sejam usadas senhas para um Grupo de Mesmo Nível que sejam mais fracas do que o permitido para uma senha de logon.

Esta configuração controla esse comportamento de validação.  Se definida como 1, essa validação não será realizada e qualquer senha será permitida.  Se definida como 0, a validação será executada.
      </string>
    </stringTable>
    <presentationTable>
      <presentation id="PNRP_SeedServer_1">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Servidor Semente</label>
        </textBox>
        <checkBox refId="PNRP_Dont_Include_Microsoft_SeedServer">Não usar o servidor semente global PNRP da Microsoft</checkBox>
      </presentation>
      <presentation id="PNRP_SeedServer_2">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Servidor Semente</label>
        </textBox>
      </presentation>
      <presentation id="PNRP_SeedServer_3">
        <textBox refId="PNRP_SeedServer_Name">
          <label>Servidor Semente</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
