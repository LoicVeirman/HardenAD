<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>digite o nome para exibição aqui</displayName>
  <description>digite a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="DFSDiscoverDC">Configura a frequência com que um cliente do DFS descobre os controladores de domínio</string>
      <string id="DFSDiscoverDC_Help">Esta configuração de política permite definir a frequência com que um cliente do sistema de arquivos distribuídos (DFS) tenta descobrir controladores de domínio em uma rede. Por padrão, um cliente DFS tenta descobrir controladores de domínio a cada 15 minutos.

Se habilitar esta configuração de política, você poderá configurar a frequência com que um cliente DFS tenta descobrir controladores de domínio. Este valor é especificado em minutos.

Se você desabilitar ou não definir esta configuração de política, o valor padrão de 15 minutos será aplicado.

Observação: o valor mínimo que pode ser selecionado é 15 minutos. Se você tentar definir essa configuração para um valor menor que 15 minutos, será aplicado o valor padrão de 15 minutos.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DFSDiscoverDC">
        <decimalTextBox refId="DFSDiscoverDialog" defaultValue="15">Tempo em minutos:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
