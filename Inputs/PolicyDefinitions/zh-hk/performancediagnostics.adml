<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="BootScenarioCategory">Windows 開機效能診斷</string>
      <string id="BootScenarioExecutionPolicyExplain">決定 Windows 開機效能診斷的執行層級。

如果您啟用這個原則設定，則必須從下拉式功能表選擇一個執行層級。如果您選取只偵測問題並進行疑難排解，Diagnostic Policy Service (DPS) 將偵測 Windows 開機效能問題，並嘗試判斷其根本原因。系統會在偵測到這些根本原因時將之記錄到事件日誌，但不會採取任何修正操作。如果您選取偵測、疑難排解及解析，DPS 會偵測 Windows 開機效能問題，並指出使用者可以使用的輔助解決方式。

如果您停用這個原則設定，Windows 將無法偵測、疑難排解或解析任何由 DPS 所處理的 Windows 開機效能問題。

如果沒有進行此原則設定，DPS 預設會啟用 Windows 開機效能以進行解析。

這個原則設定只有在未設定整個診斷狀況執行原則時才會生效。

系統或服務都不需重新啟動，此原則即可生效:變更會立即生效。

這個原則設定只有在 Diagnostic Policy Service 處於執行中狀態時才會生效。當服務停止或停用時，不會執行診斷狀況。您可以使用 Microsoft Management Console 中的 [服務] 嵌入式管理單元來設定 DPS。</string>
      <string id="ResumeScenarioCategory">Windows 待命/繼續效能診斷</string>
      <string id="ResumeScenarioExecutionPolicyExplain">決定 Windows 待命/繼續效能診斷的執行層級。

如果您啟用這個原則設定，則必須從下拉式功能表選擇一個執行層級。如果您選取只偵測問題並進行疑難排解，Diagnostic Policy Service (DPS) 將偵測 Windows 待命/繼續效能問題，並嘗試判斷其根本原因。系統會在偵測到這些根本原因時將之記錄到事件日誌，但不會採取任何修正操作。如果您選取偵測、疑難排解及解析，DPS 會偵測 Windows 待命/繼續效能問題，並指出使用者可以使用的輔助解決方式。

如果您停用這個原則設定，Windows 將無法偵測、疑難排解或解析任何由 DPS 所處理的 Windows 待命/繼續效能問題。

如果沒有進行此原則設定，DPS 預設會啟用 Windows 待命/繼續效能以進行解析。

這個原則設定只有在未設定整個診斷狀況執行原則時才會生效。

系統或服務都不需重新啟動，此原則即可生效:變更會立即生效。

這個原則設定只有在 Diagnostic Policy Service 處於執行中狀態時才會生效。當服務停止或停用時，不會執行診斷狀況。您可以使用 Microsoft Management Console 中的 [服務] 嵌入式管理單元來設定 DPS。</string>
      <string id="ShellScenarioCategory">Windows 系統回應效能診斷</string>
      <string id="ShellScenarioExecutionPolicyExplain">決定 Windows 系統回應診斷的執行層級。

如果您啟用這個原則設定，則必須從下拉式功能表選擇一個執行層級。如果您選取只偵測問題並進行疑難排解，Diagnostic Policy Service (DPS) 將偵測 Windows 系統回應問題，並嘗試判斷其根本原因。系統會在偵測到這些根本原因時將之記錄到事件日誌，但不會採取任何修正操作。如果您選取偵測、疑難排解及解析，DPS 會偵測 Windows 系統回應問題，並指出使用者可以使用的輔助解決方式。

如果您停用這個原則設定，Windows 將無法偵測、疑難排解或解析任何由 DPS 所處理的 Windows 系統回應問題。

如果沒有進行此原則設定，DPS 預設會啟用 Windows 系統回應以進行解析。

這個原則設定只有在未設定整個診斷狀況執行原則時才會生效。

系統或服務都不需重新啟動，此原則即可生效:變更會立即生效。

這個原則設定只有在 Diagnostic Policy Service 處於執行中狀態時才會生效。當服務停止或停用時，不會執行診斷狀況。您可以使用 Microsoft Management Console 中的 [服務] 嵌入式管理單元來設定 DPS。</string>
      <string id="ShutdownScenarioCategory">Windows 關機效能診斷</string>
      <string id="ShutdownScenarioExecutionPolicyExplain">決定 Windows 關機效能診斷的執行層級。

如果您啟用這個原則設定，則必須從下拉式功能表選擇一個執行層級。如果您選取只偵測問題並進行疑難排解，Diagnostic Policy Service (DPS) 將偵測 Windows 關機效能問題，並嘗試判斷其根本原因。系統會在偵測到這些根本原因時將之記錄到事件日誌，但不會採取任何修正操作。如果您選取偵測、疑難排解及解析，DPS 會偵測 Windows 關機效能問題，並指出使用者可以使用的輔助解決方式。

如果您停用這個原則設定，Windows 將無法偵測、疑難排解或解析任何由 DPS 所處理的 Windows 關機效能問題。

如果沒有進行此原則設定，DPS 預設會啟用 Windows 關機效能以進行解析。

這個原則設定只有在未設定整個診斷狀況執行原則時才會生效。

系統或服務都不需重新啟動，此原則即可生效:變更會立即生效。

這個原則設定只有在 Diagnostic Policy Service 處於執行中狀態時才會生效。當服務停止或停用時，不會執行診斷狀況。您可以使用 Microsoft Management Console 中的 [服務] 嵌入式管理單元來設定 DPS。</string>
      <string id="WdiScenarioExecutionPolicy">設定狀況執行層級</string>
      <string id="WdiScenarioExecutionPolicyLevelResolution">偵測、疑難排解及解析</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">僅進行偵測與疑難排解</string>
    </stringTable>
    <presentationTable>
      <presentation id="WdiScenarioExecutionPolicy_1">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">狀況執行層級</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_2">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">狀況執行層級</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_3">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">狀況執行層級</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_4">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">狀況執行層級</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
