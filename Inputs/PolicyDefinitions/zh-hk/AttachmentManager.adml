<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="AM_AM">附件管理員</string>
      <string id="AM_CallIOfficeAntiVirus">開啟附件時通知防毒程式</string>
      <string id="AM_EstimateFileHandlerRisk">檔案附件的信任邏輯</string>
      <string id="AM_ExplainCallIOfficeAntiVirus">這個原則設定可讓您管理已登錄防毒程式的通知行為。如果已登錄多種程式，將會全部通知。如果已登錄的防毒程式已經執行即時檢查或即時掃描傳送到電腦的電子郵件伺服器上的所有檔案，任何其他呼叫都是多餘的。

如果您啟用這個原則設定，Windows 會命令已登錄的防毒程式在使用者開啟附件檔案時掃描該檔案。如果防毒程式失敗，將無法開啟附件。

如果您停用這個原則設定，Windows 就不會在開啟附件檔案時呼叫已登錄防毒程式。

如果您未設定這個原則設定，Windows 就不會在開啟附件檔案時呼叫已登錄防毒程式。</string>
      <string id="AM_ExplainEstimateFileHandlerRisk">這個原則設定可以讓您設定 Windows 判斷附件檔案風險所使用的邏輯。

偏好檔案處理常式指示 Windows 使用檔案處理常式資料，而不要使用檔案類型資料。例如，信任 notepad.exe，但是不要信任 .txt 檔案。

偏好檔案類型指示 Windows 使用檔案類型資料，而不要使用檔案處理常式資料。例如，無論檔案處理常式為何，都信任 .txt 檔案。

同時使用檔案處理常式及類型資料是最嚴格的選項。Windows 選擇比較嚴格的建議來代替其他選項，這將會造成使用者看到較多的信任提示。

如果您啟用這個原則設定，您可以選擇 Windows 處理風險評定資料的順序。

如果您停用這個原則設定，Windows 會使用偏好以檔案處理常式代替檔案類型的預設信任邏輯。

如果您未設定這個原則設定，Windows 會使用偏好以檔案處理常式代替檔案類型的預設信任邏輯。</string>
      <string id="AM_ExplainMarkZoneOnSavedAtttachments">這個原則設定可以讓您管理 Windows 是否用資訊的來源區域來標示附件檔案 (例如，受限、網際網路、內部網路、本機)。這必須透過 NTFS 才能正確運作，在 FAT32 上會失敗，不會另外通知。如果沒有保留區域資訊，Windows 無法正確評定風險。

如果您啟用這個原則設定，Windows 就不會標示附件檔案的區域資訊。

如果您停用這個原則設定，Windows 會標示附件檔案的區域資訊。

如果您未設定這個原則設定，Windows 會標示附件檔案的區域資訊。</string>
      <string id="AM_ExplainRemoveZoneInfo">這個原則設定可以讓您管理使用者是否可以透過按一下檔案內容頁上的 [解除封鎖] 按鈕，或使用安全警告對話方塊裡的核取方塊，手動移除已儲存附件檔案的區域資訊。移除區域資訊讓使用者可以開啟 Windows 禁止使用者開啟、具有潛在危險的附件檔案。

如果您啟用這個原則設定，Windows 會隱藏核取方塊以及 [解除封鎖] 按鈕。

如果您停用這個原則設定，Windows 會顯示核取方塊以及 [解除封鎖] 按鈕。

如果您未設定這個原則設定，Windows 會隱藏核取方塊以及 [解除封鎖] 按鈕。</string>
      <string id="AM_ExplainSetFileRiskLevel">這個原則設定可以讓您管理檔案類型的預設風險層級。如果要完全自訂檔案附件的風險層級，您可能還需要設定檔案附件的信任邏輯。

高度風險: 如果附件是在高度風險檔案類型的清單裡，而且來自受限區域，Windows 就會禁止使用者存取該檔案。如果檔案是來自網際網路區域，Windows 就會在使用者存取檔案前先提示使用者。

中度風險: 如果附件是在中度風險檔案類型的清單裡，而且來自受限區域或網際網路區域，Windows 就會在使用者存取檔案前先提示使用者。

低度風險: 如果附件是在低度風險檔案類型清單裡，無論檔案的區域資訊為何，Windows 都不會在使用者存取檔案前先提示使用者。

如果您啟用這個原則設定，就可以指定檔案類型的預設風險層級。

如果您停用這個原則設定，Windows 就會將預設風險層級設定為中度。

如果您未設定這個原則設定，Windows 就會設定將預設風險層級設定為中度。</string>
      <string id="AM_ExplainSetHighRiskInclusion">這個原則設定可以讓您設定高度風險檔案類型的清單。如果附件是在高度風險檔案類型的清單裡，而且來自受限區域，Windows 就會禁止使用者存取該檔案。如果檔案是來自網際網路區域，Windows 就會在使用者存取檔案前先提示使用者。這個包含清單優先於中度風險和低度風險的包含清單 (副檔名會列在多個包含清單裡)。

如果您啟用這個原則設定，就可以建立一個自訂的高度風險檔案類型清單。

如果您停用這個原則設定，Windows 就會使用內建的高度風險檔案類型清單。

如果您未設定這個原則設定，Windows 就會使用內建的高度風險檔案類型清單。</string>
      <string id="AM_ExplainSetLowRiskInclusion">這個原則設定可以讓您設定低度風險檔案類型的清單。如果附件是在低度風險檔案類型清單裡，無論檔案的區域資訊為何，Windows 都不會在使用者存取檔案前先提示使用者。這個包含清單會覆寫 Windows 的內建高度風險檔案類型清單，且其優先順序低於高度風險或中度風險包含清單 (副檔名會列在多個包含清單裡)。

如果您啟用這個原則設定，就可以指定低度風險的檔案類型。

如果您停用這個原則設定，Windows 就會使用預設信任邏輯。

如果您未設定這個原則設定，Windows 就會使用預設信任邏輯。</string>
      <string id="AM_ExplainSetModRiskInclusion">這個原則設定可以讓您設定中度風險檔案類型的清單。如果附件是在中度風險檔案類型的清單裡，而且來自受限區域或網際網路區域，Windows 就會在使用者存取檔案前先提示使用者。這個包含清單會覆寫 Windows 的內建潛在高度風險檔案類型清單，而且它的優先順序高於低度風險包含清單，但低於高度風險包含清單 (副檔名會列在多個包含清單裡)。

如果您啟用這個原則設定，就可以指定中度風險的檔案類型。

如果您停用這個原則設定，Windows 就會使用預設信任邏輯。

如果您未設定這個原則設定，Windows 就會使用預設信任邏輯。</string>
      <string id="AM_HandlerBased">偏好檔案處理常式</string>
      <string id="AM_HandlerDisabled">偏好檔案類型</string>
      <string id="AM_HandlerMostSecure">查詢檔案處理常式和類型</string>
      <string id="AM_HighRisk">高度風險</string>
      <string id="AM_LowRisk">低度風險</string>
      <string id="AM_MarkZoneOnSavedAtttachments">不要保留檔案附件的區域資訊</string>
      <string id="AM_ModRisk">中度風險</string>
      <string id="AM_RemoveZoneInfo">隱藏移除區域資訊的機制</string>
      <string id="AM_SetFileRiskLevel">檔案附件的預設風險層級</string>
      <string id="AM_SetHighRiskInclusion">高度風險檔案類型的包含清單</string>
      <string id="AM_SetLowRiskInclusion">低度風險檔案類型的包含清單</string>
      <string id="AM_SetModRiskInclusion">中度風險檔案類型的包含清單</string>
    </stringTable>
    <presentationTable>
      <presentation id="AM_EstimateFileHandlerRisk">
        <dropdownList refId="AM_TrustLogicSetting" defaultItem="0">判定風險的依據為</dropdownList>
      </presentation>
      <presentation id="AM_SetFileRiskLevel">
        <dropdownList refId="AM_RiskLevel" defaultItem="1">設定預設風險層級</dropdownList>
      </presentation>
      <presentation id="AM_SetHighRiskInclusion">
        <textBox refId="AM_InstructHighRiskInclusionList">
          <label>指定高度風險副檔名 (包含前置句點，例如 .cmd;.exe;)。</label>
        </textBox>
      </presentation>
      <presentation id="AM_SetLowRiskInclusion">
        <textBox refId="AM_InstructLowRiskInclusionList">
          <label>指定低度風險副檔名 (包含前置句點，例如 .bmp;.gif;)。</label>
        </textBox>
      </presentation>
      <presentation id="AM_SetModRiskInclusion">
        <textBox refId="AM_InstructModRiskInclusionList">
          <label>指定中度風險副檔名 (包含前置句點，例如 .doc;.pdf;)。</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
