<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>復原</displayName>
  <description>復原</description>
  <resources>
    <stringTable>
      <string id="WinRE">復原</string>
      <string id="ConfigureWinRESetup">允許將系統還原到預設狀態</string>
      <string id="ConfigureWinRESetup_help">        需求: Windows 7
     描述: 這個原則設定會控制使用者能否存取 [復原] (在 [控制台] 中) 中的選項，將電腦還原到原始狀態，或從使用者建立的系統映像還原。

    如果您啟用或未設定這個原則設定，[復原] (在 [控制台] 中) 的 [進階復原方法] 頁面上會顯示 [使用先前建立的系統映像來復原電腦] 和 [重新安裝 Windows] (或 [將電腦回復出廠狀態]) 項目，讓使用者能夠將電腦還原到原始狀態，或從使用者建立的系統映像還原。這是預設值。

    如果您停用這個原則設定，則 [復原] (在 [控制台] 中) 中沒有 [使用先前建立的系統映像來復原電腦] 和 [重新安裝 Windows] (或 [將電腦回復出廠狀態]) 項目。但是在這個原則設定為停用狀態下，使用者仍然可以重新啟動電腦，然後存取 [系統修復選項] 功能表 (如果可使用)，來將電腦還原到原始狀態，或從使用者建立的系統映像還原。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
