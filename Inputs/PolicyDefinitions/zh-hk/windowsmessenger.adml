<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">不要一開始就自動啟動 Windows Messenger</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">這個原則設定可防止在登入時自動執行 Windows Messenger。

如果您啟用這個原則設定，當使用者登入時，不會自動載入 Windows Messenger。

如果您停用或未設定這個原則設定，會在登入時自動載入 Windows Messenger。

注意: 這個原則設定只是單純防止一開始就啟動 Windows Messenger。如果使用者當時自行叫用並使用 Windows Messenger，還是會載入 Windows Messenger。

使用者也可以在 Windows Messenger 使用者介面的 [工具] 功能表的 [喜好設定] 索引標籤中設定這個行為。

注意: 如果您不希望使用者使用 Windows Messenger，請啟用 [不允許執行 Windows Messenger] 原則設定。

注意: 您可以同時在 [電腦設定] 及 [使用者設定] 中使用這個原則設定。如果同時在這兩處設定這個原則設定，[電腦設定] 中的設定值優先順序較高。</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">不要一開始就自動啟動 Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Comp">不允許執行 Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">這個原則設定可讓您防止執行 Windows Messenger。

如果您啟用這個原則設定，Windows Messenger 不會執行。

如果您停用或未設定這個原則設定，就可以使用 Windows Messenger。

注意: 如果您啟用這個原則設定，遠端協助也無法使用 Windows Messenger。

注意: 您可以同時在 [電腦設定] 及 [使用者設定] 中使用這個原則設定。如果同時在這兩處設定這個原則設定，[電腦設定] 中的設定值優先順序較高。</string>
      <string id="WinMSG_NoWindowsMsg_User">不允許執行 Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
