<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2012 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <!-- General -->
      <string id="SettingSyncCategory">同步您的設定</string>

      <!-- Main policy -->
      <string id="DisableSettingSync">不要同步</string>
      <string id="DisableSettingSync_Help">禁止與這部電腦進行雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [同步您的設定] 開關。

如果您啟用這個原則設定，[同步您的設定] 會關閉，且不會同步這部電腦上的任何 [同步您的設定] 群組。

使用 [允許使用者開啟同步] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，[同步您的設定] 預設會開啟而且可由使用者加以設定。</string>

      <!-- Application Settings -->
      <string id="DisableApplicationSettingSync">不要同步應用程式設定</string>
      <string id="DisableApplicationSettingSync_Help">禁止與這部電腦進行 [應用程式設定] 群組雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [應用程式設定] 群組。

如果您啟用這個原則設定，不會同步 [應用程式設定] 群組。

使用 [允許使用者開啟同步應用程式設定] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，同步 [應用程式設定] 群組預設會開啟而且可由使用者加以設定。</string>

      <!-- Credentials -->
      <string id="DisableCredentialsSettingSync">不要同步密碼</string>
      <string id="DisableCredentialsSettingSync_Help">禁止與這部電腦進行 [密碼] 群組雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [密碼] 群組。

如果您啟用這個原則設定，不會同步 [密碼] 群組。

使用 [允許使用者開啟同步密碼] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，同步 [密碼] 群組預設會開啟而且可由使用者加以設定。</string>

      <!-- Personalization -->
      <string id="DisablePersonalizationSettingSync">不要同步個人化</string>
      <string id="DisablePersonalizationSettingSync_Help">禁止與這部電腦進行 [個人化] 群組雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [個人化] 群組。

如果您啟用這個原則設定，不會同步 [個人化] 群組。

使用 [允許使用者開啟同步個人化] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，同步 [個人化] 群組預設會開啟而且可由使用者加以設定。</string>

      <!-- AppSync -->
      <string id="DisableAppSyncSettingSync">不要同步應用程式</string>
      <string id="DisableAppSyncSettingSync_Help">
        
        禁止與這部電腦進行 "AppSync" 群組雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 "AppSync" 群組。

如果您啟用這個原則設定，不會同步 "AppSync" 群組。

使用 [允許使用者開啟應用程式同步] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，同步 "AppSync" 群組預設會開啟而且可由使用者加以設定。
      
      </string>

      <!-- Windows -->
      <string id="DisableWindowsSettingSync">不要同步其他 Windows 設定</string>
      <string id="DisableWindowsSettingSync_Help">禁止與這部電腦進行 [其他 Windows 設定] 群組雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [其他 Windows 設定] 群組。

如果您啟用這個原則設定，不會同步 [其他 Windows 設定] 群組。

使用 [允許使用者開啟同步其他 Windows 設定] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，同步 [其他 Windows 設定] 群組預設會開啟而且可由使用者加以設定。</string>

      <!-- Desktop Theme -->
      <string id="DisableDesktopThemeSettingSync">不要同步桌面個人化</string>
      <string id="DisableDesktopThemeSettingSync_Help">禁止與這部電腦進行 [桌面個人化] 群組雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [桌面個人化] 群組。

如果您啟用這個原則設定，不會同步 [桌面個人化] 群組。

使用 [允許使用者開啟同步桌面個人化] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，同步 [桌面個人化] 群組預設會開啟而且可由使用者加以設定。
      
      </string>

      <!-- WebBrowser -->
      <string id="DisableWebBrowserSettingSync">不要同步瀏覽器設定</string>
      <string id="DisableWebBrowserSettingSync_Help">禁止與這部電腦進行 [瀏覽器] 群組雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [瀏覽器] 群組。 [瀏覽器] 群組包含如歷程記錄和我的最愛等設定及資訊。

如果您啟用這個原則設定，不會同步 [瀏覽器] 群組 (如歷程記錄和我的最愛等資訊)。

使用 [允許使用者開啟同步瀏覽器] 選項讓同步預設為關閉但不停用。

如果您未設定或停用這個設定，同步 [瀏覽器] 群組預設會開啟而且可由使用者加以設定。
      
      </string>

      <!-- Paid Networks -->
      <string id="DisableSyncOnPaidNetwork">不要在計量付費連線上進行同步</string>
      <string id="DisableSyncOnPaidNetwork_Help">使用計量付費網際網路連線時，禁止與這部電腦進行雙向同步。 這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [在計量付費連線上同步設定] 開關。

如果您啟用這個原則設定，會關閉在計量付費連線上進行同步，當這部電腦使用計量付費連線時不會進行同步。

如果您未設定或停用這個設定，使用者可自行設定在計量付費連線上進行同步。
      
      </string>

      <!-- StartLayout -->
      <string id="DisableStartLayoutSettingSync">不要同步開始設定</string>
      <string id="DisableStartLayoutSettingSync_Help">
        
        禁止與這部電腦進行 [開始配置] 群組雙向同步。這會關閉和停用 [電腦設定] 中 [同步您的設定] 頁面上的 [開始配置] 群組。

        如果您啟用這個原則設定，不會同步 [開始配置] 群組。

        使用 [允許使用者開啟同步開始] 選項讓同步預設為關閉但不停用。

        如果您未設定或停用這個設定，同步 [開始配置] 群組預設會開啟而且可由使用者加以設定。
      
      </string>
    </stringTable>
    <presentationTable>

      <!-- Main policy -->
      <presentation id="DisableSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步。</checkBox>
      </presentation>

      <!-- Application Settings -->
      <presentation id="DisableApplicationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步應用程式設定。</checkBox>
      </presentation>

      <!-- Credentials -->
      <presentation id="DisableCredentialsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步密碼。</checkBox>
      </presentation>

      <!-- Personalization -->
      <presentation id="DisablePersonalizationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步個人化。</checkBox>
      </presentation>

      <!-- AppSync -->
      <presentation id="DisableAppSyncSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步 AppSync。</checkBox>
      </presentation>

      <!-- Windows -->
      <presentation id="DisableWindowsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步其他 Windows 設定。</checkBox>
      </presentation>

      <!-- Desktop Theme -->
      <presentation id="DisableDesktopThemeSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步桌面個人化。</checkBox>
      </presentation>

      <!-- WebBrowser -->
      <presentation id="DisableWebBrowserSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步瀏覽器。</checkBox>
      </presentation>
      
      <!-- StartLayout -->
      <presentation id="DisableStartLayoutSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">允許使用者開啟同步開始配置。</checkBox>
      </presentation>

    </presentationTable>
  </resources>
</policyDefinitionResources>
