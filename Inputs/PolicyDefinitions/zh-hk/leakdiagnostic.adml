<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="WdiScenarioCategory">Windows 記憶體遺漏診斷</string>
      <string id="WdiScenarioExecutionPolicy">設定狀況執行層級</string>
      <string id="WdiScenarioExecutionPolicyExplain">這個原則設定可決定「診斷原則服務」(DPS) 是否會診斷記憶體遺漏問題。

如果您啟用或未設定這個原則設定，DPS 預設會啟用「Windows 記憶體遺漏診斷」。

如果您停用這個原則設定，則 DPS 不能診斷記憶體遺漏問題。

這個原則設定只會在下列情況下生效:
-- 如果未設定診斷範圍內的狀況執行原則時。
-- 當「診斷原則服務」處於執行狀態時。當服務停止或已停用時，就不會執行診斷狀況。

注意: DPS 可透過 Microsoft Management Console 的 [服務] 嵌入式管理單元來設定。

此原則不需重新啟動作業系統或服務即可生效。變更會立即生效。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
