<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="ProcessTSUserLogonAsync">透過遠端桌面服務登入時，允許非同步使用者群組原則處理

</string>
      <string id="ProcessTSUserLogonAsync_Help">透過遠端桌面服務登入時，這個原則設定允許 Microsoft Windows 非同步處理使用者群組原則設定。Windows Vista 及 Windows XP 的預設處理模式為非同步處理使用者群組原則設定。

根據預設值，Window Server 會同步處理使用者群組原則設定。

如果您啟用此原則設定，透過遠端桌面服務登入時，Windows 就會非同步套用使用者群組原則設定。

如果您停用或未設定這個原則設定，Windows Server 就會同步套用使用者群組原則設定。

注意: 此原則設定只會套用至執行遠端桌面服務的電腦。</string>
      <string id="PolicyPolicies">群組原則</string>
    </stringTable>  
</resources>
</policyDefinitionResources>
