<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="QosDBMC_BestEffort_Help">指定有最佳成就服務類型 (ServiceTypeBestEffort) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於符合流量規格的封包。

如果您啟用這個設定，您可以變更與最佳成就服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMC_ControlledLoad_Help">指定有受控制的載入服務類型 (ServiceTypeControlledLoad) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於符合流量規格的封包。

如果您啟用這個設定，您可以變更與受控制的載入服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 24 (0x18)。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMC_Guaranteed_Help">指定有保證服務類型 (ServiceTypeGuaranteed) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於符合流量規格的封包。

如果您啟用這個設定，您可以變更與保證服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 40 (0x28)。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMC_NetworkControl_Help">指定有網路控制服務類型 (ServiceTypeNetworkControl) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於符合流量規格的封包。

如果您啟用這個設定，您可以變更與網路控制服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 48 (0x30)。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMC_Qualitative_Help">指定有性質服務類型 (ServiceTypeQualitative) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於符合流量規格的封包。

如果您啟用這個設定，您可以變更與性質服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMNC_BestEffort_Help">指定有最佳成就服務類型 (ServiceTypeBestEffort) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於不符合流量規格的封包。

如果您啟用這個設定，您可以變更與最佳成就服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMNC_ControlledLoad_Help">指定有受控制的載入服務類型 (ServiceTypeControlledLoad) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於不符合流量規格的封包。

如果您啟用這個設定，您可以變更與受控制的載入服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMNC_Guaranteed_Help">指定有保證服務類型 (ServiceTypeGuaranteed) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於不符合流量規格的封包。

如果您啟用這個設定，您可以變更與保證服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMNC_NetworkControl_Help">指定有網路控制服務類型 (ServiceTypeNetworkControl) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於不符合流量規格的封包。

如果您啟用這個設定，您可以變更與網路控制服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDBMNC_Qualitative_Help">指定有性質服務類型 (ServiceTypeQualitative) 之封包的備用 Layer-3 Differentiated Services Code Point (DSCP) 值。封包排程器將對應的 DSCP 數值插入封包的 IP 標頭。

此設定只適用於不符合流量規格的封包。

如果您啟用這個設定，您可以變更與性質服務類型相關聯的 DSCP 預設值。

如果您停用這個設定，系統會採用 DSCP 的預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 DSCP 數值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosDiffservByteMappingConforming">合格封包的 DSCP 數值</string>
      <string id="QosDiffservByteMappingNonConforming">不合格封包的 DSCP 數值</string>
      <string id="QosMaxOutstandingSends">限制未送的封包</string>
      <string id="QosMaxOutstandingSends_Help">指定系統許可的未送封包最大數目。當未送封包數目達到此限制時，[封包排程器] 會延後網路卡的所有子任務，直到封包數目下降到限制值以下為止。

[未送封包] 是指 [封包排程器] 已提交到網路卡上等待傳輸、但尚未送出的封包。

如果您啟用這個設定，您就可以限制未送封包的數目。

如果您停用或未設定這個設定，則這個設定不會影響系統。

重要事項: 如果已經在登錄中為特定網路卡指定未送封包最大數目，則在設定網路卡時，將略過此設定。</string>
      <string id="QosNonBestEffortLimit">限制可保留的頻寬</string>
      <string id="QosNonBestEffortLimit_Help">決定系統可保留的連線頻寬百分比。這個數值會限制系統上所有執行程式的連結頻寬保留值。

根據預設，[封包排程器] 會將系統連線頻寬限制在 80%，不過您可以使用此設定來覆寫預設值。

如果您啟用這個設定，您可以使用 [頻寬限制] 方塊來調整系統可保留的總頻寬。

如果您停用或未設定這個設定，系統就會使用預設值，也就是連線的 80%。

重要事項: 如果已經在登錄中為特定網路卡設定頻寬限制，則在設定網路卡時，將略過此設定。</string>
      <string id="QosPsched">QoS 封包排程器</string>
      <string id="QosServiceTypeBestEffort_C">最佳成就服務類型</string>
      <string id="QosServiceTypeBestEffort_NC">最佳成就服務類型</string>
      <string id="QosServiceTypeBestEffort_PV">最佳成就服務類型</string>
      <string id="QosServiceTypeControlledLoad_C">控制載入服務類型</string>
      <string id="QosServiceTypeControlledLoad_NC">控制載入服務類型</string>
      <string id="QosServiceTypeControlledLoad_PV">控制載入服務類型</string>
      <string id="QosServiceTypeGuaranteed_C">保證服務類型</string>
      <string id="QosServiceTypeGuaranteed_NC">保證服務類型</string>
      <string id="QosServiceTypeGuaranteed_PV">保證服務類型</string>
      <string id="QosServiceTypeNetworkControl_C">網路控制服務類型</string>
      <string id="QosServiceTypeNetworkControl_NC">網路控制服務類型</string>
      <string id="QosServiceTypeNetworkControl_PV">網路控制服務類型</string>
      <string id="QosServiceTypeNonConforming">不合格的封包</string>
      <string id="QosServiceTypeQualitative_C">性質服務類型</string>
      <string id="QosServiceTypeQualitative_NC">性質服務類型</string>
      <string id="QosServiceTypeQualitative_PV">性質服務類型</string>
      <string id="QosTimerResolution">設定計時器解析</string>
      <string id="QosTimerResolution_Help">決定 [封包排程器] 在排定封包傳輸時所使用的最小時間單位。[封包排程器] 在排定封包傳輸時所用的頻率不能比此項目之數值所允許的頻率還要頻繁。

如果您啟用這個設定，您就可以覆寫系統預設的計時器解析，通常以 10 毫秒為單位。

如果您停用或未設定這個設定，這個設定並不會影響系統。

重要事項: 如果已經在登錄中為特定網路卡指定計時器解析，則在設定網路卡時，將略過此設定。</string>
      <string id="QosUPM_BestEffort_Help">指定有最佳成就服務類型 (ServiceTypeBestEffort) 封包的備用連結層 (Layer-2) 優先順序值。封包排程器會將對應的優先順序值插入封包的 Layer-2 標頭。

如果您啟用這個設定，您可以變更與最佳成就服務類型相關聯的優先順序預設值。

如果您停用這個設定，系統將使用優先順序預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 Layer-2 優先順序值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosUPM_ControlledLoad_Help">指定有受控制的載入服務類型 (ServiceTypeControlledLoad) 封包的備用連結層 (Layer-2) 優先順序值。封包排程器會將對應的優先順序值插入封包的 Layer-2 標頭。

如果您啟用這個設定，您可以變更與受控制的載入服務類型相關聯的優先順序預設值。

如果您停用這個設定，系統將使用優先順序預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 Layer-2 優先順序值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosUPM_Guaranteed_Help">指定有保證服務類型 (ServiceTypeGuaranteed) 封包的備用連結層 (Layer-2) 優先順序值。封包排程器會將對應的優先順序值插入封包的 Layer-2 標頭。

如果您啟用這個設定，您可以變更與保證服務類型相關聯的優先順序預設值。

如果您停用這個設定，系統將使用優先順序預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 Layer-2 優先順序值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosUPM_NetworkControl_Help">指定有網路控制服務類型 (ServiceTypeNetworkControl) 封包的備用連結層 (Layer-2) 優先順序值。封包排程器會將對應的優先順序值插入封包的 Layer-2 標頭。

如果您啟用這個設定，您可以變更與網路控制服務類型相關聯的優先順序預設值。

如果您停用這個設定，系統將使用優先順序預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 Layer-2 優先順序值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosUPM_NonConforming_Help">指定與流量規格不相符之封包的備用連結層 (Layer-2) 優先順序值。封包排程器會將對應的優先順序值插入封包的 Layer-2 標頭。

如果您啟用這個設定，您可以變更與不一致封包相關聯的優先順序預設值。

如果您停用這個設定，系統將使用優先順序預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定不一致封包的 Layer-2 優先順序值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosUPM_Qualitative_Help">指定有性質服務類型 (ServiceTypeQualitative) 封包的備用連結層 (Layer-2) 優先順序值。封包排程器會將對應的優先順序值插入封包的 Layer-2 標頭。

如果您啟用這個設定，您可以變更與性質服務類型相關聯的優先順序預設值。

如果您停用這個設定，系統將使用優先順序預設值 0。

重要事項: 如果已經在登錄中為特定網路卡指定此服務類型的 Layer-2 優先順序值，則在設定網路卡時，將略過此設定。</string>
      <string id="QosUserPriorityMapping">層級-2 優先順序值</string>
    </stringTable>
    <presentationTable>
      <presentation id="QosMaxOutstandingSends">
        <decimalTextBox refId="QosMaxOutstandingSends_Box" defaultValue="65535">封包數目:    </decimalTextBox>
      </presentation>
      <presentation id="QosNonBestEffortLimit">
        <decimalTextBox refId="QosNonBestEffortLimit_Box" defaultValue="80" spinStep="10">頻寬限制 (%):    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeBestEffort_C">
        <text>輸入 DSCP 數值合格封包，包含  </text>
        <text>最佳成就服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeBestEffort_NC">
        <text>輸入 DSCP 數值不合格封包，包含  </text>
        <text>最佳成就服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeBestEffort_PV">
        <text>輸入優先順序值給封包，包含  </text>
        <text>最佳成就服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="0">優先順序值    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeControlledLoad_C">
        <text>輸入 DSCP 數值合格封包，包含  </text>
        <text>控制載入服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="24">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeControlledLoad_NC">
        <text>輸入 DSCP 數值不合格封包，包含  </text>
        <text>控制載入服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeControlledLoad_PV">
        <text>輸入優先順序值給封包，包含  </text>
        <text>控制載入服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="4">優先順序值    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeGuaranteed_C">
        <text>輸入 DSCP 數值合格封包，包含  </text>
        <text>保證服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="40">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeGuaranteed_NC">
        <text>輸入 DSCP 數值不合格封包，包含  </text>
        <text>保證服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeGuaranteed_PV">
        <text>輸入優先順序值給封包，包含  </text>
        <text>保證服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="5">優先順序值    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNetworkControl_C">
        <text>輸入 DSCP 數值合格封包，包含  </text>
        <text>網路控制服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="48">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNetworkControl_NC">
        <text>輸入 DSCP 數值不合格封包，包含  </text>
        <text>網路控制服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNetworkControl_PV">
        <text>輸入優先順序值給封包，包含  </text>
        <text>網路控制服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="7">優先順序值    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeNonConforming">
        <text>為不合格封包輸入優先順序值。</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue">優先順序值    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeQualitative_C">
        <text>輸入 DSCP 數值合格封包，包含  </text>
        <text>性質服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeQualitative_NC">
        <text>輸入 DSCP 數值不合格封包，包含  </text>
        <text>性質服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosDSCPValue" defaultValue="0">DSCP 數值:    </decimalTextBox>
      </presentation>
      <presentation id="QosServiceTypeQualitative_PV">
        <text>輸入優先順序值給封包，包含  </text>
        <text>性質服務類型</text>
        <text> </text>
        <decimalTextBox refId="QosPriorityValue" defaultValue="0">優先順序值    </decimalTextBox>
      </presentation>
      <presentation id="QosTimerResolution">
        <decimalTextBox refId="QosTimerResolution_Box" defaultValue="10">計時器單位 (毫秒):    </decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
