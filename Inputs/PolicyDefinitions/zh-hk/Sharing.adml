<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="NoInplaceSharing">防止使用者共用其設定檔內的檔案。</string>
      <string id="NoInplaceSharing_Help">這個原則設定會指定使用者是否可以共用設定檔內的檔案。根據預設，系統管理員在電腦中加以選擇之後，便允許使用者與其網路上的其他使用者共用他們設定檔內的檔案。 系統管理員可以使用共用精靈，在電腦上進行選擇，以共用使用者設定檔內的檔案。

如果您啟用這個原則設定，使用者將不能使用共用精靈，來共用其設定檔內的檔案。 同時，共用精靈也不會在 %root%users 上建立共用，而且該精靈只可用來在資料夾上建立 SMB 共用。

如果您停用或未設定這個原則設定，系統管理員在電腦中加以選擇之後，使用者便可共用其使用者設定檔中的檔案。</string>
      <string id="Sharing">網路共用</string>
      <string id="Sharing_Help">包含控制檔案與資料夾共用行為的設定。</string>
      <string id="DisableHomeGroup">防止電腦加入家用群組</string>
      <string id="DisableHomeGroup_Help">這個原則設定會指定使用者是否可以將電腦新增至家用群組。根據預設，使用者可以將他們的電腦新增到私人網路上的家用群組。

如果您啟用這個原則設定，使用者就無法將電腦新增至家用群組。這個原則設定不影響其他網路共用功能。

如果您停用或未設定這個原則設定，則使用者可以將電腦新增到家用群組。但是已加入網域的電腦上的資料不會與家用群組共用。

預設不會設定這個原則設定。

您必須重新啟動電腦，這個原則設定才會生效。</string>
      <string id="HomeGroup">HomeGroup</string>
      <string id="HomeGroup_Help">含有控制 HomeGroup 行為的設定。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
