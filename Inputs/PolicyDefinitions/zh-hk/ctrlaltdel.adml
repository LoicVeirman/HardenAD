<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="CADOptions">Ctrl+Alt+Del 選項</string>
      <string id="DisableChangePassword">移除變更密碼</string>
      <string id="DisableChangePassword_Help">這個原則設定可防止使用者自行要求變更他們的 Windows 密碼。

如果您啟用這個原則設定，當您按下 Ctrl+Alt+Del 時，不會顯示 [Windows 安全性] 對話方塊中的 [變更密碼] 按鈕。

不過，當系統出現提示時，使用者仍然可以變更他們的密碼。當系統管理員需要新密碼，或者當使用者的密碼快要到期時，系統會提示使用者提供新密碼。</string>
      <string id="DisableLockComputer">移除鎖定電腦</string>
      <string id="DisableLockComputer_Help">這個原則設定可防止使用者鎖定系統。

在鎖定之後，桌面會被隱藏，而且系統無法使用。只有鎖定系統的使用者或是系統管理員才能解除鎖定。

如果您啟用這個原則設定，使用者將無法在鍵盤上使用 Ctrl+Alt+Del 鎖定電腦。

如果您停用或未設定這個原則設定，則使用者將可以在鍵盤上使用 Ctrl+Alt+Del 鎖定電腦。

提示: 如果要鎖定電腦但不加以設定，請按 Ctrl+Alt+Delete，然後按一下 [鎖定此電腦]。</string>
      <string id="DisableTaskMgr">移除工作管理員</string>
      <string id="DisableTaskMgr_Help">這個原則設定可防止使用者啟動工作管理員。

工作管理員 (taskmgr.exe) 可以讓使用者啟動或結束程式、監視電腦執行效能、檢視及監視電腦上所有執行中的程式 (包含系統服務)、尋找程式的執行檔名稱，以及變更程式執行中的處理程序之優先順序。

如果您啟用這個原則設定，使用者便無法存取工作管理員。如果使用者嘗試啟動工作管理員，會出現訊息，說明某個原則禁止執行這個動作。

如果您停用或未設定這個原則設定，使用者將可以存取工作管理員來啟動或結束程式、監視電腦執行效能、檢視及監視電腦上所有執行中的程式 (包含系統服務)、尋找程式的執行檔名稱，以及變更程式執行中的處理程序之優先順序。</string>
      <string id="NoLogoff">移除登出</string>
      <string id="NoLogOff_Help">這個原則設定會停用或移除將使用者登出系統的所有功能表項目和按鈕。

如果您啟用這個原則設定，使用者按下 Ctrl+Alt+Del 後，不會看到 [登出] 功能表項目。這樣可以避免使用者登出，只能重新啟動或關閉電腦，也可以避免使用者按一下 [開始] 功能表的 [登出] 時登出。

另請參閱 [移除開始功能表上的登出] 原則設定。

如果您停用或未設定這個原則設定，使用者可以在按下 Ctrl+Alt+Del 後看到並選取登出功能表項目。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
