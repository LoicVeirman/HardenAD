<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Fichier du modèle de stratégie de groupe TCPIP</displayName>
  <description>Ce fichier admx décrit le modèle de stratégie des composants TCPIP</description>
  <resources>
    <stringTable>
      <string id="TCPIP">Paramètres TCPIP</string>
      <string id="Ipv6Transition">Technologies de transition IPv6</string>

      <string id="ISATAP_State">Définir l’état ISATAP</string>
      <string id="ISATAP_Router_Name">Définir le nom du routeur ISATAP</string>
      <string id="6to4_State">Définir l’état ISATAP</string>
      <string id="6to4_Router_Name">Définir le nom de relais 6to4</string>
      <string id="6to4_Router_Name_Resolution_Interval">Définir l’intervalle de résolution de nom de relais 6to4</string>
      <string id="Teredo_State">Définir l’état de Teredo</string>
      <string id="Teredo_Server_Name">Définir le nom du serveur Teredo</string>
      <string id="Teredo_Refresh_Rate">Définir la fréquence de rafraîchissement de Teredo</string>
      <string id="Teredo_Client_Port">Définir le port client Teredo</string>
      <string id="Teredo_Default_Qualified">Définir la qualification par défaut de Teredo</string>

      <string id="DefaultState">État par défaut</string>
      <string id="EnabledState">État activé</string>
      <string id="DisabledState">État désactivé</string>
      <string id="TeredoClient">Client</string>
      <string id="TeredoEnterpriseClient">Client Entreprise</string>
      <string id="IPHTTPS_ClientState">Définir l’état IP-HTTPS</string>
      <string id="IPHTTPS_ClientUrl">URL IP-HTTPS</string>

      <string id="ISATAP_State_Help">Ce paramètre de stratégie vous permet de configurer le protocole ISATAP (Intra-Site Automatic Tunnel Addressing Protocol), une technologie de tunneling automatique adresse à routeur et hôte à hôte, hôte à routeur et routeur à hôte utilisée pour assurer la connectivité IPv6 monodiffusion entre les hôtes IPv6 sur un réseau intranet IPv4.

Si vous désactivez ou ne configurez pas ce paramètre de stratégie, c’est le paramètre de l’hôte local qui est utilisé.

Si vous activez ce paramètre de stratégie, vous pouvez configurer le protocole ISATAP avec l’un des paramètres suivants :

État par défaut de la stratégie : aucune interface présente sur l’hôte.

État activé de la stratégie : si le nom ISATAP est correctement résolu, le protocole ISATAP est configuré sur l’hôte avec une adresse locale du lien et une adresse pour chaque préfixe reçu du routeur ISATAP via la configuration automatique d’adresse sans état. Si le nom ISATAP n’est pas correctement résolu, l’hôte dispose d’une interface ISATAP configurée avec une adresse locale du lien.

État désactivé de la stratégie : aucune interface ISATAP n’est présente sur l’hôte.

</string>

      <string id="ISATAP_Router_Name_Help">Ce paramètre de stratégie vous permet de spécifier le nom de routeur ou l’adresse IPv4 d’un routeur ISATAP.

Si vous activez ce paramètre de stratégie, vous pouvez spécifier le nom de routeur ou l’adresse IPv4 d’un routeur ISATAP. Si vous entrez l’adresse IPv4 du routeur ISATAP dans la zone de texte, les services DNS ne sont pas requis.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, c’est le paramètre de l’hôte local qui est utilisé.



</string>


      <string id="6to4_State_Help">Ce paramètre de stratégie vous permet de configurer le protocole 6to4, une technologie d’attribution d’adresses et de tunneling automatique routeur à routeur utilisée pour assurer la connectivité IPv6 monodiffusion entre les hôtes et les sites IPv6 sur le réseau Internet IPv4. Le protocole 6to4 utilise le préfixe d’adresse globale 2002:WWXX:YYZZ::/48 dans lequel les lettres sont une représentation hexadécimale de l’adresse IPv4 globale (w.x.y.z) attribuée à un site.

Si vous désactivez ou ne configurez pas ce paramètre de stratégie, c’est le paramètre de l’hôte local qui est utilisé.

Si vous activez ce paramètre de stratégie, vous pouvez configurer le protocole 6to4 avec l’un des paramètres suivants :

État par défaut de la stratégie : 6to4 est désactivé et la connectivité avec 6to4 n’est pas disponible.

État activé de la stratégie : si une adresse IPv4 globale est présente, l’hôte dispose d’une interface 6to4. En l’absence d’adresse IPv4 globale, l’hôte ne dispose d’aucune interface 6to4.

État désactivé de la stratégie : le protocole 6to4 est désactivé et la connectivité à 6to4 n’est pas disponible.

</string>

      <string id="6to4_Router_Name_Help">Ce paramètre de stratégie vous permet de spécifier le nom de relais 6to4 d’un hôte 6to4. Un relais 6to4 est utilisé comme passerelle par défaut pour le trafic réseau IPv6 envoyé par l’hôte 6to4. Le paramètre Nom de relais 6to4 n’a aucun effet si la connectivité 6to4 n’est pas disponible sur l’hôte.

Si vous activez ce paramètre de stratégie, vous pouvez spécifier le nom de relais d’un hôte 6to4.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, c’est le paramètre de l’hôte local qui est utilisé, et vous ne pouvez pas spécifier de nom de relais pour un hôte 6to4.



</string>

      <string id="6to4_Router_Name_Resolution_Interval_Help">Ce paramètre de stratégie vous permet de spécifier l’intervalle de résolution du nom de relais. Le paramètre Intervalle de résolution de nom de relais 6to4 n’a aucun effet si la connectivité 6to4 n’est pas disponible sur l’hôte.

Si vous activez ce paramètre de stratégie, vous pouvez spécifier le délai au bout duquel le nom de relais est régulièrement résolu.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, c’est le paramètre de l’hôte local qui est utilisé.



</string>

      <string id="Teredo_State_Help">Ce paramètre de stratégie vous permet de configurer Teredo, une technologie d’attribution d’adresses et de tunneling automatique qui assure la connectivité IPv6 monodiffusion sur le réseau Internet IPv4.

Si vous désactivez ou ne configurez pas ce paramètre de stratégie, les paramètres de l’hôte local sont utilisés.

Si vous activez ce paramètre de stratégie, vous pouvez configurer Teredo avec un des paramètres suivants :

Valeur par défaut : l’état par défaut est « Client ».

Désactivé : aucune interface Teredo n’est présente sur l’hôte.

Client : l’interface Teredo n’est présente que si l’hôte n’appartient pas à un réseau qui inclut un contrôleur de domaine.

Client Entreprise : l’interface Teredo est toujours présente, même si l’hôte appartient à un réseau qui inclut un contrôleur de domaine.



</string>

      <string id="Teredo_Server_Name_Help">Ce paramètre de stratégie vous permet de spécifier le nom du serveur Teredo. Ce nom de serveur est utilisé sur l’ordinateur client Teredo sur lequel ce paramètre de stratégie est appliqué.

Si vous activez ce paramètre de stratégie, vous pouvez spécifier un nom de serveur Teredo qui s’applique à un client Teredo.

Si vous désactivez ou ne configurez pas ce paramètre de stratégie, les paramètres locaux de l’ordinateur sont utilisés pour déterminer le nom du serveur Teredo.



</string>

      <string id="Teredo_Refresh_Rate_Help">Ce paramètre de stratégie vous permet de configurer la fréquence de rafraîchissement Teredo.

Remarque : régulièrement (par défaut, toutes les 30 secondes), les clients Teredo envoient un paquet de sollicitation de routeur unique au serveur Teredo. En réponse, le serveur Teredo envoie un paquet de publication de routeur. Ce paquet périodique actualise le mappage d’adresse IP et de port UDP dans la table de traduction du périphérique NAT du client Teredo.

Si vous activez ce paramètre de stratégie, vous pouvez spécifier la fréquence de rafraîchissement.  Si vous choisissez une fréquence de rafraîchissement supérieure au délai de mappage de port dans le périphérique NAT du client Teredo, Teredo risque d’arrêter de fonctionner ou la connectivité peut être intermittente.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, la fréquence de rafraîchissement est configurée en utilisant les paramètres locaux de l’ordinateur. La fréquence de rafraîchissement par défaut est de 30 secondes.



</string>

      <string id="Teredo_Client_Port_Help">Ce paramètre de stratégie vous permet de sélectionner le port UDP utilisé par le client Teredo pour envoyer des paquets. Si vous laissez la valeur par défaut 0, le système d’exploitation sélectionne un port (recommandé). Si vous sélectionnez un port UDP déjà utilisé par un système, l’initialisation du client Teredo échoue.

Si vous activez ce paramètre de stratégie, vous pouvez personnaliser un port UDP pour le client Teredo.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, c’est le paramètre de l’hôte local qui est utilisé.



</string>

      <string id="Teredo_Default_Qualified_Help">Ce paramètre de stratégie vous permet de définir Teredo comme étant prêt à communiquer (processus appelé qualification). Par défaut, Teredo passe à l’état dormant lorsqu’il n’est pas utilisé. Le processus de qualification le fait sortir de l’état dormant.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, c’est le paramètre de l’hôte local qui est utilisé.

Ce paramètre de stratégie ne contient qu’un seul état :

État activé de la stratégie : si le paramètre Qualification par défaut est activé, Teredo tente immédiatement une qualification et reste qualifié si le processus de qualification aboutit.

</string>

      <string id="IPHTTPS_ClientState_Help">Ce paramètre de stratégie vous permet de configurer IP-HTTPS, une technologie de tunneling qui utilise le protocole HTTPS pour assurer la connectivité IP à un réseau distant.

Si vous désactivez ou ne configurez pas ce paramètre de stratégie, les paramètres de l’hôte local sont utilisés.

Si vous activez ce paramètre de stratégie, vous pouvez spécifier l’URL d’un serveur IP-HTTPS. Vous pouvez configurer IP-HTTPS avec l’un des paramètres suivants :

État par défaut de la stratégie : l’interface IP-HTTPS est utilisée lorsqu’il n’y a aucune autre option de connectivité.

État activé de la stratégie : l’interface IP-HTTPS est toujours présente, même si l’hôte dispose d’autres options de connectivité.

État désactivé de la stratégie : aucune interface IP-HTTPS n’est présente sur l’hôte.



</string>

      <string id="Parameters">Paramètres</string>
      <string id="Windows_Scaling_Heuristics_State">Définir l’état de la méthode heuristique de mise à l’échelle des fenêtres</string>
      <string id="Windows_Scaling_Heuristics_Help">Ce paramètre de stratégie vous permet de configurer la méthode heuristique de mise à l’échelle des fenêtres. La méthode heuristique de mise à l’échelle des fenêtres est un algorithme permettant d’identifier les problèmes de connectivité et de débit causés par de nombreux pare-feu et autres zones intermédiaires qui n’interprètent pas correctement l’option Mise à l’échelle des fenêtres.

Si vous ne configurez pas ce paramètre de stratégie, les paramètres de l’hôte local sont utilisés.

Si vous activez ce paramètre de stratégie, la méthode heuristique de mise à l’échelle des fenêtres est activée et le système essaie d’identifier les problèmes de connectivité et de débit, et de prendre les mesures appropriées.

Si vous désactivez ce paramètre de stratégie, la méthode heuristique de mise à l’échelle des fenêtres est désactivée et le système n’essaie pas d’identifier les problèmes de connectivité et de débit causés par les pare-feu et autres zones intermédiaires.



</string>

      <string id="IP_Stateless_Autoconfiguration_Limits_State">Définir l’état des limites d’autoconfiguration sans état des adresses IP</string>
      <string id="IP_Stateless_Autoconfiguration_Limits_Help">Ce paramètre de stratégie vous permet de configurer les limites d’autoconfiguration sans état des adresses IP.

Si vous activez ou ne configurez pas ce paramètre de stratégie, les limites d’autoconfiguration des adresses IP sont activées et le système limite le nombre d’adresses et d’itinéraires autoconfigurés.

Si vous désactivez ce paramètre de stratégie, les limites d’autoconfiguration sans état des adresses IP sont désactivées et le système ne limite pas le nombre d’adresses et d’itinéraires autoconfigurés.



</string>

</stringTable>

<presentationTable>

      <presentation id="StateSelection">
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Sélectionnez l’un des états suivants :</dropdownList>
      </presentation>

      <presentation id="RouterName">
        <textBox refId="RouterNameBox">
          <label>Entrez un nom de routeur ou de relais :</label>
        </textBox>
      </presentation>

      <presentation id="RouterNameResolutionInterval">
        <text>Ce paramètre vous permet de personnaliser l’intervalle de résolution de nom de relais 6to4</text>
        <decimalTextBox refId="RouterNameResolutionIntervalBox" defaultValue="1440">Minutes :  </decimalTextBox>
      </presentation>

      <presentation id="TeredoServerName">
        <textBox refId="TeredoServerNameBox">
          <label>Entrez un nom de serveur Teredo :</label>
        </textBox>
      </presentation>

      <presentation id="TeredoRefreshRate">
        <text>Ce paramètre vous permet de personnaliser la fréquence de rafraîchissement du client Teredo</text>
        <text>La plage est comprise entre 1et 86 400 secondes (1 jour).</text>
        <decimalTextBox refId="TeredoRefreshRateBox" defaultValue="30">Secondes :  </decimalTextBox>
      </presentation>

      <presentation id="TeredoClientPort">
        <text>Ce paramètre vous permet de personnaliser le port client Teredo</text>
        <text>La plage est comprise entre 0 et 65 535. La valeur par défaut (recommandée) est 0, ce qui permet au système local de choisir le port.</text>
        <decimalTextBox refId="TeredoClientPortBox" defaultValue="0"> </decimalTextBox>
      </presentation>

      <presentation id="IPHTTPS_ClientState">
        <textBox refId="IPHTTPSClientUrlBox">
          <label>Entrez l’URL IPHTTPS :</label>
        </textBox>
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Sélectionnez l’état de l’interface parmi les options suivantes :</dropdownList>
      </presentation>

</presentationTable>

</resources>
</policyDefinitionResources>
