<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2010 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Paramètres de stratégie de groupe du service de réseau local sans fil</displayName>
  <description>Paramètres de la stratégie de groupe du service de réseau local sans fil</description>
  <resources>
    <stringTable>
      <string id="WlanSvc_Category">Service de réseau local sans fil</string>
      <string id="NetworkCost_Category">Coût de connexion au réseau WLAN</string>
      <string id="WlanSettings_Category">Paramètres de réseau local sans fil</string>
      <string id="SetCost">Définir le coût</string>
      <string id="SetCost_Help">Ce paramètre de stratégie configure le coût des connexions au réseau local sans fil (WLAN, Wireless LAN) sur l’ordinateur local.

Si ce paramètre de stratégie est activé, une liste déroulante contenant les valeurs de coût possibles est activée. La sélection d’une des valeurs suivantes dans la liste définit le coût de toutes les connexions WLAN sur l’ordinateur local :

- Non restreint : l’utilisation de cette connexion est illimitée et n’est pas restreinte par les frais d’utilisation et les contraintes de capacité. 

- Fixe : l’utilisation de cette connexion n’est pas limitée par les frais d’utilisation et les contraintes de capacité jusqu’à une limite de données définie. 

- Variable : cette connexion est facturée par octet.

Si vous désactivez ce paramètre de stratégie ou ne le configurez pas, le coût des connexions WLAN est non restreint par défaut.
      
      </string>
      <string id="Cost_DropDownList_Unrestricted">Non restreint</string>
      <string id="Cost_DropDownList_Fixed">Fixe</string>
      <string id="Cost_DropDownList_Variable">Variable</string>
      <string id="Wireless_Display_Category">Affichage sans fil</string>
      <string id="Wireless_Display_PINEnforced">Exiger le couplage de code PIN</string>
      <string id="Wireless_Display_PINEnforced_Help">Cette stratégie s'applique aux connexions d'affichage sans fil. Cette stratégie signifie que l'utilisation d'un code PIN pour le couplage aux périphériques d'affichage sans fil est nécessaire au lieu de facultatif.

À l'inverse, cela signifie que le bouton de commande n'est pas autorisé.

Si ce paramètre de stratégie est désactivé ou s'il n'est pas configuré, le couplage rapide est autorisé par défaut (mais pas nécessairement préféré).
      </string>
      <string id="Wireless_Display_PINPreferred">Préférer le couplage de code PIN</string>
      <string id="Wireless_Display_PINPreferred_Help">Cette stratégie s'applique aux connexions d'affichage sans fil. Cette stratégie modifie l'ordre de préférence des méthodes de couplage.

Lorsqu'il est activé, les connexions préfèrent un code confidentiel pour le couplage aux périphériques d'affichage sans fil à la méthode de couplage par bouton de commande.

Si ce paramètre de stratégie est désactivé ou s'il n'est pas configuré, le couplage rapide est préféré par défaut (si autorisé par d'autres stratégies).
      </string>
      <string id="WiFiSense">Autoriser Windows à se connecter automatiquement aux points d'accès ouverts suggérés, aux réseaux partagés par les contacts et aux points d'accès offrant des services payants</string>
      <string id="WiFiSense_Help">Ce paramètre de stratégie détermine si les utilisateurs peuvent activer les paramètres de réseau local sans fil suivants : « Se connecter aux points d'accès ouverts suggérés », « Se connecter aux réseaux partagés par mes contacts » et « Activer les services payants ».

« Se connecter aux points d'accès ouverts suggérés » permet à Windows de connecter automatiquement les utilisateurs aux points d'accès ouverts qu'il connaît grâce à des réseaux participatifs auxquels d'autres utilisateurs de Windows sont connectés.

« Se connecter aux réseaux partagés par mes contacts » permet à Windows de se connecter automatiquement aux réseaux que les contacts de l'utilisateur ont partagés avec eux, et permet aux utilisateurs sur ce périphérique de partager les réseaux avec leurs contacts.

« Activer les services payants » permet à Windows de se connecter temporairement à des points d'accès ouverts pour déterminer si des services payants sont disponibles.

Si ce paramètre de stratégie est désactivé, « Se connecter aux points d'accès ouverts suggérés », « Se connecter aux réseaux partagés par mes contacts » et « Activer les services payants » sont désactivés et les utilisateurs sur ce périphérique ne peuvent pas les activer.

Si ce paramètre de stratégie n'est pas configuré ou s'il est activé, les utilisateurs peuvent choisir d'activer ou de désactiver « Se connecter aux points d'accès ouverts suggérés » ou « Se connecter aux réseaux partagés par mes contacts ».
      </string>
    </stringTable>
    <presentationTable>
      <presentation id="SetCost_Presentation">
        <dropdownList refId="Cost_Dropdownlist" defaultItem="0">Sélectionnez une valeur de coût de connexion WLAN à définir :</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
