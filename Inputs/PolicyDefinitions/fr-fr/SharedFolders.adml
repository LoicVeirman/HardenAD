<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Entrer le nom complet ici</displayName>
  <description>Entrer la description ici</description>
  <resources>
    <stringTable>
      <string id="PublishDfsRoots">Permettre à des racines DFS d’être publiées</string>
      <string id="PublishDfsRoots_Help">Ce paramètre de stratégie détermine si l’utilisateur peut publier des racines DFS dans les services de domaine Active Directory (AD DS).

Si vous activez ou ne configurez pas ce paramètre de stratégie, les utilisateurs peuvent utiliser l’option « Publier dans Active Directory » pour publier des racines DFS en tant que dossiers partagés dans Active Directory.

Si vous désactivez ce paramètre de stratégie, les utilisateurs ne peuvent pas publier de racines DFS dans AD DS et l’option « Publier dans Active Directory » est désactivée. Remarque : la valeur par défaut est d’autoriser la publication de dossiers partagés lorsque ce paramètre n’est pas configuré.</string>
      <string id="PublishSharedFolders">Permettre à des dossiers partagés d’être publiés</string>
      <string id="PublishSharedFolders_Help">Ce paramètre de stratégie détermine si l’utilisateur peut publier des dossiers partagés dans les services de domaine Active Directory (AD DS).

Si vous activez ou ne configurez pas ce paramètre de stratégie, les utilisateurs peuvent utiliser l’option « Publier dans Active Directory » dans le composant logiciel enfichable Dossiers partagés pour publier des dossiers partagés dans AD DS.

Si vous désactivez ce paramètre de stratégie, les utilisateurs ne peuvent pas publier de dossiers partagés dans AS DS et l’option « Publier dans Active Directory » est désactivée. Remarque : la valeur par défaut est d’autoriser la publication de dossiers partagés lorsque ce paramètre n’est pas configuré.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
