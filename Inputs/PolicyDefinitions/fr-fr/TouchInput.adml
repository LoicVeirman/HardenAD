<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>entrer le nom complet ici</displayName>
  <description>entrer la description ici</description>
  <resources>
    <stringTable>
      <string id="TouchInput">Saisie tactile</string>
      <string id="TouchInputOff">Désactiver la saisie tactile du Tablet PC</string>
      <string id="TouchInputOff_Help">Désactiver la saisie tactile du Tablet PC

Désactive la saisie tactile, qui permet aux utilisateurs d'interagir avec leur ordinateur à l'aide de leurs doigts.

Si vous activez ce paramètre, l'utilisateur ne peut pas effectuer de saisie tactile. Il ne peut pas utiliser la saisie tactile ou les gestes tactiles, tels que cliquer ou double-cliquer avec le doigt, le pointeur tactile, ni d'autres fonctionnalités tactiles spécifiques.

Si vous désactivez ce paramètre, l'utilisateur peut effectuer des saisies tactiles à l'aide de gestes, du pointeur tactile et d'autres fonctionnalités tactiles spécifiques.

Si vous ne configurez pas ce paramètre, la saisie tactile est activée par défaut.

Remarque : les modifications apportées à ce paramètre sont sans effet tant que l'utilisateur ne ferme pas la session.</string>
      <string id="SingleFingerPanningOff">Désactiver le défilement tactile</string>
      <string id="SingleFingerPanningOff_Help">Désactiver le défilement 
Désactive le défilement tactile, qui permet aux utilisateurs de faire défiler tactilement le contenu des fenêtres. Sur un PC compatible équipé d'un numériseur tactile, les utilisateurs peuvent par défaut faire défiler le contenu d'une zone déroulante en le faisant directement glisser vers le haut ou vers le bas.

Si vous activez ce paramètre, l'utilisateur ne peut pas faire défiler les fenêtres en mode tactile. 

Si vous désactivez ce paramètre, l'utilisateur peut faire défiler les fenêtres en mode tactile.

Si vous ne configurez pas ce paramètre, le défilement tactile est activé par défaut.

Remarque : les modifications apportées à ce paramètre sont sans effet tant que l'utilisateur ne ferme pas la session.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
