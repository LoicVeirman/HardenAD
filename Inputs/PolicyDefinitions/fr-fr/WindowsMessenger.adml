<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>entrer le nom complet ici</displayName>
  <description>entrer la description ici</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">Ne pas démarrer initialement Windows Messenger de manière automatique</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">Ce paramètre de stratégie empêche Windows Messenger de s’exécuter automatiquement à l’ouverture de session. 

Si vous activez ce paramètre de stratégie, Windows Messenger n’est pas chargé automatiquement lors de l’ouverture de session d’un utilisateur.

Si vous désactivez ce paramètre de stratégie ou ne le configurez pas, Windows Messenger est chargé automatiquement à l’ouverture de session.

Remarque : ce paramètre de stratégie empêche simplement Windows Messenger de s’exécuter initialement. Ensuite, si l’utilisateur appelle Windows Messenger pour l’utiliser, Windows Messenger sera chargé.

L’utilisateur peut également configurer ce comportement dans l’onglet Préférences du menu Outils de l’interface utilisateur de Windows Messenger.

Remarque : si vous ne voulez pas que les utilisateurs utilisent Windows Messenger, activez le paramètre de stratégie « Ne pas autoriser l’exécution de Windows Messenger ».

Remarque : ce paramètre de stratégie est disponible à la fois sous Configuration ordinateur et Configuration utilisateur. Si les deux sont présents, la version Configuration ordinateur de ce paramètre de stratégie est prioritaire.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">Ne pas démarrer initialement Windows Messenger de manière automatique</string>
      <string id="WinMSG_NoWindowsMsg_Comp">Ne pas autoriser l’exécution de Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">Ce paramètre de stratégie permet d’empêcher l’exécution de Windows Messenger.

Si vous désactivez ce paramètre de stratégie, Windows Messenger ne s’exécute pas.

Si vous désactivez ce paramètre de stratégie ou ne le configurez pas, Windows Messenger peut être utilisé.

Remarque : si vous activez ce paramètre de stratégie, Assistance à distance peut également utiliser Windows Messenger.

Remarque : ce paramètre de stratégie est disponible à la fois sous Configuration ordinateur et Configuration utilisateur. Si les deux sont présents, la version Configuration ordinateur de ce paramètre de stratégie est prioritaire.</string>
      <string id="WinMSG_NoWindowsMsg_User">Ne pas autoriser l’exécution de Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
