<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>entrer le nom complet ici</displayName>
  <description>entrer la description ici</description>
  <resources>
    <stringTable>
      <string id="BootScenarioCategory">Diagnostics des performances de démarrage Windows</string>
      <string id="BootScenarioExecutionPolicyExplain">Détermine le niveau d’exécution des Diagnostics des performances du démarrage Windows.

Si vous activez ce paramètre de stratégie, vous devez sélectionner un niveau d’exécution dans le menu déroulant. Si vous sélectionnez la détection des problèmes et le dépannage uniquement, le service de stratégie de diagnostic détecte les problèmes de performances du démarrage Windows et tente d’en déterminer les causes principales. Ces causes sont enregistrées dans le journal des événements lorsqu’elles sont détectées, mais aucune action corrective n’est prise. Si vous sélectionnez la détection, le dépannage et la résolution, le service de stratégie de diagnostic détecte les problèmes de performances du démarrage Windows et indique à l’utilisateur que la résolution assistée est disponible.

Si vous désactivez ce paramètre de stratégie, Windows ne peut pas détecter, dépanner ou résoudre les problèmes de performances du démarrage Windows qui sont gérés par le service de stratégie de diagnostic.

Si vous ne configurez pas ce paramètre de stratégie, le service de stratégie de diagnostic active par défaut la résolution des performances du démarrage Windows.

Ce paramètre de stratégie ne prend effet que si la stratégie d’exécution des scénarios de diagnostic n’est pas configurée.

La validation de cette stratégie n’impose pas le redémarrage du système ou du service : les modifications sont immédiates.

Ce paramètre de stratégie ne prend effet que lorsque le service de stratégie de diagnostic est en cours d’exécution. Lorsque le service est arrêté ou désactivé, les scénarios de diagnostic ne sont pas exécutés.  Vous pouvez configurer le service de stratégie de diagnostic à l’aide du composant logiciel enfichable Services installé sur Microsoft Management Console.</string>
      <string id="ResumeScenarioCategory">Diagnostics des performances de la veille/reprise Windows</string>
      <string id="ResumeScenarioExecutionPolicyExplain">Détermine le niveau d’exécution des Diagnostics des performances de la veille/reprise Windows.

Si vous activez ce paramètre de stratégie, vous devez sélectionner un niveau d’exécution dans le menu déroulant. Si vous sélectionnez la détection des problèmes et le dépannage uniquement, le service de stratégie de diagnostic détecte les problèmes de performances de la veille/reprise Windows et tente d’en déterminer les causes principales. Ces causes sont enregistrées dans le journal des événements lorsqu’elles sont détectées, mais aucune action corrective n’est prise. Si vous sélectionnez la détection, le dépannage et la résolution, le service de stratégie de diagnostic détecte les problèmes de performances de la veille/reprise Windows et indique à l’utilisateur que la résolution assistée est disponible.

Si vous désactivez ce paramètre de stratégie, Windows ne peut pas détecter, dépanner ou résoudre les problèmes de performances de la veille/reprise Windows qui sont gérés par le service de stratégie de diagnostic.

Si vous ne configurez pas ce paramètre de stratégie, le service de stratégie de diagnostic active par défaut la résolution des performances de la veille/reprise Windows.

Ce paramètre de stratégie ne prend effet que si la stratégie d’exécution des scénarios de diagnostic n’est pas configurée.

La validation de cette stratégie n’impose pas le redémarrage du système ou du service : les modifications sont immédiates.

Ce paramètre de stratégie ne prend effet que lorsque le service de stratégie de diagnostic est en cours d’exécution. Lorsque le service est arrêté ou désactivé, les scénarios de diagnostic ne sont pas exécutés.  Vous pouvez configurer le service de stratégie de diagnostic à l’aide du composant logiciel enfichable Services installé sur Microsoft Management Console.</string>
      <string id="ShellScenarioCategory">Diagnostics des performances de réactivité du système Windows</string>
      <string id="ShellScenarioExecutionPolicyExplain">Détermine le niveau d’exécution des Diagnostics des performances de réactivité du système Windows.

Si vous activez ce paramètre de stratégie, vous devez sélectionner un niveau d’exécution dans le menu déroulant. Si vous sélectionnez la détection des problèmes et le dépannage uniquement, le service de stratégie de diagnostic détecte les problèmes de réactivité du système Windows et tente d’en déterminer les causes principales. Ces causes sont enregistrées dans le journal des événements lorsqu’elles sont détectées, mais aucune action corrective n’est prise. Si vous sélectionnez la détection, le dépannage et la résolution, le service de stratégie de diagnostic détecte les problèmes de réactivité du système Windows et indique à l’utilisateur que la résolution assistée est disponible.

Si vous désactivez ce paramètre de stratégie, Windows ne peut pas détecter, dépanner ou résoudre les problèmes de réactivité du système Windows qui sont gérés par le service de stratégie de diagnostic.

Si vous ne configurez pas ce paramètre de stratégie, le service de stratégie de diagnostic active par défaut la résolution de la réactivité du système Windows.

Ce paramètre de stratégie ne prend effet que si la stratégie d’exécution des scénarios de diagnostic n’est pas configurée.

La validation de cette stratégie n’impose pas le redémarrage du système ou du service : les modifications sont immédiates.

Ce paramètre de stratégie ne prend effet que lorsque le service de stratégie de diagnostic est en cours d’exécution. Lorsque le service est arrêté ou désactivé, les scénarios de diagnostic ne sont pas exécutés.  Vous pouvez configurer le service de stratégie de diagnostic à l’aide du composant logiciel enfichable Services installé sur Microsoft Management Console.</string>
      <string id="ShutdownScenarioCategory">Diagnostics des performances de l’arrêt de Windows</string>
      <string id="ShutdownScenarioExecutionPolicyExplain">Détermine le niveau d’exécution des Diagnostics des performances de l’arrêt de Windows.

Si vous activez ce paramètre de stratégie, vous devez sélectionner un niveau d’exécution dans le menu déroulant. Si vous sélectionnez la détection des problèmes et le dépannage uniquement, le service de stratégie de diagnostic détecte les problèmes de performances de l’arrêt de Windows et tente d’en déterminer les causes principales. Ces causes sont enregistrées dans le journal des événements lorsqu’elles sont détectées, mais aucune action corrective n’est prise. Si vous sélectionnez la détection, le dépannage et la résolution, le service de stratégie de diagnostic détecte les problèmes de performances de l’arrêt de Windows et indique à l’utilisateur que la résolution assistée est disponible.

Si vous désactivez ce paramètre de stratégie, Windows ne peut pas détecter, dépanner ou résoudre les problèmes de performances de l’arrêt de Windows qui sont gérés par le service de stratégie de diagnostic.

Si vous ne configurez pas ce paramètre de stratégie, le service de stratégie de diagnostic active par défaut la résolution des performances de l’arrêt de Windows.

Ce paramètre de stratégie ne prend effet que si la stratégie d’exécution des scénarios de diagnostic n’est pas configurée.

La validation de cette stratégie n’impose pas le redémarrage du système ou du service : les modifications sont immédiates.

Ce paramètre de stratégie ne prend effet que lorsque le service de stratégie de diagnostic est en cours d’exécution. Lorsque le service est arrêté ou désactivé, les scénarios de diagnostic ne sont pas exécutés.  Vous pouvez configurer le service de stratégie de diagnostic à l’aide du composant logiciel enfichable Services installé sur Microsoft Management Console.</string>
      <string id="WdiScenarioExecutionPolicy">Configurer le niveau d’exécution de scénario</string>
      <string id="WdiScenarioExecutionPolicyLevelResolution">Détection, dépannage et résolution</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Détection et dépannage uniquement</string>
    </stringTable>
    <presentationTable>
      <presentation id="WdiScenarioExecutionPolicy_1">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Niveau d’exécution de scénario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_2">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Niveau d’exécution de scénario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_3">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Niveau d’exécution de scénario</dropdownList>
      </presentation>
      <presentation id="WdiScenarioExecutionPolicy_4">
        <dropdownList refId="WdiScenarioExecutionPolicyLevel" noSort="true" defaultItem="1">Niveau d’exécution de scénario</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
