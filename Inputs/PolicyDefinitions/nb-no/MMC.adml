<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>skriv inn visningsnavn her</displayName>
  <description>skriv inn beskrivelse her</description>
  <resources>
    <stringTable>
      <string id="MMC">Microsoft Management Console</string>
      <string id="MMC_ActiveXControl">ActiveX-kontroll</string>
      <string id="MMC_ExtendView">Utvidet visning (Webvisning)</string>
      <string id="MMC_ExtensionSnapins">Snapin-utvidelse</string>
      <string id="MMC_LinkToWeb">Kobling til webadresse</string>
      <string id="MMC_RESTRICT">Begrensede/tillatte snapin-moduler</string>
      <string id="MMC_Restrict_Author">Forby brukere å bruke utformingsmodus</string>
      <string id="MMC_restrict_Author_Explain">Forby brukere å bruke utformingsmodus.

Denne innstillingen hindrer at brukere åpner MMC (Microsoft Management Console) i utformingsmodus, eksplisitt åpner konsollfiler i utformingsmodus, og åpner konsollfiler som kan åpnes i utformingsmodus som standard.

Resultatet er at brukere ikke kan opprette konsollfiler eller legge til eller fjerne snapin-moduler. Fordi de ikke kan åpne konsollfiler i utformingsmodus, kan de heller ikke bruke verktøyene som filene inneholder.

Denne innstillingen tillater at brukere åpner MMC-konsollfiler, for eksempel de på menyen Administrative verktøy i Windows 2000 Server-serien eller Windows Server 2003-serien, i brukermodus.  Brukere kan likevel ikke åpne et tomt MMC-konsollvindu fra Start-menyen. (Klikk Start, Kjør og skriv inn mmc for å åpne MMC). Brukere kan heller ikke åpne et tomt MMC-konsollvindu fra en ledetekst.

Hvis du deaktiverer denne policyen eller lar være å konfigurere den, kan brukere bruke utformingsmodus og åpne konsollfiler i utformingsmodus.</string>
      <string id="MMC_Restrict_Explain">Tillater eller hindrer bruk av denne snapin-modulen.

Hvis du aktiverer denne innstillingen, er snapin-modulen tillatt. Hvis du deaktiverer denne innstillingen, er snapin-modulen ikke tillatt. 

Hvis denne innstillingen ikke konfigureres, vil innstillingen for policyen Begrens brukere til listen over eksplisitt tillatte snapin-moduler avgjøre om denne snapin-modulen skal tillates eller ikke.

--  Hvis innstillingen Begrens brukere til listen over eksplisitt tillatte snapin-moduler er aktivert, kan ikke brukere bruke snapin-moduler, bortsett fra de som eksplisitt er tillatt.

    Aktiver denne innstillingen for å eksplisitt tillate bruk av denne snapin-modulen. Hvis denne innstillingen ikke er konfigurert (eller deaktivert), er ikke denne snapin-modulen tillatt.

--  Hvis Begrens brukere til listen over eksplisitt tillatte snapin-moduler er deaktivert eller ikke konfigurert, kan brukere bruke alle snapin-moduler, bortsett fra de som eksplisitt nektes.

    Deaktiver denne innstillingen for å eksplisitt hindre bruk av denne snapin-modulen. Hvis denne innstillingen ikke er konfigurert (eller aktivert), er denne snapin-modulen tillatt.

Når en snapin-modul ikke tillates, blir den ikke vist i vinduet Legg til / fjern snapin-modul i MMC. Når en bruker åpner en konsollfil som inkluderer en nektet snapin-modul, vil konsollfilen åpnes, men snapin-modulen vil ikke vises.</string>
      <string id="MMC_Restrict_To_Permitted_Snapins">Begrens brukere til listen over eksplisitt tillatte snapin-moduler</string>
      <string id="MMC_restrict_To_Permitted_Snapins_Explain">Lar deg selektivt tillate eller forhindre bruken av snapin-moduler for Microsoft Management Console (MMC).

--  Hvis du aktiverer denne innstillingen, tillates ingen snapin-moduler, bortsett fra de du eksplisitt tillater. Bruk denne innstillingen hvis du planlegger å hindre bruk av de fleste snapin-moduler.

    For eksplisitt å tillate en snapin-modul åpner du mappen for innstillingen Begrensede/tillatte snapin-moduler, og aktiver deretter innstillingene som representerer snapin-modulen du vil tillate. Hvis en snapin-modulinnstilling i mappen er deaktivert eller ikke konfigurert, er ikke snapin-modulen tillatt.

--  Hvis du deaktiverer denne innstillingen eller lar være å konfigurere den, vil alle snapin-moduler tillates, bortsett fra de du eksplisitt ikke tillater. Bruk denne innstillingen hvis du planlegger å tillate bruk av de fleste snapin-moduler.

    For eksplisitt å forby en snapin-modul åpner du mappen for innstillingen Begrensede/tillatte snapin-moduler og deaktiver deretter innstillingene som representerer snapin-modulen du vil forby. Hvis en snapin-modulinnstilling i mappen er deaktivert eller ikke konfigurert, vil snapin-modulen tillates.

Når en snapin-modul ikke tillates, vises den ikke i vinduet Legg til / fjern snapin-modul i MMC. Når en bruker åpner en konsollfil som inkluderer en nektet snapin-modul, vil konsollfilen åpnes, men snapin-modulen vil ikke vises.

Obs: Hvis denne innstillingen aktiveres, og ingen innstillinger i mappen Begrensede/tillatte snapin-moduler er aktivert, kan ikke brukere bruke noen MMC-snapin-moduler.</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
