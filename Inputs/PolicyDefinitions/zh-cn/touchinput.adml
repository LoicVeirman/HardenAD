<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="TouchInput">触摸屏输入</string>
      <string id="TouchInputOff">关闭 Tablet PC 触摸屏输入</string>
      <string id="TouchInputOff_Help">关闭 Tablet PC 触摸屏输入

关闭触摸屏输入，触摸屏输入使用户能够使用手指与计算机交互。

如果启用此设置，则用户将不能使用触摸屏进行输入。用户将不能使用触摸屏输入或触摸屏手势(如点击和双击)、触摸屏指针和触摸屏的其他特定功能。

如果禁用此设置，则用户可以通过使用手势、触摸指针和触摸的其他特定功能来使用触摸屏进行输入。

如果未配置此设置，则默认情况下将启用触摸屏输入。

注意: 对此设置的更改要在用户注销后才能生效。</string>
      <string id="SingleFingerPanningOff">关闭触控平移</string>
      <string id="SingleFingerPanningOff_Help">关闭平移
关闭触控平移，通过触控平移，用户可以通过触摸在窗口内平移。在带触控数字化器的兼容 PC 上，默认情况下，用户可以通过直接上下拖动滚动内容，在滚动区域内滚动或平移。

如果启用此设置，则用户将不能通过触摸平移窗口。

如果禁用此设置，则用户可以通过触摸平移窗口。

如果未配置此设置，默认情况下，触控平移为启用状态。

注意: 对此设置的更改要在用户注销后才能生效。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
