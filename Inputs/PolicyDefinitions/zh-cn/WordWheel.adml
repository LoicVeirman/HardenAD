<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="CustomSearch">自定义即时搜索 Internet 搜索提供程序</string>
      <string id="CustomSearch_Explain">设置自定义 Internet 搜索提供程序的菜单名称和 URL。

如果启用此设置，会将指定的菜单名称和 URL 用于 Internet 搜索。

如果未配置或禁用了此设置，将使用默认的 Internet 搜索提供程序。</string>
      <string id="NoSearchInternetInWordWheel">在“搜索”下拉框中隐藏“搜索 Internet”链接。</string>
      <string id="NoSearchInternetInWordWheel_Explain">使用此策略设置，可以管理“搜索 Internet”链接是否出现在“开始”菜单和“即时搜索”框下拉列表中。

如果启用此策略设置，则“搜索 Internet”链接不会显示在“开始”菜单和“即时搜索”框下拉列表中。

如果未配置或禁用了此策略设置，则“搜索 Internet”链接将出现在“开始”菜单和“即时搜索”框下拉列表中。</string>
      <string id="WordWheelAlwaysUseExplicitCommit">在执行搜索之前需要显式提交。</string>
      <string id="WordWheelAlwaysUseExplicitCommit_Explain">使用此策略设置，可以管理在执行搜索之前是否需要进行特定的确认。

如果启用此策略设置，则执行搜索之前，需要进行特定的确认。

如果未配置或禁用了此策略设置，则不需要特定的确认。</string>
      <string id="WordwheelPolicies">即时搜索</string>
    </stringTable>
    <presentationTable>
      <presentation id="CustomSearch">
        <textBox refId="CustomSearch_NamePrompt">
          <label>从中加载“即时搜索”菜单中字符串的字符串或 DLL 资源。</label>
        </textBox>
        <textBox refId="CustomSearch_URLPrompt">
          <label>调用自定义 Internet 搜索时所使用的 URL，并且用“%w”指示搜索术语。</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
