<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>便携工作区</displayName>
  <description>此文件中包含便携工作区策略设置。</description>
  <resources>
    <stringTable>
      <string id="PortableOperatingSystem">便携操作系统</string>
      <string id="PortableOperatingSystem_Launcher_DisplayName">Windows To Go 默认启动选项</string>
      <string id="PortableOperatingSystem_Launcher_Help">



此策略设置控制电脑在连接到包含 Windows To Go 的 USB 设备时是否引导到 Windows To Go，并控制用户是否可以使用“Windows To Go 启动选项控制面板”项对此进行更改。

如果启用此设置，则会启用连接到 USB 设备时引导到 Windows To Go，并且用户不能使用“Windows To Go 启动选项控制面板”项对此进行更改。

如果禁用此设置，则不会启用连接到 USB 设备时引导到 Windows To Go，除非用户在 BIOS 或其他引导顺序配置中手动配置该选项。

如果未配置此设置，则作为管理员组成员的用户可使用“Windows To Go 启动选项控制面板”项进行更改。</string>
      <string id="PortableOperatingSystem_Hibernate_DisplayName">从 Windows To Go 工作区启动时允许休眠(S4)</string>
      <string id="PortableOperatingSystem_Hibernate_Help">



指定从 Windows To Go 工作区启动的电脑是否可以使用休眠睡眠状态(S4)。

如果启用此设置，则从 Windows To Go 工作区启动的 Windows 可使电脑休眠。

如果禁用或未配置此设置，则从 Windows To Go 工作区启动的 Windows 不可使电脑休眠。</string>
      <string id="PortableOperatingSystem_Sleep_DisplayName">从 Windows To Go 工作区启动时不允许待机睡眠状态(S1-S3)</string>
      <string id="PortableOperatingSystem_Sleep_Help">



指定从 Windows To Go 工作区启动的电脑是否可以使用待机睡眠状态(S1-S3)。

如果启用此设置，则从 Windows To Go 工作区启动的 Windows 不可使电脑进入待机睡眠状态。

如果禁用或不配置此设置，则从 Windows To Go 工作区启动的 Windows 可使电脑进入待机睡眠状态。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
