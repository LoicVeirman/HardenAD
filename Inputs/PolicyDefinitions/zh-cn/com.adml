<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="AppMgmt_COM_SearchForCLSID">下载缺少的 COM 组件</string>
      <string id="AppMgmt_COM_SearchForCLSID_Help">此策略设置指导系统在 Active Directory 中搜索程序需要但缺少的组件对象模型(COM)组件。

许多 Windows 程序(如 MMC 管理单元)都使用由 COM 组件提供的接口。只有在 Windows 内部已注册了所需的组件时，这些程序才能执行其所有功能。

如果启用此策略设置但未注册组件，则系统将在 Active Directory 中搜索该组件，如果找到，则下载该组件。之后的搜索可能会使某些程序启动或运行缓慢。

如果禁用或未配置此策略设置，则程序将在没有注册的情况下继续运行。这样，程序可能会无法执行其所有功能，或者可能会停止。

“计算机配置”和“用户配置”文件夹中均包括此设置。如果对这两项设置都进行了配置，则“计算机配置”中的设置优先于“用户配置”中的设置。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
