<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="DFSDiscoverDC">配置 DFS 客户端发现域控制器的频率</string>
      <string id="DFSDiscoverDC_Help">使用此策略设置，可以配置分布式文件系统(DFS)客户端尝试发现网络中的域控制器的频率。默认情况下，DFS 客户端尝试每隔 15 分钟发现一次域控制器。

如果启用此策略设置，则可以配置 DFS 客户端尝试发现域控制器的频率。以分钟为单位指定此值。

如果禁用或未配置此策略设置，则应用默认值 15 分钟。

注意: 可以选择的最小值为 15 分钟。如果尝试将此值设置为小于 15 分钟，则将应用默认值 15 分钟。</string>
    </stringTable>
    <presentationTable>
      <presentation id="DFSDiscoverDC">
        <decimalTextBox refId="DFSDiscoverDialog" defaultValue="15">以分钟为单位的时间:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
