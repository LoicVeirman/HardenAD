<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="DisableThumbnails">关闭缩略图显示并仅显示图标。</string>
      <string id="DisableThumbnails_Help">使用此策略设置，可以配置文件资源管理器在本地计算机上显示缩略图图像或图标的方式。

默认情况下，文件资源管理器显示缩略图图像。

如果启用此策略设置，则文件资源管理器将仅显示图标且从不显示缩略图图像。

如果禁用或未配置此策略设置，则文件资源管理器仅显示缩略图图像。</string>
      <string id="DisableThumbnailsOnNetworkFolders">在网络文件夹上关闭缩略图显示并仅显示图标</string>
      <string id="DisableThumbnailsOnNetworkFolders_Help">使用此策略设置，可以配置文件资源管理器在网络文件夹上显示缩略图图像或图标的方式。

默认情况下，文件资源管理器在网络文件夹上显示缩略图图像。

如果启用此策略设置，则文件资源管理器在网络文件夹上将仅显示图标且从不显示缩略图图像。

如果禁用或未配置此策略设置，则文件资源管理器在网络文件夹上仅显示缩略图图像。</string>
      <string id="DisableThumbsDBOnNetworkFolders">关闭隐藏的 thumbs.db 文件中的缩略图缓存</string>
      <string id="DisableThumbsDBOnNetworkFolders_Help">关闭隐藏的 thumbs.db 文件中的缩略图缓存。

使用此策略设置,可配置文件资源管理器将位于网络文件夹中的项目的缩略图缓存在隐藏的 thumbs.db 文件中。

如果启用此策略设置，则文件资源管理器将不会创建、读取或写入 thumbs.db 文件。

如果禁用或未配置此策略设置，则文件资源管理器将创建、读取并写入 thumbs.db 文件。

</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
