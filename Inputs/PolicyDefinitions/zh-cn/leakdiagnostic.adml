<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="WdiScenarioCategory">Windows 内存泄漏诊断</string>
      <string id="WdiScenarioExecutionPolicy">配置方案执行级别</string>
      <string id="WdiScenarioExecutionPolicyExplain">此策略设置确定诊断策略服务(DPS)是否诊断内存泄漏问题。

如果启用或未配置此策略设置，则默认情况下，DPS 启用 Windows 内存泄漏诊断。

如果禁用此策略设置，则 DPS 不能诊断内存泄漏问题。

此策略设置只有在以下情况下才会生效: 
-- 未配置诊断级方案执行策略。
-- 诊断策略服务处于运行状态。当此服务已停用或禁用时，将不会执行诊断方案。

注意: 可以使用 Microsoft 管理控制台的服务管理单元来配置 DPS。

无须重新启动操作系统或重新启动服务，此策略即可生效。更改将立即生效。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
