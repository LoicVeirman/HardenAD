<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="NoInplaceSharing">阻止用户共享其配置文件内的文件。</string>
      <string id="NoInplaceSharing_Help">此策略设置指定用户是否可共享其配置文件内的文件。默认情况下，管理员选择计算机后，会允许用户将其配置文件内的文件共享给其网络上的其他用户。 管理员可以通过使用共享向导共享其配置文件内的文件来选择计算机。

如果启用此策略设置，则用户将无法使用共享向导共享其配置文件内的文件。 同时，共享向导也不能在 %root%\users 中创建共享，而只能用于在文件夹上创建 SMB 共享。

如果禁用或未配置此策略设置，则管理员选择计算机后，用户就能够在其用户配置文件外共享文件。</string>
      <string id="Sharing">网络共享</string>
      <string id="Sharing_Help">包含控制文件和文件夹共享行为的设置。</string>
      <string id="DisableHomeGroup">阻止计算机加入家庭组</string>
      <string id="DisableHomeGroup_Help">此策略设置指定用户是否可将计算机添加到家庭组中。默认情况下，用户可以将其计算机添加到专用网络上的家庭组中。

如果启用此策略设置，则用户无法将计算机添加到家庭组中。此策略设置不会影响其他网络共享功能。

如果禁用或未配置此策略设置，则用户可以将计算机添加到家庭组中。但是，加入到域的计算机上的数据不会与家庭组共享。

默认情况下不会配置此策略设置。

必须重新启动计算机才能使此策略设置生效。</string>
      <string id="HomeGroup">家庭组</string>
      <string id="HomeGroup_Help">包含控制家庭组行为的设置。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
