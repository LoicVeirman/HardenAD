<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="PublishDfsRoots">允许发布 DFS 根目录</string>
      <string id="PublishDfsRoots_Help">此策略设置确定用户是否可在 Active Directory 域服务(AD DS)中发布 DFS 根目录。

如果启用或未配置此策略设置，则用户可使用“在 Active Directory 中发布”选项在 AD DS 中发布作为共享文件夹的 DFS 根目录。

如果禁用此策略设置，则用户将无法在 AD DS 中发布 DFS 根目录，并且“在 Active Directory 中发布”选项处于禁用状态。注意: 默认为在没有配置此设置时允许发布共享文件夹。</string>
      <string id="PublishSharedFolders">允许发布共享文件夹</string>
      <string id="PublishSharedFolders_Help">此策略设置确定用户是否可在 Active Directory 域服务(AD DS)中发布共享文件夹。

如果启用或未配置此策略设置，则用户可使用“共享文件夹”管理单元中的“在 Active Directory 中发布”选项在 AD DS 中发布共享文件夹。

如果禁用此策略设置，则用户将无法在 AD DS 中发布共享文件夹，并且“在 Active Directory 中发布”选项处于禁用状态。注意: 默认为在没有配置此设置时允许发布共享文件夹。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
