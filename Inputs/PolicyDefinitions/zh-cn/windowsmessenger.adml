<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">初始化时不自动启动 Windows Messenger</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">此策略设置阻止 Windows Messenger 在用户登录时自动运行。

如果启用此策略设置，则 Windows Messenger 不会在用户登录时自动加载。

如果禁用或未配置此策略设置，则 Windows Messenger 会在用户登录时自动加载。

注意: 此策略设置仅阻止 Windows Messenger 在初始化时运行。如果用户那时调用和使用 Windows Messenger，则 Windows Messenger 将被加载。

用户也可以在 Windows Messenger 用户界面的“工具”菜单上的“首选项”选项卡上配置此行为。

注意: 如果不希望用户使用 Windows Messenger，请启用“不允许运行 Windows Messenger”策略设置。

注意: “计算机配置”和“用户配置”下都有此策略设置。如果这两处设置都启用，则“计算机配置”中的设置优先于“用户配置”中的设置。</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">初始化时不自动启动 Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Comp">不允许运行 Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">使用此策略设置可以禁止 Windows Messenger 运行。

如果启用此策略设置，则 Windows Messenger 不会运行。

如果禁用或未配置此策略设置，则可以使用 Windows Messenger。

注意: 如果启用此策略设置，则远程协助也无法使用 Windows Messenger。

注意: “计算机配置”和“用户配置”下都有此策略设置。如果这两处设置都启用，则“计算机配置”中的设置优先于“用户配置”中的设置。</string>
      <string id="WinMSG_NoWindowsMsg_User">不允许运行 Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
