<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>buraya görünen adı girin</displayName>
  <description>buraya açıklamayı girin</description>
  <resources>
    <stringTable>
      <string id="DFSDiscoverDC">DFS istemcisinin hangi sıklıkla etki alanı denetleyicilerini keşfedeceğini yapılandır</string>
      <string id="DFSDiscoverDC_Help">Bu ilke ayarı, Bir Dağıtılmış Dosya Sistemi (DFS) istemcisinin, ağdaki etki alanı denetleyicilerini ne sıklıkla keşfetmeye çalışacağını yapılandırmanıza olanak tanır. Varsayılan olarak, bir DFS istemcisi her 15 dakikada bir etki alanı denetleyicilerini keşfetmeye çalışır.

Bu ilke ayarını etkinleştirirseniz DFS istemcisinin etki alanı denetleyicilerini hangi sıklıkla keşfetmeye çalışacağını yapılandırabilirsiniz. Bu değer dakika cinsinden belirlenir.

Bu ilke ayarını devre dışı bırakırsanız ya da yapılandırmazsanız varsayılan değer olan 15 dakika geçerli olacaktır.

Not: Seçebileceğiniz en küçük değer 15 dakikadır. Bu ayar için 15 dakikadan küçük bir değer belirlemeye çalışırsanız, varsayılan değer olan 15 dakika geçerli olur.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DFSDiscoverDC">
        <decimalTextBox refId="DFSDiscoverDialog" defaultValue="15">Süre (dakika):</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
